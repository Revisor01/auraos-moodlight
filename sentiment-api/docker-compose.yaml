version: '3.8'

services:
  # ===== News Sentiment Analyzer (Main App) =====
  news-analyzer:
    build: .
    container_name: moodlight-analyzer
    ports:
      - "6237:6237"
    environment:
      - FLASK_ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATABASE_URL=postgresql://moodlight:${POSTGRES_PASSWORD}@postgres:5432/moodlight
      - REDIS_URL=redis://redis:6379/0
      - DEFAULT_HEADLINES_PER_SOURCE=1
    depends_on:
      - postgres
      - redis
    volumes:
      - ./:/app
    networks:
      - moodlight-net
    deploy:
      resources:
        limits:
          memory: 2G
    restart: unless-stopped
    labels:
      - "com.centurylinklabs.watchtower.enable=false"

  # ===== PostgreSQL Database =====
  postgres:
    image: postgres:16-alpine
    container_name: moodlight-postgres
    environment:
      POSTGRES_DB: moodlight
      POSTGRES_USER: moodlight
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    # Ports nur intern im Docker-Netzwerk
    expose:
      - "5432"
    networks:
      - moodlight-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U moodlight"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ===== Redis Cache =====
  redis:
    image: redis:7-alpine
    container_name: moodlight-redis
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru --appendonly yes
    volumes:
      - redis_data:/data
    # Ports nur intern im Docker-Netzwerk
    expose:
      - "6379"
    networks:
      - moodlight-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

networks:
  moodlight-net:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
