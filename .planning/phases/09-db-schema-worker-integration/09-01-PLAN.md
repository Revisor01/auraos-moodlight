---
phase: 09-db-schema-worker-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/init.sql
  - sentiment-api/migrations/001_add_feeds_table.sql
autonomous: true
requirements:
  - FEED-01
  - FEED-06

must_haves:
  truths:
    - "init.sql enthält die feeds-Tabelle mit allen 7 Feldern (id, name, url, active, last_fetched_at, error_count, created_at, updated_at)"
    - "init.sql enthält die Default-Feeds als INSERT-Statement — ohne Focus.de, mit 11 Feeds"
    - "migrations/001_add_feeds_table.sql existiert und ist idempotent (CREATE TABLE IF NOT EXISTS + ON CONFLICT DO NOTHING)"
  artifacts:
    - path: "sentiment-api/init.sql"
      provides: "Erweitertes Schema für neue Deployments"
      contains: "CREATE TABLE IF NOT EXISTS feeds"
    - path: "sentiment-api/migrations/001_add_feeds_table.sql"
      provides: "Migration für das laufende Produktionssystem"
      exports: []
  key_links:
    - from: "sentiment-api/migrations/001_add_feeds_table.sql"
      to: "PostgreSQL auf server.godsapp.de"
      via: "manuell: docker exec -i moodlight-postgres psql -U moodlight -d moodlight"
      pattern: "001_add_feeds_table"
---

<objective>
feeds-Tabelle in PostgreSQL definieren: init.sql für neue Deployments erweitern und ein separates Migrations-SQL für das laufende Produktionssystem erstellen.

Purpose: Das laufende PostgreSQL-Volume auf dem Server enthält bereits Produktionsdaten. init.sql wird nicht erneut ausgeführt — daher braucht es ein separates Migrations-Skript. Nach dieser Phase ist das Schema für Plan 02 (Python-Integration) bereit.
Output: Zwei SQL-Dateien — init.sql (erweitert) und migrations/001_add_feeds_table.sql (neu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@sentiment-api/init.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: feeds-Tabelle in init.sql ergänzen</name>
  <files>sentiment-api/init.sql</files>

  <read_first>
    - sentiment-api/init.sql — vollständige Datei lesen, um Stil, Sektions-Pattern und Position für die neue Sektion zu verstehen
  </read_first>

  <action>
Direkt vor dem Abschnitt `-- ===== HELPER FUNCTIONS =====` in init.sql eine neue Sektion `-- ===== FEEDS TABLE =====` einfügen.

Exakter SQL-Block der einzufügen ist:

```sql
-- ===== FEEDS TABLE =====
-- RSS-Feeds für Sentiment-Analyse — verwaltet via API (Phase 10)
CREATE TABLE IF NOT EXISTS feeds (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    url TEXT NOT NULL UNIQUE,
    active BOOLEAN NOT NULL DEFAULT TRUE,
    last_fetched_at TIMESTAMPTZ,
    error_count INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Indizes für Performance
CREATE INDEX IF NOT EXISTS idx_feeds_active ON feeds(active);
CREATE INDEX IF NOT EXISTS idx_feeds_url ON feeds(url);

COMMENT ON TABLE feeds IS 'RSS-Feeds für Sentiment-Analyse';
COMMENT ON COLUMN feeds.active IS 'Inaktive Feeds werden vom Worker ignoriert';
COMMENT ON COLUMN feeds.error_count IS 'Anzahl aufeinanderfolgender Fehler beim Feed-Abruf';

-- Default-Feeds (ohne Focus.de — FEED-06)
INSERT INTO feeds (name, url) VALUES
    ('Zeit', 'https://newsfeed.zeit.de/index'),
    ('Tagesschau', 'https://www.tagesschau.de/xml/rss2'),
    ('Sueddeutsche', 'https://rss.sueddeutsche.de/rss/Alles'),
    ('FAZ', 'https://www.faz.net/rss/aktuell/'),
    ('Die Welt', 'https://www.welt.de/feeds/latest.rss'),
    ('Handelsblatt', 'https://www.handelsblatt.com/contentexport/feed/schlagzeilen'),
    ('n-tv', 'https://www.n-tv.de/rss'),
    ('Stern', 'https://www.stern.de/feed/standard/alle-nachrichten/'),
    ('Telekom', 'https://www.t-online.de/feed.rss'),
    ('TAZ', 'https://taz.de/!p4608;rss/'),
    ('Deutschlandfunk', 'https://www.deutschlandfunk.de/nachrichten-100.rss')
ON CONFLICT (url) DO NOTHING;
```

Außerdem den KOMMENTARE-Block am Ende der Datei um zwei Zeilen ergänzen:
```sql
COMMENT ON TABLE feeds IS 'RSS-Feeds für Sentiment-Analyse';
COMMENT ON COLUMN feeds.url IS 'UNIQUE — verhindert doppelte Feed-Einträge';
```
  </action>

  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS feeds" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/init.sql</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CREATE TABLE IF NOT EXISTS feeds" sentiment-api/init.sql` gibt genau 1 Treffer
    - `grep "last_fetched_at" sentiment-api/init.sql` gibt mindestens 1 Treffer
    - `grep "error_count" sentiment-api/init.sql` gibt mindestens 1 Treffer
    - `grep "ON CONFLICT (url) DO NOTHING" sentiment-api/init.sql` gibt mindestens 1 Treffer
    - `grep "Focus" sentiment-api/init.sql` gibt 0 Treffer (Focus.de darf nicht enthalten sein)
    - `grep -c "INSERT INTO feeds" sentiment-api/init.sql` gibt genau 1 Treffer mit 11 Value-Zeilen
    - `grep "idx_feeds_active" sentiment-api/init.sql` gibt mindestens 1 Treffer
  </acceptance_criteria>

  <done>init.sql enthält die feeds-Tabelle mit allen 7 Feldern, zwei Indizes, COMMENT-Statements und einem idempotenten INSERT für 11 Default-Feeds (kein Focus.de).</done>
</task>

<task type="auto">
  <name>Task 2: Migrations-SQL für Produktionssystem erstellen</name>
  <files>sentiment-api/migrations/001_add_feeds_table.sql</files>

  <read_first>
    - sentiment-api/init.sql — die gerade ergänzte feeds-Sektion als Vorlage für identisches SQL im Migrations-File
  </read_first>

  <action>
Verzeichnis `sentiment-api/migrations/` anlegen und darin `001_add_feeds_table.sql` erstellen.

Exakter Inhalt der Datei:

```sql
-- Migrations-Skript: 001_add_feeds_table.sql
-- Zweck: feeds-Tabelle für laufendes Produktionssystem anlegen
-- Datum: Phase 9 — DB-Schema & Worker-Integration
--
-- Ausführung auf dem Server (einmalig):
--   docker exec -i moodlight-postgres psql -U moodlight -d moodlight < migrations/001_add_feeds_table.sql
--
-- Das Skript ist idempotent (kann mehrfach ausgeführt werden ohne Fehler).

-- ===== FEEDS TABLE =====
CREATE TABLE IF NOT EXISTS feeds (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    url TEXT NOT NULL UNIQUE,
    active BOOLEAN NOT NULL DEFAULT TRUE,
    last_fetched_at TIMESTAMPTZ,
    error_count INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Indizes
CREATE INDEX IF NOT EXISTS idx_feeds_active ON feeds(active);
CREATE INDEX IF NOT EXISTS idx_feeds_url ON feeds(url);

COMMENT ON TABLE feeds IS 'RSS-Feeds für Sentiment-Analyse';
COMMENT ON COLUMN feeds.active IS 'Inaktive Feeds werden vom Worker ignoriert';
COMMENT ON COLUMN feeds.error_count IS 'Anzahl aufeinanderfolgender Fehler beim Feed-Abruf';
COMMENT ON COLUMN feeds.url IS 'UNIQUE — verhindert doppelte Feed-Einträge';

-- Default-Feeds einfügen (ohne Focus.de — FEED-06: gibt 404 zurück)
INSERT INTO feeds (name, url) VALUES
    ('Zeit', 'https://newsfeed.zeit.de/index'),
    ('Tagesschau', 'https://www.tagesschau.de/xml/rss2'),
    ('Sueddeutsche', 'https://rss.sueddeutsche.de/rss/Alles'),
    ('FAZ', 'https://www.faz.net/rss/aktuell/'),
    ('Die Welt', 'https://www.welt.de/feeds/latest.rss'),
    ('Handelsblatt', 'https://www.handelsblatt.com/contentexport/feed/schlagzeilen'),
    ('n-tv', 'https://www.n-tv.de/rss'),
    ('Stern', 'https://www.stern.de/feed/standard/alle-nachrichten/'),
    ('Telekom', 'https://www.t-online.de/feed.rss'),
    ('TAZ', 'https://taz.de/!p4608;rss/'),
    ('Deutschlandfunk', 'https://www.deutschlandfunk.de/nachrichten-100.rss')
ON CONFLICT (url) DO NOTHING;

-- Bestätigung
DO $$
BEGIN
    RAISE NOTICE 'Migration 001_add_feeds_table.sql erfolgreich ausgeführt.';
    RAISE NOTICE 'Feeds in Tabelle: %', (SELECT COUNT(*) FROM feeds);
END $$;
```
  </action>

  <verify>
    <automated>test -f /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/migrations/001_add_feeds_table.sql && echo "exists"</automated>
  </verify>

  <acceptance_criteria>
    - Datei `sentiment-api/migrations/001_add_feeds_table.sql` existiert
    - `grep "CREATE TABLE IF NOT EXISTS feeds" sentiment-api/migrations/001_add_feeds_table.sql` gibt 1 Treffer
    - `grep "ON CONFLICT (url) DO NOTHING" sentiment-api/migrations/001_add_feeds_table.sql` gibt 1 Treffer
    - `grep "Focus" sentiment-api/migrations/001_add_feeds_table.sql` gibt 0 Treffer
    - `grep "docker exec" sentiment-api/migrations/001_add_feeds_table.sql` gibt 1 Treffer (Ausführungsanweisung im Kommentar)
    - Die Datei hat 11 INSERT-Zeilen (keine 12. Zeile für Focus.de)
  </acceptance_criteria>

  <done>Migrations-Datei existiert, ist idempotent ausführbar, enthält 11 Default-Feeds ohne Focus.de, und dokumentiert den Ausführungsbefehl für den Server.</done>
</task>

</tasks>

<verification>
```bash
# feeds-Sektion in init.sql vorhanden
grep "CREATE TABLE IF NOT EXISTS feeds" sentiment-api/init.sql

# Focus.de in keiner SQL-Datei enthalten
grep -r "Focus" sentiment-api/init.sql sentiment-api/migrations/

# Migrations-File existiert und ist idempotent
grep "IF NOT EXISTS" sentiment-api/migrations/001_add_feeds_table.sql

# 11 Feeds (keine 12)
grep -c "https://" sentiment-api/migrations/001_add_feeds_table.sql
```
</verification>

<success_criteria>
- init.sql enthält feeds-Tabelle mit 7 Feldern + 2 Indizes + 11 Default-Feeds (kein Focus.de)
- migrations/001_add_feeds_table.sql existiert, ist idempotent, enthält identisches Schema + Ausführungsanweisung
- Beide Dateien sind im Git-Repository
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-db-schema-worker-integration/09-01-SUMMARY.md` erstellen.
</output>
