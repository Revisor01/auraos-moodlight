---
phase: 17-dynamische-skalierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/database.py
autonomous: true
requirements:
  - SCALE-01
  - SCALE-02

must_haves:
  truths:
    - "database.py hat eine Methode get_score_percentiles(days=7), die P20/P40/P60/P80 sowie Min/Max/Median zurückgibt"
    - "Die Methode berechnet Schwellwerte aus echten DB-Daten der letzten N Tage, nicht aus Konstanten"
    - "Fallback-Werte werden geliefert wenn weniger als 3 Datenpunkte im Fenster vorhanden sind"
    - "compute_led_index(score, thresholds) mappt einen Rohwert auf LED-Index 0–4 basierend auf dynamischen Schwellwerten"
  artifacts:
    - path: "sentiment-api/database.py"
      provides: "get_score_percentiles() Methode + compute_led_index() Hilfsfunktion"
      exports: ["get_score_percentiles", "compute_led_index"]
  key_links:
    - from: "database.py:get_score_percentiles"
      to: "sentiment_history.sentiment_score"
      via: "PostgreSQL percentile_cont() über die letzten N Tage"
      pattern: "percentile_cont.*WITHIN GROUP.*ORDER BY sentiment_score"
---

<objective>
Perzentil-Berechnung im Database-Layer implementieren: neue Methode `get_score_percentiles(days=7)` in der `Database`-Klasse und Module-level Hilfsfunktion `compute_led_index()`.

Purpose: SCALE-01 und SCALE-02 erfüllen — Grundlage für dynamisches Score-Mapping statt fester Schwellwerte. Das ist die einzige Abhängigkeit von Plan 02.
Output: `database.py` mit zwei neuen Funktionen, die per Unit-Test verifizierbar sind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dynamische-skalierung/17-CONTEXT.md

<interfaces>
<!-- Bestehende Database-Klasse — Executor braucht diese Signaturen -->
<!-- Aus sentiment-api/database.py -->

```python
class Database:
    def __init__(self, database_url: str): ...
    def get_cursor(self, cursor_factory=None): ...  # contextmanager
    # Existierende Query-Methoden als Referenz:
    def get_latest_sentiment(self) -> Optional[Dict[str, Any]]: ...
    def get_statistics(self) -> Dict[str, Any]: ...
    # Verwendetes Muster für Multi-Query in einem Cursor:
    with self.get_cursor() as cur:
        cur.execute(query, params)
        result = cur.fetchone()
```

Tabelle: `sentiment_history`
- Spalten: `id`, `timestamp TIMESTAMPTZ`, `sentiment_score FLOAT`, `category`, `headlines_analyzed`, `source_count`, `api_response_time_ms`, `metadata`, `created_at`
- Index: `idx_sentiment_timestamp ON sentiment_history(timestamp DESC)`

PostgreSQL-Funktion für Perzentile (verfügbar in PG16):
```sql
percentile_cont(0.20) WITHIN GROUP (ORDER BY sentiment_score)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_score_percentiles() und compute_led_index() implementieren</name>
  <files>sentiment-api/database.py</files>
  <behavior>
    - get_score_percentiles(days=7) gibt Dict zurück: {"p20": float, "p40": float, "p60": float, "p80": float, "min": float, "max": float, "median": float, "count": int, "fallback": bool}
    - Bei count >= 3: echte Perzentile aus DB
    - Bei count < 3: fallback=True, feste Werte {"p20": -0.5, "p40": -0.2, "p60": 0.1, "p80": 0.4} (gleichmäßig über den Score-Bereich verteilt)
    - compute_led_index(score: float, thresholds: dict) -> int (0–4): score < p20 → 0, score < p40 → 1, score < p60 → 2, score < p80 → 3, sonst → 4
    - compute_led_index ist eine module-level Funktion (nicht Methode der Klasse)
  </behavior>
  <action>
Am Ende der Database-Klasse (nach get_statistics) neue Methode einfügen:

```python
def get_score_percentiles(self, days: int = 7) -> Dict[str, Any]:
    """
    Berechnet historische Perzentile der Sentiment-Scores der letzten N Tage.

    Args:
        days: Anzahl Tage für das historische Fenster (default: 7)

    Returns:
        Dict mit p20, p40, p60, p80, min, max, median, count, fallback
    """
    FALLBACK = {
        "p20": -0.5, "p40": -0.2, "p60": 0.1, "p80": 0.4,
        "min": -1.0, "max": 1.0, "median": 0.0,
        "count": 0, "fallback": True
    }

    query = """
        SELECT
            COUNT(*) AS count,
            percentile_cont(0.20) WITHIN GROUP (ORDER BY sentiment_score) AS p20,
            percentile_cont(0.40) WITHIN GROUP (ORDER BY sentiment_score) AS p40,
            percentile_cont(0.50) WITHIN GROUP (ORDER BY sentiment_score) AS median,
            percentile_cont(0.60) WITHIN GROUP (ORDER BY sentiment_score) AS p60,
            percentile_cont(0.80) WITHIN GROUP (ORDER BY sentiment_score) AS p80,
            MIN(sentiment_score) AS min,
            MAX(sentiment_score) AS max
        FROM sentiment_history
        WHERE timestamp >= NOW() - INTERVAL '%s days';
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query, (days,))
            row = cur.fetchone()
            if not row or row['count'] < 3:
                logger.warning(
                    f"Zu wenige Datenpunkte für Perzentil-Berechnung "
                    f"(count={row['count'] if row else 0}, days={days}) — verwende Fallback"
                )
                if row:
                    FALLBACK['count'] = int(row['count'])
                return FALLBACK
            return {
                "p20": round(float(row['p20']), 4),
                "p40": round(float(row['p40']), 4),
                "p60": round(float(row['p60']), 4),
                "p80": round(float(row['p80']), 4),
                "min": round(float(row['min']), 4),
                "max": round(float(row['max']), 4),
                "median": round(float(row['median']), 4),
                "count": int(row['count']),
                "fallback": False
            }
    except Exception as e:
        logger.error(f"Fehler bei Perzentil-Berechnung: {e}")
        return FALLBACK
```

Nach dem `get_database()` / `get_cache()` Singleton-Block am Datei-Ende module-level Funktion einfügen:

```python
def compute_led_index(score: float, thresholds: Dict[str, Any]) -> int:
    """
    Mappt einen Sentiment-Score auf LED-Index 0–4 basierend auf Perzentil-Schwellwerten.

    Args:
        score: Sentiment-Score (-1.0 bis +1.0)
        thresholds: Dict aus get_score_percentiles() mit Schlüsseln p20, p40, p60, p80

    Returns:
        LED-Index 0 (sehr negativ) bis 4 (sehr positiv)
    """
    if score < thresholds.get('p20', -0.5):
        return 0
    if score < thresholds.get('p40', -0.2):
        return 1
    if score < thresholds.get('p60', 0.1):
        return 2
    if score < thresholds.get('p80', 0.4):
        return 3
    return 4
```

Darauf achten: Den Import `Dict` aus `typing` gibt es bereits (Zeile 16). Kein neuer Import nötig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python3 -c "
from database import compute_led_index

# compute_led_index mit Fallback-Werten testen
t = {'p20': -0.5, 'p40': -0.2, 'p60': 0.1, 'p80': 0.4}
assert compute_led_index(-0.8, t) == 0, 'Score -0.8 soll 0 sein'
assert compute_led_index(-0.3, t) == 1, 'Score -0.3 soll 1 sein'
assert compute_led_index(-0.1, t) == 2, 'Score -0.1 soll 2 sein'
assert compute_led_index(0.2, t) == 3, 'Score 0.2 soll 3 sein'
assert compute_led_index(0.6, t) == 4, 'Score 0.6 soll 4 sein'
# Grenzwerte
assert compute_led_index(-0.5, t) == 1, 'Score == p20 soll 1 sein (nicht < p20)'
assert compute_led_index(-0.2, t) == 2, 'Score == p40 soll 2 sein'
print('compute_led_index: alle Assertions bestanden')
"
    </automated>
  </verify>
  <done>
    - `get_score_percentiles(days=7)` ist in der Database-Klasse als Methode verfügbar
    - `compute_led_index(score, thresholds)` ist als module-level Funktion in `database.py` verfügbar
    - `from database import compute_led_index` funktioniert ohne Import-Fehler
    - Bei count < 3: fallback=True wird zurückgegeben, kein Exception-Raise
    - Alle compute_led_index Assertions bestehen
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python3 -c "
from database import compute_led_index, get_database
# Import prüfen
print('Import OK')
# Signatur prüfen
import inspect
db_class = get_database.__module__
print('module:', db_class)
"
```
</verification>

<success_criteria>
- `database.py` enthält `Database.get_score_percentiles()` mit korrekter PostgreSQL-Query (percentile_cont)
- `database.py` enthält module-level `compute_led_index()` mit richtiger 5-Stufen-Logik
- Fallback greift bei count < 3, kein Crash
- `from database import compute_led_index` funktioniert
</success_criteria>

<output>
Nach Abschluss `.planning/phases/17-dynamische-skalierung/17-01-SUMMARY.md` erstellen.
</output>
