---
phase: 17-dynamische-skalierung
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - sentiment-api/moodlight_extensions.py
autonomous: true
requirements:
  - SCALE-03

must_haves:
  truths:
    - "GET /api/moodlight/current gibt Felder raw_score, percentile, led_index, thresholds, historical zurück"
    - "led_index (0–4) basiert auf dynamischen Perzentil-Schwellwerten aus der DB, nicht aus festen Konstanten"
    - "Bei gleichem raw_score liefert die API einen anderen led_index, wenn sich der historische Bereich verschoben hat"
    - "Redis-Cache wird nach Schema-Änderung mit neuem Key invalidiert (kein Stale-Cache mit altem Format)"
    - "Fallback-Modus wird korrekt signalisiert (thresholds.fallback = true im Response)"
  artifacts:
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "Erweiterter /api/moodlight/current Endpoint"
      contains: "thresholds, raw_score, percentile, led_index, historical"
  key_links:
    - from: "moodlight_extensions.py:get_moodlight_current"
      to: "database.py:get_score_percentiles + compute_led_index"
      via: "Import und Aufruf nach Cache MISS"
      pattern: "get_score_percentiles|compute_led_index"
    - from: "Redis Cache Key"
      to: "moodlight:current:v2"
      via: "Versionierter Cache-Key verhindert altes Format"
      pattern: "moodlight:current:v2"
---

<objective>
`/api/moodlight/current` um Skalierungs-Kontext erweitern: Perzentile aus der DB berechnen, LED-Index dynamisch mappen, Redis-Cache mit versioniertem Key aktualisieren.

Purpose: SCALE-03 erfüllen — ESP32 und Dashboard erhalten vollständigen Kontext für Phase 18.
Output: Erweiterter API-Endpoint mit `raw_score`, `percentile`, `led_index`, `thresholds`, `historical` im Response.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dynamische-skalierung/17-CONTEXT.md
@.planning/phases/17-dynamische-skalierung/17-01-SUMMARY.md

<interfaces>
<!-- Aus 17-01 neu verfügbar in database.py -->

```python
# Neue Methode in Database-Klasse:
def get_score_percentiles(self, days: int = 7) -> Dict[str, Any]:
    """
    Returns: {
        "p20": float, "p40": float, "p60": float, "p80": float,
        "min": float, "max": float, "median": float,
        "count": int, "fallback": bool
    }
    """

# Neue module-level Funktion:
def compute_led_index(score: float, thresholds: Dict[str, Any]) -> int:
    """Returns 0–4"""
```

<!-- Bestehender /api/moodlight/current Response (vor Erweiterung) -->
```json
{
  "status": "success",
  "timestamp": "2026-03-27T09:00:00",
  "sentiment": -0.23,
  "category": "negativ",
  "headlines_analyzed": 85,
  "next_update_minutes": 30,
  "cached": false
}
```

<!-- Aktueller Cache-Key: 'moodlight:current' (5 Min TTL) -->
<!-- Neuer Cache-Key: 'moodlight:current:v2' — verhindert Stale-Responses mit altem Format -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/moodlight/current um Skalierungs-Kontext erweitern</name>
  <files>sentiment-api/moodlight_extensions.py</files>
  <action>
Änderungen in `moodlight_extensions.py`:

**1. Import erweitern** (Zeile 15, nach `from database import get_database, get_cache`):
```python
from database import get_database, get_cache, compute_led_index
```

**2. Cache-Key-Konstante** am Anfang von `register_moodlight_endpoints()` oder als Modul-Konstante vor der Funktion einfügen:
```python
CURRENT_CACHE_KEY = 'moodlight:current:v2'
```

**3. In `get_moodlight_current()`:** Alle Vorkommen von `'moodlight:current'` durch `CURRENT_CACHE_KEY` ersetzen (es gibt genau 3: cache.get, cache.set, cache.delete in cache/clear).

**4. Nach `sentiment_score = latest['sentiment_score']`** die Perzentile berechnen und Response erweitern:

```python
# Dynamische Schwellwerte aus DB (7-Tage-Fenster)
thresholds = db.get_score_percentiles(days=7)
led_index = compute_led_index(sentiment_score, thresholds)

# Perzentil-Position des aktuellen Scores bestimmen (0.0–1.0)
# Lineare Interpolation zwischen min und max
score_range = thresholds['max'] - thresholds['min']
if score_range > 0:
    percentile = round((sentiment_score - thresholds['min']) / score_range, 3)
    percentile = max(0.0, min(1.0, percentile))
else:
    percentile = 0.5
```

**5. Response-Dict** (komplettes Replacement des bestehenden `response = {...}` Blocks):
```python
response = {
    "status": "success",
    "timestamp": latest['timestamp'].isoformat(),
    "sentiment": round(sentiment_score, 4),
    "raw_score": round(sentiment_score, 4),
    "category": latest['category'],
    "led_index": led_index,
    "percentile": percentile,
    "thresholds": {
        "p20": thresholds['p20'],
        "p40": thresholds['p40'],
        "p60": thresholds['p60'],
        "p80": thresholds['p80'],
        "fallback": thresholds['fallback']
    },
    "historical": {
        "min": thresholds['min'],
        "max": thresholds['max'],
        "median": thresholds['median'],
        "count": thresholds['count'],
        "window_days": 7
    },
    "headlines_analyzed": latest.get('headlines_analyzed', 0),
    "next_update_minutes": 30,
    "cached": False
}
```

**6. Im Cache-HIT-Zweig** (oben in der Funktion): `cached_data` wird unverändert zurückgegeben — das ist korrekt, da der neue `CURRENT_CACHE_KEY` nur neue Responses speichert. Kein weiterer Eingriff nötig.

**7. In `clear_moodlight_cache()`:** `cache.delete('moodlight:current')` durch `cache.delete(CURRENT_CACHE_KEY)` ersetzen. Zusätzlich auch den alten Key löschen für saubere Migration:
```python
cache.delete(CURRENT_CACHE_KEY)
cache.delete('moodlight:current')  # Alten Key beim nächsten Clear-Aufruf bereinigen
```

NICHT verändern: Alle anderen Endpoints (`/history`, `/trend`, `/stats`, etc.) bleiben unberührt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python3 -c "
import ast, sys
with open('moodlight_extensions.py', 'r') as f:
    src = f.read()

# Syntax check
try:
    ast.parse(src)
    print('Syntax OK')
except SyntaxError as e:
    print(f'SYNTAX ERROR: {e}')
    sys.exit(1)

# Prüfe neue Felder im Source
required = ['raw_score', 'led_index', 'percentile', 'thresholds', 'historical', 'CURRENT_CACHE_KEY', 'compute_led_index', 'get_score_percentiles']
for field in required:
    if field not in src:
        print(f'FEHLT: {field}')
        sys.exit(1)
    else:
        print(f'OK: {field}')

# Alten Cache-Key darf nur noch in clear-Funktion vorkommen (Migration)
import re
cache_old = [m.start() for m in re.finditer(\"'moodlight:current'\", src) if 'clear' not in src[max(0,m.start()-200):m.start()]]
if cache_old:
    print(f'WARNUNG: Alter Cache-Key noch {len(cache_old)}x ohne clear-Kontext gefunden')
else:
    print('Cache-Key Migration OK')
print('Alle Checks bestanden')
"
    </automated>
  </verify>
  <done>
    - `GET /api/moodlight/current` Response enthält: `raw_score`, `led_index`, `percentile`, `thresholds` (mit p20/p40/p60/p80/fallback), `historical` (mit min/max/median/count/window_days)
    - Cache-Key ist `moodlight:current:v2` (kein altes Format im Cache)
    - Python-Syntax ist valide (`ast.parse` bestanden)
    - `compute_led_index` und `get_score_percentiles` werden importiert und aufgerufen
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python3 -c "
import ast
with open('moodlight_extensions.py') as f:
    src = f.read()
ast.parse(src)
print('Syntax OK')
for field in ['raw_score', 'led_index', 'percentile', 'thresholds', 'historical', 'CURRENT_CACHE_KEY']:
    assert field in src, f'Fehlt: {field}'
    print(f'Vorhanden: {field}')
"
```
</verification>

<success_criteria>
- Response von `/api/moodlight/current` enthält alle neuen Felder laut SCALE-03
- led_index basiert auf `compute_led_index()` mit dynamischen Schwellwerten (SCALE-01)
- Schwellwerte kommen aus `get_score_percentiles()` (SCALE-02)
- Redis nutzt neuen Cache-Key `moodlight:current:v2` — kein Stale-Format
- Kein bestehender Endpoint ist broken (Syntax-Check besteht)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/17-dynamische-skalierung/17-02-SUMMARY.md` erstellen.
</output>
