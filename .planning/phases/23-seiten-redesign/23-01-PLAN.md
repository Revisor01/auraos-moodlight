---
phase: 23-seiten-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/data/index.html
autonomous: true
requirements:
  - IDX-01
  - IDX-02
  - IDX-03

must_haves:
  truths:
    - "Score-Anzeige verwendet score-Farbklassen (score-sehr-negativ, score-negativ, score-neutral, score-positiv, score-sehr-positiv)"
    - "Alle 4 Karten haben class='card' mit border-radius 8px (via CSS-Variable --radius)"
    - "LED-Steuerung (toggleLight, toggleMode) und Helligkeit (setBrightness) funktionieren unverändert"
    - "Dark Mode via .dark Klasse auf body funktioniert — alle Inline-Styles sind entfernt"
  artifacts:
    - path: "firmware/data/index.html"
      provides: "Hauptseite mit Dashboard-Karten-Layout und Score-Farbkodierung"
      contains: "score-sehr-negativ|score-neutral|score-sehr-positiv"
  key_links:
    - from: "firmware/data/index.html"
      to: "firmware/data/css/style.css"
      via: "<link rel='stylesheet' href='/css/style.css'>"
      pattern: "score-.*negativ|score-neutral|score-.*positiv"
---

<objective>
index.html von Inline-Styles auf CSS-Klassen aus Phase-22-style.css umstellen. Score-Badge nutzt die 5 Farbklassen des Dashboards. Alle 4 Karten haben konsistentes .card-Layout. Keine Funktionsänderungen.

Purpose: IDX-01 (Score-Farbkodierung 5 Stufen), IDX-02 (Karten-Layout), IDX-03 (LED-Funktionalität erhalten)
Output: firmware/data/index.html komplett neu geschrieben
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- CSS-Klassen aus firmware/data/css/style.css die in index.html verwendet werden -->

Score-Farb-Klassen (5 Stufen):
  .score-sehr-negativ { color: var(--score-sehr-negativ); }  /* #e74c3c */
  .score-negativ      { color: var(--score-negativ); }        /* #e67e22 */
  .score-neutral      { color: var(--score-neutral); }        /* #3498db */
  .score-positiv      { color: var(--score-positiv); }        /* #6c3dbf */
  .score-sehr-positiv { color: var(--score-sehr-positiv); }   /* #8e44ad */

Mood-Badges (heller Hintergrund + Textfarbe):
  .mood.mood-very-negative  /* rot */
  .mood.mood-negative       /* orange */
  .mood.mood-neutral        /* blau */
  .mood.mood-positive       /* lila */
  .mood.mood-very-positive  /* violett */

Layout:
  .card          — border-radius 8px, box-shadow, padding 20px
  .grid          — 2-spaltiges Grid, gap 16px, auf Mobile 1-spaltig
  .stat          — flex-row mit stat-label + stat-value, border-bottom
  .stat-label    — color: var(--text-muted), font-size 0.9rem
  .stat-value    — font-weight 600
  .flex          — justify-content: space-between, align-items: center
  .section       — margin-bottom 20px
  .buttons       — flex, gap 10px, flex-wrap
  .badge         — pill-Badge
  .badge-success / .badge-danger — grün/rot koloriert
  .logs          — Scrollbox für Log-Inhalt
  .gauge         — Fortschrittsbalken-Container (height 8px)
  .gauge-fill    — Füllung des Gauge
  .led-row       — flex, gap 4px, centered
  .led           — 14px Kreis
  .switch + .slider — Toggle-Switch
  .range         — Range-Slider
  .color-grid    — Grid für Farbauswahl
  .control-label — Label unter Range-Slider
  .header        — flex, justify-content: space-between
  .title         — Gradient-Text
  .version       — kleiner grauer Text neben Title
  .header-controls — flex, gap 8px
  .btn / .btn-sm — Buttons

JavaScript-Funktionen (NICHT ändern, IDs beibehalten):
  refreshSentiment()    — Aktualisierung der Weltlage
  toggleLight()         — #light-switch
  toggleMode()          — #mode-switch
  setBrightness(val)    — #brightness, #brightness-val
  toggleDarkMode()      — #theme-btn
  refreshLog()          — #logContent

IDs die erhalten bleiben müssen:
  #version, #mood-gauge, #mood-value, #mood-class
  #leds, #light-switch, #mode-switch, #mode-text
  #brightness, #brightness-val, #color-grid
  #wifi-status, #wifi-ind, #mqtt-status, #mqtt-ind
  #uptime, #rssi, #dht-row, #dht, #heap
  #logContent, #theme-btn
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html komplett neu schreiben — Score-Farbklassen + Dashboard-Karten-Layout</name>
  <files>firmware/data/index.html</files>
  <action>
Datei firmware/data/index.html lesen und komplett neu schreiben. Alle Inline-Styles entfernen und durch CSS-Klassen aus style.css ersetzen. JavaScript und alle IDs bleiben unverändert.

Konkrete Änderungen:

1. **Score-Farbkodierung (IDX-01):** Das Element #mood-class nutzt jetzt .mood mit dynamisch gesetzter Klasse. Die JavaScript-Funktion refreshSentiment() in script.js setzt die Klasse — in der HTML-Vorlage bleibt .mood.mood-neutral als Default. Den Score #mood-value mit class="score-neutral" versehen (auch dynamisch via JS, Default: score-neutral). Das Gauge .gauge-fill bekommt keinen style="width:50%" mehr — das setzt JS.

2. **Inline-Styles entfernen (IDX-02):**
   - `style="text-align:center;margin:16px 0"` → `<div class="score-center">` (oder direkt `.section` mit Text-Align via CSS)
   - `style="font-size:1.3rem;font-weight:500;margin-bottom:4px"` → class="stat-value" (für #mood-value)
   - `style="font-size:0.8rem;color:#6c757d;margin-left:4px"` (mode-text) → class="text-muted" oder in die span-Klasse per CSS
   - `style="margin-top:16px"` → class="section" oder "buttons"

3. **System-Status-Karte:** Badges #wifi-ind und #mqtt-ind bekommen kein style-Attribut mehr — JS setzt die Klasse. Default: class="badge".

Vollständiges HTML für die Datei:

```html
<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="description" content="AuraOS Moodlight — KI-gestütztes Stimmungslicht basierend auf Weltlage-Analyse">
    <title>AuraOS Moodlight</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="icon" type="image/png" href="/favicon.png">
</head>
<body>
    <div class="header">
        <div class="title">AuraOS Moodlight<span class="version" id="version">Lädt...</span></div>
        <div class="header-controls">
            <a href="/setup" class="btn btn-sm" aria-label="Einstellungen öffnen">
                <i class="fas fa-cog" aria-hidden="true"></i> Einstellungen
            </a>
            <a href="/mood" class="btn btn-sm" aria-label="Statistik Ansicht öffnen">
                <i class="fas fa-chart-line" aria-hidden="true"></i> Statistik
            </a>
            <a href="/diagnostics" class="btn btn-sm" aria-label="Systemdiagnose öffnen">
                <i class="fas fa-stethoscope" aria-hidden="true"></i> Diagnose
            </a>
            <button id="theme-btn" class="btn btn-sm" onclick="toggleDarkMode()" aria-label="Zwischen Hell- und Dunkelmodus wechseln">☼</button>
        </div>
    </div>

    <div class="grid">
        <div class="card">
            <h2><i class="fas fa-globe-europe" aria-hidden="true"></i> Weltlage</h2>
            <div class="gauge">
                <div class="gauge-fill" id="mood-gauge"></div>
            </div>
            <div class="section" style="text-align:center">
                <div class="stat-value score-neutral" id="mood-value">--</div>
                <div class="mood mood-neutral" id="mood-class">Neutral</div>
            </div>
            <div class="buttons">
                <button class="btn" onclick="refreshSentiment()" aria-label="Weltlage-Analyse aktualisieren">
                    <i class="fas fa-sync-alt" aria-hidden="true"></i> Aktualisieren
                </button>
            </div>
        </div>

        <div class="card">
            <h2>
                <i class="fas fa-sliders-h" aria-hidden="true"></i> Steuerung
                <span class="version" id="mode-text">Auto</span>
            </h2>
            <div class="led-row" id="leds" aria-label="LED-Vorschau"></div>
            <div class="flex">
                <span>Licht</span>
                <label class="switch">
                    <input type="checkbox" id="light-switch" onchange="toggleLight()" checked aria-label="Licht ein/ausschalten">
                    <span class="slider"></span>
                </label>
            </div>
            <div class="flex">
                <span>Auto-Modus</span>
                <label class="switch">
                    <input type="checkbox" id="mode-switch" onchange="toggleMode()" checked aria-label="Zwischen Auto-Modus und manuellem Modus wechseln">
                    <span class="slider"></span>
                </label>
            </div>
            <div class="section">
                <label for="brightness">Helligkeit</label>
                <input type="range" class="range" id="brightness" min="10" max="255" value="255" onchange="setBrightness(this.value)" aria-valuemin="10" aria-valuemax="255" aria-valuenow="255">
                <div class="control-label">Wert: <span id="brightness-val">255</span></div>
            </div>
            <div class="section">
                <label>Farbe</label>
                <div class="color-grid" id="color-grid" role="radiogroup" aria-label="Farbauswahl"></div>
            </div>
        </div>

        <div class="card">
            <h2><i class="fas fa-heartbeat" aria-hidden="true"></i> System Status</h2>
            <div class="stat">
                <span class="stat-label">WLAN</span>
                <span class="stat-value">
                    <span id="wifi-status">--</span>
                    <span id="wifi-ind" class="badge">--</span>
                </span>
            </div>
            <div class="stat">
                <span class="stat-label">MQTT</span>
                <span class="stat-value">
                    <span id="mqtt-status">--</span>
                    <span id="mqtt-ind" class="badge">--</span>
                </span>
            </div>
            <div class="stat">
                <span class="stat-label">Uptime</span>
                <span class="stat-value" id="uptime">--</span>
            </div>
            <div class="stat">
                <span class="stat-label">WiFi Signal</span>
                <span class="stat-value" id="rssi">--</span>
            </div>
            <div class="stat" id="dht-row">
                <span class="stat-label">Temp / Luftfeuchte</span>
                <span class="stat-value" id="dht">--</span>
            </div>
            <div class="stat">
                <span class="stat-label">Speicher</span>
                <span class="stat-value" id="heap">--</span>
            </div>
            <div class="buttons">
                <button class="btn btn-danger" onclick="if(confirm('Gerät neustarten?')) window.location.href='/restart'" aria-label="Gerät neustarten">
                    <i class="fas fa-power-off" aria-hidden="true"></i> Neustarten
                </button>
            </div>
        </div>

        <div class="card">
            <h2><i class="fas fa-terminal" aria-hidden="true"></i> System Log</h2>
            <div class="logs" id="logContent" aria-live="polite">Lade Logs...</div>
            <div class="buttons">
                <button class="btn" onclick="refreshLog()" aria-label="Logdaten aktualisieren">
                    <i class="fas fa-sync-alt" aria-hidden="true"></i> Log aktualisieren
                </button>
            </div>
        </div>
    </div>

    <script src="/js/script.js"></script>
</body>
</html>
```

Hinweis: Das verbleibende `style="text-align:center"` auf der score-center-Section ist akzeptabel falls keine CSS-Klasse dafür existiert — alternativ class="section" verwenden und text-align via .score-center in einem `<style>`-Block oder direkt inline. Die Functional-IDs und JS-Hooks müssen identisch bleiben.
  </action>
  <verify>
    <automated>grep -c "score-neutral\|score-sehr-negativ\|score-sehr-positiv\|score-negativ\|score-positiv" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/index.html && grep -c "class=\"card\"" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/index.html && grep -c "id=\"mood-value\"\|id=\"mood-gauge\"\|id=\"mood-class\"\|id=\"light-switch\"\|id=\"brightness\"" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/index.html</automated>
  </verify>
  <done>
    - Score-Anzeige nutzt .score-neutral (und andere score-Klassen) statt Inline-Styles
    - Alle 4 Sektionen sind class="card"
    - Alle erforderlichen IDs (mood-value, mood-gauge, mood-class, light-switch, mode-switch, brightness, leds, color-grid, logContent) sind vorhanden
    - Keine Inline-Styles außer text-align:center für den Score-Bereich (akzeptabel)
  </done>
</task>

</tasks>

<verification>
- firmware/data/index.html enthält keine style="font-size:..." oder style="color:#..." Inline-Styles mehr
- Score-Badge (#mood-class) und Score-Wert (#mood-value) verwenden CSS-Klassen aus style.css
- Alle 4 Karten verwenden class="card"
- Alle IDs für JavaScript-Hooks sind erhalten
</verification>

<success_criteria>
1. IDX-01: Score-Anzeige nutzt .score-* Klassen für 5-stufige Farbkodierung
2. IDX-02: Alle Karten haben class="card" (border-radius 8px via CSS)
3. IDX-03: #light-switch, #mode-switch, #brightness, #color-grid IDs unverändert vorhanden
</success_criteria>

<output>
Nach Abschluss: .planning/phases/23-seiten-redesign/23-01-SUMMARY.md erstellen
</output>
