---
phase: 23-seiten-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/data/setup.html
autonomous: true
requirements:
  - SET-01
  - SET-02

must_haves:
  truths:
    - "Alle Tab-Inhalte (WLAN, MQTT, Einstellungen, Farben, Update, Hardware, Info) sind erreichbar und funktional"
    - "Formular-Elemente (input, label, form-group) nutzen CSS-Klassen aus style.css ohne Inline-Styles"
    - "Update-Tab: Firmware- und UI-Upload mit startFullUpdate() funktioniert unverändert"
    - "Der Info-Tab zeigt Speichernutzung mit Fortschrittsbalken"
  artifacts:
    - path: "firmware/data/setup.html"
      provides: "Einstellungsseite mit Dashboard-konsistenten Formularen"
      contains: "form-group\|card-header\|alert alert-info"
  key_links:
    - from: "firmware/data/setup.html"
      to: "firmware/data/css/style.css"
      via: "<link rel='stylesheet' href='/css/style.css'>"
      pattern: "form-group\|nav-tabs\|tab-content"
---

<objective>
setup.html Inline-Styles entfernen, Formular-Design auf style.css-Klassen vereinheitlichen. Alle 7 Tabs (WLAN, MQTT, Einstellungen, Farben, Update, Hardware, Info) bleiben vollständig funktional. Kein JavaScript-Refactoring.

Purpose: SET-01 (Formular-Design konsistent), SET-02 (alle Einstellungen funktional)
Output: firmware/data/setup.html komplett neu geschrieben
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- CSS-Klassen aus firmware/data/css/style.css die in setup.html verwendet werden -->

Formulare:
  .form-group        — margin-bottom 16px
  .form-row          — flex-row für nebeneinander liegende Felder
  .form-col          — flex: 1 innerhalb form-row
  .help-text         — color: text-muted, font-size 0.8rem
  label              — display: block, font-weight 600, margin-bottom 6px
  input[type="text"] — border, border-radius, padding, full-width
  input[type="password"] — wie text
  input[type="number"]   — wie text

Navigation:
  .nav-tabs          — flex-Liste, border-bottom
  .tab-link          — Einzelner Tab-Link
  .tab-link.active   — aktiver Tab mit Primary-Farbe und border-bottom
  .tab-content       — display: none
  .tab-content.active — display: block

Karten:
  .card              — border-radius 8px, box-shadow, padding 20px
  .card-header       — padding-bottom 12px, border-bottom, margin-bottom 16px

Alerts:
  .alert.alert-info    — blauer Hinweis-Block
  .alert.alert-warning — gelber Warnungs-Block

Utilities:
  .flex              — justify-content: space-between, align-items: center
  .switch + .slider  — Toggle-Switch
  .buttons           — flex, gap 10px
  .btn / .btn-success / .btn-danger / .btn-warning — Buttons
  .hidden            — display: none
  .custom-file       — File-Input-Wrapper
  .custom-file-input — verstecktes file input
  .custom-file-label — Sichtbares Label als Trigger
  .upload-progress   — Fortschrittsbalken-Container
  .upload-progress-bar — Füllung
  .progress-container — Wrapper für Fortschrittsbalken
  .section           — margin-bottom 20px
  .wifi-list         — Liste gefundener WLANs
  .color-settings    — Wrapper für Farbeinstellungen
  .stat-value        — font-weight 600

JavaScript-Hooks (IDs die erhalten bleiben müssen):
  #version, #theme-btn
  Tab-Links: data-tab="wifi|mqtt|api|colors|ui-update|hardware|about"
  Tab-Panels: #wifi-tab, #mqtt-tab, #api-tab, #colors-tab, #ui-update-tab, #hardware-tab, #about-tab

  WLAN-Tab:
    #wifi-ssid, #wifi-pass, #wifi-list, #wifi-status-info, #scan-spinner
    scanWifi(), saveWiFiSettings(), resetWiFi()

  MQTT-Tab:
    #mqtt-enabled, #mqtt-settings, #mqtt-server, #mqtt-user, #mqtt-pass
    saveMQTTSettings()

  API-Tab:
    #api-url, #mood-interval, #dht-enabled, #dht-interval, #test-spinner
    saveAPISettings(), testAPI()

  Farben-Tab:
    #color-settings
    saveColorSettings(), resetDefaultColors()

  Update-Tab (Script INLINE bleibt):
    #ui-file, #ui-file-label, #fw-file, #fw-file-label
    #update-progress, #update-step, #update-bar, #update-status, #update-btn
    #current-version
    startFullUpdate(), checkUpdateReady(), doUpload()

  Hardware-Tab:
    #led-pin, #dht-pin, #num-leds
    saveHardwareSettings()

  Info-Tab:
    #software-version, #firmware-version, #esp-chip, #mac-address
    #storage-progress-bar, #storage-used, #storage-total, #storage-percent, #storage-free
    showAllSettings(), resetAllSettings()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: setup.html komplett neu schreiben — Dashboard-konsistente Formulare, alle 7 Tabs</name>
  <files>firmware/data/setup.html</files>
  <action>
Datei firmware/data/setup.html lesen und komplett neu schreiben. Alle Inline-Styles entfernen. CSS-Klassen aus style.css verwenden. Das gesamte inline JavaScript (Update-Tab: startFullUpdate, checkUpdateReady, doUpload, version-fetch) bleibt vollständig erhalten — keine Zeile JS ändern.

Ziel ist ein sauberes HTML-Dokument ohne style="..." Attribute, das dieselbe Funktionalität wie bisher hat aber visuell dem Dashboard entspricht.

Die wichtigsten strukturellen Punkte:
- Jeder Tab-Inhalt ist in ein class="card" eingebettet
- Formular-Felder verwenden class="form-group" + label + input
- Erläuterungstexte verwenden class="alert alert-info" (statt reinem p)
- Warningtexte verwenden class="alert alert-warning"
- Trennende Überschriften (wie "DHT Konfiguration" in api-tab) verwenden class="card-header"
- Buttons in .buttons-Wrapper
- Toggle-Switches nutzen .flex + .switch + .slider
- Der Update-Fortschrittsbereich #update-progress darf kein style="display:none" mehr haben — alternativ class="hidden" verwenden, aber das JavaScript setzt progress.style.display='block' — daher MUSS der style="display:none" Initial-Wert für #update-progress erhalten bleiben (da JS display: block setzt, kein Klassen-Toggle)

Das vollständige HTML schreiben — es ist ein Komplett-Rewrite der Datei. Inhalt der Datei lesen und dann mit sauberem Markup neu schreiben. Reihenfolge der Tabs und alle data-tab Attribute bleiben identisch.

Wichtig: Der komplette `<script>`-Block zwischen Update-Tab und Hardware-Tab (ca. Zeilen 296–388 im Original) bleibt unverändert an seiner Position. Das Skript enthält die Funktionen checkUpdateReady, startFullUpdate, doUpload und den version-fetch.
  </action>
  <verify>
    <automated>grep -c "class=\"card\"" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/setup.html && grep -c "id=\"wifi-ssid\"\|id=\"mqtt-server\"\|id=\"api-url\"\|id=\"update-btn\"\|id=\"led-pin\"" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/setup.html && grep -c "startFullUpdate\|doUpload\|checkUpdateReady" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/setup.html</automated>
  </verify>
  <done>
    - Alle Tab-Panels haben class="card" als Wrapper
    - Kein Inline-Style außer style="display:none" für #update-progress (JS-Anforderung)
    - Alle Formular-Felder nutzen .form-group
    - Inline-Script mit startFullUpdate/doUpload/checkUpdateReady ist unverändert erhalten
    - Alle IDs (#wifi-ssid, #mqtt-server, #api-url, #led-pin, #update-btn, etc.) vorhanden
  </done>
</task>

</tasks>

<verification>
- Alle 7 Tabs sind per data-tab und zugehörigem id="*-tab" korrekt verknüpft
- Kein style="color:" oder style="font-size:" mehr (außer Update-Progress display:none)
- JavaScript bleibt vollständig erhalten
- Formular-Elemente haben CSS-Klassen statt Inline-Styles
</verification>

<success_criteria>
1. SET-01: Formular-Felder nutzen class="form-group", Buttons class="btn btn-success/btn-danger", Alerts class="alert alert-info/alert-warning"
2. SET-02: Alle 7 Tabs öffnen sich, WiFi/MQTT/API/Farben/Update/Hardware/Info-Funktionen sind bedienbar
</success_criteria>

<output>
Nach Abschluss: .planning/phases/23-seiten-redesign/23-02-SUMMARY.md erstellen
</output>
