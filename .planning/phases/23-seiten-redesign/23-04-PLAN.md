---
phase: 23-seiten-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/data/diagnostics.html
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02

must_haves:
  truths:
    - "System-Info-Karten (.stat-card) für Speicher, CPU, Dateisystem, WiFi sind im Dashboard-Karten-Stil"
    - "Chart-Bereich (Systemmetriken) mit canvas#metrics-chart funktioniert — Chart.js unverändert"
    - "Diagnose-Aktionen (cleanupFiles, resetAllSettings, restartDevice) sind aufrufbar"
    - "Inline-Styles (style='display:flex;flex-direction:column;...') durch CSS-Klassen ersetzt"
    - "Inline-Script (loadMetrics, formatBytes, etc.) ist vollständig erhalten"
  artifacts:
    - path: "firmware/data/diagnostics.html"
      provides: "Diagnosseite mit Dashboard-Karten für System-Metriken"
      contains: "stat-card\|class=\"card\"\|class=\"buttons\""
  key_links:
    - from: "firmware/data/diagnostics.html"
      to: "firmware/data/css/style.css"
      via: "<link rel='stylesheet' href='/css/style.css'>"
      pattern: "stat-card\|dashboard-container\|dashboard-item"
---

<objective>
diagnostics.html Inline-Styles entfernen. System-Info-Karten nutzen .stat-card im Dashboard-Stil. Diagnose-Aktionen-Bereich nutzt .buttons statt style="flex-direction:column". Alle JavaScript-Funktionen bleiben unverändert.

Purpose: DIAG-01 (System-Info-Karten im Dashboard-Stil), DIAG-02 (Update-Bereich funktional)
Output: firmware/data/diagnostics.html komplett neu geschrieben
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- CSS-Klassen aus firmware/data/css/style.css und mood.css die in diagnostics.html verwendet werden -->

Dashboard-Layout (aus mood.css):
  .dashboard-container   — Grid-Wrapper
  .dashboard-item        — einzelnes Panel
  .dashboard-item.full-width — volle Breite
  .chart-header          — flex-Header
  .chart-header-info     — linke Seite chart-header
  .chart-subtitle        — grauer Untertitel
  .chart-container       — Canvas-Wrapper
  .stats-grid            — 4-spaltiges Statistik-Grid
  .stat-card             — einzelne Karte mit Icon + Label + Value
  .stat-icon             — Icon-Zeile
  .stat-label            — Beschriftung (muted)
  .stat-value            — font-weight 600
  .stat-value.positive   — grüne Farbe
  .stat-value.negative   — rote Farbe
  .stat-value.warning    — gelbe Farbe (falls vorhanden)

Style aus style.css:
  .card                  — Karten-Container
  .buttons               — flex, gap 10px, flex-wrap
  .btn                   — Button
  .btn-danger / .btn-warning — rote/gelbe Buttons
  .logs                  — Scrollbox
  .header                — flex, justify-content: space-between
  .title / .version      — Titel + kleine Versionsanzeige

Dark-Mode-spezifische Inline-Styles in diagnostics.html:
  <style>
    .dark details summary { color: #adb5bd; }
    .dark details > div { border-color: #495057; }
  </style>
  Diese können entfernt werden, da details/summary nicht in der Seite verwendet werden.

JavaScript (ALLE Funktionen erhalten):
  loadMetrics()          — fetch('/api/system/metrics'), updateMetricsChart()
  updateMetricsChart()   — Chart.js Bar-Chart mit metrics-chart canvas
  runFullDiagnostics()   — fetch('/api/system/diagnose')
  cleanupFiles()         — fetch('/api/system/cleanup')
  runArchiveProcess()    — fetch('/api/archives/process')
  resetAllSettings()     — fetch('/factoryreset', POST)
  restartDevice()        — window.location.href='/restart'
  formatBytes()          — Bytes-Formatter
  formatTime()           — Sekunden-Formatter
  DOMContentLoaded       — dark mode, loadMetrics(), setInterval(30s)

IDs die erhalten bleiben müssen:
  #version, #theme-btn, #system-status-text
  #metrics-chart
  #free-heap, #heap-frag
  #cpu-temp, #uptime
  #fs-usage, #fs-free
  #wifi-signal, #wifi-status
  #system-log

Problem: #wifi-status wird in diagnostics.html UND index.html verwendet — das ist OK, beide Seiten sind getrennte Dokumente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: diagnostics.html neu schreiben — Inline-Styles entfernen, Diagnose-Aktionen mit .buttons</name>
  <files>firmware/data/diagnostics.html</files>
  <action>
Datei firmware/data/diagnostics.html lesen und komplett neu schreiben.

HTML-Änderungen:
1. Den `<style>`-Block im `<head>` (dark details...) entfernen — details/summary wird nicht verwendet
2. Den Diagnose-Aktionen-Bereich (aktuell mit `style="margin-top: 20px; display: flex; flex-direction: column; gap: 15px;"`) ersetzen:
   - Statt eines einzigen .buttons-Divs mit flex-direction:column, jeden Button in einem separaten .section-Wrapper oder die Buttons in einem .buttons-Div ohne den Column-Style
   - Alternativ: .buttons-Klasse verwenden (flex, gap 10px, flex-wrap) — die Buttons stacken auf Mobile automatisch

   Konkret:
   ```html
   <div class="buttons" style="flex-direction:column">
   ```
   → class="buttons" reicht, das flex-direction:column entfernen. Die Buttons passen bei wenig Breite automatisch in eine Spalte durch flex-wrap.

3. Die zweite .dashboard-item (Log + Diagnose-Aktionen) strukturell beibehalten — kein style="..." mehr

4. Die Systemmetriken-Karten (.stats-grid / .stat-card) haben derzeit Doppel-Werte (Label + Value für 2 Metriken):
   ```html
   <div class="stat-card">
     <div class="stat-icon">...</div>
     <div class="stat-label">Freier Speicher</div>
     <div class="stat-value" id="free-heap">--</div>
     <div class="stat-label">Fragmentierung</div>
     <div class="stat-value" id="heap-frag">--</div>
   </div>
   ```
   Diese Struktur ist funktional und bleibt erhalten — kein Umbau.

JavaScript-Änderungen:
- Keines. Das gesamte `<script>`-Block bleibt byte-für-byte erhalten.
- Einzige Ausnahme: Der DOMContentLoaded-Block lädt dark mode aus localStorage — das bleibt ebenfalls unverändert.

Vollständige Struktur der neuen Datei:
- `<!DOCTYPE html>` + head mit style.css, mood.css, Chart.js, moment.js, Font Awesome, favicon
- body: header > .title + .header-controls
- .dashboard-container > .dashboard-item.full-width (chart-header + button-row + canvas#metrics-chart)
- .stats-grid > 4x .stat-card
- .dashboard-container > .dashboard-item (logs) + .dashboard-item (diagnose-aktionen)
- Vollständiger `<script>`-Block unverändert
  </action>
  <verify>
    <automated>grep -c "id=\"free-heap\"\|id=\"cpu-temp\"\|id=\"fs-usage\"\|id=\"wifi-signal\"\|id=\"metrics-chart\"\|id=\"system-log\"" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/diagnostics.html && grep -c "loadMetrics\|updateMetricsChart\|cleanupFiles\|resetAllSettings" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/diagnostics.html && grep "style=" /Users/simonluthe/Documents/auraos-moodlight/firmware/data/diagnostics.html | grep -v "display:none" | wc -l</automated>
  </verify>
  <done>
    - Alle IDs (#free-heap, #cpu-temp, #fs-usage, #wifi-signal, #metrics-chart, #system-log) vorhanden
    - JavaScript-Funktionen (loadMetrics, updateMetricsChart, cleanupFiles, resetAllSettings) erhalten
    - Keine Inline-Styles außer unvermeidlichen (z.B. display:none falls vorhanden)
    - Dritte grep-Ausgabe: 0 oder nur unvermeidliche style-Attribute
  </done>
</task>

</tasks>

<verification>
- diagnostics.html enthält keine style="display:flex;flex-direction:column..." mehr
- Der `<style>`-Block für details/summary ist entfernt
- Chart.js-Canvas (#metrics-chart) und alle stat-value IDs sind vorhanden
- Alle JavaScript-Funktionen im Inline-Script sind unverändert
</verification>

<success_criteria>
1. DIAG-01: .stats-grid + .stat-card für Speicher, CPU, Dateisystem, WiFi im Dashboard-Stil (keine Inline-Styles)
2. DIAG-02: loadMetrics(), Chart.js-Chart, Diagnose-Aktionen (cleanup, reset, restart) sind vollständig funktional
</success_criteria>

<output>
Nach Abschluss: .planning/phases/23-seiten-redesign/23-04-SUMMARY.md erstellen
</output>
