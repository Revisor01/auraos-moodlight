---
phase: 02-backend-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/shared_config.py
  - sentiment-api/app.py
  - sentiment-api/background_worker.py
  - sentiment-api/moodlight_extensions.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "RSS-Feed-Liste existiert nur einmal in shared_config.py — app.py und background_worker.py importieren von dort"
    - "Score 0.35 ergibt ueberall 'sehr positiv', Score 0.15 ergibt 'positiv' — konsistente Kategorien"
    - "/api/dashboard und /api/logs geben 404 zurueck"
  artifacts:
    - path: "sentiment-api/shared_config.py"
      provides: "RSS_FEEDS dict und get_sentiment_category() Funktion"
      contains: "RSS_FEEDS"
    - path: "sentiment-api/app.py"
      provides: "Importiert RSS_FEEDS und get_sentiment_category aus shared_config"
      contains: "from shared_config import"
    - path: "sentiment-api/background_worker.py"
      provides: "Importiert RSS_FEEDS und get_sentiment_category aus shared_config"
      contains: "from shared_config import"
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "Importiert get_sentiment_category als get_category_from_score"
      contains: "from shared_config import"
  key_links:
    - from: "sentiment-api/app.py"
      to: "sentiment-api/shared_config.py"
      via: "Python import"
      pattern: "from shared_config import"
    - from: "sentiment-api/background_worker.py"
      to: "sentiment-api/shared_config.py"
      via: "Python import"
      pattern: "from shared_config import"
    - from: "sentiment-api/moodlight_extensions.py"
      to: "sentiment-api/shared_config.py"
      via: "Python import"
      pattern: "from shared_config import get_sentiment_category as get_category_from_score"
---

<objective>
Shared Config erstellen und duplizierte Definitionen konsolidieren

Purpose: RSS-Feed-Liste und Sentiment-Kategorie-Thresholds existieren dreifach mit abweichenden Werten. Eine Single Source of Truth verhindert inkonsistente Kategorisierung. Gleichzeitig werden tote API-Endpoints entfernt.
Output: sentiment-api/shared_config.py als einzige Definition, alle drei Consumer importieren von dort, /api/dashboard und /api/logs entfernt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-hardening/02-RESEARCH.md

@sentiment-api/app.py
@sentiment-api/background_worker.py
@sentiment-api/moodlight_extensions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared_config.py erstellen und Imports in allen Consumern einrichten</name>
  <files>sentiment-api/shared_config.py, sentiment-api/app.py, sentiment-api/background_worker.py, sentiment-api/moodlight_extensions.py</files>
  <read_first>sentiment-api/app.py, sentiment-api/background_worker.py, sentiment-api/moodlight_extensions.py</read_first>
  <action>
1. Neue Datei `sentiment-api/shared_config.py` erstellen mit exakt diesem Inhalt:

```python
# sentiment-api/shared_config.py
# Einzige Quelle fuer RSS-Feed-Liste und Sentiment-Kategorisierung

RSS_FEEDS = {
    "Zeit": "https://newsfeed.zeit.de/index",
    "Tagesschau": "https://www.tagesschau.de/xml/rss2",
    "Sueddeutsche": "https://rss.sueddeutsche.de/rss/Alles",
    "FAZ": "https://www.faz.net/rss/aktuell/",
    "Die Welt": "https://www.welt.de/feeds/latest.rss",
    "Handelsblatt": "https://www.handelsblatt.com/contentexport/feed/schlagzeilen",
    "n-tv": "https://www.n-tv.de/rss",
    "Focus": "https://rss.focus.de/fol/XML/rss_folnews.xml",
    "Stern": "https://www.stern.de/feed/standard/alle-nachrichten/",
    "Telekom": "https://www.t-online.de/feed.rss",
    "TAZ": "https://taz.de/!p4608;rss/",
    "Deutschlandfunk": "https://www.deutschlandfunk.de/nachrichten-100.rss"
}


def get_sentiment_category(score: float) -> str:
    """Bestimmt Sentiment-Kategorie aus Score. Einzige Definition im gesamten Projekt."""
    if score >= 0.30:
        return "sehr positiv"
    elif score >= 0.10:
        return "positiv"
    elif score >= -0.20:
        return "neutral"
    elif score >= -0.50:
        return "negativ"
    else:
        return "sehr negativ"
```

Die Thresholds sind 0.30/0.10/-0.20/-0.50 — das sind die canonical Werte aus background_worker.py und moodlight_extensions.py. NICHT die abweichenden app.py Werte (0.85/0.2).

2. In `sentiment-api/app.py`:
   - Am Anfang der Imports (nach den bestehenden imports) hinzufuegen: `from shared_config import RSS_FEEDS, get_sentiment_category`
   - Das lokale `rss_feeds = {...}` Dict (ca. Zeile 55-68) komplett entfernen und ersetzen durch: `rss_feeds = RSS_FEEDS`
   - Die lokale Kategorie-Logik (ca. Zeile 205-209, das `if score > 0.85: strength = "sehr_positiv"...` Pattern) ersetzen durch Aufruf von `get_sentiment_category(score)`. ACHTUNG: Pruefen ob die Variable `strength` oder ein anderer Name verwendet wird und den Returnwert entsprechend zuweisen.

3. In `sentiment-api/background_worker.py`:
   - Am Modul-Anfang (bei den Imports) hinzufuegen: `from shared_config import RSS_FEEDS, get_sentiment_category`
   - Das lokale `rss_feeds = {...}` Dict (ca. Zeile 137-150) in der `_fetch_headlines()` Methode entfernen und stattdessen `RSS_FEEDS` verwenden (entweder direkt oder `rss_feeds = RSS_FEEDS` als Modul-Level Alias)
   - Die lokale `_get_category()` Methode (ca. Zeile 216-227) entfernen und stattdessen `get_sentiment_category` verwenden. Alle Aufrufe von `self._get_category(score)` durch `get_sentiment_category(score)` ersetzen.

4. In `sentiment-api/moodlight_extensions.py`:
   - Am Anfang hinzufuegen: `from shared_config import get_sentiment_category as get_category_from_score`
   - Die lokale `get_category_from_score()` Funktion (ca. Zeile 24-35) komplett entfernen — der Import-Alias uebernimmt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "from shared_config import RSS_FEEDS, get_sentiment_category; assert len(RSS_FEEDS) == 12; assert get_sentiment_category(0.35) == 'sehr positiv'; assert get_sentiment_category(0.15) == 'positiv'; assert get_sentiment_category(0.0) == 'neutral'; assert get_sentiment_category(-0.3) == 'negativ'; assert get_sentiment_category(-0.6) == 'sehr negativ'; print('shared_config OK')" && python -c "import ast; tree = ast.parse(open('app.py').read()); imports = [n for n in ast.walk(tree) if isinstance(n, ast.ImportFrom) and n.module == 'shared_config']; assert len(imports) > 0, 'app.py missing shared_config import'; print('app.py import OK')" && python -c "import ast; tree = ast.parse(open('background_worker.py').read()); imports = [n for n in ast.walk(tree) if isinstance(n, ast.ImportFrom) and n.module == 'shared_config']; assert len(imports) > 0, 'background_worker.py missing shared_config import'; print('background_worker.py import OK')" && python -c "import ast; tree = ast.parse(open('moodlight_extensions.py').read()); imports = [n for n in ast.walk(tree) if isinstance(n, ast.ImportFrom) and n.module == 'shared_config']; assert len(imports) > 0; print('moodlight_extensions.py import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `sentiment-api/shared_config.py` existiert mit `RSS_FEEDS` (12 Eintraege) und `get_sentiment_category()` (Thresholds 0.30/0.10/-0.20/-0.50)
    - `grep -c "from shared_config import" sentiment-api/app.py` ergibt mindestens 1
    - `grep -c "from shared_config import" sentiment-api/background_worker.py` ergibt mindestens 1
    - `grep -c "from shared_config import" sentiment-api/moodlight_extensions.py` ergibt mindestens 1
    - Kein lokales `rss_feeds = {` Dict mehr in app.py (ausser dem Alias `rss_feeds = RSS_FEEDS`)
    - Keine lokale `_get_category` Methode mehr in background_worker.py
    - Keine lokale `get_category_from_score` Funktion mehr in moodlight_extensions.py
  </acceptance_criteria>
  <done>RSS_FEEDS und get_sentiment_category() existieren einmalig in shared_config.py. Alle drei Consumer importieren von dort. Score 0.35 ergibt ueberall "sehr positiv".</done>
</task>

<task type="auto">
  <name>Task 2: Tote API-Endpoints /api/dashboard und /api/logs entfernen</name>
  <files>sentiment-api/app.py</files>
  <read_first>sentiment-api/app.py</read_first>
  <action>
In `sentiment-api/app.py` die beiden toten Routen komplett entfernen (ca. Zeilen 346-354):

1. Den `/api/dashboard` Route-Block entfernen:
```python
@app.route('/api/dashboard')
def get_dashboard_data():
  # Hier Daten fuer das Dashboard generieren
  return jsonify({...})
```

2. Den `/api/logs` Route-Block entfernen:
```python
@app.route('/api/logs')
def get_logs():
  # Hier Logs bereitstellen
  return jsonify({...})
```

Beide Funktionsdefinitionen inklusive Dekorator komplett loeschen. Nach dem Entfernen gibt Flask automatisch 404 zurueck.

WICHTIG: Die exakten Zeilennummern koennen sich durch Task 1 verschoben haben — nach dem Funktionsnamen `get_dashboard_data` und `get_logs` suchen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "import ast; tree = ast.parse(open('app.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'get_dashboard_data' not in funcs, 'get_dashboard_data still exists'; assert 'get_logs' not in funcs, 'get_logs still exists'; print('Dead endpoints removed OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/dashboard" sentiment-api/app.py` ergibt 0
    - `grep -c "api/logs" sentiment-api/app.py` ergibt 0
    - `grep -c "get_dashboard_data" sentiment-api/app.py` ergibt 0
    - `grep -c "get_logs" sentiment-api/app.py` ergibt 0
    - app.py ist weiterhin syntaktisch valide (python -c "import ast; ast.parse(open('app.py').read())")
  </acceptance_criteria>
  <done>/api/dashboard und /api/logs existieren nicht mehr in app.py. Beide URLs geben 404 zurueck.</done>
</task>

</tasks>

<verification>
- `python -c "from shared_config import RSS_FEEDS, get_sentiment_category"` importiert fehlerfrei
- `get_sentiment_category(0.35)` == "sehr positiv" in allen Kontexten
- Kein lokales rss_feeds Dict mehr in app.py oder background_worker.py
- Kein lokales get_category/get_category_from_score mehr in background_worker.py oder moodlight_extensions.py
- /api/dashboard und /api/logs Routen nicht mehr in app.py vorhanden
</verification>

<success_criteria>
- shared_config.py ist die einzige Definition fuer RSS_FEEDS und Sentiment-Kategorien
- Alle drei Consumer (app.py, background_worker.py, moodlight_extensions.py) importieren von shared_config
- Tote Endpoints entfernt
- Alle Python-Dateien syntaktisch valide
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-hardening/02-01-SUMMARY.md`
</output>
