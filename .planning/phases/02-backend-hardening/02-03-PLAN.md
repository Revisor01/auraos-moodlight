---
phase: 02-backend-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: [REPO-01, REPO-02]

must_haves:
  truths:
    - ".env ist in .gitignore — versehentliches Committen von Secrets unmoeglich"
    - "releases/ ist in .gitignore und aus dem Git-Index entfernt"
    - "*.tmp.html ist in .gitignore"
    - "setup.html.tmp.html existiert nicht mehr als tracked file"
  artifacts:
    - path: ".gitignore"
      provides: "Eintraege fuer .env, releases/, *.tmp.html"
      contains: ".env"
  key_links:
    - from: ".gitignore"
      to: "sentiment-api/.env"
      via: "Git ignore rule"
      pattern: "\\.env"
---

<objective>
Repository-Hygiene: .gitignore ergaenzen und getrackte Artefakte entfernen

Purpose: .env (mit API-Keys und DB-Passwoertern) fehlt in .gitignore — Risiko fuer versehentliches Committen. releases/ enthaelt Binaerdateien die via GitHub Releases verteilt werden sollten, nicht im Repo. setup.html.tmp.html ist eine temporaere Datei die nicht ins Repo gehoert.
Output: .gitignore mit .env, releases/, *.tmp.html. Getrackte Dateien aus Git-Index entfernt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore ergaenzen und getrackte Dateien aus Git-Index entfernen</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
1. In `.gitignore` am Ende folgende Bloecke hinzufuegen:

```gitignore

# Secrets — API-Keys und DB-Passwoerter
.env
sentiment-api/.env

# Releases (Binaries via GitHub Releases, nicht im Repo)
releases/

# Temporaere HTML-Dateien
*.tmp.html
```

2. Getrackte Dateien aus dem Git-Index entfernen (Dateien bleiben auf dem Filesystem, werden aber nicht mehr getrackt):

```bash
# Temporaere HTML-Datei aus Index entfernen
git rm --cached firmware/data/setup.html.tmp.html 2>/dev/null || true

# releases/ Verzeichnis aus Index entfernen (falls getrackt)
git rm -r --cached releases/ 2>/dev/null || true

# .env aus Index entfernen (falls getrackt — wahrscheinlich nicht, aber sicher ist sicher)
git rm --cached .env 2>/dev/null || true
git rm --cached sentiment-api/.env 2>/dev/null || true
```

3. Verifizieren mit `git status` dass die Dateien als "deleted" im Staging-Bereich erscheinen (nur aus Index entfernt, nicht vom Filesystem).

HINWEIS: Die Binaerdateien bleiben in der Git-History — History-Bereinigung (BFG/filter-branch) ist out of scope fuer diesen Milestone.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -q "^\.env$" .gitignore && echo ".env in gitignore OK" && grep -q "^releases/" .gitignore && echo "releases/ in gitignore OK" && grep -q "tmp\.html" .gitignore && echo "*.tmp.html in gitignore OK" && python3 -c "
import subprocess
result = subprocess.run(['git', 'ls-files', '--cached', 'firmware/data/setup.html.tmp.html'], capture_output=True, text=True)
if result.stdout.strip():
    print('FAIL: setup.html.tmp.html still tracked')
    exit(1)
print('setup.html.tmp.html untracked OK')
result2 = subprocess.run(['git', 'ls-files', '--cached', 'releases/'], capture_output=True, text=True)
if result2.stdout.strip():
    print('FAIL: releases/ still tracked')
    exit(1)
print('releases/ untracked OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^\.env$" .gitignore` findet einen Treffer
    - `grep "^sentiment-api/\.env$" .gitignore` findet einen Treffer
    - `grep "^releases/" .gitignore` findet einen Treffer
    - `grep "tmp\.html" .gitignore` findet einen Treffer
    - `git ls-files --cached firmware/data/setup.html.tmp.html` gibt keine Ausgabe (nicht mehr getrackt)
    - `git ls-files --cached releases/` gibt keine Ausgabe (nicht mehr getrackt)
  </acceptance_criteria>
  <done>.env, releases/ und *.tmp.html sind in .gitignore. Bisher getrackte Dateien (setup.html.tmp.html, releases/*) sind aus dem Git-Index entfernt.</done>
</task>

</tasks>

<verification>
- .gitignore enthaelt .env, sentiment-api/.env, releases/, *.tmp.html
- setup.html.tmp.html ist nicht mehr getrackt
- releases/ Inhalt ist nicht mehr getrackt
- git status zeigt keine .env Dateien als untracked (weil gitignore greift)
</verification>

<success_criteria>
- Kein Risiko mehr dass .env versehentlich committed wird
- Keine Binaerdateien (releases/) mehr im Git-Index
- Keine temporaeren HTML-Dateien mehr getrackt
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-hardening/02-03-SUMMARY.md`
</output>
