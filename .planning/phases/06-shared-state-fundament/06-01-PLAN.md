---
phase: 06-shared-state-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/app_state.h
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "app_state.h existiert und definiert ein AppState-Struct mit allen geteilten globalen Variablen"
    - "AppState ist in logische Gruppen unterteilt (WiFi, LED, Sentiment, MQTT, DHT, System)"
    - "pio run baut fehlerfrei mit dem neuen Header (noch keine Migration)"
  artifacts:
    - path: "firmware/src/app_state.h"
      provides: "Zentrales AppState-Struct fuer alle Module"
      contains: "struct AppState"
  key_links:
    - from: "firmware/src/app_state.h"
      to: "firmware/src/config.h"
      via: "#include config.h fuer DEFAULT_* Konstanten"
      pattern: '#include "config.h"'
---

<objective>
AppState-Struct vollstaendig definieren — alle globalen Variablen aus moodlight.cpp als Struct-Member abbilden, gruppiert nach zukuenftigem Modul-Schnitt.

Purpose: Definiertes Interface fuer Phase 7 Modul-Extraktion. Module greifen auf AppState zu, nicht auf lose extern-Variablen.
Output: `firmware/src/app_state.h` mit vollstaendigem AppState-Struct und einer globalen `extern AppState appState;` Deklaration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@firmware/src/config.h
@firmware/src/moodlight.cpp (first 230 lines — all global variable declarations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppState-Struct in app_state.h definieren</name>
  <files>firmware/src/app_state.h</files>
  <read_first>firmware/src/moodlight.cpp (lines 1-230 and 318-381 for utility instances and JsonBufferPool)</read_first>
  <action>
Erstelle `firmware/src/app_state.h` mit folgendem Inhalt:

1. Include-Guard `#ifndef APP_STATE_H` / `#define APP_STATE_H`
2. `#include "config.h"` fuer DEFAULT_* Konstanten
3. `#include <Arduino.h>` fuer String, uint32_t etc.
4. Forward-Deklaration: Keine ArduinoHA/NeoPixel/DHT-Includes — diese Hardware-Objekte bleiben ausserhalb des AppState (sie sind nicht "shared state" sondern Library-Instanzen).

Das `struct AppState` enthaelt diese Member, gruppiert nach Modul:

**WiFi-Gruppe:**
- `String wifiSSID = ""`
- `String wifiPassword = ""`
- `bool wifiConfigured = false`
- `bool isInConfigMode = false`
- `bool wifiReconnectActive = false`
- `unsigned long disconnectStartMs = 0`
- `unsigned long lastWifiCheck = 0`
- `unsigned long wifiReconnectDelay = 5000`
- `int wifiReconnectAttempts = 0`
- `bool wifiWasConnected = false`
- `unsigned long apModeStartTime = 0`

**LED-Gruppe:**
- `int ledPin = DEFAULT_LED_PIN`
- `int numLeds = DEFAULT_NUM_LEDS`
- `uint8_t manualBrightness = DEFAULT_LED_BRIGHTNESS`
- `bool lightOn = true`
- `bool autoMode = true`
- `uint32_t manualColor = 0x00FFFFFF` (initialisiert als weiss, wird spaeter von Preferences ueberschrieben — NICHT pixels.Color() aufrufen da pixels hier nicht existiert)
- `int currentLedIndex = 2`
- `int lastLedIndex = 2`
- `volatile bool ledUpdatePending = false`
- `volatile uint32_t ledColors[MAX_LEDS]` (initialisiert mit `= {0}`)
- `volatile uint8_t ledBrightness = DEFAULT_LED_BRIGHTNESS`
- `volatile bool ledClear = false`
- `SemaphoreHandle_t ledMutex = NULL`
- `uint32_t customColors[5] = {0xFF0000, 0xFFA500, 0x1E90FF, 0x545DF0, 0x8A2BE2}`
- `bool firstLedShowDone = false`
- `bool isPulsing = false`
- `unsigned long pulseStartTime = 0`
- `int statusLedIndex = DEFAULT_NUM_LEDS - 1`
- `unsigned long statusLedBlinkStart = 0`
- `bool statusLedState = false`
- `int statusLedMode = 0`

**Sentiment-Gruppe:**
- `float sentimentScore = 0.0` (umbenennen von lastSentimentScore)
- `String sentimentCategory = "neutral"` (umbenennen von lastSentimentCategory)
- `unsigned long moodUpdateInterval = DEFAULT_MOOD_UPDATE_INTERVAL`
- `unsigned long lastMoodUpdate = 0`
- `bool initialAnalysisDone = false`
- `unsigned long lastSuccessfulSentimentUpdate = 0`
- `bool sentimentAPIAvailable = true`
- `int consecutiveSentimentFailures = 0`
- `String apiUrl = DEFAULT_NEWS_API_URL`

**MQTT-Gruppe:**
- `String mqttServer = ""`
- `String mqttUser = ""`
- `String mqttPassword = ""`
- `bool mqttEnabled = false`
- `unsigned long lastMqttReconnectAttempt = 0`
- `unsigned long lastMqttHeartbeat = 0`
- `bool mqttWasConnected = false`
- `bool sendingInitialStates = false`

**DHT/Sensor-Gruppe:**
- `bool dhtEnabled = true`
- `int dhtPin = DEFAULT_DHT_PIN`
- `unsigned long dhtUpdateInterval = DEFAULT_DHT_READ_INTERVAL`
- `unsigned long lastDHTUpdate = 0`
- `float currentTemp = NAN`
- `float currentHum = NAN`

**System-Gruppe:**
- `bool rebootNeeded = false`
- `unsigned long rebootTime = 0`
- `bool initialStartupPhase = true`
- `unsigned long startupTime = 0`
- `bool timeInitialized = false`
- `unsigned long lastSystemHealthCheckTime = 0`
- `unsigned long lastStatusLog = 0`
- `bool settingsNeedSaving = false`
- `unsigned long lastSettingsSaved = 0`

**Log-Gruppe:**
- `String logBuffer[20]` (verwende Literal statt LOG_BUFFER_SIZE — der Wert ist im Struct fix)
- `int logIndex = 0`

WICHTIG — Was NICHT ins AppState gehoert (bleibt als separate Globals):
- Library-Instanzen: `pixels`, `dht`, `preferences`, `server`, `dnsServer`
- ArduinoHA-Objekte: `device`, `mqtt`, `haLight`, `haSentimentScore`, etc.
- WiFiClient-Instanzen: `wifiClientHA`, `wifiClientHTTP`
- Utility-Instanzen: `watchdog`, `memMonitor`, `fileOps`, `netDiag`, `sysHealth`
- `JsonBufferPool jsonPool` (eigene Struct mit eigenem Mutex)
- Konstanten: `SOFTWARE_VERSION`, `MAX_RECONNECT_DELAY`, `SENTIMENT_FALLBACK_TIMEOUT`, `MAX_SENTIMENT_FAILURES`, `STARTUP_GRACE_PERIOD`, `STATUS_LED_GRACE_MS`, `STATUS_LOG_INTERVAL`, `AP_TIMEOUT`, `REBOOT_DELAY`, `MQTT_HEARTBEAT_INTERVAL`, `HEALTH_CHECK_INTERVAL`, `DNS_PORT`, `LOG_BUFFER_SIZE`, `JSON_BUFFER_SIZE`, `JSON_BUFFER_COUNT`, `ntpServer`, `gmtOffset_sec`, `daylightOffset_sec` — Konstanten gehoeren nach config.h oder bleiben lokal
- `ColorDefinition` struct und `colorNames[]` — bleiben als standalone, werden spaeter nach led_controller.h verschoben
- `mac[6]` — wird nur einmal in setupHA() gelesen
- `floatToString()` — Hilfsfunktion, kein State

Am Ende der Datei:
```cpp
extern AppState appState;
```

Kommentare in Deutsch. Jede Gruppe mit einem Kommentar-Header.

NICHT: `AppState appState;` Definition — das kommt in Plan 02 in moodlight.cpp.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "struct AppState" firmware/src/app_state.h` gibt 1 zurueck
- `grep "wifiSSID" firmware/src/app_state.h` findet den WiFi-Member
- `grep "ledPin" firmware/src/app_state.h` findet den LED-Member
- `grep "sentimentScore" firmware/src/app_state.h` findet den Sentiment-Member
- `grep "mqttServer" firmware/src/app_state.h` findet den MQTT-Member
- `grep "dhtEnabled" firmware/src/app_state.h` findet den DHT-Member
- `grep "rebootNeeded" firmware/src/app_state.h` findet den System-Member
- `grep "extern AppState appState" firmware/src/app_state.h` findet die extern-Deklaration
- `pio run` baut fehlerfrei (Header wird noch nicht included, daher kein Impact)
  </acceptance_criteria>
  <done>app_state.h existiert mit vollstaendigem AppState-Struct, alle ~60 geteilten Variablen als Member definiert, pio run baut durch</done>
</task>

</tasks>

<verification>
- `firmware/src/app_state.h` existiert
- Struct enthaelt alle Variablen-Gruppen (WiFi, LED, Sentiment, MQTT, DHT, System, Log)
- `pio run` in firmware/ baut fehlerfrei
</verification>

<success_criteria>
AppState-Struct ist vollstaendig definiert und ready fuer die Migration in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-state-fundament/06-01-SUMMARY.md`
</output>
