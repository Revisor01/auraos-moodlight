---
phase: 08-konsolidierung-qualitaet
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - firmware/src/moodlight.cpp
  - firmware/src/led_controller.h
  - firmware/src/led_controller.cpp
  - firmware/src/web_server.h
  - firmware/src/web_server.cpp
autonomous: true
requirements: [ARCH-02, ARCH-03]

must_haves:
  truths:
    - "moodlight.cpp hat maximal 200 Zeilen"
    - "setup() enthaelt nur Modul-Init-Aufrufe, keine Inline-Logik"
    - "loop() enthaelt nur Modul-Aufrufe und Timing-Guards, keine Business-Logik"
    - "pio run baut fehlerfrei ohne Linking-Fehler"
  artifacts:
    - path: "firmware/src/moodlight.cpp"
      provides: "Schlanke Orchestrierung: setup() + loop()"
      max_lines: 200
    - path: "firmware/src/led_controller.h"
      provides: "updatePulse() Deklaration"
      contains: "updatePulse"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/led_controller.h"
      via: "updatePulse() Aufruf in loop()"
      pattern: "updatePulse"
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/web_server.h"
      via: "runSystemHealthCheck() Aufruf in loop()"
      pattern: "runSystemHealthCheck"
---

<objective>
moodlight.cpp auf max 200 Zeilen reduzieren durch Extraktion der verbleibenden Inline-Logik in bestehende Module.

Purpose: Die letzten grossen Code-Bloecke in loop() (Pulse-Animation ~38 Zeilen, System-Health-Check ~110 Zeilen) und in setup() (MQTT-Init-Block ~35 Zeilen, Watchdog/Utility-Init) werden in Modul-Funktionen verschoben. moodlight.cpp wird zur reinen Orchestrierungsdatei.
Output: moodlight.cpp <= 200 Zeilen, nur setup()/loop() mit Modul-Aufrufen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-konsolidierung-qualitaet/08-02-SUMMARY.md
@firmware/src/moodlight.cpp
@firmware/src/led_controller.h
@firmware/src/web_server.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pulse-Logik und Health-Check in Module extrahieren</name>
  <files>firmware/src/led_controller.h, firmware/src/led_controller.cpp, firmware/src/web_server.h, firmware/src/web_server.cpp, firmware/src/moodlight.cpp</files>
  <action>
**read_first:** firmware/src/moodlight.cpp (vollstaendig, Stand nach Plan 02), firmware/src/led_controller.h, firmware/src/led_controller.cpp (erste 20 Zeilen), firmware/src/web_server.h, firmware/src/web_server.cpp (erste 40 Zeilen)

**Step 1 — Pulse-Animation nach led_controller extrahieren:**

Neue Funktion `updatePulse()` in led_controller.h/.cpp:
- Deklaration in led_controller.h: `void updatePulse();`
- Implementierung in led_controller.cpp: Verschiebe den gesamten Pulse-Block aus loop() (ca. Zeilen 509-546 im Original, nach Plan 01/02 andere Zeilennummern)
- Die Funktion prueft `appState.isPulsing && appState.lightOn` intern
- Verwendet `static unsigned long lastPulseUpdate` intern
- Benoetigt `#include <math.h>` fuer `sin()`, `fmod()` (pruefen ob schon inkludiert)
- Verwendet config.h Konstanten: `LOOP_PULSE_UPDATE_MS`, `DEFAULT_WAVE_DURATION`, `PULSE_AUTO_DISABLE_CYCLES`, `DEFAULT_WAVE_MIN_BRIGHTNESS`, `DEFAULT_WAVE_MAX_BRIGHTNESS`, `DEFAULT_LED_BRIGHTNESS`

In moodlight.cpp loop(): Ersetze den gesamten Pulse-Block durch: `updatePulse();`

**Step 2 — System-Health-Check nach web_server extrahieren:**

Neue Funktion `runSystemHealthCheck()` in web_server.h/.cpp:
- Deklaration in web_server.h: `void runSystemHealthCheck();`
- Implementierung in web_server.cpp: Verschiebe den gesamten Health-Check-Block aus loop() (ca. Zeilen 548-658 im Original)
- Die Funktion enthaelt:
  - Memory-Analyse (`memMonitor.update()`)
  - Systemstatistiken als JSON in LittleFS speichern
  - sysHealth.update() und Restart-Empfehlung
  - Geplanten Neustart pruefen (Preferences "syshealth")
  - Speicherplatz-Pruefung
  - `appState.lastSystemHealthCheckTime = currentMillis;`
- Benoetigt extern-Zugriff auf: `memMonitor`, `sysHealth`, `preferences` (diese sind bereits in ihren jeweiligen Modulen oder noch in moodlight.cpp)
- WICHTIG: `watchdog`, `memMonitor`, `netDiag`, `sysHealth` Instanzen muessen fuer die Funktion erreichbar sein. Optionen:
  a) Instanzen als Parameter uebergeben (sauber aber verbose)
  b) Instanzen in web_server.cpp als extern deklarieren (pragmatisch)
  c) Instanzen nach web_server.cpp verschieben (am besten)

Empfehlung: Option (c) — Verschiebe `WatchdogManager watchdog`, `MemoryMonitor memMonitor`, `SafeFileOps fileOps`, `NetworkDiagnostics netDiag`, `SystemHealthCheck sysHealth` nach web_server.cpp. Fuege in web_server.h hinzu:
```cpp
extern WatchdogManager watchdog;
extern MemoryMonitor memMonitor;
extern SafeFileOps fileOps;
extern NetworkDiagnostics netDiag;
extern SystemHealthCheck sysHealth;
```
web_server.h muss dann `#include "MoodlightUtils.h"` inkludieren (fuer die Typen).
In moodlight.cpp: Entferne die Instanz-Definitionen, inkludiere web_server.h (schon vorhanden).

In moodlight.cpp loop(): Ersetze den gesamten Health-Check-Block (ca. 110 Zeilen) durch:
```cpp
if (millis() - appState.lastSystemHealthCheckTime >= HEALTH_CHECK_INTERVAL) {
    runSystemHealthCheck();
}
```

**Step 3 — setup() verschlanken:**

a) MQTT-Init-Block (Zeilen ~337-370) in eine neue Funktion `initMQTT()` in mqtt_handler extrahieren:
   NEIN — dieser Block ist zu eng mit setup()-Fluss verknuepft (Reihenfolge WiFi->NTP->Server->MQTT). STATTDESSEN: Block als `connectMQTTOnStartup()` in mqtt_handler.h/.cpp verschieben.

   Aber: Analyse zeigt, dass setup() ca. 210 Zeilen hat. Nach Entfernung der MQTT-Block (~35 Zeilen) und der Utility-Init-Zeilen (~20 Zeilen) bleiben ~155 Zeilen. Das ist akzeptabel.

   ENTSCHEIDUNG: setup() Inline-Logik nur verschieben wenn noetig fuer das 200-Zeilen-Ziel. Zuerst Zeilencount nach Step 1+2 pruefen.

b) Watchdog-Init (Zeilen ~195-208, ca. 14 Zeilen ESP_IDF_VERSION bedingt): In `initWatchdog()` Funktion in web_server.cpp kapseln (neben den anderen System-Utils). In moodlight.cpp nur `initWatchdog();` aufrufen.

c) Stack-Check-Block (Zeilen ~210-221): Entfernen — hat keinen Effekt auf das Laufzeitverhalten, ist reiner Debug-Output beim Start. `loopTaskStackSize` wird nirgends verwendet.

d) `esp_log_level_set()`-Bloecke (Zeilen ~224-228, 5 Zeilen): Behalten — sind kurz und gehoeren logisch in setup().

e) Welcome-Banner (Zeilen ~240-248): In `printWelcomeBanner()` kapseln oder beibehalten — kostet nur 8 Zeilen.

**Step 4 — Finale Pruefung und Bereinigung:**

Nach allen Extraktionen: `wc -l firmware/src/moodlight.cpp` ausfuehren.

Erwartete Reduktion:
- debug() entfernt in Plan 02: -63 Zeilen
- floatToString() entfernt in Plan 02: -6 Zeilen
- Hardware-Instanzen entfernt in Plan 02: -10 Zeilen
- Dead Code entfernt in Plan 01: -25 Zeilen
- Extern const entfernt in Plan 01: -15 Zeilen
- Pulse-Block extrahiert: -38 Zeilen
- Health-Check-Block extrahiert: -110 Zeilen
- Utility-Instanzen verschoben: -8 Zeilen
- Stack-Check entfernt: -12 Zeilen
- Watchdog-Init extrahiert: -14 Zeilen
- NTP-Konstanten entfernt in Plan 01: -4 Zeilen
= ca. -305 Zeilen von 677 = ~372 Zeilen

Falls noch ueber 200: Pruefe ob der MQTT-Init-Block (35 Zeilen) und/oder die erste LED-Init in loop() (Zeilen ~407-421, 15 Zeilen) extrahiert werden koennen.

Moegliche weitere Extraktionen falls noetig:
- `firstLedShowDone`-Block in loop() -> `initFirstLEDUpdate()` in led_controller
- MQTT-Init-Block in setup() -> `connectMQTTOnStartup()` in mqtt_handler
- WiFi-connect + NTP + mDNS Block in setup() -> `connectAndInitNetwork()` in wifi_manager

Ziel: Exakt <= 200 Zeilen. Includes zaehlen mit.

**Step 5 — Unnoetige Includes in moodlight.cpp entfernen:**
Nach allen Verschiebungen pruefen welche `#include`-Zeilen noch benoetigt werden. Entferne Includes die nur fuer verschobenen Code gebraucht wurden:
- `<DHT.h>` -> nur wenn `dht.begin()` noch in setup() ist (sonst entfernen)
- `<DNSServer.h>` -> nur wenn `dnsServer.processNextRequest()` noch in loop() ist
- `<Preferences.h>` -> nur wenn preferences noch direkt genutzt wird
- `"esp_log.h"` -> nur wenn `esp_log_level_set()` in setup() bleibt
- `"esp_wifi.h"` -> nur wenn `esp_wifi_set_ps()` in setup() bleibt
- `"freertos/task.h"` -> entfernen wenn Stack-Check-Block entfernt
- `<time.h>` -> entfernen wenn Health-Check verschoben

ACHTUNG: Nicht zu aggressiv entfernen — manche Includes werden transitiv von den Modul-Headern gebraucht. Im Zweifel behalten und `pio run` pruefen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5 && echo "---" && wc -l src/moodlight.cpp</automated>
  </verify>
  <done>
    - `wc -l firmware/src/moodlight.cpp` zeigt <= 200
    - moodlight.cpp enthaelt nur: Includes, AppState-Instanz, setup(), loop()
    - setup() ruft nur Modul-Init-Funktionen auf (initFS, loadSettings, setupWebServer, startWiFiStation, setupHA, etc.)
    - loop() ruft nur Modul-Funktionen auf (watchdog.autoFeed, server.handleClient, processLEDUpdates, updatePulse, updateStatusLED, getSentiment, readAndPublishDHT, runSystemHealthCheck, logSystemStatus, etc.)
    - Keine Business-Logik inline in setup() oder loop() (ausser einfache if-Guards)
    - `pio run` baut fehlerfrei — keine Linking-Fehler, keine undefined references
    - acceptance_criteria: `wc -l firmware/src/moodlight.cpp | awk '{print ($1 <= 200) ? "PASS" : "FAIL"}'`
  </done>
</task>

</tasks>

<verification>
- `pio run` baut fehlerfrei
- `wc -l firmware/src/moodlight.cpp` <= 200
- `grep -c "void " firmware/src/moodlight.cpp` liefert 2 (nur setup und loop)
- Keine Funktionsdefinitionen ausser setup() und loop() in moodlight.cpp
</verification>

<success_criteria>
moodlight.cpp ist auf max 200 Zeilen reduziert und enthaelt nur noch setup()/loop() mit Modul-Aufrufen. Build ist gruen. Kein Linking-Fehler.
</success_criteria>

<output>
After completion, create `.planning/phases/08-konsolidierung-qualitaet/08-03-SUMMARY.md`
</output>
