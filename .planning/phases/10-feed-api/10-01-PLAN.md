---
phase: 10-feed-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/database.py
  - sentiment-api/moodlight_extensions.py
autonomous: false
requirements:
  - FEED-02
  - FEED-03
  - FEED-04
  - FEED-05

must_haves:
  truths:
    - "GET /api/moodlight/feeds gibt JSON-Array mit id, name, url, active, last_fetched_at, error_count zurück"
    - "POST /api/moodlight/feeds mit erreichbarer URL legt Feed in DB an und gibt 201 zurück"
    - "POST /api/moodlight/feeds mit nicht erreichbarer URL gibt 422 mit Fehlermeldung zurück"
    - "DELETE /api/moodlight/feeds/<id> löscht Feed und gibt 204 zurück; unbekannte ID gibt 404"
  artifacts:
    - path: "sentiment-api/database.py"
      provides: "get_all_feeds(), add_feed(), delete_feed() Methoden"
      exports: ["get_all_feeds", "add_feed", "delete_feed"]
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "GET /api/moodlight/feeds, POST /api/moodlight/feeds, DELETE /api/moodlight/feeds/<int:feed_id>"
  key_links:
    - from: "GET /api/moodlight/feeds"
      to: "database.get_all_feeds()"
      via: "db = get_database(); feeds = db.get_all_feeds()"
      pattern: "get_all_feeds"
    - from: "POST /api/moodlight/feeds"
      to: "database.add_feed()"
      via: "db.add_feed(name, url) nach URL-Validierung"
      pattern: "add_feed"
    - from: "DELETE /api/moodlight/feeds/<id>"
      to: "database.delete_feed()"
      via: "db.delete_feed(feed_id)"
      pattern: "delete_feed"
---

<objective>
Feed-CRUD-API implementieren: drei Endpoints (GET, POST, DELETE) in moodlight_extensions.py, gestützt auf drei neue DB-Methoden in database.py.

Purpose: Externe Clients (Browser, curl, künftiges Web-Interface) können die Feed-Liste lesen und verwalten.
Output: Drei funktionierende REST-Endpoints gegen die feeds-Tabelle aus Phase 9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@sentiment-api/database.py
@sentiment-api/moodlight_extensions.py
</context>

<interfaces>
<!-- Bestehende Methoden in database.py die als Vorlage dienen -->

```python
# Vorlage: get_active_feeds() — gibt nur aktive Feeds zurück, nur id/name/url
# get_all_feeds() soll ALLE Feeds mit allen Feldern zurückgeben

def get_active_feeds(self) -> List[Dict[str, Any]]:
    query = """
        SELECT id, name, url
        FROM feeds
        WHERE active = TRUE
        ORDER BY name ASC;
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query)
            results = cur.fetchall()
            return [dict(row) for row in results]
    except Exception as e:
        logger.error(f"Fehler beim Laden der aktiven Feeds: {e}")
        return []

# Vorlage: save_sentiment() — zeigt commit-Pattern mit self.conn.commit()
def save_sentiment(self, ...):
    ...
    with self.get_cursor() as cur:
        cur.execute(query, (params,))
        result_id = cur.fetchone()[0]
        self.conn.commit()
```

<!-- feeds-Tabelle Schema (aus init.sql Phase 9) -->
```sql
CREATE TABLE IF NOT EXISTS feeds (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    url TEXT NOT NULL UNIQUE,
    active BOOLEAN NOT NULL DEFAULT TRUE,
    last_fetched_at TIMESTAMP WITH TIME ZONE,
    error_count INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
```

<!-- Bestehende Endpoint-Vorlage aus moodlight_extensions.py -->
```python
@app.route('/api/moodlight/devices', methods=['GET'])
def get_moodlight_devices():
    try:
        db = get_database()
        devices = db.get_active_devices(hours=hours)
        # Timestamps konvertieren
        for device in devices:
            if isinstance(device.get('last_seen'), datetime):
                device['last_seen'] = device['last_seen'].isoformat()
        return jsonify({...})
    except Exception as e:
        logger.error(f"Fehler in /api/moodlight/devices: {e}", exc_info=True)
        return jsonify({"status": "error", "message": str(e)}), 500
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DB-Methoden get_all_feeds, add_feed, delete_feed in database.py</name>
  <files>sentiment-api/database.py</files>

  <read_first>
    - sentiment-api/database.py (vollständig) — Klasse Database, get_cursor(), get_active_feeds(), save_sentiment() als Commit-Vorlage
  </read_first>

  <action>
Drei neue Methoden direkt nach der bestehenden `get_active_feeds()` Methode (ca. Zeile 380) in die `Database`-Klasse einfügen.

**get_all_feeds()** — liefert ALLE Feeds (aktiv + inaktiv) mit allen Feldern:
```python
def get_all_feeds(self) -> List[Dict[str, Any]]:
    """
    Hole alle RSS-Feeds aus der Datenbank (aktiv und inaktiv).

    Returns:
        Liste von Dicts mit id, name, url, active, last_fetched_at, error_count, created_at.
        Leere Liste bei Fehler.
    """
    query = """
        SELECT id, name, url, active, last_fetched_at, error_count, created_at
        FROM feeds
        ORDER BY name ASC;
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query)
            results = cur.fetchall()
            return [dict(row) for row in results]
    except Exception as e:
        logger.error(f"Fehler beim Laden aller Feeds: {e}")
        return []
```

**add_feed()** — legt neuen Feed an, UNIQUE-Constraint der feeds-Tabelle abfangen:
```python
def add_feed(self, name: str, url: str) -> Optional[Dict[str, Any]]:
    """
    Füge neuen RSS-Feed in die Datenbank ein.

    Args:
        name: Anzeigename des Feeds
        url: Feed-URL (muss einzigartig sein)

    Returns:
        Dict mit id, name, url, active, created_at des neu angelegten Feeds.
        None bei Fehler oder Duplikat (psycopg2.errors.UniqueViolation).

    Raises:
        ValueError: Wenn die URL bereits existiert (UNIQUE-Constraint).
    """
    query = """
        INSERT INTO feeds (name, url, active)
        VALUES (%s, %s, TRUE)
        RETURNING id, name, url, active, created_at;
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query, (name, url))
            result = cur.fetchone()
            self.conn.commit()
            logger.info(f"Neuer Feed angelegt: {name} ({url})")
            return dict(result)
    except psycopg2.errors.UniqueViolation:
        self.conn.rollback()
        raise ValueError(f"Feed-URL bereits vorhanden: {url}")
    except Exception as e:
        if self.conn:
            try:
                self.conn.rollback()
            except Exception:
                pass
        logger.error(f"Fehler beim Anlegen des Feeds: {e}")
        raise
```

**delete_feed()** — löscht Feed, gibt True/False zurück:
```python
def delete_feed(self, feed_id: int) -> bool:
    """
    Lösche RSS-Feed aus der Datenbank.

    Args:
        feed_id: ID des zu löschenden Feeds

    Returns:
        True wenn Feed gefunden und gelöscht, False wenn ID nicht existiert.
    """
    query = """
        DELETE FROM feeds
        WHERE id = %s
        RETURNING id;
    """
    try:
        with self.get_cursor() as cur:
            cur.execute(query, (feed_id,))
            deleted = cur.fetchone()
            self.conn.commit()
            if deleted:
                logger.info(f"Feed gelöscht: ID={feed_id}")
                return True
            else:
                logger.warning(f"Feed nicht gefunden: ID={feed_id}")
                return False
    except Exception as e:
        if self.conn:
            try:
                self.conn.rollback()
            except Exception:
                pass
        logger.error(f"Fehler beim Löschen des Feeds {feed_id}: {e}")
        raise
```

Hinweis: `psycopg2.errors.UniqueViolation` ist in psycopg2 direkt verfügbar (kein zusätzlicher Import nötig — psycopg2 ist bereits importiert).
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
import ast, sys
with open('database.py') as f:
    src = f.read()
tree = ast.parse(src)
methods = []
for node in ast.walk(tree):
    if isinstance(node, ast.FunctionDef):
        methods.append(node.name)
missing = [m for m in ['get_all_feeds', 'add_feed', 'delete_feed'] if m not in methods]
if missing:
    print('FEHLER: Fehlende Methoden:', missing)
    sys.exit(1)
print('OK: get_all_feeds, add_feed, delete_feed vorhanden')
"
</automated>
  </verify>

  <acceptance_criteria>
    - `get_all_feeds()` steht in database.py, Query selektiert id, name, url, active, last_fetched_at, error_count, created_at
    - `add_feed(name, url)` steht in database.py, nutzt INSERT ... RETURNING, ruft self.conn.commit() auf, wirft ValueError bei UniqueViolation
    - `delete_feed(feed_id)` steht in database.py, nutzt DELETE ... WHERE id = %s RETURNING id, gibt True/False zurück, ruft self.conn.commit() auf
    - Python-Syntax valide: `python -c "import ast; ast.parse(open('database.py').read()); print('OK')"` gibt OK aus
  </acceptance_criteria>

  <done>Drei neue Methoden in database.py eingefügt, Syntaxcheck besteht, commit/rollback-Pattern konsistent mit restlichem DB-Layer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Feed-Endpoints GET, POST, DELETE in moodlight_extensions.py + Legacy-Cleanup app.py</name>
  <files>sentiment-api/moodlight_extensions.py, sentiment-api/app.py</files>

  <read_first>
    - sentiment-api/moodlight_extensions.py (vollständig) — register_moodlight_endpoints(), bestehende Endpoint-Struktur, Imports
    - sentiment-api/app.py (Zeilen 410-460) — configure_feeds() mit defektem global rss_feeds (aus VERIFICATION.md bekannt)
  </read_first>

  <action>
**Teil A: moodlight_extensions.py — drei Feed-Endpoints**

Import-Ergänzung am Dateianfang (nach `import time`):
```python
import requests as http_requests
```

Direkt vor `logger.info("Moodlight API-Endpunkte registriert")` (letzte Zeile der Funktion `register_moodlight_endpoints`) drei neue Routen einfügen:

```python
    # ===== FEED-VERWALTUNG ENDPOINTS =====

    @app.route('/api/moodlight/feeds', methods=['GET'])
    def get_moodlight_feeds():
        """
        Alle RSS-Feeds mit Status zurückgeben (FEED-02)
        """
        try:
            db = get_database()
            feeds = db.get_all_feeds()

            # Timestamps zu ISO-Format konvertieren
            for feed in feeds:
                if isinstance(feed.get('last_fetched_at'), datetime):
                    feed['last_fetched_at'] = feed['last_fetched_at'].isoformat()
                elif feed.get('last_fetched_at') is None:
                    feed['last_fetched_at'] = None
                if isinstance(feed.get('created_at'), datetime):
                    feed['created_at'] = feed['created_at'].isoformat()

            return jsonify({
                "status": "success",
                "count": len(feeds),
                "feeds": feeds
            })

        except Exception as e:
            logger.error(f"Fehler in GET /api/moodlight/feeds: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500


    @app.route('/api/moodlight/feeds', methods=['POST'])
    def add_moodlight_feed():
        """
        Neuen RSS-Feed hinzufügen mit URL-Validierung (FEED-03, FEED-05)

        Body (JSON): {"url": "https://...", "name": "Feed-Name"}
        Gibt 201 bei Erfolg, 400 bei fehlenden Feldern, 409 bei Duplikat, 422 bei nicht erreichbarer URL.
        """
        try:
            data = request.get_json(silent=True)
            if not data:
                return jsonify({"status": "error", "message": "JSON-Body erwartet"}), 400

            url = data.get('url', '').strip()
            name = data.get('name', '').strip()

            if not url:
                return jsonify({"status": "error", "message": "Feld 'url' fehlt oder leer"}), 400
            if not name:
                return jsonify({"status": "error", "message": "Feld 'name' fehlt oder leer"}), 400

            # URL-Validierung: Erreichbarkeit prüfen (FEED-05)
            try:
                resp = http_requests.get(url, timeout=5, allow_redirects=True, stream=True)
                resp.close()
                if resp.status_code >= 400:
                    return jsonify({
                        "status": "error",
                        "message": f"Feed-URL nicht erreichbar: HTTP {resp.status_code}"
                    }), 422
            except http_requests.exceptions.Timeout:
                return jsonify({
                    "status": "error",
                    "message": "Feed-URL nicht erreichbar: Timeout (5s)"
                }), 422
            except http_requests.exceptions.ConnectionError as e:
                return jsonify({
                    "status": "error",
                    "message": f"Feed-URL nicht erreichbar: Verbindungsfehler"
                }), 422
            except http_requests.exceptions.RequestException as e:
                return jsonify({
                    "status": "error",
                    "message": f"Feed-URL ungültig: {str(e)}"
                }), 422

            # In DB anlegen
            db = get_database()
            try:
                new_feed = db.add_feed(name=name, url=url)
            except ValueError as e:
                # Duplikat (UniqueViolation)
                return jsonify({"status": "error", "message": str(e)}), 409

            # Timestamps konvertieren
            if isinstance(new_feed.get('created_at'), datetime):
                new_feed['created_at'] = new_feed['created_at'].isoformat()

            logger.info(f"Feed hinzugefügt via API: {name} ({url})")
            return jsonify({"status": "success", "feed": new_feed}), 201

        except Exception as e:
            logger.error(f"Fehler in POST /api/moodlight/feeds: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500


    @app.route('/api/moodlight/feeds/<int:feed_id>', methods=['DELETE'])
    def delete_moodlight_feed(feed_id):
        """
        RSS-Feed entfernen (FEED-04)

        Gibt 204 bei Erfolg, 404 wenn ID nicht gefunden.
        """
        try:
            db = get_database()
            deleted = db.delete_feed(feed_id)

            if not deleted:
                return jsonify({"status": "error", "message": f"Feed nicht gefunden: ID={feed_id}"}), 404

            logger.info(f"Feed entfernt via API: ID={feed_id}")
            return '', 204

        except Exception as e:
            logger.error(f"Fehler in DELETE /api/moodlight/feeds/{feed_id}: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
```

**Teil B: app.py — Legacy-Endpoint /api/feedconfig entfernen**

Den defekten `configure_feeds()`-Endpoint aus app.py entfernen. Laut VERIFICATION.md (Zeilen ~414-449) enthält er `global rss_feeds` + `rss_feeds = new_feeds` auf eine nicht existente Variable und war bereits vor Phase 9 nur ein In-Memory-Stub ohne Persistenz.

Entfernen: Die Route `@app.route('/api/feedconfig', ...)` und die gesamte Funktion `configure_feeds()`.

Hinweis: `requests` ist in `requirements.txt` als `requests==2.32.2` bereits vorhanden — der Import als `http_requests` vermeidet Namenskollision mit dem lokalen `request`-Objekt von Flask.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
import ast, sys

# moodlight_extensions.py prüfen
with open('moodlight_extensions.py') as f:
    src = f.read()
ast.parse(src)
checks = [
    ('/api/moodlight/feeds', 'GET-Route vorhanden'),
    ('add_moodlight_feed', 'POST-Handler vorhanden'),
    ('delete_moodlight_feed', 'DELETE-Handler vorhanden'),
    ('get_all_feeds', 'DB-Aufruf vorhanden'),
    ('add_feed', 'add_feed Aufruf vorhanden'),
    ('delete_feed', 'delete_feed Aufruf vorhanden'),
    ('http_requests', 'requests-Import vorhanden'),
    ('timeout=5', 'URL-Validierung mit Timeout'),
]
for pattern, desc in checks:
    if pattern not in src:
        print(f'FEHLER: {desc} — \"{pattern}\" nicht gefunden')
        sys.exit(1)
    print(f'OK: {desc}')

# app.py: feedconfig entfernt?
with open('app.py') as f:
    app_src = f.read()
if 'configure_feeds' in app_src or '/api/feedconfig' in app_src:
    print('FEHLER: /api/feedconfig noch in app.py vorhanden')
    sys.exit(1)
print('OK: Legacy-Endpoint /api/feedconfig entfernt')

print('Alle Checks bestanden.')
"
</automated>
  </verify>

  <acceptance_criteria>
    - `GET /api/moodlight/feeds` ist in moodlight_extensions.py registriert, ruft `db.get_all_feeds()` auf
    - `POST /api/moodlight/feeds` validiert URL mit `http_requests.get(url, timeout=5)`, gibt 422 bei Fehler, 409 bei Duplikat, 201 bei Erfolg
    - `DELETE /api/moodlight/feeds/<int:feed_id>` gibt 204 bei Erfolg, 404 wenn `delete_feed()` False zurückgibt
    - `import requests as http_requests` am Anfang der Datei vorhanden
    - `configure_feeds()` und `/api/feedconfig` aus app.py entfernt
    - Syntaxcheck beider Dateien: `python -c "import ast; ast.parse(open('moodlight_extensions.py').read()); ast.parse(open('app.py').read()); print('OK')"` gibt OK aus
  </acceptance_criteria>

  <done>Drei Endpoints in moodlight_extensions.py implementiert, URL-Validierung (5s Timeout, HTTP-Status-Prüfung) vorhanden, Legacy-Endpoint /api/feedconfig aus app.py entfernt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deployment-Verifikation — Feed-API auf Produktionssystem testen</name>
  <what-built>
    Feed-CRUD-API komplett: GET/POST/DELETE /api/moodlight/feeds gegen die feeds-Tabelle aus Phase 9, mit URL-Validierung beim Hinzufügen. Legacy-Endpoint /api/feedconfig aus app.py entfernt.
  </what-built>

  <how-to-verify>
Deploy auf server.godsapp.de:
```bash
# 1. Push + Deploy
git add sentiment-api/database.py sentiment-api/moodlight_extensions.py sentiment-api/app.py
git commit -m "feat(10-01): Feed-CRUD-API — GET/POST/DELETE /api/moodlight/feeds"
git push
# Portainer Webhook deployt automatisch via GitHub Actions
```

Warte ca. 2-3 Min bis neues Image deployed ist, dann testen:

```bash
# GET — Feed-Liste abrufen (mind. 11 Feeds erwartet)
curl -s http://analyse.godsapp.de/api/moodlight/feeds | python3 -m json.tool

# POST — Gültiger Feed hinzufügen (erreichbare URL)
curl -s -X POST http://analyse.godsapp.de/api/moodlight/feeds \
  -H "Content-Type: application/json" \
  -d '{"url": "https://www.tagesschau.de/xml/rss2/", "name": "Tagesschau Test"}' \
  | python3 -m json.tool
# Erwartet: status=success, HTTP 201, Feed-Objekt mit ID

# POST — Ungültige URL (nicht erreichbar)
curl -s -o /dev/null -w "%{http_code}" -X POST http://analyse.godsapp.de/api/moodlight/feeds \
  -H "Content-Type: application/json" \
  -d '{"url": "https://nicht-erreichbar.example.invalid/feed", "name": "Test"}'
# Erwartet: 422

# DELETE — Angelegte Feed-ID aus erstem POST-Test einsetzen
curl -s -o /dev/null -w "%{http_code}" -X DELETE http://analyse.godsapp.de/api/moodlight/feeds/<ID>
# Erwartet: 204

# DELETE — Unbekannte ID
curl -s -o /dev/null -w "%{http_code}" -X DELETE http://analyse.godsapp.de/api/moodlight/feeds/99999
# Erwartet: 404
```
  </how-to-verify>

  <resume-signal>Schreib "approved" wenn alle 5 curl-Tests die erwarteten Status-Codes liefern, oder beschreibe Fehler.</resume-signal>
  <files>sentiment-api/database.py, sentiment-api/moodlight_extensions.py, sentiment-api/app.py</files>
  <action>Commit, push, warte auf Portainer-Deployment (ca. 2-3 Min), führe die 5 curl-Tests durch.</action>
  <verify>Alle 5 curl-Tests liefern die erwarteten HTTP-Status-Codes (GET: 200, POST-gültig: 201, POST-ungültig: 422, DELETE-vorhanden: 204, DELETE-unbekannt: 404).</verify>
  <done>Feed-CRUD-API auf Produktionssystem verifiziert — alle 4 ROADMAP Success Criteria für Phase 10 erfüllt.</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:

1. `python -c "import ast; ast.parse(open('database.py').read())"` — kein Syntaxfehler
2. `python -c "import ast; ast.parse(open('moodlight_extensions.py').read())"` — kein Syntaxfehler
3. `grep -n "get_all_feeds\|add_feed\|delete_feed" database.py` — alle drei Methoden vorhanden
4. `grep -n "configure_feeds\|/api/feedconfig" app.py` — kein Treffer (Legacy entfernt)
5. Manuell: curl-Tests gegen Produktions-API (Task 3 Checkpoint)
</verification>

<success_criteria>
- GET /api/moodlight/feeds gibt JSON mit Feed-Array zurück (id, name, url, active, last_fetched_at, error_count)
- POST /api/moodlight/feeds mit erreichbarer URL gibt 201 + Feed-Objekt zurück
- POST /api/moodlight/feeds mit nicht erreichbarer URL gibt 422 + Fehlermeldung zurück
- DELETE /api/moodlight/feeds/<id> mit gültiger ID gibt 204 zurück
- DELETE /api/moodlight/feeds/<id> mit unbekannter ID gibt 404 zurück
- Legacy-Endpoint /api/feedconfig aus app.py entfernt
</success_criteria>

<output>
Nach Completion: `.planning/phases/10-feed-api/10-01-SUMMARY.md` erstellen.
</output>
