---
phase: 07-modul-extraktion
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - firmware/src/wifi_manager.h
  - firmware/src/wifi_manager.cpp
  - firmware/src/moodlight.cpp
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "WiFi-Funktionen (AP-Mode, Station-Connect, Reconnect, Scan) leben in wifi_manager.cpp"
    - "moodlight.cpp ruft WiFi-Funktionen ueber wifi_manager.h auf"
    - "pio run baut fehlerfrei durch"
  artifacts:
    - path: "firmware/src/wifi_manager.h"
      provides: "Header mit WiFi-Funktionsdeklarationen"
      exports: ["safeWiFiConnect", "startAPMode", "startWiFiStation", "scanWiFiNetworks", "checkAndReconnectWifi", "startAPModeWithServer", "initTime"]
    - path: "firmware/src/wifi_manager.cpp"
      provides: "Implementierung aller WiFi-Funktionen"
      contains: "checkAndReconnectWifi"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/wifi_manager.h"
      via: "#include"
      pattern: '#include "wifi_manager.h"'
---

<objective>
WiFi-Manager aus moodlight.cpp extrahieren (MOD-01).

Purpose: WiFi ist weitgehend standalone — braucht nur AppState und ein paar Hardware-Globals. Enthaelt AP-Mode, Captive Portal, Station-Connect, Reconnect mit Backoff, NTP-Init.

Output: wifi_manager.h + wifi_manager.cpp, moodlight.cpp ohne WiFi-Code
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modul-extraktion/07-CONTEXT.md

@firmware/src/app_state.h
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: wifi_manager.h/.cpp erstellen und WiFi-Funktionen verschieben</name>
  <files>firmware/src/wifi_manager.h, firmware/src/wifi_manager.cpp, firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp, firmware/src/app_state.h, firmware/src/config.h</read_first>
  <action>
1. Erstelle `firmware/src/wifi_manager.h` mit:
   - `#pragma once`
   - `#include "app_state.h"`
   - `#include <Arduino.h>`
   - Funktionsdeklarationen:
     - `void initTime();`
     - `bool safeWiFiConnect(const String &ssid, const String &password, unsigned long timeout = 15000);`
     - `void startAPMode();`
     - `bool startWiFiStation();`
     - `String scanWiFiNetworks();`
     - `void checkAndReconnectWifi();`
     - `void startAPModeWithServer();`
     - `void processDNS();`

2. Erstelle `firmware/src/wifi_manager.cpp` mit:
   - `#include "wifi_manager.h"`
   - Noetige Includes: `<WiFi.h>`, `<DNSServer.h>`, `<WebServer.h>`, `<ArduinoJson.h>`, `"esp_wifi.h"`, `<ESPmDNS.h>`, `<time.h>`
   - `extern`-Deklarationen fuer globale Objekte aus moodlight.cpp:
     - `extern AppState appState;`
     - `extern DNSServer dnsServer;`
     - `extern WebServer server;`
     - `extern Adafruit_NeoPixel pixels;` (fuer startAPMode LED-Animation)
     - `extern void debug(const String &message);`
     - `extern void debug(const __FlashStringHelper *message);`
     - `extern void setStatusLED(int mode);` (aufgerufen in checkAndReconnectWifi/startAPMode)
   - `extern const byte DNS_PORT;`
   - `extern const unsigned long MAX_RECONNECT_DELAY;`
   - `extern const unsigned long STATUS_LED_GRACE_MS;`
   - `extern const unsigned long AP_TIMEOUT;`
   - NTP-Konstanten muessen MIT verschoben werden (ntpServer, gmtOffset_sec, daylightOffset_sec) ODER als `extern` deklariert werden
   - CaptiveRequestHandler-Klasse MIT verschieben (Zeilen 548-576) — wird nur in startAPMode/startAPModeWithServer gebraucht. Da sie `server` und `WebServer` braucht, muss `#include <WebServer.h>` enthalten sein.
   - Verschiebe folgende Funktionen aus moodlight.cpp:
     a. `initTime()` (Zeile 315-348)
     b. `safeWiFiConnect()` (Zeile 819-855)
     c. `startAPMode()` (Zeile 858-892)
     d. `startWiFiStation()` (Zeile 895-924)
     e. `scanWiFiNetworks()` (Zeile 927-951)
     f. `processDNS()` (Zeile 579-585)
     g. `checkAndReconnectWifi()` (Zeile 3670-3784)
     h. `startAPModeWithServer()` (Zeile 3922-3973)
     i. `CaptiveRequestHandler` Klasse (Zeile 548-576)

3. In `moodlight.cpp`:
   - Fuege `#include "wifi_manager.h"` ein
   - Entferne alle verschobenen Funktionsdefinitionen und die CaptiveRequestHandler-Klasse
   - Die NTP-Konstanten (ntpServer, gmtOffset_sec, daylightOffset_sec) werden entweder mit verschoben oder extern deklariert — je nachdem ob sie nur von WiFi-Code benutzt werden (ja, nur initTime braucht sie -> mit verschieben)
   - Die Aufrufe in setup() und loop() bleiben unveraendert

ACHTUNG: `startWiFiStation()` ruft `initTime()` auf — beide sind im selben Modul, kein Problem.
ACHTUNG: `startAPMode()` und `startAPModeWithServer()` rufen `setStatusLED()` auf — `setStatusLED` bleibt vorerst in moodlight.cpp (wird in Plan 03 zum LED-Controller verschoben), daher als extern deklarieren.
ACHTUNG: `startAPModeWithServer()` ruft `server.begin()` und `server.addHandler()` auf — `server` ist extern.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "void checkAndReconnectWifi\|void startAPMode\|bool startWiFiStation\|void initTime" firmware/src/wifi_manager.cpp` ergibt 4
    - `grep -c "void checkAndReconnectWifi\|void startAPMode\(\)\|bool startWiFiStation\(\)\|void initTime\(\)" firmware/src/moodlight.cpp` ergibt 0
    - `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle WiFi-Funktionen + CaptiveRequestHandler leben in wifi_manager.cpp, moodlight.cpp inkludiert den Header, Build ist gruen</done>
</task>

</tasks>

<verification>
- `pio run` muss ohne Fehler durchlaufen
- `grep -rn "checkAndReconnectWifi\|startAPMode\|startWiFiStation" firmware/src/wifi_manager.cpp` zeigt Funktionsdefinitionen
- `grep -rn '#include "wifi_manager.h"' firmware/src/moodlight.cpp` zeigt Include
</verification>

<success_criteria>
- wifi_manager.h/.cpp existieren mit allen WiFi-Funktionen
- CaptiveRequestHandler lebt in wifi_manager.cpp
- moodlight.cpp enthaelt keine WiFi-Funktionsdefinitionen mehr
- `pio run` baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/07-modul-extraktion/07-02-SUMMARY.md`
</output>
