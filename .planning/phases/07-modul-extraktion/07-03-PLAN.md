---
phase: 07-modul-extraktion
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - firmware/src/led_controller.h
  - firmware/src/led_controller.cpp
  - firmware/src/moodlight.cpp
autonomous: true
requirements: [MOD-02]

must_haves:
  truths:
    - "LED-Funktionen (updateLEDs, processLEDUpdates, setStatusLED, updateStatusLED, pulseCurrentColor) leben in led_controller.cpp"
    - "Farb-Hilfsfunktionen (ColorDefinition, uint32ToColorDef, getColorDefinition) leben in led_controller.h/.cpp"
    - "pio run baut fehlerfrei durch"
  artifacts:
    - path: "firmware/src/led_controller.h"
      provides: "Header mit LED-Funktions- und Struct-Deklarationen"
      exports: ["updateLEDs", "processLEDUpdates", "setStatusLED", "updateStatusLED", "pulseCurrentColor", "getColorDefinition", "ColorDefinition"]
    - path: "firmware/src/led_controller.cpp"
      provides: "Implementierung aller LED-Funktionen"
      contains: "processLEDUpdates"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/led_controller.h"
      via: "#include"
      pattern: '#include "led_controller.h"'
---

<objective>
LED-Controller aus moodlight.cpp extrahieren (MOD-02).

Purpose: LED-Steuerung ist ein eigenstaendiger Bereich — NeoPixel-Zugriff, Mutex-geschuetzte Updates, Status-LED, Pulse-Animation. Wird von vielen Stellen aufgerufen (MQTT-Callbacks, WebServer, Sentiment-Handler), daher muss der Header ueberall inkludierbar sein.

Output: led_controller.h + led_controller.cpp, moodlight.cpp ohne LED-Code
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modul-extraktion/07-CONTEXT.md

@firmware/src/app_state.h
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: led_controller.h/.cpp erstellen und LED-Funktionen verschieben</name>
  <files>firmware/src/led_controller.h, firmware/src/led_controller.cpp, firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp, firmware/src/app_state.h, firmware/src/config.h</read_first>
  <action>
1. Erstelle `firmware/src/led_controller.h` mit:
   - `#pragma once`
   - `#include "app_state.h"`
   - `#include <Adafruit_NeoPixel.h>`
   - `ColorDefinition` Struct-Definition (aus Zeile 54-59 von moodlight.cpp)
   - `colorNames` extern-Deklaration: `extern const char *colorNames[5];`
   - Funktionsdeklarationen:
     - `ColorDefinition uint32ToColorDef(uint32_t color);`
     - `ColorDefinition getColorDefinition(int index);`
     - `void updateLEDs();`
     - `void setStatusLED(int mode);`
     - `void updateStatusLED();`
     - `void processLEDUpdates();`
     - `void pulseCurrentColor();`

2. Erstelle `firmware/src/led_controller.cpp` mit:
   - `#include "led_controller.h"`
   - `extern AppState appState;`
   - `extern Adafruit_NeoPixel pixels;`
   - `extern void debug(const String &message);`
   - `extern void debug(const __FlashStringHelper *message);`
   - `const char *colorNames[5] = { "Sehr negativ", "Negativ", "Neutral", "Positiv", "Sehr positiv" };` — VERSCHIEBEN aus moodlight.cpp
   - Verschiebe folgende Funktionen aus moodlight.cpp:
     a. `uint32ToColorDef()` (Zeile 72-79)
     b. `getColorDefinition()` (Zeile 82-86)
     c. `updateLEDs()` (Zeile 351-403)
     d. `setStatusLED()` (Zeile 406-444)
     e. `updateStatusLED()` (Zeile 446-493)
     f. `processLEDUpdates()` (Zeile 495-544)
     g. `pulseCurrentColor()` (Zeile 2919-2972)

3. In `moodlight.cpp`:
   - Fuege `#include "led_controller.h"` ein
   - Entferne: ColorDefinition struct (Zeile 54-59), colorNames array (Zeile 64-69), uint32ToColorDef (72-79), getColorDefinition (82-86)
   - Entferne alle verschobenen Funktionsdefinitionen (updateLEDs, setStatusLED, updateStatusLED, processLEDUpdates, pulseCurrentColor)
   - Die `floatToString()` Hilfsfunktion (Zeile 89-94) bleibt in moodlight.cpp — sie wird von mehreren Stellen gebraucht und ist eine generische Utility

ACHTUNG: `updateLEDs()` benutzt `pixels.Color()` — `pixels` wird als extern deklariert.
ACHTUNG: `processLEDUpdates()` benutzt `pixels.clear()`, `pixels.setBrightness()`, `pixels.setPixelColor()`, `pixels.show()` — alles ueber extern pixels.
ACHTUNG: Nachdem `setStatusLED` verschoben ist, muessen wifi_manager.cpp und spaetere Module `setStatusLED` ueber `#include "led_controller.h"` oder `extern void setStatusLED(int)` finden. WiFi-Manager (Plan 02) hat bereits `extern void setStatusLED(int mode);` — das funktioniert weiterhin, da der Linker die Definition in led_controller.cpp findet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "void updateLEDs\|void setStatusLED\|void processLEDUpdates\|void pulseCurrentColor\|void updateStatusLED" firmware/src/led_controller.cpp` ergibt 5
    - `grep "struct ColorDefinition" firmware/src/led_controller.h` findet das Struct
    - `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle LED-Funktionen und ColorDefinition leben in led_controller.h/.cpp, moodlight.cpp inkludiert den Header, Build ist gruen</done>
</task>

</tasks>

<verification>
- `pio run` muss ohne Fehler durchlaufen
- `grep -rn "updateLEDs\|setStatusLED\|processLEDUpdates" firmware/src/led_controller.cpp` zeigt Funktionsdefinitionen
- `grep -rn '#include "led_controller.h"' firmware/src/moodlight.cpp` zeigt Include
</verification>

<success_criteria>
- led_controller.h/.cpp existieren mit allen LED-Funktionen und ColorDefinition
- moodlight.cpp enthaelt keine LED-Funktionsdefinitionen mehr
- `pio run` baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/07-modul-extraktion/07-03-SUMMARY.md`
</output>
