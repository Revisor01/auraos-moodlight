---
phase: 12-headline-persistenz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/init.sql
  - sentiment-api/migration_phase12.sql
  - sentiment-api/database.py
  - sentiment-api/background_worker.py
  - sentiment-api/app.py
autonomous: true
requirements:
  - HEAD-01

must_haves:
  truths:
    - "Nach jeder automatischen Sentiment-Analyse sind die Headlines mit Einzel-Scores in der headlines-Tabelle abrufbar"
    - "Jeder headlines-Eintrag hat einen gültigen FK zu sentiment_history (NOT NULL) und optional zu feeds (NULLABLE)"
    - "Die bestehende Sentiment-Analyse läuft ohne Fehler oder Performance-Einbußen durch"
    - "Bei einem Fehler in save_headlines() bleibt der Sentiment-Score in sentiment_history erhalten"
  artifacts:
    - path: "sentiment-api/migration_phase12.sql"
      provides: "Idempotentes Migration-SQL für das Produktionssystem"
      contains: "CREATE TABLE IF NOT EXISTS headlines"
    - path: "sentiment-api/init.sql"
      provides: "headlines-Tabelle im initialen Schema (für Neuinstallationen)"
      contains: "headlines"
    - path: "sentiment-api/database.py"
      provides: "save_headlines() Bulk-INSERT Methode"
      exports: ["save_headlines"]
    - path: "sentiment-api/background_worker.py"
      provides: "Erweiterter _perform_update() mit Headline-Persistenz, _fetch_headlines() mit feed_id"
      contains: "save_headlines"
    - path: "sentiment-api/app.py"
      provides: "feed_id Durchreichung in analyze_headlines_openai_batch()"
      contains: "feed_id"
  key_links:
    - from: "background_worker.py _fetch_headlines()"
      to: "headlines-Dict"
      via: "feed_row['id'] als feed_id"
      pattern: "feed_id.*feed_row"
    - from: "background_worker.py _perform_update()"
      to: "database.py save_headlines()"
      via: "sentiment_history_id Rückgabewert von save_sentiment()"
      pattern: "sentiment_history_id = db\\.save_sentiment"
    - from: "database.py save_headlines()"
      to: "headlines Tabelle"
      via: "executemany INSERT"
      pattern: "executemany"
---

<objective>
Das Backend speichert bei jeder automatischen Sentiment-Analyse die einzelnen Headlines mit Einzel-Scores dauerhaft in PostgreSQL. Aktuell gehen die Einzel-Daten nach jedem Analyse-Zyklus verloren — nur der aggregierte Durchschnitts-Score wird in sentiment_history gespeichert.

Purpose: Fundament für Headline-Transparenz (HEAD-01) — spätere Dashboard- und Client-Features bauen auf diesen gespeicherten Headlines auf.
Output: headlines-Tabelle in DB, save_headlines()-Methode, erweiterter Background Worker, migration_phase12.sql für Produktionssystem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@sentiment-api/init.sql
@sentiment-api/database.py
@sentiment-api/background_worker.py
@sentiment-api/app.py
</context>

<interfaces>
<!-- Bestehende Interfaces die der Executor direkt nutzt -->

Aus sentiment-api/database.py:
```python
# Bestehende Methode — gibt bereits id zurück, Aufrufer ignoriert es aktuell
def save_sentiment(
    self,
    sentiment_score: float,
    category: str,
    headlines_analyzed: int,
    source_count: int = 12,
    api_response_time_ms: Optional[int] = None,
    metadata: Optional[Dict] = None
) -> int:
    ...  # RETURNING id → return result_id (Zeile 174)

# Bestehende Methode — gibt id, name, url zurück
def get_active_feeds(self) -> List[Dict[str, Any]]:
    # SELECT id, name, url FROM feeds WHERE active = TRUE

# Context Manager für sichere Cursor-Nutzung
@contextmanager
def get_cursor(self, cursor_factory=None):
    ...  # yields cursor, auto-reconnect via _ensure_connection()
```

Aus sentiment-api/background_worker.py:
```python
# _fetch_headlines() aktuell — gibt feed_id NICHT mit durch
headlines.append({
    "headline": headline_text,
    "source": source,   # Feed-Name (String)
    "link": link        # OHNE feed_id
})

# _perform_update() aktuell — ignoriert Rückgabewert von save_sentiment()
db.save_sentiment(...)  # Rückgabewert wird NICHT assigned

# analyze_function Aufruf — Ergebnis enthält results-Liste
analysis_result = self.analyze_function(headlines)
# analysis_result = {
#   "results": [{"headline": "...", "source": "Tagesschau", "sentiment": -0.3, "strength": "negativ", ...}],
#   "total_sentiment": -0.47,
#   "statistics": {"analyzed_count": 11, ...}
# }
```

Aus sentiment-api/app.py (analyze_headlines_openai_batch, Zeilen 192-197):
```python
# Bestehende results.append — original_headline_obj wird transparent durchgereicht
results.append({
    "headline": original_headline_obj['headline'].strip(),
    "source": original_headline_obj.get('source', 'unknown'),
    # feed_id fehlt hier — muss ergänzt werden
    "sentiment": score,
    "strength": strength
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DB-Schema — headlines-Tabelle in init.sql + migration_phase12.sql</name>
  <files>sentiment-api/init.sql, sentiment-api/migration_phase12.sql</files>
  <read_first>
    - sentiment-api/init.sql — vollständig lesen, Einfügestelle nach feeds-Block (nach Zeile ~89) finden
  </read_first>
  <action>
    1. In sentiment-api/init.sql nach dem feeds-Block (nach dem COMMENT ON COLUMN feeds.url Eintrag) einen neuen Abschnitt einfügen:

    ```sql
    -- ===== HEADLINES TABLE =====
    -- Einzelne Headlines mit Einzel-Scores pro Sentiment-Analyse (Phase 12)
    CREATE TABLE IF NOT EXISTS headlines (
        id SERIAL PRIMARY KEY,
        sentiment_history_id INTEGER NOT NULL REFERENCES sentiment_history(id) ON DELETE CASCADE,
        feed_id INTEGER REFERENCES feeds(id) ON DELETE SET NULL,
        headline TEXT NOT NULL,
        source_name VARCHAR(100) NOT NULL,
        sentiment_score FLOAT NOT NULL CHECK (sentiment_score BETWEEN -1.0 AND 1.0),
        strength VARCHAR(20) NOT NULL,
        link TEXT,
        analyzed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_headlines_sentiment_history ON headlines(sentiment_history_id);
    CREATE INDEX IF NOT EXISTS idx_headlines_feed ON headlines(feed_id);
    CREATE INDEX IF NOT EXISTS idx_headlines_analyzed_at ON headlines(analyzed_at DESC);

    COMMENT ON TABLE headlines IS 'Einzelne Headlines mit Einzel-Scores pro Sentiment-Analyse';
    COMMENT ON COLUMN headlines.sentiment_history_id IS 'FK zu sentiment_history — welche Analyse';
    COMMENT ON COLUMN headlines.feed_id IS 'FK zu feeds — NULLABLE falls Feed gelöscht wurde';
    COMMENT ON COLUMN headlines.strength IS 'Kategorie: sehr negativ, negativ, neutral, positiv, sehr positiv';
    ```

    2. Neue Datei sentiment-api/migration_phase12.sql erstellen (für einmalige Ausführung auf dem Produktionssystem, da init.sql dort nicht erneut läuft):

    ```sql
    -- Migration Phase 12: headlines-Tabelle für Headline-Persistenz
    -- Sicher bei Mehrfachausführung (IF NOT EXISTS Guards)
    -- Ausführung: docker exec -i <postgres-container> psql -U moodlight -d moodlight < migration_phase12.sql

    SET TIME ZONE 'Europe/Berlin';

    CREATE TABLE IF NOT EXISTS headlines (
        id SERIAL PRIMARY KEY,
        sentiment_history_id INTEGER NOT NULL REFERENCES sentiment_history(id) ON DELETE CASCADE,
        feed_id INTEGER REFERENCES feeds(id) ON DELETE SET NULL,
        headline TEXT NOT NULL,
        source_name VARCHAR(100) NOT NULL,
        sentiment_score FLOAT NOT NULL CHECK (sentiment_score BETWEEN -1.0 AND 1.0),
        strength VARCHAR(20) NOT NULL,
        link TEXT,
        analyzed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_headlines_sentiment_history ON headlines(sentiment_history_id);
    CREATE INDEX IF NOT EXISTS idx_headlines_feed ON headlines(feed_id);
    CREATE INDEX IF NOT EXISTS idx_headlines_analyzed_at ON headlines(analyzed_at DESC);

    COMMENT ON TABLE headlines IS 'Einzelne Headlines mit Einzel-Scores pro Sentiment-Analyse';
    COMMENT ON COLUMN headlines.sentiment_history_id IS 'FK zu sentiment_history — welche Analyse';
    COMMENT ON COLUMN headlines.feed_id IS 'FK zu feeds — NULLABLE falls Feed gelöscht wurde';
    COMMENT ON COLUMN headlines.strength IS 'Kategorie: sehr negativ, negativ, neutral, positiv, sehr positiv';

    DO $$
    BEGIN
        RAISE NOTICE 'Migration Phase 12: headlines-Tabelle bereit';
    END $$;
    ```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS headlines" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/init.sql && grep -c "sentiment_history_id" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/migration_phase12.sql</automated>
  </verify>
  <acceptance_criteria>
    - init.sql enthält headlines-Tabelle mit allen 9 Spalten (id, sentiment_history_id, feed_id, headline, source_name, sentiment_score, strength, link, analyzed_at)
    - sentiment_history_id ist NOT NULL mit REFERENCES sentiment_history(id) ON DELETE CASCADE
    - feed_id ist NULLABLE mit REFERENCES feeds(id) ON DELETE SET NULL
    - migration_phase12.sql existiert, ist idempotent (IF NOT EXISTS), enthält denselben DDL-Block
    - Alle 3 Indizes in beiden Dateien vorhanden
  </acceptance_criteria>
  <done>init.sql und migration_phase12.sql enthalten die headlines-Tabellendefinition mit korrekten FK-Constraints</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: database.py — save_headlines() Bulk-INSERT Methode</name>
  <files>sentiment-api/database.py</files>
  <read_first>
    - sentiment-api/database.py — vollständig lesen, insbesondere save_sentiment() (Zeilen 131-182) als Muster, und get_cursor() Context Manager
  </read_first>
  <behavior>
    - save_headlines(sentiment_history_id=42, results=[]) → gibt 0 zurück, kein DB-INSERT
    - save_headlines(sentiment_history_id=42, results=[{"headline": "Test", "source": "Tagesschau", "feed_id": 1, "sentiment": -0.3, "strength": "negativ", "link": "http://..."}]) → gibt 1 zurück, 1 Zeile in headlines-Tabelle
    - save_headlines() mit results ohne "feed_id" → feed_id=None im INSERT (NULLABLE FK erlaubt das)
    - save_headlines() bei DB-Fehler → Exception wird geloggt und weitergeworfen (Aufrufer behandelt es)
    - Rückgabewert ist immer int (Anzahl gespeicherter Rows)
  </behavior>
  <action>
    In database.py nach der save_sentiment()-Methode (nach Zeile 182) die neue Methode save_headlines() einfügen:

    ```python
    def save_headlines(
        self,
        sentiment_history_id: int,
        results: list
    ) -> int:
        """
        Speichere analysierte Headlines in der Datenbank.

        Args:
            sentiment_history_id: FK zu sentiment_history.id
            results: Liste von Dicts (headline, source, feed_id, sentiment, strength, link optional)

        Returns:
            Anzahl gespeicherter Headlines (0 wenn results leer)
        """
        if not results:
            return 0

        query = """
            INSERT INTO headlines
            (sentiment_history_id, feed_id, headline, source_name, sentiment_score, strength, link)
            VALUES (%s, %s, %s, %s, %s, %s, %s)
        """
        rows = [
            (
                sentiment_history_id,
                r.get('feed_id'),
                r['headline'],
                r.get('source', 'unknown'),
                r['sentiment'],
                r.get('strength', 'neutral'),
                r.get('link')
            )
            for r in results
        ]

        try:
            with self.get_cursor() as cur:
                cur.executemany(query, rows)
                self.conn.commit()
                logger.info(f"Headlines gespeichert: {len(rows)} Einträge für sentiment_history_id={sentiment_history_id}")
                return len(rows)
        except Exception as e:
            if self.conn:
                try:
                    self.conn.rollback()
                except Exception:
                    pass
            logger.error(f"Fehler beim Speichern der Headlines: {e}")
            raise
    ```

    Kein Import nötig — alle Dependencies (psycopg2, logger, get_cursor) bereits vorhanden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && python3 -c "
import ast, sys
with open('sentiment-api/database.py') as f:
    tree = ast.parse(f.read())
methods = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'save_headlines' in methods, 'save_headlines fehlt'
print('OK: save_headlines gefunden in database.py')
print('Alle Methoden:', [m for m in methods if not m.startswith('_') or m == 'save_headlines'])
"</automated>
  </verify>
  <acceptance_criteria>
    - save_headlines() ist in der Database-Klasse vorhanden (nach save_sentiment())
    - Methode nimmt sentiment_history_id: int und results: list entgegen
    - Leere results → sofortiger return 0, kein DB-Aufruf
    - executemany (kein einzelnes INSERT pro Headline)
    - Bei Exception: rollback, logger.error, raise (kein Schlucken des Fehlers)
    - Rückgabewert: len(rows) als int
  </acceptance_criteria>
  <done>save_headlines() in Database-Klasse, nutzt executemany, ist konsistent mit dem Muster von save_sentiment()</done>
</task>

<task type="auto">
  <name>Task 3: background_worker.py + app.py — feed_id durchreichen und Headline-Persistenz aktivieren</name>
  <files>sentiment-api/background_worker.py, sentiment-api/app.py</files>
  <read_first>
    - sentiment-api/background_worker.py — _fetch_headlines() (Zeilen 131-193) und _perform_update() (Zeilen 69-129) vollständig lesen
    - sentiment-api/app.py — analyze_headlines_openai_batch() finden, Zeilen ~192-197 (results.append Block)
  </read_first>
  <action>
    **Änderung 1: background_worker.py — _fetch_headlines() — feed_id mitführen**

    In _fetch_headlines() in Zeile ~182 (headlines.append-Block) feed_id aus feed_row ergänzen:

    ```python
    # VORHER:
    headlines.append({
        "headline": headline_text,
        "source": source,
        "link": link
    })

    # NACHHER:
    headlines.append({
        "headline": headline_text,
        "source": source,
        "link": link,
        "feed_id": feed_row['id']  # NEU: numerische Feed-ID für DB-FK
    })
    ```

    **Änderung 2: background_worker.py — _perform_update() — save_headlines() aufrufen**

    Den bestehenden db.save_sentiment()-Aufruf (Zeile ~104-115) so anpassen, dass der Rückgabewert assigned wird. Danach save_headlines() aufrufen. Exception in save_headlines() wird gecatcht und geloggt, aber NICHT weitergeworfen (Sentiment-Score bleibt erhalten):

    ```python
    # VORHER (Zeile ~104):
    db.save_sentiment(
        sentiment_score=sentiment_score,
        category=category,
        headlines_analyzed=headlines_analyzed,
        source_count=feed_count,
        api_response_time_ms=response_time_ms,
        metadata={...}
    )

    logger.info(f"Sentiment in DB gespeichert: {sentiment_score:.3f}")

    # NACHHER:
    sentiment_history_id = db.save_sentiment(
        sentiment_score=sentiment_score,
        category=category,
        headlines_analyzed=headlines_analyzed,
        source_count=feed_count,
        api_response_time_ms=response_time_ms,
        metadata={
            'sentiment_distribution': stats.get('sentiment_distribution', {}),
            'worker': 'background',
            'timestamp': datetime.now().isoformat()
        }
    )

    logger.info(f"Sentiment in DB gespeichert: {sentiment_score:.3f} (id={sentiment_history_id})")

    # NEU: Headlines persistieren (sekundär — Fehler bricht Update nicht ab)
    try:
        headline_results = analysis_result.get('results', [])
        if headline_results:
            saved_count = db.save_headlines(
                sentiment_history_id=sentiment_history_id,
                results=headline_results
            )
            logger.info(f"Headlines persistiert: {saved_count} Einträge")
        else:
            logger.warning("Keine Einzel-Headlines in analysis_result vorhanden")
    except Exception as headline_err:
        logger.error(f"Fehler beim Persistieren der Headlines (Sentiment bleibt gespeichert): {headline_err}")
    ```

    **Änderung 3: app.py — feed_id in results.append ergänzen**

    In analyze_headlines_openai_batch() den results.append-Block um feed_id erweitern (original_headline_obj wird transparent durchgereicht):

    ```python
    # VORHER:
    results.append({
        "headline": original_headline_obj['headline'].strip(),
        "source": original_headline_obj.get('source', 'unknown'),
        "sentiment": score,
        "strength": strength
    })

    # NACHHER:
    results.append({
        "headline": original_headline_obj['headline'].strip(),
        "source": original_headline_obj.get('source', 'unknown'),
        "feed_id": original_headline_obj.get('feed_id'),  # durchgereicht für DB-Persistenz
        "sentiment": score,
        "strength": strength
    })
    ```

    Achtung: Nicht die gesamte Funktion umschreiben — nur den results.append-Block minimal erweitern. Die Funktion hat ggf. mehrere solche Blöcke (Batch-Verarbeitung) — alle relevanten results.append-Aufrufe in der Funktion prüfen und feed_id ergänzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && python3 -c "
import ast, sys

# Prüfe background_worker.py
with open('sentiment-api/background_worker.py') as f:
    bw_content = f.read()

assert 'feed_id' in bw_content, 'feed_id fehlt in background_worker.py'
assert 'save_headlines' in bw_content, 'save_headlines fehlt in background_worker.py'
assert 'sentiment_history_id = db.save_sentiment' in bw_content, 'save_sentiment Rückgabewert nicht assigned'
print('OK: background_worker.py korrekt angepasst')

# Prüfe app.py
with open('sentiment-api/app.py') as f:
    app_content = f.read()

assert 'feed_id' in app_content, 'feed_id fehlt in app.py'
print('OK: app.py korrekt angepasst')

# Syntax-Check beider Dateien
for fname in ['sentiment-api/background_worker.py', 'sentiment-api/app.py']:
    with open(fname) as f:
        try:
            ast.parse(f.read())
            print(f'OK: {fname} — valides Python')
        except SyntaxError as e:
            print(f'FEHLER: {fname} — SyntaxError: {e}')
            sys.exit(1)
"</automated>
  </verify>
  <acceptance_criteria>
    - _fetch_headlines() reicht feed_id (numerische ID aus feed_row['id']) in jedem headlines-Dict mit
    - _perform_update() assigned den Rückgabewert von save_sentiment() als sentiment_history_id
    - _perform_update() ruft db.save_headlines(sentiment_history_id, headline_results) auf
    - Exception in save_headlines() wird gecatcht, geloggt, aber NICHT weitergeworfen
    - app.py results.append enthält "feed_id": original_headline_obj.get('feed_id')
    - Beide Dateien sind syntaktisch valides Python (kein SyntaxError)
    - Bestehende Funktionalität (save_sentiment, Cache-Invalidierung) bleibt unverändert
  </acceptance_criteria>
  <done>Background Worker persistiert Headlines nach jeder Analyse. feed_id wird lückenlos durch den Datenpfad mitgeführt. Bei save_headlines()-Fehler bleibt der Sentiment-Score gespeichert.</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:

1. Python-Syntax aller geänderten Dateien prüfen:
   ```bash
   cd /Users/simonluthe/Documents/auraos-moodlight
   python3 -m py_compile sentiment-api/database.py && echo "database.py OK"
   python3 -m py_compile sentiment-api/background_worker.py && echo "background_worker.py OK"
   python3 -m py_compile sentiment-api/app.py && echo "app.py OK"
   ```

2. Strukturprüfung — alle kritischen Symbole vorhanden:
   ```bash
   grep -n "def save_headlines" sentiment-api/database.py
   grep -n "sentiment_history_id = db.save_sentiment" sentiment-api/background_worker.py
   grep -n "db.save_headlines" sentiment-api/background_worker.py
   grep -n "feed_id.*feed_row" sentiment-api/background_worker.py
   grep -n "feed_id.*original_headline_obj" sentiment-api/app.py
   ```

3. migration_phase12.sql vorhanden und vollständig:
   ```bash
   grep -c "CREATE TABLE IF NOT EXISTS headlines" sentiment-api/migration_phase12.sql
   grep -c "IF NOT EXISTS idx_headlines" sentiment-api/migration_phase12.sql
   ```

Hinweis zur Produktionsmigration: migration_phase12.sql muss einmalig auf dem Server ausgeführt werden:
`docker exec -i <postgres-container> psql -U moodlight -d moodlight < migration_phase12.sql`
Der Executor dokumentiert diesen Schritt in der SUMMARY.
</verification>

<success_criteria>
- headlines-Tabelle ist in init.sql und migration_phase12.sql definiert (idempotent, korrekte FKs)
- Database.save_headlines() nutzt executemany, gibt int zurück, wirft bei Fehler Exception
- _fetch_headlines() reicht feed_id in jedem Dict mit
- _perform_update() assigned sentiment_history_id und ruft save_headlines() mit try/except auf
- app.py reicht feed_id durch analyze_headlines_openai_batch() durch
- Alle drei Python-Dateien sind syntaktisch valide
- Kein bestehender Code wurde ohne Notwendigkeit verändert
</success_criteria>

<output>
Nach Abschluss `.planning/phases/12-headline-persistenz/12-01-SUMMARY.md` erstellen mit:
- Was implementiert wurde (headlines-Tabelle, save_headlines(), Worker-Erweiterung)
- Welche Dateien geändert wurden mit kurzer Beschreibung der Änderung
- Produktionsmigrations-Hinweis: Befehl zum Ausführen von migration_phase12.sql auf dem Server
- Bekannte Limitierungen: /api/news-Route speichert keine Headlines (feed_id=None, akzeptabel per CONTEXT)
- Status: COMPLETE
</output>
