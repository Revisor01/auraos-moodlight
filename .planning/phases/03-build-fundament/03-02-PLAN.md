---
phase: 03-build-fundament
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - build-release.sh
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
must_haves:
  truths:
    - "`./build-release.sh minor` bumpt die Version in config.h und erzeugt ein Combined-TGZ"
    - "`./build-release.sh` ohne Argument gibt einen Usage-Fehler aus"
    - "Das Combined-TGZ enthaelt sowohl UI-Dateien als auch firmware.ino.bin"
    - "Bei pio run Fehler bricht das Script ab und setzt die Version zurueck"
    - "Nach erfolgreichem Build wird automatisch ein git commit erstellt"
  artifacts:
    - path: "build-release.sh"
      provides: "Komplettes Build-Release-Script mit Version-Bump, TGZ-Erstellung, Auto-Commit"
      contains: "bump_version"
  key_links:
    - from: "build-release.sh"
      to: "firmware/src/config.h"
      via: "grep + sed fuer Version-Bump"
      pattern: "MOODLIGHT_VERSION"
    - from: "build-release.sh"
      to: "firmware/.pio/build/esp32dev/firmware.bin"
      via: "pio run Build-Output"
      pattern: "firmware\\.bin"
    - from: "build-release.sh"
      to: "releases/"
      via: "TGZ-Artefakt-Erstellung"
      pattern: "AuraOS.*\\.tgz"
---

<objective>
Bestehendes build-release.sh komplett ersetzen durch ein neues Script das Version-Bump, Combined-TGZ-Erstellung und Auto-Commit in einem Durchlauf erledigt.

Purpose: Aktuelles Script hat 5 Bugs (kein Version-Bump-Argument, kein Combined-TGZ, kein Auto-Commit, `|| true` verschluckt Fehler, kein Empty-VERSION-Guard). Kompletter Ersatz ist sauberer als Patching.
Output: Ein funktionierendes `build-release.sh` das alle BUILD-Requirements erfuellt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-fundament/03-RESEARCH.md
@.planning/phases/03-build-fundament/03-01-SUMMARY.md

<interfaces>
<!-- Aus config.h -->
Zeile 5: #define MOODLIGHT_VERSION "9.0"

<!-- Build-Output Pfad -->
firmware/.pio/build/esp32dev/firmware.bin

<!-- UI-Dateien in firmware/data/ -->
index.html, setup.html, mood.html, diagnostics.html, js/, css/
(setup.html.tmp.html existiert — muss per --exclude ausgeschlossen werden)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build-Release-Script komplett ersetzen</name>
  <files>build-release.sh</files>
  <read_first>build-release.sh, firmware/src/config.h (Zeilen 1-7)</read_first>
  <action>
Ersetze den gesamten Inhalt von `build-release.sh` mit einem neuen Script. Das Script muss folgende Struktur haben:

```bash
#!/bin/bash
# AuraOS Moodlight - Combined Release Build Script
# Erzeugt ein versioniertes Combined-TGZ (UI + Firmware)
# Verwendung: ./build-release.sh [major|minor]

set -e

# --- Argument-Validierung (BUILD-01, BUILD-04) ---
BUMP_TYPE="${1:-}"
if [[ -z "$BUMP_TYPE" ]] || [[ ! "$BUMP_TYPE" =~ ^(major|minor)$ ]]; then
    echo "Verwendung: ./build-release.sh [major|minor]"
    echo ""
    echo "  major  - Erhoehe Major-Version (z.B. 9.1 -> 10.0)"
    echo "  minor  - Erhoehe Minor-Version (z.B. 9.1 -> 9.2)"
    echo ""
    echo "AuraOS verwendet X.Y Versionierung (kein Patch)."
    exit 1
fi

# --- Version lesen (mit Empty-Guard) ---
CONFIG_FILE="firmware/src/config.h"
VERSION=$(grep '^#define MOODLIGHT_VERSION' "$CONFIG_FILE" | cut -d'"' -f2)
if [[ -z "$VERSION" ]]; then
    echo "FEHLER: MOODLIGHT_VERSION nicht in $CONFIG_FILE gefunden"
    exit 1
fi

# --- Version bumpen (BUILD-01) ---
bump_version() {
    local current="$1"
    local type="$2"
    local major minor
    IFS='.' read -r major minor <<< "$current"
    case "$type" in
        major) echo "$((major + 1)).0" ;;
        minor) echo "${major}.$((minor + 1))" ;;
    esac
}

NEW_VERSION=$(bump_version "$VERSION" "$BUMP_TYPE")

echo "========================================"
echo "  AuraOS Combined Release Builder"
echo "========================================"
echo ""
echo "Version: ${VERSION} -> ${NEW_VERSION} (${BUMP_TYPE})"
echo ""

# --- config.h aktualisieren ---
sed -i '' "s/#define MOODLIGHT_VERSION \"${VERSION}\"/#define MOODLIGHT_VERSION \"${NEW_VERSION}\"/" "$CONFIG_FILE"

# --- Trap: Bei Fehler Version zuruecksetzen + tmp-Dateien aufraeumen ---
cleanup() {
    rm -f /tmp/auraos_combined.tar /tmp/firmware.ino.bin
}
rollback() {
    echo ""
    echo "FEHLER: Build fehlgeschlagen — Version wird zurueckgesetzt auf ${VERSION}"
    sed -i '' "s/#define MOODLIGHT_VERSION \"${NEW_VERSION}\"/#define MOODLIGHT_VERSION \"${VERSION}\"/" "$CONFIG_FILE"
    cleanup
}
trap rollback ERR
trap cleanup EXIT

# --- Firmware kompilieren (BUILD-04: kein || true, direkter pio run) ---
echo "[1/4] Kompiliere Firmware..."
cd firmware
pio run
cd ..
echo "   -> Firmware kompiliert"

# Verify firmware.bin exists
if [[ ! -f "firmware/.pio/build/esp32dev/firmware.bin" ]]; then
    echo "FEHLER: firmware.bin nicht gefunden nach Build"
    exit 1
fi

# --- Combined-TGZ erstellen (BUILD-02) ---
echo "[2/4] Erstelle Combined-TGZ..."
RELEASE_DIR="releases/v${NEW_VERSION}"
mkdir -p "$RELEASE_DIR"

# Schritt 1: UI-Dateien als unkomprimiertes TAR
(cd firmware/data && tar -cf /tmp/auraos_combined.tar \
    index.html setup.html mood.html diagnostics.html js/ css/ \
    --exclude="*.tmp.*" --exclude="*.tgz" --exclude="*.tar")

# Schritt 2: firmware.bin als firmware.ino.bin anhaengen (ESP32-targz Naming-Convention)
cp firmware/.pio/build/esp32dev/firmware.bin /tmp/firmware.ino.bin
tar -rf /tmp/auraos_combined.tar -C /tmp firmware.ino.bin

# Schritt 3: Gzippen
TGZ_NAME="AuraOS-${NEW_VERSION}.tgz"
gzip -c /tmp/auraos_combined.tar > "${RELEASE_DIR}/${TGZ_NAME}"
echo "   -> ${TGZ_NAME}: $(ls -lh "${RELEASE_DIR}/${TGZ_NAME}" | awk '{print $5}')"

# --- TGZ verifizieren ---
echo "[3/4] Verifiziere TGZ-Inhalt..."
TAR_CONTENTS=$(tar -tzf "${RELEASE_DIR}/${TGZ_NAME}")
if ! echo "$TAR_CONTENTS" | grep -q "firmware.ino.bin"; then
    echo "FEHLER: firmware.ino.bin fehlt im TGZ"
    exit 1
fi
if ! echo "$TAR_CONTENTS" | grep -q "index.html"; then
    echo "FEHLER: index.html fehlt im TGZ"
    exit 1
fi
echo "   -> TGZ verifiziert (UI-Dateien + firmware.ino.bin vorhanden)"

# --- Git Commit (BUILD-03: nur nach erfolgreichem Build) ---
echo "[4/4] Committe Version-Bump + Release-Artefakt..."
git add "$CONFIG_FILE" "${RELEASE_DIR}/${TGZ_NAME}"
git commit -m "Release: AuraOS v${NEW_VERSION}"

echo ""
echo "========================================"
echo "  Build erfolgreich!"
echo "========================================"
echo ""
echo "Version:  ${NEW_VERSION}"
echo "Artefakt: ${RELEASE_DIR}/${TGZ_NAME}"
echo "Commit:   $(git log --oneline -1)"
echo ""
echo "TGZ-Inhalt:"
tar -tzf "${RELEASE_DIR}/${TGZ_NAME}"
echo ""
```

WICHTIGE DETAILS:
- `set -e` am Anfang — Script bricht bei jedem Fehler ab (BUILD-04)
- `pio run` direkt ohne Pipe, ohne `|| true` (BUILD-04)
- `trap rollback ERR` setzt Version in config.h zurueck bei Fehler
- `trap cleanup EXIT` raeumt /tmp-Dateien auf
- Zwei-Schritt tar+gzip fuer macOS-Kompatibilitaet (kein GNU --transform)
- `sed -i ''` fuer macOS BSD sed
- `patch` als Argument wird abgelehnt (AuraOS nutzt nur X.Y)
- firmware.ino.bin Naming-Convention fuer ESP32-targz tarGzStreamUpdater
- TGZ-Verifikation vor dem Commit
  </action>
  <verify>
    <automated>grep -c "bump_version" /Users/simonluthe/Documents/auraos-moodlight/build-release.sh && grep -c "trap rollback ERR" /Users/simonluthe/Documents/auraos-moodlight/build-release.sh && grep -c "pio run" /Users/simonluthe/Documents/auraos-moodlight/build-release.sh && ! grep -q '|| true' /Users/simonluthe/Documents/auraos-moodlight/build-release.sh && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bump_version" build-release.sh returns >= 1
    - grep -c "trap rollback ERR" build-release.sh returns 1
    - grep -q "|| true" build-release.sh returns false (exit code 1 = kein || true vorhanden)
    - grep -c "firmware.ino.bin" build-release.sh returns >= 2
    - grep -c "AuraOS-.*\.tgz" build-release.sh returns >= 1
    - grep -c "git commit" build-release.sh returns 1
    - grep -c 'BUMP_TYPE.*major|minor' build-release.sh returns >= 1 (akzeptiert nur major/minor)
    - head -1 build-release.sh returns "#!/bin/bash"
  </acceptance_criteria>
  <done>build-release.sh ist komplett ersetzt. Script akzeptiert major/minor als Argument (BUILD-01), erzeugt Combined-TGZ mit UI+firmware.ino.bin (BUILD-02), committed nach erfolgreichem Build (BUILD-03), bricht bei pio-Fehler ab und setzt Version zurueck (BUILD-04).</done>
</task>

<task type="auto">
  <name>Task 2: Build-Script Smoke-Test mit minor Bump</name>
  <files>firmware/src/config.h, releases/</files>
  <read_first>build-release.sh, firmware/src/config.h (Zeilen 1-7)</read_first>
  <action>
Fuehre `./build-release.sh minor` aus und verifiziere das Ergebnis:

1. Fuehre `./build-release.sh minor` aus (Version 9.0 -> 9.1)
2. Pruefe dass config.h jetzt Version "9.1" enthaelt
3. Pruefe dass `releases/v9.1/AuraOS-9.1.tgz` existiert
4. Pruefe TGZ-Inhalt: `tar -tzf releases/v9.1/AuraOS-9.1.tgz` muss firmware.ino.bin und index.html enthalten
5. Pruefe git log: letzter Commit muss "Release: AuraOS v9.1" sein
6. Pruefe Usage-Fehler: `./build-release.sh` ohne Argument muss mit Exit-Code 1 enden
7. Pruefe Usage-Fehler: `./build-release.sh patch` muss mit Exit-Code 1 enden

HINWEIS: Da dies einen echten Build + Commit ausfuehrt, ist dies gleichzeitig der Funktionstest fuer alle BUILD-Requirements.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -q 'MOODLIGHT_VERSION "9.1"' firmware/src/config.h && test -f releases/v9.1/AuraOS-9.1.tgz && tar -tzf releases/v9.1/AuraOS-9.1.tgz | grep -q firmware.ino.bin && tar -tzf releases/v9.1/AuraOS-9.1.tgz | grep -q index.html && git log --oneline -1 | grep -q "v9.1" && echo "ALL SMOKE TESTS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'MOODLIGHT_VERSION "9.1"' firmware/src/config.h returns match
    - test -f releases/v9.1/AuraOS-9.1.tgz returns true
    - tar -tzf releases/v9.1/AuraOS-9.1.tgz | grep firmware.ino.bin returns match
    - tar -tzf releases/v9.1/AuraOS-9.1.tgz | grep index.html returns match
    - git log --oneline -1 | grep "v9.1" returns match
    - ./build-release.sh exits with code 1 (Usage-Fehler)
    - ./build-release.sh patch exits with code 1 (ungueltig)
  </acceptance_criteria>
  <done>build-release.sh minor hat erfolgreich Version 9.0 -> 9.1 gebumpt, Combined-TGZ mit UI+Firmware erzeugt, und automatisch committed. Usage-Fehler bei fehlendem/ungueltigem Argument bestaetigt.</done>
</task>

</tasks>

<verification>
1. `./build-release.sh` ohne Argument → Usage-Fehler, Exit 1
2. `./build-release.sh patch` → Usage-Fehler, Exit 1
3. `./build-release.sh minor` → Version bumpt, Combined-TGZ erstellt, git commit erfolgt
4. TGZ enthaelt firmware.ino.bin + alle UI-Dateien
5. config.h zeigt neue Version
6. git log zeigt Auto-Commit
</verification>

<success_criteria>
- build-release.sh akzeptiert major/minor und lehnt andere Argumente ab (BUILD-01)
- Combined-TGZ enthaelt UI-Dateien UND firmware.ino.bin (BUILD-02)
- Git commit erfolgt automatisch nach erfolgreichem Build (BUILD-03)
- Kein `|| true` im Script, pio run bricht bei Fehler ab (BUILD-04)
- Version-Rollback bei Build-Fehler funktioniert (trap ERR)
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-fundament/03-02-SUMMARY.md`
</output>
