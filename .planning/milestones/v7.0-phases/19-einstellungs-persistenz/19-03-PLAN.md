---
phase: 19-einstellungs-persistenz
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - sentiment-api/background_worker.py
  - sentiment-api/moodlight_extensions.py
autonomous: true
requirements:
  - CFG-03
  - API-01

must_haves:
  truths:
    - "SentimentUpdateWorker hat eine reconfigure(interval_seconds, headlines_per_source)-Methode"
    - "Der Worker liest headlines_per_source aus DB bei jedem Update-Zyklus"
    - "GET /api/moodlight/settings gibt aktuelle Einstellungen zurück (API Key maskiert)"
    - "PUT /api/moodlight/settings aktualisiert Einstellungen in der DB und rekonfiguriert den Worker sofort"
  artifacts:
    - path: "sentiment-api/background_worker.py"
      provides: "reconfigure()-Methode + dynamisches headlines_per_source"
      contains: "def reconfigure"
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "GET und PUT /api/moodlight/settings Endpoints"
      contains: "api/moodlight/settings"
  key_links:
    - from: "sentiment-api/moodlight_extensions.py"
      to: "sentiment-api/background_worker.py"
      via: "get_background_worker().reconfigure(...)"
      pattern: "get_background_worker\\(\\)\\.reconfigure"
    - from: "sentiment-api/moodlight_extensions.py"
      to: "sentiment-api/database.py"
      via: "get_database().set_setting(key, value)"
      pattern: "set_setting\\("
---

<objective>
Worker-Reconfigure-Methode implementieren und GET/PUT /api/moodlight/settings Endpoints anlegen. Einstellungsänderungen werden sofort wirksam ohne Container-Neustart.

Purpose: CFG-03 + API-01 — Runtime-Reload und die API-Schnittstelle, die Phase 21 (Dashboard-UI) konsumiert.
Output: background_worker.py mit reconfigure(); moodlight_extensions.py mit zwei neuen Endpoints
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-einstellungs-persistenz/19-CONTEXT.md
@.planning/phases/19-einstellungs-persistenz/19-02-SUMMARY.md

<interfaces>
<!-- SentimentUpdateWorker — bestehende Felder und Methoden -->

Aus sentiment-api/background_worker.py:
```python
class SentimentUpdateWorker:
    def __init__(self, app, analyze_function, interval_seconds=1800):
        self.app = app
        self.analyze_function = analyze_function
        self.interval_seconds = interval_seconds  # <-- muss via reconfigure() änderbar sein
        self.running = False
        self.thread = None

    def _worker_loop(self):
        time.sleep(10)
        while self.running:
            try:
                with self.app.app_context():
                    self._perform_update()
            except Exception as e:
                logger.error(f"Fehler im Background Worker: {e}", exc_info=True)
            # interval_seconds wird hier gelesen:
            logger.info(f"Nächstes Update in {self.interval_seconds} Sekunden...")
            time.sleep(self.interval_seconds)

    def _fetch_headlines(self):
        num_headlines_per_source = 1  # <-- hardcoded, muss aus DB/self gelesen werden
```

Singleton-Funktion (Zeilen 211–232):
```python
_worker = None

def start_background_worker(app, analyze_function, interval_seconds=1800):
    global _worker
    if _worker is not None:
        return _worker
    _worker = SentimentUpdateWorker(app, analyze_function, interval_seconds)
    _worker.start()
    return _worker

def stop_background_worker():
    global _worker
    if _worker is not None:
        _worker.stop()
        _worker = None
```

<!-- api_login_required und Endpoint-Muster in moodlight_extensions.py -->

Aus sentiment-api/moodlight_extensions.py:
```python
def api_login_required(f):
    """Decorator für API-Endpoints: gibt 401 JSON zurück statt Redirect."""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not session.get('authenticated'):
            return jsonify({"status": "error", "message": "Nicht authentifiziert"}), 401
        return f(*args, **kwargs)
    return decorated_function

# Endpoint-Muster (POST mit @api_login_required):
@app.route('/api/moodlight/cache/clear', methods=['POST'])
@api_login_required
def clear_moodlight_cache():
    try:
        ...
        return jsonify({"status": "success", "message": "Cache gelöscht"})
    except Exception as e:
        logger.error(f"...: {e}", exc_info=True)
        return jsonify({"status": "error", "message": str(e)}), 500
```

<!-- Neue database.py-Methoden aus Plan 02 -->
```python
# Auf der Database-Klasse verfügbar nach Plan 02:
db.get_setting(key: str, default=None) -> Optional[str]
db.set_setting(key: str, value: str) -> bool
db.get_all_settings() -> Dict[str, str]
```

<!-- app.py — anthropic_client ist global -->
```python
# In app.py (global):
anthropic_client = None  # oder Anthropic-Instanz
_admin_password_hash = None  # oder werkzeug-Hash
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconfigure() in SentimentUpdateWorker + dynamisches headlines_per_source</name>
  <files>sentiment-api/background_worker.py</files>
  <action>
Zwei Änderungen in sentiment-api/background_worker.py:

**1. `self.headlines_per_source` als Instanzvariable im `__init__` ergänzen:**

```python
def __init__(self, app, analyze_function, interval_seconds=1800):
    self.app = app
    self.analyze_function = analyze_function
    self.interval_seconds = interval_seconds
    self.headlines_per_source = 1  # NEU: dynamisch via reconfigure() änderbar
    self.running = False
    self.thread = None
```

**2. Neue Methode `reconfigure()` nach `stop()` einfügen:**

```python
def reconfigure(self, interval_seconds: int = None, headlines_per_source: int = None):
    """
    Rekonfiguriere Worker-Parameter zur Laufzeit (ohne Neustart).

    Args:
        interval_seconds: Neues Update-Intervall in Sekunden (None = unverändert)
        headlines_per_source: Neue Headlines-Anzahl pro Feed (None = unverändert)
    """
    if interval_seconds is not None and interval_seconds > 0:
        old_interval = self.interval_seconds
        self.interval_seconds = interval_seconds
        logger.info(f"Worker-Intervall geändert: {old_interval}s → {interval_seconds}s")

    if headlines_per_source is not None and headlines_per_source > 0:
        old_headlines = self.headlines_per_source
        self.headlines_per_source = headlines_per_source
        logger.info(f"Worker-Headlines geändert: {old_headlines} → {headlines_per_source}")
```

**3. In `_fetch_headlines()` den hardcoded Wert durch `self.headlines_per_source` ersetzen:**

Zeile 161: `num_headlines_per_source = 1` ersetzen durch:
```python
num_headlines_per_source = self.headlines_per_source
```

**4. Neue Accessor-Funktion `get_background_worker()` am Ende der Datei ergänzen** (nach `stop_background_worker()`):

```python
def get_background_worker() -> 'SentimentUpdateWorker':
    """Hole die laufende Worker-Instanz (für reconfigure()-Aufrufe aus API-Endpoints)."""
    global _worker
    return _worker
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && python3 -c "import ast; ast.parse(open('sentiment-api/background_worker.py').read()); print('Syntax OK')" && grep -c "def reconfigure" sentiment-api/background_worker.py && grep "self.headlines_per_source" sentiment-api/background_worker.py</automated>
  </verify>
  <done>Syntax OK; `grep -c "def reconfigure"` gibt 1; `grep "self.headlines_per_source"` findet Treffer in __init__ und _fetch_headlines</done>
</task>

<task type="auto">
  <name>Task 2: GET/PUT /api/moodlight/settings in moodlight_extensions.py</name>
  <files>sentiment-api/moodlight_extensions.py</files>
  <action>
Am Ende der `register_moodlight_endpoints(app)`-Funktion in moodlight_extensions.py (vor dem abschließenden `logger.info("Moodlight API-Endpunkte registriert")`), zwei neue Endpoints einfügen:

**Import ergänzen:** Ganz oben in der Datei (nach den bestehenden Imports) folgendes hinzufügen, falls nicht bereits vorhanden:
```python
from background_worker import get_background_worker
import anthropic as anthropic_sdk
```

**GET /api/moodlight/settings:**
```python
@app.route('/api/moodlight/settings', methods=['GET'])
@api_login_required
def get_moodlight_settings():
    """
    Aktuelle Backend-Einstellungen zurückgeben (CFG-01, API-01).
    API Key wird maskiert zurückgegeben: erste 10 Zeichen + '****'
    """
    try:
        db = get_database()
        settings = db.get_all_settings()

        # analysis_interval: Sekunden → Minuten für Anzeige
        interval_seconds = int(settings.get('analysis_interval', '1800'))

        # anthropic_api_key maskieren
        raw_key = settings.get('anthropic_api_key', '')
        if raw_key and len(raw_key) > 10:
            masked_key = raw_key[:10] + '****'
        elif raw_key:
            masked_key = '****'
        else:
            masked_key = ''

        # admin_password_hash nie zurückgeben — nur ob gesetzt
        has_password = bool(settings.get('admin_password_hash', '').strip())

        return jsonify({
            "status": "success",
            "settings": {
                "analysis_interval_seconds": interval_seconds,
                "analysis_interval_minutes": round(interval_seconds / 60, 1),
                "headlines_per_source": int(settings.get('headlines_per_source', '1')),
                "anthropic_api_key": masked_key,
                "has_admin_password": has_password
            }
        })

    except Exception as e:
        logger.error(f"Fehler in GET /api/moodlight/settings: {e}", exc_info=True)
        return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
```

**PUT /api/moodlight/settings:**
```python
@app.route('/api/moodlight/settings', methods=['PUT'])
@api_login_required
def update_moodlight_settings():
    """
    Einstellungen aktualisieren und sofort wirksam machen (CFG-03, API-01).

    Body (JSON, alle Felder optional):
      {
        "analysis_interval_minutes": 30,   -- Analyse-Frequenz in Minuten
        "headlines_per_source": 2,          -- Headlines pro RSS-Feed
        "anthropic_api_key": "sk-ant-...",  -- Neuer API Key (leer = unverändert)
        "admin_password": "neues_pw",       -- Neues Admin-Passwort
        "current_password": "altes_pw"      -- Pflichtfeld wenn admin_password gesetzt
      }
    """
    try:
        data = request.get_json(silent=True)
        if not data:
            return jsonify({"status": "error", "message": "JSON-Body erwartet"}), 400

        db = get_database()
        worker = get_background_worker()
        changes = []

        # --- analysis_interval ---
        if 'analysis_interval_minutes' in data:
            try:
                minutes = float(data['analysis_interval_minutes'])
                if minutes < 1:
                    return jsonify({"status": "error", "message": "analysis_interval_minutes muss >= 1 sein"}), 400
                seconds = int(minutes * 60)
                db.set_setting('analysis_interval', str(seconds))
                if worker:
                    worker.reconfigure(interval_seconds=seconds)
                changes.append(f"analysis_interval={seconds}s")
            except (ValueError, TypeError):
                return jsonify({"status": "error", "message": "analysis_interval_minutes muss eine Zahl sein"}), 400

        # --- headlines_per_source ---
        if 'headlines_per_source' in data:
            try:
                count = int(data['headlines_per_source'])
                if count < 1:
                    return jsonify({"status": "error", "message": "headlines_per_source muss >= 1 sein"}), 400
                db.set_setting('headlines_per_source', str(count))
                if worker:
                    worker.reconfigure(headlines_per_source=count)
                changes.append(f"headlines_per_source={count}")
            except (ValueError, TypeError):
                return jsonify({"status": "error", "message": "headlines_per_source muss eine ganze Zahl sein"}), 400

        # --- anthropic_api_key ---
        if 'anthropic_api_key' in data:
            new_key = str(data['anthropic_api_key']).strip()
            if new_key:  # Leer = unverändert
                db.set_setting('anthropic_api_key', new_key)
                # anthropic_client in app.py neu initialisieren
                try:
                    import app as main_app
                    main_app.anthropic_client = anthropic_sdk.Anthropic(api_key=new_key, timeout=45.0)
                    logger.info("Anthropic Client mit neuem API Key neu initialisiert")
                except Exception as e:
                    logger.error(f"Fehler beim Neu-Initialisieren des Anthropic Client: {e}")
                changes.append("anthropic_api_key=****")

        # --- admin_password ---
        if 'admin_password' in data:
            new_password = str(data.get('admin_password', '')).strip()
            current_password = str(data.get('current_password', '')).strip()

            if not new_password:
                return jsonify({"status": "error", "message": "admin_password darf nicht leer sein"}), 400
            if not current_password:
                return jsonify({"status": "error", "message": "current_password ist Pflichtfeld beim Passwort-Ändern"}), 400

            # Altes Passwort prüfen
            try:
                import app as main_app
                from werkzeug.security import check_password_hash, generate_password_hash
                if not main_app._admin_password_hash or not check_password_hash(main_app._admin_password_hash, current_password):
                    return jsonify({"status": "error", "message": "Aktuelles Passwort falsch"}), 403
                new_hash = generate_password_hash(new_password)
                db.set_setting('admin_password_hash', new_hash)
                main_app._admin_password_hash = new_hash
                changes.append("admin_password=****")
            except Exception as e:
                logger.error(f"Fehler beim Passwort-Ändern: {e}")
                return jsonify({"status": "error", "message": "Fehler beim Passwort-Ändern"}), 500

        if not changes:
            return jsonify({"status": "ok", "message": "Keine Änderungen angegeben"}), 200

        logger.info(f"Einstellungen aktualisiert: {', '.join(changes)}")
        return jsonify({
            "status": "success",
            "message": f"Einstellungen gespeichert: {', '.join(changes)}",
            "changes": changes
        })

    except Exception as e:
        logger.error(f"Fehler in PUT /api/moodlight/settings: {e}", exc_info=True)
        return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && python3 -c "import ast; ast.parse(open('sentiment-api/moodlight_extensions.py').read()); print('Syntax OK')" && grep -c "api/moodlight/settings" sentiment-api/moodlight_extensions.py</automated>
  </verify>
  <done>Syntax OK; `grep -c "api/moodlight/settings"` gibt mindestens 2 (GET + PUT route decorator)</done>
</task>

</tasks>

<verification>
- `python3 -c "import ast; ast.parse(open('sentiment-api/background_worker.py').read())"` → kein Fehler
- `python3 -c "import ast; ast.parse(open('sentiment-api/moodlight_extensions.py').read())"` → kein Fehler
- `grep "def reconfigure" sentiment-api/background_worker.py` → 1 Treffer
- `grep "def get_background_worker" sentiment-api/background_worker.py` → 1 Treffer
- `grep "api/moodlight/settings" sentiment-api/moodlight_extensions.py` → mind. 2 Treffer (GET + PUT)
- `curl -s http://localhost:6237/api/moodlight/settings` → 401 (nicht eingeloggt = korrekt)
</verification>

<success_criteria>
- PUT /api/moodlight/settings mit analysis_interval_minutes=15 ändert worker.interval_seconds auf 900 ohne Neustart
- GET /api/moodlight/settings gibt anthropic_api_key maskiert zurück (sk-ant-api0...****)
- Fehlerhafte Eingaben (Buchstaben in Zahlenfeldern, leeres admin_password) werden mit 400 abgelehnt
- Passwort-Änderung ohne current_password → 400; falsches current_password → 403
</success_criteria>

<output>
Nach Abschluss `.planning/phases/19-einstellungs-persistenz/19-03-SUMMARY.md` erstellen.
</output>
