---
phase: 20-manueller-analyse-trigger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/background_worker.py
  - sentiment-api/moodlight_extensions.py
autonomous: true
requirements: [API-02, CTRL-01]

must_haves:
  truths:
    - "POST /api/moodlight/analyze/trigger startet eine vollständige Sentiment-Analyse und gibt das Ergebnis zurück"
    - "Der Endpoint erfordert eine aktive Session (401 ohne Login)"
    - "Das Ergebnis enthält sentiment_score, category und headlines_analyzed"
    - "Fehler in der Analyse geben 500 mit JSON-Fehlermeldung zurück, nicht einen Python-Traceback"
  artifacts:
    - path: "sentiment-api/background_worker.py"
      provides: "trigger()-Methode auf SentimentUpdateWorker"
      contains: "def trigger"
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "POST /api/moodlight/analyze/trigger Endpoint"
      contains: "analyze/trigger"
  key_links:
    - from: "sentiment-api/moodlight_extensions.py"
      to: "background_worker.SentimentUpdateWorker._perform_update"
      via: "worker.trigger()"
      pattern: "worker\\.trigger"
    - from: "sentiment-api/moodlight_extensions.py"
      to: "@api_login_required"
      via: "Decorator auf trigger-Endpoint"
      pattern: "api_login_required"
---

<objective>
Trigger-Endpoint für manuelle Sentiment-Analyse implementieren.

Purpose: Ermöglicht dem Dashboard-Nutzer, eine sofortige Analyse außerhalb des 30-Minuten-Intervalls auszulösen.
Output: POST /api/moodlight/analyze/trigger — führt _perform_update() synchron aus und gibt Ergebnis als JSON zurück.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aus sentiment-api/background_worker.py -->

```python
class SentimentUpdateWorker:
    def __init__(self, app, analyze_function, interval_seconds=1800): ...
    def reconfigure(self, interval_seconds=None, headlines_per_source=None): ...
    def _perform_update(self): ...  # vollständige Analyse-Logik, kein Rückgabewert (void)
    def _fetch_headlines(self): ...  # gibt list[dict] zurück

def get_background_worker() -> SentimentUpdateWorker: ...
```

<!-- Aus sentiment-api/moodlight_extensions.py -->

```python
def api_login_required(f): ...  # Decorator: gibt 401 JSON zurück wenn nicht eingeloggt

# Registrierungsmuster für neue Endpoints:
def register_moodlight_endpoints(app):
    @app.route('/api/moodlight/...', methods=['POST'])
    @api_login_required
    def endpoint_name():
        ...
```

<!-- Aus sentiment-api/database.py (pattern) -->
```python
db = get_database()
latest = db.get_latest_sentiment()
# latest dict: {sentiment_score, category, headlines_analyzed, timestamp, ...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: trigger()-Methode zum SentimentUpdateWorker hinzufügen</name>
  <files>sentiment-api/background_worker.py</files>
  <read_first>sentiment-api/background_worker.py — vollständig (bereits gelesen, kein erneuter Read nötig)</read_first>
  <action>
    Die `_perform_update()`-Methode gibt nichts zurück (void) — für den Trigger-Endpoint brauchen wir das Ergebnis.

    Neue public Methode `trigger()` in der Klasse `SentimentUpdateWorker` hinzufügen, direkt nach `reconfigure()` (vor `_worker_loop`):

    ```python
    def trigger(self) -> dict:
        """
        Führe sofortige Sentiment-Analyse durch (für manuellen Trigger vom Dashboard).
        Gibt Ergebnis-Dict zurück oder wirft Exception bei Fehler.

        Returns:
            dict mit: sentiment_score, category, headlines_analyzed, source_count, duration_seconds
        """
        import time as _time
        start = _time.time()
        logger.info("=== Manueller Trigger: Starte Sentiment-Update ===")

        # Headlines holen
        headlines = self._fetch_headlines()
        if not headlines:
            raise RuntimeError("Keine Headlines von den Feeds erhalten")

        feed_count = len(get_database().get_active_feeds())

        # Sentiment analysieren
        analysis_result = self.analyze_function(headlines)
        if not analysis_result or 'total_sentiment' not in analysis_result:
            raise RuntimeError("Analyse lieferte kein verwertbares Ergebnis")

        sentiment_score = analysis_result['total_sentiment']
        stats = analysis_result.get('statistics', {})
        headlines_analyzed = stats.get('analyzed_count', len(headlines))

        # In DB speichern
        db = get_database()
        from shared_config import get_sentiment_category
        category = get_sentiment_category(sentiment_score)
        response_time_ms = int((_time.time() - start) * 1000)

        sentiment_history_id = db.save_sentiment(
            sentiment_score=sentiment_score,
            category=category,
            headlines_analyzed=headlines_analyzed,
            source_count=feed_count,
            api_response_time_ms=response_time_ms,
            metadata={
                'sentiment_distribution': stats.get('sentiment_distribution', {}),
                'worker': 'manual_trigger',
                'timestamp': datetime.now().isoformat()
            }
        )

        # Headlines persistieren (Fehler bricht nicht ab)
        try:
            headline_results = analysis_result.get('results', [])
            if headline_results:
                db.save_headlines(sentiment_history_id=sentiment_history_id, results=headline_results)
        except Exception as e:
            logger.error(f"Fehler beim Persistieren der Headlines (manueller Trigger): {e}")

        # Redis-Cache invalidieren
        get_cache().delete('moodlight:current:v2')
        get_cache().delete('moodlight:current')

        elapsed = _time.time() - start
        logger.info(f"=== Manueller Trigger abgeschlossen in {elapsed:.2f}s ===")

        return {
            'sentiment_score': sentiment_score,
            'category': category,
            'headlines_analyzed': headlines_analyzed,
            'source_count': feed_count,
            'duration_seconds': round(elapsed, 2)
        }
    ```

    HINWEIS: `import time as _time` innerhalb der Methode verwenden, um Konflikt mit dem Modul-Level `import time` zu vermeiden — oder den bereits vorhandenen `time`-Import auf Modul-Ebene nutzen (der existiert bereits). In dem Fall einfach `time.time()` und `_time` weglassen.

    Da `import time` bereits auf Zeile 8 existiert, `time.time()` direkt nutzen und `import time as _time` weglassen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "from background_worker import SentimentUpdateWorker; print('trigger' in dir(SentimentUpdateWorker))"</automated>
  </verify>
  <done>
    `SentimentUpdateWorker` hat eine public `trigger()`-Methode, die ein dict zurückgibt (kein void wie `_perform_update`). Import-Check läuft ohne Fehler.
  </done>
  <acceptance_criteria>
    - `trigger()` ist public (kein Underscore-Präfix)
    - Gibt `dict` zurück mit keys: `sentiment_score`, `category`, `headlines_analyzed`, `source_count`, `duration_seconds`
    - Invalidiert Redis-Cache nach Analyse (beide Keys: `moodlight:current:v2` und `moodlight:current`)
    - Speichert Ergebnis in DB mit `worker: 'manual_trigger'` im metadata-Dict
    - Wirft `RuntimeError` (kein stiller Return) wenn keine Headlines oder Analyse-Fehler
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: POST /api/moodlight/analyze/trigger Endpoint registrieren</name>
  <files>sentiment-api/moodlight_extensions.py</files>
  <read_first>sentiment-api/moodlight_extensions.py — Zeilen 340-365 (Muster von clear_moodlight_cache als Vorlage)</read_first>
  <action>
    Neuen Endpoint direkt nach dem `clear_moodlight_cache`-Endpoint (nach Zeile 364) und VOR den Feed-Verwaltungs-Endpoints einfügen, innerhalb von `register_moodlight_endpoints(app)`:

    ```python
    # ===== MANUELLER ANALYSE-TRIGGER (CTRL-01, API-02) =====
    @app.route('/api/moodlight/analyze/trigger', methods=['POST'])
    @api_login_required
    def trigger_moodlight_analysis():
        """
        Startet sofortige Sentiment-Analyse (manueller Trigger vom Dashboard).
        Synchroner Request — dauert ca. 10–30 Sekunden.
        Gibt Analyse-Ergebnis zurück: sentiment_score, category, headlines_analyzed.
        """
        try:
            worker = get_background_worker()
            if worker is None:
                return jsonify({
                    "status": "error",
                    "message": "Background Worker nicht verfügbar"
                }), 503

            with worker.app.app_context():
                result = worker.trigger()

            logger.info(f"Manueller Trigger abgeschlossen: score={result['sentiment_score']:.3f}")

            return jsonify({
                "status": "success",
                "sentiment_score": result['sentiment_score'],
                "category": result['category'],
                "headlines_analyzed": result['headlines_analyzed'],
                "source_count": result['source_count'],
                "duration_seconds": result['duration_seconds']
            })

        except RuntimeError as e:
            logger.error(f"Manueller Trigger fehlgeschlagen (erwartet): {e}")
            return jsonify({"status": "error", "message": str(e)}), 422
        except Exception as e:
            logger.error(f"Fehler in POST /api/moodlight/analyze/trigger: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
    ```

    WICHTIG: Der Endpoint wird bereits in einem Flask App-Context aufgerufen (Flask Request-Handling). Die `worker.trigger()`-Methode ruft intern `get_database()` auf — das funktioniert nur mit aktivem App-Context. Da der Request-Handler bereits im App-Context läuft, ist `with worker.app.app_context()` korrekt, um sicherzustellen dass DB-Zugriff funktioniert (analog zu `_worker_loop`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "import moodlight_extensions; print('trigger_moodlight_analysis registrierbar')"</automated>
  </verify>
  <done>
    `POST /api/moodlight/analyze/trigger` ist registriert, mit `@api_login_required` geschützt.
    Ohne Login: 401 JSON. Mit Login: startet Analyse, gibt Ergebnis zurück.
    422 bei `RuntimeError` (keine Headlines / Analyse-Fehler), 500 bei unerwartetem Fehler.
  </done>
  <acceptance_criteria>
    - Endpoint `POST /api/moodlight/analyze/trigger` existiert in `register_moodlight_endpoints()`
    - `@api_login_required` Decorator ist gesetzt
    - Response bei Erfolg: `{"status": "success", "sentiment_score": ..., "category": ..., "headlines_analyzed": ..., "source_count": ..., "duration_seconds": ...}`
    - `worker is None` → 503 (Worker noch nicht gestartet)
    - `RuntimeError` → 422 mit Fehlermeldung
    - Unerwartete Exception → 500
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api
# Syntax-Check beider Dateien
python -m py_compile background_worker.py && echo "background_worker.py OK"
python -m py_compile moodlight_extensions.py && echo "moodlight_extensions.py OK"

# Methode existiert
python -c "from background_worker import SentimentUpdateWorker; assert hasattr(SentimentUpdateWorker, 'trigger'), 'trigger fehlt'; print('OK')"

# Endpoint-Registrierung läuft ohne Fehler
python -c "from moodlight_extensions import register_moodlight_endpoints; print('Import OK')"
```
</verification>

<success_criteria>
- `SentimentUpdateWorker.trigger()` existiert und gibt dict zurück
- `POST /api/moodlight/analyze/trigger` ist in `moodlight_extensions.py` registriert
- Beide Dateien kompilieren ohne Syntaxfehler
- Ohne Session → 401, mit Session → startet Analyse
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/20-manueller-analyse-trigger/20-01-SUMMARY.md` erstellen.
</output>
