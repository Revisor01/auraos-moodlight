---
phase: 18-esp32-dashboard-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/sensor_manager.cpp
  - firmware/src/sensor_manager.h
autonomous: true
requirements: [ESP-01, ESP-02]

must_haves:
  truths:
    - "ESP32 nutzt led_index aus der API-Response statt mapSentimentToLED() aufzurufen"
    - "Wenn die API kein led_index-Feld liefert, greift mapSentimentToLED() als Fallback"
    - "Bei thresholds.fallback=true erscheint ein Hinweis im Debug-Log"
  artifacts:
    - path: "firmware/src/sensor_manager.cpp"
      provides: "Erweitertes JSON-Parsing in getSentiment(): led_index, percentile, thresholds.fallback"
      contains: "doc[\"led_index\"]"
    - path: "firmware/src/sensor_manager.h"
      provides: "mapSentimentToLED() bleibt deklariert als Fallback-Funktion"
  key_links:
    - from: "firmware/src/sensor_manager.cpp:getSentiment()"
      to: "handleSentiment()"
      via: "led_index aus JSON-Response direkt an handleSentiment() übergeben"
      pattern: "doc\\[.led_index.\\]"
---

<objective>
ESP32 liest den vorberechneten led_index direkt aus der /api/moodlight/current Response und übergibt ihn an handleSentiment(). Die hardcoded Schwellwerte in mapSentimentToLED() fungieren nur noch als Fallback wenn das Feld fehlt.

Purpose: ESP-01 + ESP-02 — dynamische Skalierung auf dem Gerät ohne Firmware-Anpassung bei Backend-Änderungen.
Output: sensor_manager.cpp mit erweitertem JSON-Parsing; mapSentimentToLED() bleibt als Legacy-Fallback erhalten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dynamische-skalierung/17-02-SUMMARY.md

<interfaces>
<!-- API-Response-Felder (Phase 17 — moodlight_extensions.py) -->
<!-- GET /api/moodlight/current liefert seit Phase 17: -->
```json
{
  "status": "success",
  "sentiment": -0.1234,
  "raw_score": -0.1234,
  "led_index": 2,
  "percentile": 0.47,
  "category": "neutral",
  "timestamp": "2026-03-27T09:00:00Z",
  "headline_count": 42,
  "thresholds": {
    "p20": -0.35,
    "p40": -0.10,
    "p60": 0.05,
    "p80": 0.28,
    "fallback": false
  },
  "historical": {
    "min": -0.62,
    "max": 0.41,
    "median": -0.08,
    "count": 168
  }
}
```

Fallback-Fall (weniger als 3 historische Datenpunkte):
  "thresholds": { "p20": -0.5, "p40": -0.2, "p60": 0.1, "p80": 0.3, "fallback": true }

<!-- Aktuelle Signaturen (firmware/src/sensor_manager.h) -->
```cpp
int mapSentimentToLED(float sentimentScore);   // bleibt als Fallback
void handleSentiment(float sentimentScore);    // UNVERÄNDERT — erwartet float
bool safeHttpGet(const String &url, JsonDocument &doc);
void getSentiment();
```

<!-- AppState-Felder (firmware/src/app_state.h) -->
appState.apiUrl          // String — URL für /api/moodlight/current
appState.sentimentScore  // float — aktueller Rohwert
appState.currentLedIndex // int — aktuell gesetzter LED-Index
appState.initialAnalysisDone // bool
appState.consecutiveSentimentFailures // int
appState.sentimentAPIAvailable // bool
appState.lastSuccessfulSentimentUpdate // unsigned long (millis)
appState.moodUpdateInterval // unsigned long
appState.lastMoodUpdate // unsigned long
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getSentiment() — led_index aus API-Response lesen</name>
  <files>firmware/src/sensor_manager.cpp</files>
  <read_first>firmware/src/sensor_manager.cpp (Zeilen 245–351: getSentiment() vollständig)</read_first>
  <action>
In `getSentiment()` den Erfolgs-Block (ab `if (success && doc["sentiment"].is<float>())`) wie folgt erweitern:

1. `led_index` direkt aus JSON lesen, wenn vorhanden:
```cpp
if (success && doc["sentiment"].is<float>())
{
    float receivedSentiment = doc["sentiment"].as<float>();
    debug(String(F("Sentiment empfangen: ")) + String(receivedSentiment, 2));

    // Phase 18: led_index aus API-Response lesen (dynamische Skalierung)
    // Fallback auf mapSentimentToLED() wenn Feld fehlt (altes Backend)
    int apiLedIndex = -1;
    if (doc["led_index"].is<int>())
    {
        apiLedIndex = doc["led_index"].as<int>();
        apiLedIndex = constrain(apiLedIndex, 0, 4);

        // Fallback-Schwellwerte signalisiert vom Backend
        if (doc["thresholds"]["fallback"].is<bool>() && doc["thresholds"]["fallback"].as<bool>())
        {
            debug(F("Hinweis: Backend nutzt Fallback-Schwellwerte (weniger als 3 historische Datenpunkte)"));
        }
        debug(String(F("LED-Index aus API: ")) + String(apiLedIndex) +
              String(F(" (Perzentil: ")) + String(doc["percentile"].as<float>(), 2) + String(F(")")));
    }
    else
    {
        // Altes Backend ohne led_index-Feld — lokale Berechnung als Fallback
        apiLedIndex = mapSentimentToLED(receivedSentiment);
        debug(F("led_index nicht in Response — lokaler Fallback über mapSentimentToLED()"));
    }

    // handleSentiment() mit exaktem Rohwert aufrufen (LED-Index wird intern überschrieben)
    handleSentiment(receivedSentiment);

    // LED-Index aus API direkt setzen (überschreibt mapSentimentToLED() aus handleSentiment)
    if (apiLedIndex != appState.currentLedIndex || !appState.initialAnalysisDone)
    {
        appState.currentLedIndex = apiLedIndex;
        appState.lastLedIndex = apiLedIndex;
        if (appState.autoMode && appState.lightOn)
        {
            updateLEDs();
        }
    }

    appState.initialAnalysisDone = true;
    appState.lastSuccessfulSentimentUpdate = currentMillis;
    appState.consecutiveSentimentFailures = 0;
    appState.sentimentAPIAvailable = true;

    if (appState.statusLedMode == 2)
    {
        setStatusLED(0);
    }
}
```

Wichtig: `handleSentiment()` wird weiterhin aufgerufen (für HA-Werte + MQTT-Publishing), aber danach wird `appState.currentLedIndex` mit dem API-Wert überschrieben. Keine Signaturänderung an `handleSentiment()` — sie bleibt float-basiert für MQTT-Kompatibilität.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `pio run` kompiliert ohne Fehler und Warnungen
- `doc["led_index"].is&lt;int&gt;()` Prüfung ist im Code vorhanden
- `doc["thresholds"]["fallback"]` Prüfung ist vorhanden
- Fallback-Pfad via `mapSentimentToLED()` bleibt für altes Backend erhalten
- `handleSentiment()` wird weiterhin mit float-Wert aufgerufen (MQTT-Kompatibilität)
  </acceptance_criteria>
  <done>ESP32 nutzt led_index aus API-Response; kompiliert ohne Fehler; Fallback-Pfad für altes Backend bleibt erhalten</done>
</task>

</tasks>

<verification>
`pio run` im firmware/-Verzeichnis ohne Fehler.
grep "led_index" firmware/src/sensor_manager.cpp — Treffer in getSentiment().
grep "mapSentimentToLED" firmware/src/sensor_manager.cpp — mindestens 1 Treffer (Fallback-Pfad bleibt).
</verification>

<success_criteria>
1. `firmware/src/sensor_manager.cpp` enthält `doc["led_index"].is<int>()` Prüfung
2. Bei vorhandenem `led_index`-Feld wird dieser Wert genutzt (ESP-01)
3. Bei fehlendem `led_index` greift `mapSentimentToLED()` als Fallback
4. `thresholds.fallback: true` erzeugt Debug-Log-Hinweis
5. `pio run` kompiliert ohne Fehler (ESP-02 via dynamische LED-Palette)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/18-esp32-dashboard-integration/18-01-SUMMARY.md` erstellen.
</output>
