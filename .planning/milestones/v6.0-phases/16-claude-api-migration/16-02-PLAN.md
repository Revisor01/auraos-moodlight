---
phase: 16-claude-api-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - sentiment-api/app.py
autonomous: false
requirements:
  - PROMPT-01
  - PROMPT-02

must_haves:
  truths:
    - "Der Prompt enthält kalibrierte Anker-Beispiele für positive UND negative Nachrichten"
    - "Der Prompt enthält eine explizite Warnung gegen negativen Bias"
    - "Positive Schlagzeilen wie 'Wirtschaftswachstum über Erwartungen' erhalten Scores > +0.3"
    - "Neutrale Schlagzeilen (Wetterbericht, Sportergebnis) erhalten Scores nahe 0.0"
  artifacts:
    - path: "sentiment-api/app.py"
      provides: "Optimierter Sentiment-Prompt in analyze_sentiment_claude()"
      contains: "KALIBRIERUNG"
  key_links:
    - from: "analyze_sentiment_claude() Prompt"
      to: "Anthropic API"
      via: "full_prompt in messages.create()"
      pattern: "KALIBRIERUNG"
---

<objective>
Den Sentiment-Analyse-Prompt in analyze_sentiment_claude() mit kalibrierten Anker-Beispielen und einer Anti-Bias-Anweisung ausstatten. Das Ziel: Scores verteilen sich erkennbar über den vollen Bereich -1.0 bis +1.0, positive Nachrichten erzeugen tatsächlich positive Werte.

Purpose: Der migrierte Prompt aus Plan 01 ist funktional aber ohne Kalibrierung. Dieser Plan injiziert den vollständigen Prompt der in CONTEXT.md und RESEARCH.md spezifiziert wurde.
Output: analyze_sentiment_claude() in app.py mit vollständigem Prompt-Text. Deployment-Verifikation durch User.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-claude-api-migration/16-CONTEXT.md
@.planning/phases/16-claude-api-migration/16-RESEARCH.md
@.planning/phases/16-claude-api-migration/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimierten Prompt in analyze_sentiment_claude() einbauen</name>
  <files>sentiment-api/app.py</files>
  <read_first>
    - sentiment-api/app.py (nur die Funktion analyze_sentiment_claude() lesen, ca. Zeile 77–163)
  </read_first>
  <action>
Den `prompt_lines`-Block in `analyze_sentiment_claude()` ersetzen. Der bisherige Platzhalter-Prompt aus Plan 01 wird durch den vollständigen kalibrierten Prompt ersetzt.

**Exakter neuer prompt_lines-Block (VOLLSTÄNDIG, nichts kürzen):**

```python
prompt_lines = [
    "Analysiere das Sentiment jeder der folgenden deutschen Nachrichtenschlagzeilen.",
    "Gib für jede Schlagzeile eine Fließkommazahl zwischen -1.0 und +1.0 zurück.",
    "",
    "KALIBRIERUNG — diese Beispiele zeigen die erwartete Skala:",
    "- 'Flugzeugabsturz mit 200 Toten' → -0.9",
    "- 'Wirtschaftskrise verschärft sich' → -0.6",
    "- 'Regierung schließt neue Haushaltslücke' → -0.3",
    "- 'Wetterbericht für die Woche' → 0.0",
    "- 'Forschungsergebnisse veröffentlicht' → 0.0",
    "- 'Wirtschaftswachstum über Erwartungen' → +0.5",
    "- 'Friedensverhandlungen beginnen' → +0.7",
    "- 'Historischer Durchbruch bei Krebstherapie' → +0.9",
    "",
    "WICHTIG:",
    "- Bewerte die TONALITÄT der Schlagzeile, nicht das allgemeine Thema.",
    "- Sachliche Berichte über Probleme sind NICHT automatisch -0.5 oder schlechter.",
    "- Nutze den vollen Bereich: positive Nachrichten sollen positive Werte bekommen.",
    "- Im Zweifel: ausgewogene Einschätzung, kein negativer Bias.",
    "",
    "Format: Nur \"Nummer: Score\" pro Zeile (z.B. \"1: -0.3\"). Keine Erklärungen.",
    "",
    "Schlagzeilen:"
]
```

Nur den `prompt_lines = [...]`-Block ersetzen. Der Rest der Funktion (for-Schleife, API-Call, Parsing, Error-Handling) bleibt unverändert.

**Nicht ändern:** `full_prompt = "\n".join(prompt_lines)` und alles danach.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
src = open('/Users/simonluthe/Documents/auraos-moodlight/sentiment-api/app.py').read()
ast.parse(src)
print('Syntax: OK')
assert 'KALIBRIERUNG' in src, 'FEHLER: KALIBRIERUNG fehlt im Prompt'
assert 'Friedensverhandlungen beginnen' in src, 'FEHLER: Positives Kalibrierungsbeispiel fehlt'
assert 'negativer Bias' in src, 'FEHLER: Anti-Bias-Anweisung fehlt'
assert '+0.7' in src, 'FEHLER: +0.7 Ankerpunkt fehlt'
print('Prompt-Check: OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Keyword `KALIBRIERUNG` ist im Prompt-Text vorhanden
    - Positives Beispiel `'Friedensverhandlungen beginnen' → +0.7` ist vorhanden
    - Positives Beispiel `'Wirtschaftswachstum über Erwartungen' → +0.5` ist vorhanden
    - Anti-Bias-Anweisung `kein negativer Bias` ist vorhanden
    - Syntax-Check `ast.parse()` gibt keinen Fehler
    - Der Rest der Funktion (API-Call, Parsing) bleibt unverändert
  </acceptance_criteria>
  <done>analyze_sentiment_claude() enthält den vollständigen kalibrierten Prompt. python3 -c "import ast; ast.parse(open('sentiment-api/app.py').read()); print('OK')" gibt OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deployment und manuelle Prompt-Verifikation</name>
  <files>sentiment-api/app.py</files>
  <action>Kein weiterer Code zu schreiben. User deployt und verifiziert das Ergebnis manuell.</action>
  <verify>
    <automated>MISSING — Deployment-Verifikation erfordert Server-Zugang durch User</automated>
  </verify>
  <done>Health-Check zeigt anthropic.available: true, Container-Logs belegen erfolgreiche Analyse über Anthropic API.</done>
  <what-built>
    Plan 01 hat die API-Migration durchgeführt (OpenAI → Anthropic SDK). Plan 02 hat den optimierten Prompt eingebaut. Das Backend ist jetzt vollständig auf Claude Haiku umgestellt und bereit für den ersten Produktions-Test.
  </what-built>
  <how-to-verify>
1. Setze ANTHROPIC_API_KEY in der `.env` Datei auf dem Server (`/opt/auraos-moodlight/sentiment-api/.env`):
   ```
   ANTHROPIC_API_KEY=sk-ant-...
   ```
   Entferne oder kommentiere `OPENAI_API_KEY=...` aus.

2. Deploye das Backend per GitHub Push (CI/CD übernimmt Build + Deployment über Portainer):
   ```bash
   git add sentiment-api/
   git commit -m "feat(16): migrate sentiment analysis from OpenAI to Anthropic Claude Haiku"
   git push
   ```

3. Warte bis der Container neu gestartet ist. Prüfe Container-Logs:
   ```
   Muss enthalten: "Anthropic Client erfolgreich initialisiert."
   Darf NICHT enthalten: "ANTHROPIC_API_KEY Umgebungsvariable nicht gesetzt!"
   ```

4. Manueller Analyse-Test:
   ```
   GET http://analyse.godsapp.de/api/news
   ```
   Container-Logs müssen zeigen:
   ```
   Sende X Headlines zur Analyse an Anthropic API (claude-haiku-4-5-20251001)...
   Antwort von Anthropic API erhalten.
   Sentiment Berechnung: Roh-Durchschnitt=..., Verstärkt (x2.0)=...
   ```

5. Score-Verteilung prüfen:
   ```
   GET http://analyse.godsapp.de/api/moodlight/current
   ```
   Der `sentiment_score` sollte sich von den bisherigen -0.15 bis -0.35 unterscheiden.

6. Health-Check:
   ```
   GET http://analyse.godsapp.de/api/health
   ```
   Antwort muss `"anthropic": {"status": "healthy", "available": true}` enthalten (nicht "openai").
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn der Health-Check healthy zeigt und die Logs die Anthropic API verwenden. Beschreibe gefundene Probleme wenn etwas nicht stimmt.</resume-signal>
</task>

</tasks>

<verification>
Nach dem Deployment:
- Health-Endpoint zeigt `anthropic.available: true`
- Container-Logs zeigen `"Anthropic Client erfolgreich initialisiert."`
- Kein `openai`-Bezug in den Logs nach Neustart
- `/api/moodlight/current` liefert einen gültigen sentiment_score
</verification>

<success_criteria>
- Prompt enthält alle 8 Kalibrierungs-Ankerpunkte (4 negativ, 2 neutral, 2 positiv)
- Prompt enthält Anti-Bias-Anweisung ("kein negativer Bias")
- Prompt enthält Tonalitäts-Anweisung ("TONALITÄT der Schlagzeile, nicht das allgemeine Thema")
- Deployment erfolgreich, Backend antwortet via Anthropic API
- Health-Check meldet anthropic als healthy
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-claude-api-migration/16-02-SUMMARY.md` erstellen.
</output>
