---
phase: 16-claude-api-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/requirements.txt
  - sentiment-api/docker-compose.yaml
  - sentiment-api/app.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "Backend startet ohne OPENAI_API_KEY und initialisiert den Anthropic Client erfolgreich"
    - "ANTHROPIC_API_KEY wird beim Start validiert — fehlt er, erscheint eine deutliche Fehlermeldung"
    - "Der Health-Endpoint /api/health meldet 'anthropic' als Check, nicht 'openai'"
    - "analyze_headlines_batch() liefert dasselbe Dict-Format wie die alte Funktion"
  artifacts:
    - path: "sentiment-api/requirements.txt"
      provides: "anthropic==0.86.0 statt openai==1.70.0"
      contains: "anthropic"
    - path: "sentiment-api/docker-compose.yaml"
      provides: "ANTHROPIC_API_KEY Umgebungsvariable"
      contains: "ANTHROPIC_API_KEY"
    - path: "sentiment-api/app.py"
      provides: "Anthropic Client Init + analyze_headlines_batch() + Health-Check"
      exports: ["analyze_headlines_batch"]
  key_links:
    - from: "sentiment-api/app.py"
      to: "anthropic SDK"
      via: "anthropic_client.messages.create()"
      pattern: "anthropic_client\\.messages\\.create"
    - from: "sentiment-api/app.py (Zeile ~524)"
      to: "start_background_worker"
      via: "analyze_function=analyze_headlines_batch"
      pattern: "analyze_headlines_batch"
---

<objective>
OpenAI SDK durch Anthropic SDK ersetzen. Das Backend analysiert Sentiment ab sofort über Claude Haiku (claude-haiku-4-5-20251001) statt GPT-4o-mini. Alle API-Referenzen (Import, Client-Init, API-Call, Health-Check, Background-Worker-Übergabe) werden synchron umgestellt.

Purpose: Phase 17 (dynamische Skalierung) baut auf einem funktionierenden Claude-Backend auf. Die Prompt-Optimierung (Plan 02) setzt ebenfalls eine korrekt migrierte API-Schicht voraus.
Output: Modifizierte app.py mit Anthropic Client, neue requirements.txt, neue docker-compose.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-claude-api-migration/16-CONTEXT.md
@.planning/phases/16-claude-api-migration/16-RESEARCH.md
</context>

<interfaces>
<!-- Bestehendes Interface das erhalten bleiben MUSS (background_worker.py konsumiert es) -->

Von sentiment-api/background_worker.py (Zeile 214):
```python
def start_background_worker(app, analyze_function, interval_seconds=1800):
    # analyze_function: Funktion für Sentiment-Analyse
    # Wird als self.analyze_function gespeichert und mit (headlines) aufgerufen
    # Erwartet Rückgabe: {"results": [...], "total_sentiment": float, "statistics": {...}}
```

Die umbenannte Funktion analyze_headlines_batch() muss exakt diese Signatur und Rückgabestruktur behalten.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: requirements.txt und docker-compose.yaml migrieren</name>
  <files>sentiment-api/requirements.txt, sentiment-api/docker-compose.yaml</files>
  <read_first>
    - sentiment-api/requirements.txt (aktueller Inhalt)
    - sentiment-api/docker-compose.yaml (aktueller Inhalt)
  </read_first>
  <action>
In requirements.txt: Zeile `openai==1.70.0` ersetzen durch `anthropic==0.86.0`. Alle anderen Zeilen (Flask, feedparser, requests, psycopg2-binary, redis, gunicorn) bleiben unverändert.

In docker-compose.yaml, Service `news-analyzer`, Block `environment`: Zeile `- OPENAI_API_KEY=${OPENAI_API_KEY}` ersetzen durch `- ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}`. Alle anderen Umgebungsvariablen (FLASK_ENV, DATABASE_URL, REDIS_URL, DEFAULT_HEADLINES_PER_SOURCE, ADMIN_PASSWORD, SECRET_KEY) bleiben unverändert.
  </action>
  <verify>
    <automated>grep "anthropic==0.86.0" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/requirements.txt && grep -v "openai" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/requirements.txt && grep "ANTHROPIC_API_KEY" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/docker-compose.yaml && grep -v "OPENAI_API_KEY" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/docker-compose.yaml</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt enthält `anthropic==0.86.0`, kein `openai`-Eintrag mehr
    - docker-compose.yaml enthält `ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}`, kein `OPENAI_API_KEY` mehr
  </acceptance_criteria>
  <done>Beide Dateien zeigen Anthropic-Konfiguration, kein OpenAI-Rest vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: app.py — OpenAI durch Anthropic SDK ersetzen</name>
  <files>sentiment-api/app.py</files>
  <read_first>
    - sentiment-api/app.py (vollständig lesen, alle OpenAI-Stellen kartieren)
  </read_first>
  <action>
Folgende chirurgische Änderungen in app.py durchführen. KEINE anderen Codezeilen anfassen.

**1. Import (Zeile 12):**
```python
# ALT:
from openai import OpenAI, OpenAIError

# NEU:
import anthropic
from anthropic import Anthropic, APIConnectionError, RateLimitError, APIStatusError
```

**2. Client-Initialisierung (Zeilen 60–75) — ersetzen:**
```python
# --- Anthropic Client initialisieren ---
anthropic_api_key = os.environ.get("ANTHROPIC_API_KEY")
anthropic_client = None
if anthropic_api_key:
    try:
        anthropic_client = Anthropic(api_key=anthropic_api_key, timeout=45.0)
        logging.info("Anthropic Client erfolgreich initialisiert.")
    except Exception as e:
        logging.error(f"Fehler bei der Initialisierung des Anthropic Clients: {e}")
else:
    logging.error("############################################################")
    logging.error("FEHLER: ANTHROPIC_API_KEY Umgebungsvariable nicht gesetzt!")
    logging.error("Sentiment-Analyse über Anthropic API wird nicht funktionieren.")
    logging.error("############################################################")
```

**3. Funktion analyze_sentiment_openai() umbenennen und API-Call umbauen:**
Neue Signatur: `def analyze_sentiment_claude(headlines_batch: list) -> list:`

Inhalt der Funktion (nur die API-spezifischen Teile ändern, Rest beibehalten):

```python
def analyze_sentiment_claude(headlines_batch: list) -> list:
    if not anthropic_client:
        logging.error("Anthropic Client nicht verfügbar in analyze_sentiment_claude.")
        return [0.0] * len(headlines_batch)
    if not headlines_batch:
        return []

    # Prompt-Aufbau (Kalibrierungs-Ankerpunkte — in Plan 02 weiter optimiert)
    prompt_lines = [
        "Analysiere das Sentiment jeder der folgenden deutschen Nachrichtenschlagzeilen.",
        "Gib für jede Schlagzeile eine Fließkommazahl zwischen -1.0 und +1.0 zurück.",
        "",
        "Format: Nur \"Nummer: Score\" pro Zeile (z.B. \"1: -0.3\"). Keine Erklärungen.",
        "",
        "Schlagzeilen:"
    ]
    for i, headline in enumerate(headlines_batch):
        prompt_lines.append(f"{i+1}: {headline}")
    full_prompt = "\n".join(prompt_lines)

    scores = [0.0] * len(headlines_batch)

    try:
        logging.info(f"Sende {len(headlines_batch)} Headlines zur Analyse an Anthropic API (claude-haiku-4-5-20251001)...")
        response = anthropic_client.messages.create(
            model="claude-haiku-4-5-20251001",
            max_tokens=len(headlines_batch) * 15,
            temperature=1.0,
            messages=[{"role": "user", "content": full_prompt}]
        )
        raw_response_content = response.content[0].text.strip()
        logging.info("Antwort von Anthropic API erhalten.")
        logging.debug(f"Anthropic Raw Response:\n{raw_response_content}")

        lines = raw_response_content.split('\n')
        score_pattern = re.compile(r"^\s*(\d+):\s*([-+]?\d*\.?\d+)\s*$")
        parsed_count = 0
        for line in lines:
            match = score_pattern.match(line)
            if match:
                try:
                    index = int(match.group(1)) - 1
                    score = float(match.group(2))
                    if 0 <= index < len(scores):
                        scores[index] = max(-1.0, min(1.0, score))
                        parsed_count += 1
                        logging.debug(f"  Geparster Score für Index {index}: {scores[index]}")
                    else:
                        logging.warning(f"Geparster Index {index+1} außerhalb des Bereichs ({len(scores)} Headlines). Zeile: '{line}'")
                except (ValueError, IndexError) as parse_error:
                    logging.warning(f"Fehler beim Parsen der Score-Zeile '{line}': {parse_error}")
            elif line.strip():
                logging.warning(f"Unerwartetes Format in Antwortzeile von Anthropic: '{line}'")

        logging.info(f"Erfolgreich {parsed_count} Scores aus der Anthropic-Antwort geparst.")
        if parsed_count != len(headlines_batch):
            logging.warning(f"Nicht alle Scores geparst! Erwartet: {len(headlines_batch)}, Geparsed: {parsed_count}. Fehlende Scores bleiben 0.0.")

        return scores

    except APIConnectionError as e:
        logging.error(f"Anthropic API nicht erreichbar: {e}")
        return [0.0] * len(headlines_batch)
    except RateLimitError as e:
        logging.error(f"Anthropic Rate-Limit erreicht: {e}")
        return [0.0] * len(headlines_batch)
    except APIStatusError as e:
        logging.error(f"Anthropic API Fehler {e.status_code}: {e.response}")
        return [0.0] * len(headlines_batch)
    except Exception as e:
        logging.error(f"Unerwarteter Fehler bei der Anthropic Analyse: {e}")
        return [0.0] * len(headlines_batch)
```

**4. Funktion analyze_headlines_openai_batch() umbenennen:**
`def analyze_headlines_openai_batch(headlines: list):` → `def analyze_headlines_batch(headlines: list):`

Im Docstring: "Analysiert Headlines mit OpenAI" → "Analysiert Headlines mit Anthropic (Claude Haiku)"

Interne Referenz auf Zeile 200: `analyze_sentiment_openai(headline_texts)` → `analyze_sentiment_claude(headline_texts)`

Log-String auf Zeile 179: `"analyze_headlines_openai_batch aufgerufen"` → `"analyze_headlines_batch aufgerufen"`

**5. Health-Check (ca. Zeile 302–340) — zwei Änderungen:**

Den `# OpenAI-Check` Block ersetzen:
```python
# Anthropic-Check
health["checks"]["anthropic"] = {
    "status": "healthy" if anthropic_client else "unhealthy",
    "available": anthropic_client is not None
}
if not anthropic_client:
    health["status"] = "degraded"
```

**6. Fehler-Early-Return in der News-Route (ca. Zeile 345):**
```python
# ALT:
if not openai_client: return jsonify({"status": "error", "message": "Sentiment-Analyse-Dienst nicht bereit (OpenAI Problem)."}), 503

# NEU:
if not anthropic_client: return jsonify({"status": "error", "message": "Sentiment-Analyse-Dienst nicht bereit (Anthropic Problem)."}), 503
```

**7. Background-Worker-Aufruf (ca. Zeile 524):**
```python
# ALT:
analyze_function=analyze_headlines_openai_batch,

# NEU:
analyze_function=analyze_headlines_batch,
```

**8. Startup-Warnung (ca. Zeile 534):**
```python
# ALT:
if not openai_client:
     logging.warning("!!! OpenAI Client konnte nicht initialisiert werden (fehlender API Key?). API wird Fehler zurückgeben. !!!")

# NEU:
if not anthropic_client:
    logging.warning("!!! Anthropic Client konnte nicht initialisiert werden (fehlender API Key?). API wird Fehler zurückgeben. !!!")
```

Prüfen nach Änderungen: Kein `openai` oder `OpenAI` oder `gpt-4o-mini` darf mehr in app.py vorkommen (außer in Kommentaren/Strings die historisch irrelevant sind).
  </action>
  <verify>
    <automated>grep -n "openai\|OpenAI\|openai_client\|gpt-4o-mini" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/app.py | grep -v "^.*#" || echo "CLEAN: keine OpenAI-Reste in aktivem Code"</automated>
  </verify>
  <acceptance_criteria>
    - `from anthropic import Anthropic, APIConnectionError, RateLimitError, APIStatusError` ist vorhanden
    - `anthropic_client = Anthropic(api_key=anthropic_api_key, timeout=45.0)` ist vorhanden
    - `response.content[0].text.strip()` wird verwendet (nicht `response.choices[0].message.content`)
    - `temperature=1.0` im messages.create() Aufruf
    - Funktion heißt `analyze_headlines_batch` (kein `_openai_` im Namen)
    - Health-Check prüft `anthropic_client`, nicht `openai_client`
    - `start_background_worker` übergibt `analyze_function=analyze_headlines_batch`
    - Kein `openai_client` oder `OpenAI` mehr in aktivem Code
  </acceptance_criteria>
  <done>app.py nutzt ausschließlich Anthropic SDK. Syntax-Check: `python3 -c "import ast; ast.parse(open('sentiment-api/app.py').read()); print('OK')"` gibt OK zurück.</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
1. `python3 -c "import ast; ast.parse(open('/Users/simonluthe/Documents/auraos-moodlight/sentiment-api/app.py').read()); print('Syntax OK')"` — muss OK geben
2. `grep -c "anthropic" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/app.py` — muss > 5 sein
3. `grep -c "openai" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/requirements.txt` — muss 0 sein
4. `grep "analyze_headlines_batch" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/app.py | grep -v "openai"` — zeigt Umbenennung
</verification>

<success_criteria>
- requirements.txt: anthropic==0.86.0 (kein openai)
- docker-compose.yaml: ANTHROPIC_API_KEY (kein OPENAI_API_KEY)
- app.py: Alle 8 Änderungspunkte umgesetzt, Syntax-valide
- background_worker.py: KEINE Änderung nötig (konsumiert analyze_function als Referenz, Umbenennung in app.py reicht)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-claude-api-migration/16-01-SUMMARY.md` erstellen.
</output>
