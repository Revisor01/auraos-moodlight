---
phase: 09-db-schema-worker-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - sentiment-api/database.py
  - sentiment-api/background_worker.py
  - sentiment-api/app.py
  - sentiment-api/shared_config.py
autonomous: true
requirements:
  - FEED-01
  - FEED-06

must_haves:
  truths:
    - "database.py hat eine get_active_feeds()-Methode, die alle Feeds mit active=TRUE aus der feeds-Tabelle zurückgibt"
    - "background_worker.py importiert nicht mehr RSS_FEEDS aus shared_config — _fetch_headlines() liest Feeds aus der DB"
    - "background_worker.py übergibt source_count=len(feeds) statt source_count=12 an save_sentiment()"
    - "app.py importiert nicht mehr RSS_FEEDS aus shared_config — rss_feeds wird lazy aus der DB geladen"
    - "shared_config.py enthält keinen RSS_FEEDS-Dict mehr, aber get_sentiment_category() bleibt erhalten"
  artifacts:
    - path: "sentiment-api/database.py"
      provides: "get_active_feeds()-Methode der Database-Klasse"
      contains: "def get_active_feeds"
    - path: "sentiment-api/background_worker.py"
      provides: "Worker mit DB-seitigem Feed-Lesepfad"
      contains: "db.get_active_feeds()"
    - path: "sentiment-api/shared_config.py"
      provides: "Nur noch get_sentiment_category() — RSS_FEEDS entfernt"
      contains: "def get_sentiment_category"
  key_links:
    - from: "sentiment-api/background_worker.py (_fetch_headlines)"
      to: "sentiment-api/database.py (get_active_feeds)"
      via: "db = get_database(); feeds = db.get_active_feeds()"
      pattern: "get_active_feeds"
    - from: "sentiment-api/app.py (get_news route)"
      to: "sentiment-api/database.py (get_active_feeds)"
      via: "get_database().get_active_feeds() beim Request"
      pattern: "get_active_feeds"
---

<objective>
Python-Integration: get_active_feeds() in der Database-Klasse ergänzen, Background Worker und app.py auf DB-seitigen Feed-Lesepfad umstellen, RSS_FEEDS-Dict aus shared_config.py entfernen.

Purpose: Nach Plan 01 (Schema) ist die feeds-Tabelle definiert. Dieser Plan macht die Python-Codebasis frei von hardcodierten Feed-Listen. Background Worker und app.py lesen Feeds ab sofort aus PostgreSQL.
Output: database.py (neue Methode), background_worker.py (DB-Pfad), app.py (lazy DB-Laden), shared_config.py (RSS_FEEDS entfernt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-db-schema-worker-integration/09-01-SUMMARY.md
@sentiment-api/database.py
@sentiment-api/background_worker.py
@sentiment-api/app.py
@sentiment-api/shared_config.py
</context>

<interfaces>
<!-- Bestehende Methoden in database.py — Database-Klasse -->

```python
# get_cursor() Context Manager — Pattern für neue Methode
@contextmanager
def get_cursor(self, cursor_factory=None):
    # Yields: Datenbank-Cursor
    # Usage: with self.get_cursor(cursor_factory=RealDictCursor) as cur:

# save_sentiment() Signatur — source_count-Parameter
def save_sentiment(
    self,
    sentiment_score: float,
    category: str,
    headlines_analyzed: int,
    source_count: int = 12,  # <-- dieser Default wird in background_worker.py überschrieben
    api_response_time_ms: Optional[int] = None,
    metadata: Optional[Dict] = None
) -> int:

# RealDictCursor ist bereits importiert (psycopg2.extras)
from psycopg2.extras import RealDictCursor
```

<!-- shared_config.py — aktueller Stand -->
```python
# RSS_FEEDS — wird entfernt
RSS_FEEDS = { "Zeit": "...", "Tagesschau": "...", ... }  # 12 Einträge incl. Focus

# get_sentiment_category() — bleibt erhalten
def get_sentiment_category(score: float) -> str:
    if score >= 0.30: return "sehr positiv"
    elif score >= 0.10: return "positiv"
    elif score >= -0.20: return "neutral"
    elif score >= -0.50: return "negativ"
    else: return "sehr negativ"
```

<!-- background_worker.py — relevante Stellen -->
```python
# Zeile 12 — Import der entfernt wird:
from shared_config import RSS_FEEDS, get_sentiment_category

# Zeile 107 — source_count wird gefixt:
db.save_sentiment(..., source_count=12, ...)  # -> source_count=len(feeds)

# Zeile 138 — RSS_FEEDS-Nutzung die ersetzt wird:
rss_feeds = RSS_FEEDS
for source, url in rss_feeds.items():  # -> for feed_row in feeds: source = feed_row['name'], url = feed_row['url']
```

<!-- app.py — relevante Stellen -->
```python
# Zeile 10 — Import der entfernt wird:
from shared_config import RSS_FEEDS, get_sentiment_category

# Zeile 54 — globale Variable die umgestellt wird:
rss_feeds = RSS_FEEDS  # -> wird entfernt

# Zeile 341 — Schleife im get_news-Handler:
for source, url in rss_feeds.items():  # -> rss_feeds = {f['name']: f['url'] for f in get_database().get_active_feeds()} zu Beginn der Route
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: get_active_feeds() in database.py ergänzen</name>
  <files>sentiment-api/database.py</files>

  <read_first>
    - sentiment-api/database.py — vollständige Datei lesen, um Stil und Position für die neue Methode zu verstehen (nach get_active_devices(), vor get_statistics())
  </read_first>

  <action>
In der `Database`-Klasse nach der Methode `get_active_devices()` (endet bei Zeile ~356) und vor `get_statistics()` eine neue Methode `get_active_feeds()` einfügen.

Exakter Methodencode:

```python
def get_active_feeds(self) -> List[Dict[str, Any]]:
    """
    Hole alle aktiven RSS-Feeds aus der Datenbank.

    Returns:
        Liste von Dicts mit 'id', 'name' und 'url' Feldern.
        Leere Liste bei Fehler oder keinen aktiven Feeds.
    """
    query = """
        SELECT id, name, url
        FROM feeds
        WHERE active = TRUE
        ORDER BY name ASC;
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query)
            results = cur.fetchall()
            return [dict(row) for row in results]
    except Exception as e:
        logger.error(f"Fehler beim Laden der aktiven Feeds: {e}")
        return []
```

Der Rückgabetyp `List[Dict[str, Any]]` ist bereits im import-Block vorhanden (`from typing import Optional, Dict, List, Any`).
  </action>

  <verify>
    <automated>grep -n "def get_active_feeds" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/database.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep "def get_active_feeds" sentiment-api/database.py` gibt genau 1 Treffer
    - `grep "WHERE active = TRUE" sentiment-api/database.py` gibt mindestens 1 Treffer
    - `grep "ORDER BY name ASC" sentiment-api/database.py` gibt mindestens 1 Treffer
    - `grep "Fehler beim Laden der aktiven Feeds" sentiment-api/database.py` gibt 1 Treffer (Fehlerbehandlung vorhanden)
    - Die Methode gibt bei Exception eine leere Liste zurück (kein raise)
  </acceptance_criteria>

  <done>database.py enthält get_active_feeds() mit RealDictCursor-Abfrage, ORDER BY name ASC, try/except mit leerem Listen-Fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Background Worker und app.py auf DB-Pfad umstellen + shared_config.py bereinigen</name>
  <files>sentiment-api/background_worker.py, sentiment-api/app.py, sentiment-api/shared_config.py</files>

  <read_first>
    - sentiment-api/background_worker.py — vollständige Datei lesen, besonders _fetch_headlines() (ab Zeile 130) und _perform_update() (ab Zeile 69) mit dem source_count=12-Call
    - sentiment-api/app.py — Zeilen 1-60 (Imports und globale rss_feeds-Variable) und Zeilen 330-410 (get_news-Route mit for source, url in rss_feeds.items())
    - sentiment-api/shared_config.py — vollständige Datei lesen
  </read_first>

  <action>
**Änderung 1 — background_worker.py:**

(a) Import-Zeile 12 ändern: `RSS_FEEDS` aus dem Import entfernen:
```python
# Vorher:
from shared_config import RSS_FEEDS, get_sentiment_category
# Nachher:
from shared_config import get_sentiment_category
```

(b) `_fetch_headlines()` vollständig ersetzen (Zeilen 130-187). Neue Implementierung:

```python
def _fetch_headlines(self):
    """
    Hole Headlines von RSS-Feeds (aus PostgreSQL-Datenbank)
    """
    import feedparser
    import requests

    db = get_database()
    feeds = db.get_active_feeds()

    if not feeds:
        logger.warning("Keine aktiven Feeds in der Datenbank gefunden — Update übersprungen")
        return []

    headlines = []
    processed_links = set()
    num_headlines_per_source = 1  # Für Background-Worker nur 1 Headline/Quelle

    for feed_row in feeds:
        source = feed_row['name']
        url = feed_row['url']
        try:
            try:
                response = requests.get(url, timeout=15, headers={'User-Agent': 'WorldMoodAnalyzer/2.0'})
                response.raise_for_status()
                feed = feedparser.parse(response.content)
            except requests.exceptions.Timeout:
                logger.warning(f"Timeout bei {source}")
                continue
            except requests.exceptions.RequestException as e:
                logger.warning(f"Fehler beim Abrufen von {source}: {e}")
                continue

            if feed.bozo and isinstance(feed.bozo_exception, Exception):
                logger.warning(f"Feed-Fehler bei {source}: {feed.bozo_exception}")
                continue

            headlines_from_source = 0
            if feed.entries:
                for entry in feed.entries:
                    if headlines_from_source >= num_headlines_per_source:
                        break

                    link = getattr(entry, 'link', None)
                    title = getattr(entry, 'title', None)

                    if title and title.strip():
                        headline_text = title.strip()
                        unique_key = link if link else headline_text

                        if unique_key not in processed_links:
                            headlines.append({
                                "headline": headline_text,
                                "source": source,
                                "link": link
                            })
                            processed_links.add(unique_key)
                            headlines_from_source += 1

        except Exception as e:
            logger.error(f"Fehler bei {source}: {e}")

    return headlines
```

(c) In `_perform_update()` den source_count-Fix anwenden. Die Methode ruft `_fetch_headlines()` auf und speichert das Ergebnis in `headlines`. `feeds` ist in `_fetch_headlines()` lokal — daher die einfachste Lösung: einen separaten DB-Call in `_perform_update()`:

In `_perform_update()`, direkt nach `headlines = self._fetch_headlines()` und vor dem `if not headlines:` Check, diese Zeile ergänzen:
```python
feed_count = len(get_database().get_active_feeds())
```

Dann weiter unten `source_count=12` ersetzen durch `source_count=feed_count`.

Die vollständige `_perform_update()`-Stelle:
```python
# 1. Headlines von Feeds abrufen
headlines = self._fetch_headlines()
feed_count = len(get_database().get_active_feeds())  # NEU

if not headlines:
    logger.warning("Keine Headlines gefunden - Update abgebrochen")
    return
```

Und beim save_sentiment-Aufruf:
```python
db.save_sentiment(
    sentiment_score=sentiment_score,
    category=category,
    headlines_analyzed=headlines_analyzed,
    source_count=feed_count,  # vorher: source_count=12
    api_response_time_ms=response_time_ms,
    metadata={...}
)
```

---

**Änderung 2 — app.py:**

(a) Import-Zeile 10 ändern: `RSS_FEEDS` aus dem Import entfernen:
```python
# Vorher:
from shared_config import RSS_FEEDS, get_sentiment_category
# Nachher:
from shared_config import get_sentiment_category
```

(b) Zeile 53-54 (Kommentar + globale Variable) entfernen:
```python
# ENTFERNEN:
# Liste der RSS-Feeds (Single Source of Truth: shared_config.py)
rss_feeds = RSS_FEEDS
```

(c) Am Anfang der `get_news()`-Route-Funktion (Zeile 330, nach der Zeile `if not openai_client: ...`), vor dem Beginn der Feed-Schleife, diese zwei Zeilen ergänzen:
```python
# RSS-Feeds dynamisch aus DB laden (FEED-01)
rss_feeds = {f['name']: f['url'] for f in get_database().get_active_feeds()}
```

Dadurch bleibt die bestehende Schleife `for source, url in rss_feeds.items():` unverändert. Kein weiterer Umbau nötig.

(d) Den `/api/feedconfig` POST-Handler lassen wie er ist — er nutzt die lokale `rss_feeds`-Variable innerhalb des Handlers (Zeilen 411-455), das ist ein separater Request-Scope und funktioniert weiterhin.

---

**Änderung 3 — shared_config.py:**

Den `RSS_FEEDS`-Dict entfernen (Zeilen 4-17) und einen erklärenden Kommentar ergänzen:

```python
# sentiment-api/shared_config.py
# RSS_FEEDS wurde in Phase 9 entfernt — Feed-Liste liegt jetzt in PostgreSQL (feeds-Tabelle).
# get_sentiment_category() wird noch von app.py und background_worker.py genutzt — NICHT entfernen.


def get_sentiment_category(score: float) -> str:
    """Bestimmt Sentiment-Kategorie aus Score. Einzige Definition im gesamten Projekt."""
    if score >= 0.30:
        return "sehr positiv"
    elif score >= 0.10:
        return "positiv"
    elif score >= -0.20:
        return "neutral"
    elif score >= -0.50:
        return "negativ"
    else:
        return "sehr negativ"
```

Nicht vergessen: Den alten Dateikopf-Kommentar `# sentiment-api/shared_config.py` und `# Einzige Quelle fuer RSS-Feed-Liste und Sentiment-Kategorisierung` anpassen oder ersetzen.
  </action>

  <verify>
    <automated>grep -c "RSS_FEEDS" /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/background_worker.py /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/app.py /Users/simonluthe/Documents/auraos-moodlight/sentiment-api/shared_config.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep "RSS_FEEDS" sentiment-api/background_worker.py` gibt 0 Treffer
    - `grep "RSS_FEEDS" sentiment-api/app.py` gibt 0 Treffer
    - `grep "RSS_FEEDS" sentiment-api/shared_config.py` gibt 0 Treffer
    - `grep "get_active_feeds" sentiment-api/background_worker.py` gibt mindestens 1 Treffer
    - `grep "get_active_feeds" sentiment-api/app.py` gibt mindestens 1 Treffer
    - `grep "source_count=12" sentiment-api/background_worker.py` gibt 0 Treffer (hardcoded 12 entfernt)
    - `grep "source_count=feed_count" sentiment-api/background_worker.py` gibt 1 Treffer
    - `grep "def get_sentiment_category" sentiment-api/shared_config.py` gibt 1 Treffer (Funktion erhalten)
    - `grep "get_sentiment_category" sentiment-api/background_worker.py` gibt mindestens 1 Treffer (Import noch vorhanden)
    - `grep "for feed_row in feeds" sentiment-api/background_worker.py` gibt 1 Treffer (neue Schleifen-Variable)
  </acceptance_criteria>

  <done>Alle drei Python-Dateien sind umgestellt: background_worker.py liest Feeds aus DB + übergibt source_count=feed_count, app.py lädt Feeds lazy pro Request aus DB, shared_config.py enthält nur noch get_sentiment_category().</done>
</task>

</tasks>

<verification>
```bash
# Kein RSS_FEEDS-Import mehr in Python-Dateien
grep -r "RSS_FEEDS" sentiment-api/*.py

# get_active_feeds in database.py vorhanden
grep "def get_active_feeds" sentiment-api/database.py

# Worker nutzt DB-Pfad
grep "get_active_feeds" sentiment-api/background_worker.py

# source_count-Fix: kein hardcoded 12 mehr
grep "source_count=12" sentiment-api/background_worker.py

# shared_config enthält noch get_sentiment_category
grep "def get_sentiment_category" sentiment-api/shared_config.py
```
</verification>

<success_criteria>
- database.py: get_active_feeds() Methode vorhanden, gibt List[Dict] zurück, leere Liste bei Fehler
- background_worker.py: kein RSS_FEEDS-Import, _fetch_headlines() iteriert über feed_row['name']/['url'], source_count=feed_count
- app.py: kein RSS_FEEDS-Import, get_news()-Route lädt Feeds lazy aus DB, bestehende Schleifenstruktur unverändert
- shared_config.py: RSS_FEEDS-Dict entfernt, get_sentiment_category() erhalten, erklärender Kommentar vorhanden
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-db-schema-worker-integration/09-02-SUMMARY.md` erstellen.
</output>
