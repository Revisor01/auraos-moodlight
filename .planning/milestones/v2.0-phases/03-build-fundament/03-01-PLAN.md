---
phase: 03-build-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/moodlight.cpp
  - firmware/src/MoodlightUtils.cpp
  - firmware/src/MoodlightUtils.h
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - "`pio run` im firmware/-Verzeichnis kompiliert fehlerfrei durch"
    - "Der Watchdog-Timer wird mit 30s Timeout initialisiert (funktional identisch zum alten Verhalten)"
    - "Code kompiliert sowohl mit ESP-IDF 5.x als auch theoretisch mit aelteren Versionen (conditional compile)"
  artifacts:
    - path: "firmware/src/moodlight.cpp"
      provides: "Konditionaler WDT-Init im setup()"
      contains: "esp_task_wdt_config_t"
    - path: "firmware/src/MoodlightUtils.cpp"
      provides: "Konditionaler WDT-Init in WatchdogManager::begin()"
      contains: "esp_task_wdt_config_t"
    - path: "firmware/src/MoodlightUtils.h"
      provides: "Include fuer esp_idf_version.h"
      contains: "esp_idf_version.h"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "esp_task_wdt.h"
      via: "conditional compile block"
      pattern: "ESP_IDF_VERSION.*ESP_IDF_VERSION_VAL"
    - from: "firmware/src/MoodlightUtils.cpp"
      to: "esp_task_wdt.h"
      via: "conditional compile block in begin()"
      pattern: "ESP_IDF_VERSION.*ESP_IDF_VERSION_VAL"
---

<objective>
Fix den esp_task_wdt_init Compile-Fehler der durch die Arduino ESP32 Core 3.x API-Aenderung verursacht wird.

Purpose: Ohne diesen Fix kompiliert das Projekt nicht — absoluter Blocker fuer alle weiteren Build-Arbeiten.
Output: Kompilierbarer Code mit konditionaler WDT-API-Nutzung an beiden Aufrufstellen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-fundament/03-RESEARCH.md

<interfaces>
<!-- Verifizierte WDT-API aus lokaler Installation -->

Neue Signatur (ESP-IDF >= 5.0):
```c
typedef struct {
    uint32_t timeout_ms;
    uint32_t idle_core_mask;
    bool trigger_panic;
} esp_task_wdt_config_t;

esp_err_t esp_task_wdt_init(const esp_task_wdt_config_t *config);
```

Alte Signatur (ESP-IDF < 5.0):
```c
esp_err_t esp_task_wdt_init(uint32_t timeout, bool panic);
```

Versions-Check Makro:
```c
#include <esp_idf_version.h>
ESP_IDF_VERSION >= ESP_IDF_VERSION_VAL(5, 0, 0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WDT-API-Fix in moodlight.cpp und MoodlightUtils</name>
  <files>firmware/src/moodlight.cpp, firmware/src/MoodlightUtils.cpp, firmware/src/MoodlightUtils.h</files>
  <read_first>firmware/src/moodlight.cpp (Zeilen 4055-4085), firmware/src/MoodlightUtils.cpp (Zeilen 10-30), firmware/src/MoodlightUtils.h (Zeilen 1-12)</read_first>
  <action>
WICHTIG: Es gibt ZWEI Stellen mit der alten esp_task_wdt_init API die beide gefixt werden muessen.

**1. firmware/src/MoodlightUtils.h — Include hinzufuegen:**
Fuege `#include <esp_idf_version.h>` nach Zeile 8 (`#include <esp_task_wdt.h>`) ein.

**2. firmware/src/MoodlightUtils.cpp — WatchdogManager::begin() fixen (Zeile 20):**
Ersetze:
```cpp
esp_err_t err = esp_task_wdt_init(timeoutSeconds, panicOnTimeout);
```
Mit:
```cpp
#if ESP_IDF_VERSION >= ESP_IDF_VERSION_VAL(5, 0, 0)
    // Arduino ESP32 Core 3.x (ESP-IDF >= 5.0): Config-Struktur erforderlich
    esp_task_wdt_config_t wdt_config = {
        .timeout_ms    = timeoutSeconds * 1000,
        .idle_core_mask = 0,
        .trigger_panic  = panicOnTimeout
    };
    esp_err_t err = esp_task_wdt_init(&wdt_config);
#else
    // Aeltere Versionen: Parameter direkt
    esp_err_t err = esp_task_wdt_init(timeoutSeconds, panicOnTimeout);
#endif
```

**3. firmware/src/moodlight.cpp — setup() fixen (Zeile 4063):**
Fuege `#include <esp_idf_version.h>` zu den Includes hinzu (nach Zeile 24: `#include "esp_task_wdt.h"`), falls nicht schon vorhanden.

Ersetze Zeile 4063:
```cpp
esp_task_wdt_init(30, false);
```
Mit:
```cpp
#if ESP_IDF_VERSION >= ESP_IDF_VERSION_VAL(5, 0, 0)
    // Arduino ESP32 Core 3.x (ESP-IDF >= 5.0): Config-Struktur erforderlich
    esp_task_wdt_config_t wdt_config = {
        .timeout_ms    = 30000,
        .idle_core_mask = 0,
        .trigger_panic  = false
    };
    esp_task_wdt_init(&wdt_config);
#else
    // Aeltere Versionen: Parameter direkt
    esp_task_wdt_init(30, false);
#endif
```

NICHT esp_task_wdt_reconfigure verwenden — das erwartet eine bereits laufende WDT-Instanz und gibt ESP_ERR_INVALID_STATE zurueck im setup()-Kontext.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "esp_task_wdt_config_t" firmware/src/moodlight.cpp returns 1
    - grep -c "esp_task_wdt_config_t" firmware/src/MoodlightUtils.cpp returns 1
    - grep -c "esp_idf_version.h" firmware/src/MoodlightUtils.h returns 1
    - grep -c "ESP_IDF_VERSION_VAL" firmware/src/moodlight.cpp returns 1
    - grep -c "ESP_IDF_VERSION_VAL" firmware/src/MoodlightUtils.cpp returns 1
    - cd firmware && pio run exits with code 0
  </acceptance_criteria>
  <done>pio run kompiliert fehlerfrei. Beide WDT-Init-Aufrufe (moodlight.cpp setup() und MoodlightUtils WatchdogManager::begin()) verwenden die neue Config-Struktur unter ESP-IDF 5.x.</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run
Muss mit "SUCCESS" enden, kein esp_task_wdt_init Compile-Fehler.
</verification>

<success_criteria>
- `pio run` im firmware/-Verzeichnis kompiliert ohne Fehler durch
- Beide Aufrufstellen (moodlight.cpp:4063 und MoodlightUtils.cpp:20) verwenden konditionalen Compile
- esp_idf_version.h ist in beiden Dateien verfuegbar (direkt oder transitiv via MoodlightUtils.h)
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-fundament/03-01-SUMMARY.md`
</output>
