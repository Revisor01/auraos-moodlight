---
phase: 14-backend-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/database.py
  - sentiment-api/moodlight_extensions.py
autonomous: true
requirements: [HEAD-02]

must_haves:
  truths:
    - "GET /api/moodlight/headlines gibt eine JSON-Liste mit Headlines, Einzel-Scores, Feed-Namen und Zeitstempeln zurück"
    - "Der Endpoint ist ohne Authentifizierung abrufbar (öffentlich wie /current)"
    - "Die Abfrage unterstützt einen optionalen ?limit=N Parameter (default: 50)"
  artifacts:
    - path: "sentiment-api/database.py"
      provides: "get_recent_headlines() Methode in Database-Klasse"
      contains: "def get_recent_headlines"
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "GET /api/moodlight/headlines Endpoint"
      contains: "api/moodlight/headlines"
  key_links:
    - from: "moodlight_extensions.py /api/moodlight/headlines"
      to: "database.py get_recent_headlines()"
      via: "db = get_database(); db.get_recent_headlines(limit)"
      pattern: "get_recent_headlines"
---

<objective>
Headlines-API-Endpoint erstellen: Datenbankabfrage und API-Route für die letzten analysierten Schlagzeilen mit Einzel-Scores und Feed-Zuordnung.

Purpose: Das Dashboard braucht einen Endpoint der Headlines mit Feed-Name, Score und Zeitstempel liefert. Dieser Plan legt das Backend-Fundament für HEAD-02.
Output: `get_recent_headlines()` in database.py + GET /api/moodlight/headlines in moodlight_extensions.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@sentiment-api/database.py
@sentiment-api/moodlight_extensions.py
</context>

<interfaces>
<!-- Bestehende DB-Struktur (aus init.sql): -->

headlines-Tabelle:
  id, sentiment_history_id (FK), feed_id (FK, NULLABLE), headline (TEXT),
  source_name (VARCHAR 100), sentiment_score (FLOAT -1..+1), strength (VARCHAR 20),
  link (TEXT, optional), analyzed_at (TIMESTAMPTZ)

feeds-Tabelle:
  id, name, url, active, last_fetched_at, error_count, created_at

<!-- Bestehende Methoden in database.py (Cursor-Pattern): -->
def get_recent_headlines(self, limit: int = 50) -> List[Dict[str, Any]]:
    # NEU — noch nicht vorhanden, muss angelegt werden

<!-- API-Endpoint-Muster aus moodlight_extensions.py: -->
@app.route('/api/moodlight/headlines', methods=['GET'])
def get_moodlight_headlines():
    limit = request.args.get('limit', 50, type=int)
    db = get_database()
    headlines = db.get_recent_headlines(limit=limit)
    return jsonify({...})
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: get_recent_headlines() in database.py anlegen</name>
  <files>sentiment-api/database.py</files>
  <read_first>
    - sentiment-api/database.py — Cursor-Pattern (get_all_feeds, get_latest_sentiment als Vorlage)
    - sentiment-api/init.sql — headlines-Tabelle Schema (Zeilen 91-112)
  </read_first>
  <action>
Neue Methode `get_recent_headlines(self, limit: int = 50) -> List[Dict[str, Any]]` in der Database-Klasse anlegen.
Einfügen nach der `get_all_feeds()`-Methode (Zeile ~452 in database.py).

Exakte SQL-Abfrage:
```sql
SELECT
    h.id,
    h.headline,
    h.source_name,
    h.sentiment_score,
    h.strength,
    h.link,
    h.analyzed_at,
    f.name AS feed_name,
    f.id AS feed_id
FROM headlines h
LEFT JOIN feeds f ON h.feed_id = f.id
ORDER BY h.analyzed_at DESC
LIMIT %s;
```

Exakter Python-Code:
```python
def get_recent_headlines(self, limit: int = 50) -> List[Dict[str, Any]]:
    """
    Gibt die zuletzt analysierten Headlines mit Feed-Zuordnung zurück.

    Args:
        limit: Maximale Anzahl zurückgegebener Headlines (default: 50)

    Returns:
        Liste von Dicts mit id, headline, source_name, sentiment_score,
        strength, link, analyzed_at (ISO-String), feed_name, feed_id.
        Leere Liste bei Fehler.
    """
    query = """
        SELECT
            h.id,
            h.headline,
            h.source_name,
            h.sentiment_score,
            h.strength,
            h.link,
            h.analyzed_at,
            f.name AS feed_name,
            f.id AS feed_id
        FROM headlines h
        LEFT JOIN feeds f ON h.feed_id = f.id
        ORDER BY h.analyzed_at DESC
        LIMIT %s;
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query, (limit,))
            results = cur.fetchall()
            rows = []
            for row in results:
                d = dict(row)
                if isinstance(d.get('analyzed_at'), datetime):
                    d['analyzed_at'] = d['analyzed_at'].isoformat()
                rows.append(d)
            return rows
    except Exception as e:
        logger.error(f"Fehler beim Abrufen der Headlines: {e}")
        return []
```

Kein Import nötig — datetime und RealDictCursor sind bereits importiert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "import database; print('get_recent_headlines' in dir(database.Database))"</automated>
  </verify>
  <done>database.py enthält die Methode get_recent_headlines(), Python-Import funktioniert ohne Fehler</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/moodlight/headlines Endpoint registrieren</name>
  <files>sentiment-api/moodlight_extensions.py</files>
  <read_first>
    - sentiment-api/moodlight_extensions.py — register_moodlight_endpoints() Funktion (als Vorlage)
    - sentiment-api/database.py — get_recent_headlines() Signatur (nach Task 1)
  </read_first>
  <action>
Neuen Endpoint `GET /api/moodlight/headlines` in der Funktion `register_moodlight_endpoints(app)` anlegen.
Einfügen vor dem abschließenden `logger.info("Moodlight API-Endpunkte registriert")` (letzte Zeile der Funktion).

Endpoint ist öffentlich (kein @api_login_required) — ESP32 und Dashboard können ihn ohne Session aufrufen.

Exakter Python-Code (innerhalb register_moodlight_endpoints, eingerückt):
```python
    # ===== HEADLINES ENDPOINT (HEAD-02) =====
    @app.route('/api/moodlight/headlines', methods=['GET'])
    def get_moodlight_headlines():
        """
        Letzte analysierte Headlines mit Einzel-Scores und Feed-Zuordnung.

        Query-Parameter:
            limit: Anzahl Headlines (default: 50, max: 500)
        """
        try:
            limit = min(request.args.get('limit', 50, type=int), 500)
            if limit < 1:
                limit = 50

            db = get_database()
            headlines = db.get_recent_headlines(limit=limit)

            return jsonify({
                "status": "success",
                "count": len(headlines),
                "headlines": headlines
            })

        except Exception as e:
            logger.error(f"Fehler in /api/moodlight/headlines: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
```

  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "import moodlight_extensions; print('get_moodlight_headlines' in dir(moodlight_extensions) or 'api/moodlight/headlines' in open('moodlight_extensions.py').read())"</automated>
  </verify>
  <done>moodlight_extensions.py enthält den neuen Endpoint /api/moodlight/headlines, GET-Anfragen liefern JSON mit {status, count, headlines}</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
1. `python -c "from database import Database; db = Database.__new__(Database); print(callable(db.get_recent_headlines))"` — gibt True aus
2. `/api/moodlight/headlines` erscheint in der Ausgabe von `grep -n "headlines" sentiment-api/moodlight_extensions.py`
3. Python-Syntax aller geänderten Dateien fehlerfrei: `python -m py_compile sentiment-api/database.py sentiment-api/moodlight_extensions.py`
</verification>

<success_criteria>
- database.py: get_recent_headlines(limit=50) gibt Liste von Dicts zurück (id, headline, source_name, sentiment_score, strength, link, analyzed_at als ISO-String, feed_name, feed_id)
- moodlight_extensions.py: GET /api/moodlight/headlines antwortet mit {status: "success", count: N, headlines: [...]}
- ?limit=N Parameter begrenzt die Ergebnisse (max 500)
- Kein @api_login_required auf dem Endpoint
- Python-Syntax aller geänderten Dateien fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/14-backend-dashboard/14-01-SUMMARY.md` erstellen.
</output>
