---
phase: 13-authentifizierung
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - sentiment-api/app.py
  - sentiment-api/moodlight_extensions.py
  - sentiment-api/templates/feeds.html
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "GET /feeds leitet auf /login weiter wenn keine Session vorhanden ist"
    - "POST /api/moodlight/feeds gibt 401 zurück ohne gültigen Session-Cookie"
    - "DELETE /api/moodlight/feeds/<id> gibt 401 zurück ohne gültigen Session-Cookie"
    - "POST /api/moodlight/cache/clear gibt 401 zurück ohne gültigen Session-Cookie"
    - "GET /api/moodlight/current funktioniert ohne Session (ESP32-Zugriff)"
    - "GET /api/moodlight/history funktioniert ohne Session (ESP32-Zugriff)"
    - "GET /api/moodlight/feeds funktioniert ohne Session (Lesezugriff öffentlich)"
    - "feeds.html zeigt einen Abmelden-Link im Header"
  artifacts:
    - path: "sentiment-api/app.py"
      provides: "@login_required auf /feeds Route"
      contains: "@login_required"
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "api_login_required für POST/DELETE Endpoints"
      contains: "api_login_required"
    - path: "sentiment-api/templates/feeds.html"
      provides: "Abmelden-Link im Header"
      contains: "Abmelden"
  key_links:
    - from: "sentiment-api/app.py (/feeds)"
      to: "login_required Decorator"
      via: "@login_required"
      pattern: "@login_required"
    - from: "sentiment-api/moodlight_extensions.py (POST /api/moodlight/feeds)"
      to: "flask.session"
      via: "api_login_required Decorator"
      pattern: "api_login_required"
    - from: "sentiment-api/moodlight_extensions.py (DELETE /api/moodlight/feeds)"
      to: "flask.session"
      via: "api_login_required Decorator"
      pattern: "api_login_required"
---

<objective>
Auth-Decorator auf alle geschützten Routen anwenden. Öffentliche ESP32-Endpunkte bleiben unverändert.

Purpose: AUTH-02 und AUTH-03 — POST/DELETE Endpoints geben 401 ohne Session, ESP32-Endpunkte bleiben öffentlich.
Output: Vollständig geschütztes Backend-Interface. feeds.html-Route und alle Schreiboperationen erfordern Login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-authentifizierung/13-CONTEXT.md
@sentiment-api/app.py
@sentiment-api/moodlight_extensions.py
@sentiment-api/templates/feeds.html
@.planning/phases/13-authentifizierung/13-01-SUMMARY.md
</context>

<interfaces>
<!-- Nach Plan 01 verfügbar in app.py: -->
```python
def login_required(f):
    """Decorator: Leitet auf /login weiter wenn keine aktive Session vorhanden."""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not session.get('authenticated'):
            return redirect(url_for('login_page'))
        return f(*args, **kwargs)
    return decorated_function
```

<!-- Bestehende Route in app.py die geschützt werden muss: -->
```python
@app.route('/feeds')
def feed_management():
    return render_template('feeds.html')
```

<!-- Endpunkte in moodlight_extensions.py — Schreiboperationen müssen geschützt werden: -->
# POST   /api/moodlight/feeds         → add_moodlight_feed()
# DELETE /api/moodlight/feeds/<id>    → delete_moodlight_feed()
# POST   /api/moodlight/cache/clear   → clear_moodlight_cache()

<!-- Endpunkte die ÖFFENTLICH bleiben müssen (AUTH-03): -->
# GET /api/moodlight/current    → get_moodlight_current()
# GET /api/moodlight/history    → get_moodlight_history()
# GET /api/moodlight/feeds      → get_moodlight_feeds()
# GET /api/moodlight/trend      → get_moodlight_trend()
# GET /api/moodlight/stats      → get_moodlight_stats()
# GET /api/moodlight/devices    → get_moodlight_devices()

<!-- Flask-Session Import in moodlight_extensions.py ergänzen: -->
```python
from flask import jsonify, request, session
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: @login_required auf /feeds Route anwenden</name>
  <files>sentiment-api/app.py</files>
  <read_first>
    - sentiment-api/app.py (Zeilen 460-475 — /feeds Route und ihr Umfeld)
    - .planning/phases/13-authentifizierung/13-01-SUMMARY.md (login_required Decorator-Signatur)
  </read_first>
  <action>
**Einzige Änderung:** `@login_required` Decorator zur `/feeds` Route hinzufügen.

Vorher:
```python
@app.route('/feeds')
def feed_management():
    """Feed-Verwaltungsseite — UI fuer /api/moodlight/feeds CRUD"""
    return render_template('feeds.html')
```

Nachher:
```python
@app.route('/feeds')
@login_required
def feed_management():
    """Feed-Verwaltungsseite — UI fuer /api/moodlight/feeds CRUD"""
    return render_template('feeds.html')
```

Der `@login_required` Decorator wurde in Plan 01 in app.py definiert — er ist direkt verfügbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
import ast, sys
with open('app.py') as f:
    src = f.read()
# Prüfe @login_required vor feed_management
lines = src.split('\n')
found = False
for i, line in enumerate(lines):
    if '@login_required' in line and i+1 < len(lines) and ('def feed_management' in lines[i+1] or '@app.route' in lines[i-1]):
        found = True
        break
    if 'def feed_management' in line:
        # Prüfe ob @login_required in den 3 Zeilen davor
        prev = lines[max(0,i-3):i]
        if any('@login_required' in l for l in prev):
            found = True
        break
if not found:
    print('FEHLER: @login_required nicht vor feed_management gefunden')
    sys.exit(1)
print('OK — @login_required ist auf /feeds Route angewendet')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - @login_required steht direkt vor der feed_management Funktion (zwischen @app.route und def)
    - Kein anderer Code in app.py wurde verändert
    - python -m py_compile app.py gibt keinen Fehler
  </acceptance_criteria>
  <done>/feeds Route ist durch @login_required geschützt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: api_login_required Decorator für POST/DELETE Endpunkte</name>
  <files>sentiment-api/moodlight_extensions.py</files>
  <read_first>
    - sentiment-api/moodlight_extensions.py (komplett — alle Endpunkte kennen, Import-Block sehen)
  </read_first>
  <action>
**Schritt 1: Imports erweitern** (oben in moodlight_extensions.py, nach dem bestehenden `from flask import jsonify, request`):

```python
from flask import jsonify, request, session
from functools import wraps
```

**Schritt 2: api_login_required Decorator** direkt nach dem `logger = logging.getLogger(__name__)` Block, vor der `register_moodlight_endpoints` Funktion:

```python
def api_login_required(f):
    """Decorator für API-Endpoints: gibt 401 JSON zurück statt Redirect."""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not session.get('authenticated'):
            return jsonify({"status": "error", "message": "Nicht authentifiziert"}), 401
        return f(*args, **kwargs)
    return decorated_function
```

**Schritt 3: Decorator auf Schreib-Endpunkte anwenden** (innerhalb von `register_moodlight_endpoints`):

Endpunkt `add_moodlight_feed` — POST /api/moodlight/feeds:
```python
@app.route('/api/moodlight/feeds', methods=['POST'])
@api_login_required
def add_moodlight_feed():
    ...
```

Endpunkt `delete_moodlight_feed` — DELETE /api/moodlight/feeds/<id>:
```python
@app.route('/api/moodlight/feeds/<int:feed_id>', methods=['DELETE'])
@api_login_required
def delete_moodlight_feed(feed_id):
    ...
```

Endpunkt `clear_moodlight_cache` — POST /api/moodlight/cache/clear:
```python
@app.route('/api/moodlight/cache/clear', methods=['POST'])
@api_login_required
def clear_moodlight_cache():
    ...
```

**Nicht schützen** (öffentlich bleiben, per AUTH-03):
- GET /api/moodlight/current
- GET /api/moodlight/history
- GET /api/moodlight/feeds (GET ist öffentlich — nur POST ist geschützt)
- GET /api/moodlight/trend
- GET /api/moodlight/stats
- GET /api/moodlight/devices
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
import sys
with open('moodlight_extensions.py') as f:
    src = f.read()

# Prüfe api_login_required Decorator
checks = ['api_login_required', 'session.get', 'Nicht authentifiziert']
missing = [c for c in checks if c not in src]
if missing:
    print('FEHLT:', missing)
    sys.exit(1)

# Prüfe dass POST/DELETE geschützt sind
lines = src.split('\n')
protected_funcs = ['add_moodlight_feed', 'delete_moodlight_feed', 'clear_moodlight_cache']
for func in protected_funcs:
    for i, line in enumerate(lines):
        if 'def ' + func in line:
            prev3 = lines[max(0,i-3):i]
            if not any('api_login_required' in l for l in prev3):
                print(f'FEHLER: {func} hat kein @api_login_required')
                sys.exit(1)

# Prüfe dass GET-Endpunkte NICHT geschützt sind
public_funcs = ['get_moodlight_current', 'get_moodlight_history', 'get_moodlight_feeds']
for func in public_funcs:
    for i, line in enumerate(lines):
        if 'def ' + func in line:
            prev3 = lines[max(0,i-3):i]
            if any('api_login_required' in l for l in prev3):
                print(f'FEHLER: {func} sollte NICHT geschützt sein (öffentlicher Endpoint)')
                sys.exit(1)

print('OK — alle Checks bestanden')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - api_login_required Decorator ist in moodlight_extensions.py definiert (gibt 401 JSON zurück)
    - POST /api/moodlight/feeds hat @api_login_required
    - DELETE /api/moodlight/feeds/<id> hat @api_login_required
    - POST /api/moodlight/cache/clear hat @api_login_required
    - GET /api/moodlight/current hat KEIN @api_login_required
    - GET /api/moodlight/history hat KEIN @api_login_required
    - GET /api/moodlight/feeds hat KEIN @api_login_required
    - python -m py_compile moodlight_extensions.py gibt keinen Fehler
  </acceptance_criteria>
  <done>Alle Schreib-Endpunkte sind durch api_login_required geschützt. Lese-Endpunkte für ESP32 bleiben öffentlich.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Abmelden-Link in feeds.html</name>
  <files>sentiment-api/templates/feeds.html</files>
  <read_first>
    - sentiment-api/templates/feeds.html (Zeilen 36-70 — Header-Bereich lesen)
  </read_first>
  <action>
Im `<header>`-Element von feeds.html einen Abmelden-Link hinzufügen. Der Link soll als Button-ähnliches Element rechts im Header erscheinen.

Den Header-Bereich anpassen: Nach dem `<p>` mit dem Untertitel ("`Verwalte deine RSS-Nachrichtenquellen`") folgenden Link einfügen:

```html
<a href="/logout" class="logout-link">Abmelden</a>
```

CSS für `.logout-link` zum bestehenden `<style>`-Block hinzufügen (nach `.back-link`-Regel oder am Ende des Style-Blocks):

```css
.logout-link {
  display: inline-block;
  margin-top: 0.75rem;
  color: rgba(255,255,255,0.85);
  text-decoration: none;
  font-size: 0.875rem;
  border: 1px solid rgba(255,255,255,0.4);
  padding: 4px 12px;
  border-radius: 4px;
  float: right;
}
.logout-link:hover {
  background: rgba(255,255,255,0.15);
}
```

Falls im Header bereits ein `back-link` oder ähnliches Element mit `float: right` existiert, statt `float: right` einen `display: flex; justify-content: space-between;` Container verwenden — aber nur wenn nötig. Die minimale Änderung bevorzugen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
with open('templates/feeds.html') as f:
    content = f.read()
checks = ['/logout', 'Abmelden', 'logout-link']
missing = [c for c in checks if c not in content]
if missing:
    print('FEHLT in feeds.html:', missing)
    import sys; sys.exit(1)
print('OK — Abmelden-Link vorhanden')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - feeds.html enthält einen Link zu /logout mit Text "Abmelden"
    - Der Link ist stilistisch konsistent mit dem bestehenden Header-Design
    - Keine anderen Inhalte in feeds.html wurden verändert
  </acceptance_criteria>
  <done>feeds.html zeigt einen Abmelden-Link im Header.</done>
</task>

</tasks>

<verification>
Nach Ausführung aller Tasks prüfen:

1. Syntax beider Python-Dateien: `python -m py_compile sentiment-api/app.py && python -m py_compile sentiment-api/moodlight_extensions.py`
2. Gesamttest — alle Routen korrekt markiert:
   ```bash
   python -c "
   import sys
   sys.path.insert(0, 'sentiment-api')
   # Nur Syntax + Import prüfen (keine DB-Verbindung nötig)
   import py_compile
   py_compile.compile('sentiment-api/app.py', doraise=True)
   py_compile.compile('sentiment-api/moodlight_extensions.py', doraise=True)
   print('Syntax OK')
   "
   ```
3. feeds.html Abmelden-Link: Manuell prüfen ob `<a href="/logout">Abmelden</a>` im Header sichtbar wäre
</verification>

<success_criteria>
- Unauthentifizierter GET /feeds → Redirect auf /login (302)
- POST /api/moodlight/feeds ohne Cookie → 401 JSON {"status": "error", "message": "Nicht authentifiziert"}
- DELETE /api/moodlight/feeds/<id> ohne Cookie → 401 JSON
- POST /api/moodlight/cache/clear ohne Cookie → 401 JSON
- GET /api/moodlight/current ohne Cookie → normale 200 Antwort (öffentlich)
- GET /api/moodlight/history ohne Cookie → normale 200 Antwort (öffentlich)
- feeds.html zeigt Abmelden-Link
</success_criteria>

<output>
Nach Abschluss `.planning/phases/13-authentifizierung/13-02-SUMMARY.md` erstellen mit:
- Welche Endpunkte geschützt wurden (POST/DELETE)
- Welche Endpunkte öffentlich geblieben sind (GET moodlight/*)
- Wo der Abmelden-Link hinzugefügt wurde
</output>
