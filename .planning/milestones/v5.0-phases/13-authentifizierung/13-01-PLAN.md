---
phase: 13-authentifizierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/app.py
  - sentiment-api/docker-compose.yaml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03

must_haves:
  truths:
    - "Flask-App hat einen konfigurierten SECRET_KEY und ADMIN_PASSWORD aus Umgebungsvariablen"
    - "GET /login zeigt ein deutsches Login-Formular"
    - "POST /login prüft das Passwort mit werkzeug.security.check_password_hash und setzt einen Session-Cookie"
    - "POST /login mit falschem Passwort gibt 401 und zeigt Fehlermeldung"
    - "GET /logout löscht die Session und leitet auf /login weiter"
    - "login_required Decorator ist in app.py verfügbar"
    - "docker-compose.yaml enthält ADMIN_PASSWORD und SECRET_KEY Umgebungsvariablen"
  artifacts:
    - path: "sentiment-api/app.py"
      provides: "login_required Decorator, /login, /logout Routen, Session-Setup"
      contains: "login_required"
    - path: "sentiment-api/docker-compose.yaml"
      provides: "ADMIN_PASSWORD und SECRET_KEY Umgebungsvariablen"
      contains: "ADMIN_PASSWORD"
    - path: "sentiment-api/templates/login.html"
      provides: "Login-Formular mit deutschem UI"
      contains: "Anmelden"
  key_links:
    - from: "sentiment-api/app.py (POST /login)"
      to: "werkzeug.security.check_password_hash"
      via: "generate_password_hash(os.environ['ADMIN_PASSWORD'])"
      pattern: "check_password_hash"
    - from: "sentiment-api/app.py (login_required)"
      to: "flask.session"
      via: "session.get('authenticated')"
      pattern: "session.get"
---

<objective>
Auth-Infrastruktur für das Backend — Session-Setup, Login/Logout-Routen und `login_required`-Decorator.

Purpose: Fundament für Phase 2 — Decorator und Routen müssen existieren bevor andere Routen geschützt werden können.
Output: Funktionierendes Login-System mit Cookie-Session. /login und /logout funktionieren. Decorator ist nutzbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-authentifizierung/13-CONTEXT.md
@sentiment-api/app.py
@sentiment-api/docker-compose.yaml
</context>

<interfaces>
<!-- Bestehende Flask-App Imports (aus sentiment-api/app.py) -->
```python
from flask import Flask, jsonify, request, render_template
# Bereits importiert: render_template ist vorhanden

app = Flask(__name__)
# app.secret_key fehlt noch — muss gesetzt werden

# Bestehende Route die Auth brauchen wird:
@app.route('/feeds')
def feed_management():
    return render_template('feeds.html')
```

<!-- werkzeug.security ist in Flask enthalten (Flask 3.1.0 nutzt Werkzeug) -->
```python
from werkzeug.security import generate_password_hash, check_password_hash
```

<!-- Flask Session -->
```python
from flask import session, redirect, url_for
# session['authenticated'] = True  # Nach erfolgreichem Login setzen
# session.clear()                   # Beim Logout löschen
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Auth-Infrastruktur in app.py</name>
  <files>sentiment-api/app.py, sentiment-api/templates/login.html</files>
  <read_first>
    - sentiment-api/app.py (komplett — Imports, app-Instanz, alle Routen kennen)
    - sentiment-api/templates/feeds.html (Zeilen 1-80 — CSS-Variablen und Layout-Muster übernehmen)
  </read_first>
  <action>
**1. Imports erweitern** (oben in app.py, Zeile nach `from flask import Flask, jsonify, request, render_template`):
```python
from flask import Flask, jsonify, request, render_template, session, redirect, url_for
from werkzeug.security import generate_password_hash, check_password_hash
from functools import wraps
```

**2. app.secret_key setzen** (direkt nach `app = Flask(__name__)`):
```python
app.secret_key = os.environ.get('SECRET_KEY', 'dev-secret-schluessel-aendern')
# Admin-Passwort-Hash beim Start berechnen (einmal, nicht bei jedem Request)
_admin_password_raw = os.environ.get('ADMIN_PASSWORD', '')
_admin_password_hash = generate_password_hash(_admin_password_raw) if _admin_password_raw else None
if not _admin_password_raw:
    logging.warning("ADMIN_PASSWORD Umgebungsvariable nicht gesetzt — Login deaktiviert!")
```

**3. login_required Decorator** (nach der secret_key-Initialisierung, vor den Routen):
```python
def login_required(f):
    """Decorator: Leitet auf /login weiter wenn keine aktive Session vorhanden."""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not session.get('authenticated'):
            return redirect(url_for('login_page'))
        return f(*args, **kwargs)
    return decorated_function
```

**4. Login- und Logout-Routen** (vor der `/feeds`-Route, nach den API-Routen):
```python
@app.route('/login', methods=['GET', 'POST'])
def login_page():
    """Login-Seite für das Backend-Interface."""
    error = None
    if request.method == 'POST':
        password = request.form.get('password', '')
        if _admin_password_hash and check_password_hash(_admin_password_hash, password):
            session['authenticated'] = True
            session.permanent = True
            next_url = request.args.get('next') or url_for('feed_management')
            logging.info("Admin-Login erfolgreich.")
            return redirect(next_url)
        else:
            error = 'Falsches Passwort.'
            logging.warning("Fehlgeschlagener Login-Versuch.")
            return render_template('login.html', error=error), 401
    return render_template('login.html', error=error)

@app.route('/logout')
def logout():
    """Session beenden und auf Login-Seite weiterleiten."""
    session.clear()
    logging.info("Admin-Logout.")
    return redirect(url_for('login_page'))
```

**5. Session-Timeout konfigurieren** (nach `app.secret_key`-Block):
```python
from datetime import timedelta
app.permanent_session_lifetime = timedelta(hours=24)
```

**6. Neue Template-Datei** `sentiment-api/templates/login.html` erstellen.

Das Login-Template soll:
- CSS-Variablen aus feeds.html übernehmen (`--primary: #8A2BE2`, `--secondary: #1E90FF` etc.)
- Zentriertes Login-Formular, einfaches Design ohne JavaScript
- Deutsches UI: "Passwort", "Anmelden"-Button
- Fehlermeldung-Block wenn `error` gesetzt ist (roter Text, Jinja2: `{% if error %}`)
- AuraOS-Branding in der Überschrift: "AuraOS Backend"
- `<input type="password" name="password" required autofocus>`
- `<form method="POST">` (kein JavaScript nötig)

Minimales Template-Beispiel:
```html
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AuraOS — Login</title>
  <style>
    /* CSS-Variablen aus feeds.html */
    :root { --primary: #8A2BE2; --secondary: #1E90FF; --bg: #f8f9fa; --surface: #ffffff; --text: #2c3e50; --border: #e9ecef; --danger: #dc3545; }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: var(--bg); color: var(--text); display: flex; align-items: center; justify-content: center; min-height: 100vh; }
    .card { background: var(--surface); border-radius: 8px; box-shadow: 0 2px 16px rgba(0,0,0,0.12); padding: 2.5rem; width: 100%; max-width: 380px; }
    h1 { background: linear-gradient(135deg, var(--primary), var(--secondary)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-size: 1.6rem; margin-bottom: 0.25rem; }
    p.subtitle { color: #6c757d; font-size: 0.9rem; margin-bottom: 2rem; }
    label { display: block; font-size: 0.875rem; font-weight: 600; margin-bottom: 0.4rem; }
    input[type="password"] { width: 100%; padding: 0.65rem 0.9rem; border: 1px solid var(--border); border-radius: 6px; font-size: 1rem; outline: none; }
    input[type="password"]:focus { border-color: var(--primary); }
    button { width: 100%; margin-top: 1.25rem; padding: 0.75rem; background: linear-gradient(135deg, var(--primary), var(--secondary)); color: white; border: none; border-radius: 6px; font-size: 1rem; cursor: pointer; }
    .error { color: var(--danger); font-size: 0.875rem; margin-top: 0.75rem; }
  </style>
</head>
<body>
  <div class="card">
    <h1>AuraOS</h1>
    <p class="subtitle">Backend-Verwaltung</p>
    <form method="POST">
      <label for="password">Passwort</label>
      <input type="password" id="password" name="password" required autofocus placeholder="••••••••">
      <button type="submit">Anmelden</button>
      {% if error %}<p class="error">{{ error }}</p>{% endif %}
    </form>
  </div>
</body>
</html>
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
import ast, sys
with open('app.py') as f:
    src = f.read()
# Prüfe ob alle nötigen Elemente vorhanden sind
checks = ['login_required', 'check_password_hash', 'session', 'redirect', 'url_for', 'login_page', 'logout', 'ADMIN_PASSWORD', 'SECRET_KEY', 'permanent_session_lifetime']
missing = [c for c in checks if c not in src]
if missing:
    print('FEHLT in app.py:', missing)
    sys.exit(1)
import os
if not os.path.exists('templates/login.html'):
    print('FEHLT: templates/login.html')
    sys.exit(1)
with open('templates/login.html') as f:
    html = f.read()
html_checks = ['Anmelden', 'password', 'error', 'AuraOS']
missing_html = [c for c in html_checks if c not in html]
if missing_html:
    print('FEHLT in login.html:', missing_html)
    sys.exit(1)
print('OK — alle Checks bestanden')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - app.py enthält login_required Decorator, /login (GET+POST), /logout Routen
    - check_password_hash vergleicht gegen ADMIN_PASSWORD aus os.environ
    - app.secret_key wird aus SECRET_KEY Umgebungsvariable gelesen
    - session.permanent = True mit 24h lifetime
    - templates/login.html existiert mit deutschem Formular (Passwort-Input, Anmelden-Button, Fehler-Anzeige)
    - Falsches Passwort → 401 + Fehlermeldung
    - Richtiges Passwort → Redirect auf /feeds (oder ?next= URL)
  </acceptance_criteria>
  <done>Login-Infrastruktur ist funktionsfähig: /login, /logout und login_required Decorator sind in app.py vorhanden und getestet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ADMIN_PASSWORD und SECRET_KEY in docker-compose.yaml</name>
  <files>sentiment-api/docker-compose.yaml</files>
  <read_first>
    - sentiment-api/docker-compose.yaml (komplett)
  </read_first>
  <action>
Im `news-analyzer` Service unter `environment:` zwei neue Variablen ergänzen:

```yaml
environment:
  - FLASK_ENV=production
  - OPENAI_API_KEY=${OPENAI_API_KEY}
  - DATABASE_URL=postgresql://moodlight:${POSTGRES_PASSWORD}@postgres:5432/moodlight
  - REDIS_URL=redis://redis:6379/0
  - DEFAULT_HEADLINES_PER_SOURCE=1
  - ADMIN_PASSWORD=${ADMIN_PASSWORD}
  - SECRET_KEY=${SECRET_KEY}
```

Beide Variablen nutzen das `${VAR}` Pattern (konsistent mit dem bestehenden `${OPENAI_API_KEY}`).

**NICHT** `.env.example` oder `.env` Dateien anlegen — der Nutzer setzt die Werte selbst in seiner `.env` Datei.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "
import re
with open('docker-compose.yaml') as f:
    content = f.read()
checks = ['\${ADMIN_PASSWORD}', '\${SECRET_KEY}']
missing = [c for c in checks if c not in content]
if missing:
    print('FEHLT in docker-compose.yaml:', missing)
    import sys; sys.exit(1)
print('OK — ADMIN_PASSWORD und SECRET_KEY vorhanden')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yaml enthält `ADMIN_PASSWORD=${ADMIN_PASSWORD}` im news-analyzer environment
    - docker-compose.yaml enthält `SECRET_KEY=${SECRET_KEY}` im news-analyzer environment
    - Kein anderes Feld wurde verändert
  </acceptance_criteria>
  <done>docker-compose.yaml übergibt ADMIN_PASSWORD und SECRET_KEY an den Container.</done>
</task>

</tasks>

<verification>
Nach Ausführung beider Tasks prüfen:

1. Python-Syntax: `python -m py_compile sentiment-api/app.py` — kein Fehler
2. Template vorhanden: `ls sentiment-api/templates/login.html`
3. docker-compose valide: `docker-compose -f sentiment-api/docker-compose.yaml config --quiet`
4. Vollständiger Check (aus Task 1 verify-Block)
</verification>

<success_criteria>
- /login (GET) gibt 200 zurück mit HTML-Formular
- /login (POST) mit falschem Passwort gibt 401 zurück
- /login (POST) mit richtigem Passwort setzt Session-Cookie und leitet weiter
- /logout löscht Session und leitet auf /login weiter
- login_required Decorator ist importierbar und funktionsfähig
- docker-compose.yaml enthält beide neuen Umgebungsvariablen
</success_criteria>

<output>
Nach Abschluss `.planning/phases/13-authentifizierung/13-01-SUMMARY.md` erstellen mit:
- Was wurde implementiert (Decorator, Routen, Template)
- Welche Dateien wurden geändert
- Entscheidungen (werkzeug.security statt eigenem Hash, 24h Session-Timeout)
</output>
