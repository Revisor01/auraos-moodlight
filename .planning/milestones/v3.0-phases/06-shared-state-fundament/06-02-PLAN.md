---
phase: 06-shared-state-fundament
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - firmware/src/moodlight.cpp
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "moodlight.cpp referenziert geteilte Variablen ueber appState.member — keine losen Globals mehr"
    - "pio run baut fehlerfrei — Firmware-Verhalten identisch zum Stand vor der Phase"
    - "Alle bisherigen extern-faehigen Variablen sind im AppState zentralisiert"
  artifacts:
    - path: "firmware/src/moodlight.cpp"
      provides: "Migrierte Firmware mit AppState statt loser Globals"
      contains: "AppState appState"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/app_state.h"
      via: '#include "app_state.h"'
      pattern: '#include "app_state.h"'
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/app_state.h"
      via: "appState.wifiSSID statt wifiSSID"
      pattern: "appState\\."
---

<objective>
Alle geteilten globalen Variablen in moodlight.cpp durch appState.member ersetzen. Die alten Deklarationen entfernen. Am Ende baut pio run fehlerfrei und das Verhalten ist identisch.

Purpose: Nach dieser Migration kommuniziert der gesamte Code ueber ein zentrales AppState-Interface. Phase 7 kann Module extrahieren die einfach `#include "app_state.h"` machen.
Output: Migriertes `moodlight.cpp` ohne lose geteilte Globals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-state-fundament/06-01-SUMMARY.md
@firmware/src/app_state.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppState-Instanz erstellen und include hinzufuegen</name>
  <files>firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/app_state.h, firmware/src/moodlight.cpp (lines 1-50)</read_first>
  <action>
1. Fuege `#include "app_state.h"` nach `#include "config.h"` (Zeile 34) ein.
2. Fuege `AppState appState;` als globale Variable direkt nach den includes ein (vor den alten Globals).
3. Verifiziere: `pio run` baut durch (AppState existiert jetzt neben den alten Globals — noch keine Konflikte da die Member-Namen anders qualifiziert sind).

NICHT in diesem Task: Alte Globals entfernen oder umbenennen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep '#include "app_state.h"' firmware/src/moodlight.cpp` findet den Include
- `grep "^AppState appState;" firmware/src/moodlight.cpp` findet die Instanz
- `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>app_state.h ist included, AppState appState ist instanziiert, Build gruen</done>
</task>

<task type="auto">
  <name>Task 2: Globale Variablen durch appState.member ersetzen</name>
  <files>firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp (vollstaendig — alle 4500+ Zeilen muessen durchsucht werden), firmware/src/app_state.h</read_first>
  <action>
Dies ist der Kern der Migration. Fuer JEDE Variable im AppState:
1. Entferne die alte globale Deklaration (z.B. `String wifiSSID = "";`)
2. Ersetze ALLE Vorkommen im Code durch `appState.member` (z.B. `appState.wifiSSID`)

**Vollstaendige Ersetzungs-Liste (alte Variable -> neuer Zugriff):**

WiFi:
- `wifiSSID` -> `appState.wifiSSID`
- `wifiPassword` -> `appState.wifiPassword`
- `wifiConfigured` -> `appState.wifiConfigured`
- `isInConfigMode` -> `appState.isInConfigMode`
- `wifiReconnectActive` -> `appState.wifiReconnectActive`
- `disconnectStartMs` -> `appState.disconnectStartMs`
- `lastWifiCheck` -> `appState.lastWifiCheck`
- `wifiReconnectDelay` -> `appState.wifiReconnectDelay`
- `wifiReconnectAttempts` -> `appState.wifiReconnectAttempts`
- `wifiWasConnected` -> `appState.wifiWasConnected`
- `apModeStartTime` -> `appState.apModeStartTime`

LED:
- `ledPin` -> `appState.ledPin`
- `numLeds` -> `appState.numLeds`
- `manualBrightness` -> `appState.manualBrightness`
- `lightOn` -> `appState.lightOn`
- `autoMode` -> `appState.autoMode`
- `manualColor` -> `appState.manualColor`
- `currentLedIndex` -> `appState.currentLedIndex`
- `lastLedIndex` -> `appState.lastLedIndex`
- `ledUpdatePending` -> `appState.ledUpdatePending`
- `ledColors` -> `appState.ledColors` (Array — achte auf `ledColors[i]` -> `appState.ledColors[i]`)
- `ledBrightness` -> `appState.ledBrightness`
- `ledClear` -> `appState.ledClear`
- `ledMutex` -> `appState.ledMutex`
- `customColors` -> `appState.customColors` (Array)
- `firstLedShowDone` -> `appState.firstLedShowDone`
- `isPulsing` -> `appState.isPulsing`
- `pulseStartTime` -> `appState.pulseStartTime`
- `statusLedIndex` -> `appState.statusLedIndex`
- `statusLedBlinkStart` -> `appState.statusLedBlinkStart`
- `statusLedState` -> `appState.statusLedState`
- `statusLedMode` -> `appState.statusLedMode`

Sentiment:
- `lastSentimentScore` -> `appState.sentimentScore` (UMBENANNT! Suche nach `lastSentimentScore`)
- `lastSentimentCategory` -> `appState.sentimentCategory` (UMBENANNT! Suche nach `lastSentimentCategory`)
- `moodUpdateInterval` -> `appState.moodUpdateInterval`
- `lastMoodUpdate` -> `appState.lastMoodUpdate`
- `initialAnalysisDone` -> `appState.initialAnalysisDone`
- `lastSuccessfulSentimentUpdate` -> `appState.lastSuccessfulSentimentUpdate`
- `sentimentAPIAvailable` -> `appState.sentimentAPIAvailable`
- `consecutiveSentimentFailures` -> `appState.consecutiveSentimentFailures`
- `apiUrl` -> `appState.apiUrl`

MQTT:
- `mqttServer` -> `appState.mqttServer`
- `mqttUser` -> `appState.mqttUser`
- `mqttPassword` -> `appState.mqttPassword`
- `mqttEnabled` -> `appState.mqttEnabled`
- `lastMqttReconnectAttempt` -> `appState.lastMqttReconnectAttempt`
- `lastMqttHeartbeat` -> `appState.lastMqttHeartbeat`
- `mqttWasConnected` -> `appState.mqttWasConnected`
- `sendingInitialStates` -> `appState.sendingInitialStates`

DHT/Sensor:
- `dhtEnabled` -> `appState.dhtEnabled`
- `dhtPin` -> `appState.dhtPin`
- `dhtUpdateInterval` -> `appState.dhtUpdateInterval` (alte Variable heisst `dhtUpdateInterval`)
- `lastDHTUpdate` -> `appState.lastDHTUpdate`
- `lastTemp` -> `appState.currentTemp` (UMBENANNT! Suche nach `lastTemp`)
- `lastHum` -> `appState.currentHum` (UMBENANNT! Suche nach `lastHum`)

System:
- `rebootNeeded` -> `appState.rebootNeeded`
- `rebootTime` -> `appState.rebootTime`
- `initialStartupPhase` -> `appState.initialStartupPhase`
- `startupTime` -> `appState.startupTime`
- `timeInitialized` -> `appState.timeInitialized`
- `lastSystemHealthCheckTime` -> `appState.lastSystemHealthCheckTime`
- `lastStatusLog` -> `appState.lastStatusLog`
- `settingsNeedSaving` -> `appState.settingsNeedSaving`
- `lastSettingsSaved` -> `appState.lastSettingsSaved`

Log:
- `logBuffer` -> `appState.logBuffer` (Array)
- `logIndex` -> `appState.logIndex`

**ACHTUNG bei Umbenennungen:** 3 Variablen werden umbenannt:
- `lastSentimentScore` -> `appState.sentimentScore`
- `lastSentimentCategory` -> `appState.sentimentCategory`
- `lastTemp` -> `appState.currentTemp`
- `lastHum` -> `appState.currentHum`

**ACHTUNG: Kein False-Positive-Replacement!**
- `dhtPin` in `DHT dht(DEFAULT_DHT_PIN, DHT22)` — das ist der Konstruktor, hier wird DEFAULT_DHT_PIN verwendet, NICHT dhtPin. Lasse diese Zeile in Ruhe.
- `ledPin` in `pixels.setPin(ledPin)` -> `pixels.setPin(appState.ledPin)` — JA, das muss ersetzt werden.
- In String-Literalen (z.B. `"ledPin"` als JSON-Key) -> NICHT ersetzen! Nur die Variable selbst.
- `mqttUser` als Variable -> ersetzen. `"mqttUser"` als Preferences-Key -> NICHT ersetzen.

**ACHTUNG: volatile Member!**
`ledUpdatePending`, `ledColors[]`, `ledBrightness`, `ledClear` sind `volatile` im Struct. Sicherstellen dass der Zugriff ueber `appState.ledColors[i]` etc. weiterhin volatile-qualifiziert ist (ist automatisch der Fall wenn der Member im Struct als volatile deklariert ist).

**Vorgehen:** Systematisch von oben nach unten durch die Datei. Jede alte Deklaration entfernen, dann alle Referenzen in der ganzen Datei ersetzen. Nach jeder logischen Gruppe (WiFi, LED, etc.) einmal mental pruefen.

**Build nach der Migration:** `pio run` MUSS fehlerfrei durchlaufen. Wenn Kompilierungsfehler auftreten, sind Referenzen uebersehen worden — korrigieren bis der Build gruen ist.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "^String wifiSSID" firmware/src/moodlight.cpp` gibt 0 zurueck (alte Deklaration entfernt)
- `grep -c "^bool isInConfigMode" firmware/src/moodlight.cpp` gibt 0 zurueck
- `grep -c "^int ledPin" firmware/src/moodlight.cpp` gibt 0 zurueck
- `grep -c "^bool mqttEnabled" firmware/src/moodlight.cpp` gibt 0 zurueck
- `grep -c "^bool dhtEnabled" firmware/src/moodlight.cpp` gibt 0 zurueck
- `grep -c "^bool rebootNeeded" firmware/src/moodlight.cpp` gibt 0 zurueck
- `grep -c "appState\." firmware/src/moodlight.cpp` gibt > 100 zurueck (viele Referenzen migriert)
- `grep -c "^SemaphoreHandle_t ledMutex" firmware/src/moodlight.cpp` gibt 0 zurueck
- `grep -c "^uint32_t customColors" firmware/src/moodlight.cpp` gibt 0 zurueck
- `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle ~60 geteilten Variablen sind aus globalen Deklarationen entfernt und durch appState.member ersetzt. pio run baut fehlerfrei. Firmware-Verhalten ist identisch (gleiche Logik, nur anderer Zugriffspfad).</done>
</task>

</tasks>

<verification>
1. `pio run` in firmware/ baut fehlerfrei
2. Keine losen globalen Deklarationen fuer migrierte Variablen mehr in moodlight.cpp
3. `appState.` wird konsistent fuer alle geteilten Variablen verwendet
4. Library-Instanzen (pixels, dht, server, HA-Objekte) sind NICHT im AppState
5. Konstanten sind NICHT im AppState (bleiben als #define oder const)
</verification>

<success_criteria>
- `pio run` baut fehlerfrei
- Alle geteilten globalen Variablen sind im AppState zentralisiert
- `moodlight.cpp` verwendet `appState.member` statt loser Globals
- Phase 7 Module koennen ab jetzt `#include "app_state.h"` verwenden
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-state-fundament/06-02-SUMMARY.md`
</output>
