---
phase: 07-modul-extraktion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/settings_manager.h
  - firmware/src/settings_manager.cpp
  - firmware/src/moodlight.cpp
autonomous: true
requirements: [MOD-05]

must_haves:
  truths:
    - "loadSettings() und saveSettings() leben in settings_manager.cpp"
    - "moodlight.cpp ruft init_settings() / loadSettings() / saveSettings() ueber den neuen Header auf"
    - "pio run baut fehlerfrei durch"
  artifacts:
    - path: "firmware/src/settings_manager.h"
      provides: "Header mit Funktionsdeklarationen fuer Settings"
      exports: ["loadSettings", "saveSettings", "saveSettingsToFile", "loadSettingsFromFile"]
    - path: "firmware/src/settings_manager.cpp"
      provides: "Implementierung aller Settings-Funktionen"
      contains: "saveSettingsToFile"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/settings_manager.h"
      via: "#include"
      pattern: '#include "settings_manager.h"'
---

<objective>
Settings-Manager aus moodlight.cpp extrahieren (MOD-05).

Purpose: Settings wird von ALLEN anderen Modulen gebraucht (saveSettings() wird aus MQTT-Callbacks, Web-Handlern etc. aufgerufen). Daher muss dieses Modul ZUERST extrahiert werden.

Output: settings_manager.h + settings_manager.cpp, moodlight.cpp ohne Settings-Code
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modul-extraktion/07-CONTEXT.md

@firmware/src/app_state.h
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings_manager.h/.cpp erstellen und Funktionen verschieben</name>
  <files>firmware/src/settings_manager.h, firmware/src/settings_manager.cpp, firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp, firmware/src/app_state.h, firmware/src/config.h</read_first>
  <action>
1. Erstelle `firmware/src/settings_manager.h` mit:
   - `#pragma once`
   - `#include "app_state.h"` (fuer AppState-Zugriff)
   - Forward-Deklaration: `class Preferences;` (oder #include falls noetig)
   - Funktionsdeklarationen:
     - `bool saveSettingsToFile();`
     - `bool loadSettingsFromFile();`
     - `void saveSettings();`
     - `void loadSettings();`
   - Die Funktionen brauchen Zugriff auf: `appState` (extern), `preferences` (extern), `pixels` (extern, fuer loadSettingsFromFile Zeile 661: `pixels.Color()`), `fileOps` (extern), `debug()` (extern)

2. Erstelle `firmware/src/settings_manager.cpp` mit:
   - `#include "settings_manager.h"`
   - Noetige Includes: `<ArduinoJson.h>`, `<Preferences.h>`, `<Adafruit_NeoPixel.h>`, `"MoodlightUtils.h"`, `"LittleFS.h"`
   - `extern`-Deklarationen fuer globale Objekte die in moodlight.cpp definiert bleiben:
     - `extern AppState appState;`
     - `extern Preferences preferences;`
     - `extern Adafruit_NeoPixel pixels;` (wegen pixels.Color() in loadSettingsFromFile)
     - `extern SafeFileOps fileOps;`
   - `extern void debug(const String &message);` und `extern void debug(const __FlashStringHelper *message);`
   - Verschiebe folgende Funktionen aus moodlight.cpp (Zeilen ca. 587-817):
     a. `saveSettingsToFile()` (Zeile 587-632)
     b. `loadSettingsFromFile()` (Zeile 634-699)
     c. `saveSettings()` (Zeile 701-744)
     d. `loadSettings()` (Zeile 746-817)

3. In `moodlight.cpp`:
   - Fuege `#include "settings_manager.h"` nach `#include "app_state.h"` (Zeile 35) ein
   - Entferne die vier Funktionsdefinitionen (Zeilen 587-817)
   - Die Aufrufe von `loadSettings()`, `saveSettings()` etc. bleiben unveraendert — der Linker findet sie ueber den neuen Header

WARNUNG (aus Phase 6): JSON-Keys wie `"moodInterval"`, `"wifiSSID"` etc. und Preferences-Keys duerfen NICHT veraendert werden. Nur die Funktionsdefinitionen verschieben, nicht den Inhalt aendern.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "saveSettingsToFile\|loadSettingsFromFile\|void saveSettings\|void loadSettings" firmware/src/settings_manager.cpp` ergibt 4
    - `grep -c "saveSettingsToFile\|loadSettingsFromFile\|void saveSettings\|void loadSettings" firmware/src/moodlight.cpp` ergibt 0 (nur Aufrufe, keine Definitionen)
    - `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle 4 Settings-Funktionen leben in settings_manager.cpp, moodlight.cpp inkludiert den Header, Build ist gruen</done>
</task>

</tasks>

<verification>
- `pio run` muss ohne Fehler durchlaufen
- `grep -rn "saveSettings\|loadSettings" firmware/src/settings_manager.cpp` zeigt Funktionsdefinitionen
- `grep -rn '#include "settings_manager.h"' firmware/src/moodlight.cpp` zeigt Include
</verification>

<success_criteria>
- settings_manager.h/.cpp existieren mit allen 4 Settings-Funktionen
- moodlight.cpp enthaelt keine Settings-Funktionsdefinitionen mehr
- `pio run` baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/07-modul-extraktion/07-01-SUMMARY.md`
</output>
