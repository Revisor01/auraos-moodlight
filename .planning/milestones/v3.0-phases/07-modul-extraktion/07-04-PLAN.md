---
phase: 07-modul-extraktion
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - firmware/src/sensor_manager.h
  - firmware/src/sensor_manager.cpp
  - firmware/src/moodlight.cpp
autonomous: true
requirements: [MOD-06]

must_haves:
  truths:
    - "Sentiment-Fetch (getSentiment, handleSentiment, safeHttpGet) und DHT-Read (readAndPublishDHT) leben in sensor_manager.cpp"
    - "mapSentimentToLED und fetchBackendStatistics leben in sensor_manager.cpp"
    - "pio run baut fehlerfrei durch"
  artifacts:
    - path: "firmware/src/sensor_manager.h"
      provides: "Header mit Sensor/Sentiment-Funktionsdeklarationen"
      exports: ["getSentiment", "readAndPublishDHT", "handleSentiment", "mapSentimentToLED", "safeHttpGet", "fetchBackendStatistics"]
    - path: "firmware/src/sensor_manager.cpp"
      provides: "Implementierung aller Sensor/Sentiment-Funktionen"
      contains: "getSentiment"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/sensor_manager.h"
      via: "#include"
      pattern: '#include "sensor_manager.h"'
---

<objective>
Sensor & Sentiment Manager aus moodlight.cpp extrahieren (MOD-06).

Purpose: DHT-Sensorik und Sentiment-API-Abruf bilden eine logische Einheit — beide holen externe Daten und aktualisieren AppState. Sentiment braucht LED-Funktionen (mapSentimentToLED, updateLEDs via handleSentiment), daher kommt dieses Modul NACH dem LED-Controller.

Output: sensor_manager.h + sensor_manager.cpp, moodlight.cpp ohne Sensor/Sentiment-Code
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modul-extraktion/07-CONTEXT.md

@firmware/src/app_state.h
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: sensor_manager.h/.cpp erstellen und Sensor/Sentiment-Funktionen verschieben</name>
  <files>firmware/src/sensor_manager.h, firmware/src/sensor_manager.cpp, firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp, firmware/src/app_state.h, firmware/src/config.h</read_first>
  <action>
1. Erstelle `firmware/src/sensor_manager.h` mit:
   - `#pragma once`
   - `#include "app_state.h"`
   - Funktionsdeklarationen:
     - `int mapSentimentToLED(float sentimentScore);`
     - `void handleSentiment(float sentimentScore);`
     - `bool safeHttpGet(const String &url, JsonDocument &doc);`
     - `void getSentiment();`
     - `void readAndPublishDHT();`
     - `bool fetchBackendStatistics(JsonDocument& doc, int hours = 168);`
     - `void formatString(char *buffer, size_t maxLen, const char *format, ...);`

2. Erstelle `firmware/src/sensor_manager.cpp` mit:
   - `#include "sensor_manager.h"`
   - Noetige Includes: `<ArduinoJson.h>`, `<HTTPClient.h>`, `<WiFi.h>`, `<DHT.h>`, `<stdarg.h>`
   - `extern`-Deklarationen fuer globale Objekte aus moodlight.cpp:
     - `extern AppState appState;`
     - `extern DHT dht;`
     - `extern WiFiClient wifiClientHTTP;`
     - `extern void debug(const String &message);`
     - `extern void debug(const __FlashStringHelper *message);`
   - Fuer MQTT-Zugriff in handleSentiment und getSentiment:
     - `extern HAMqtt mqtt;` (oder `#include <ArduinoHA.h>` + extern)
     - `extern HASensor haSentimentScore;`
     - `extern HASensor haSentimentCategory;`
     - `extern HASensor haTemperature;`
     - `extern HASensor haHumidity;`
   - Fuer LED-Zugriff in handleSentiment:
     - `extern void updateLEDs();` (oder `#include "led_controller.h"`)
     - `extern void setStatusLED(int mode);` (oder `#include "led_controller.h"`)
   - Fuer Hilfsfunktion:
     - `extern String floatToString(float value, int decimalPlaces);`
   - Konstanten die benoetigt werden:
     - `extern const unsigned long SENTIMENT_FALLBACK_TIMEOUT;`
     - `extern const int MAX_SENTIMENT_FAILURES;`
   - Verschiebe folgende Funktionen aus moodlight.cpp:
     a. `fetchBackendStatistics()` (Zeile 961-1022)
     b. `mapSentimentToLED()` (Zeile 1619-1631)
     c. `handleSentiment()` (Zeile 1634-1705)
     d. `formatString()` (Zeile 1708-1714)
     e. `safeHttpGet()` (Zeile 3416-3472)
     f. `getSentiment()` (Zeile 3475-3580)
     g. `readAndPublishDHT()` (Zeile 3583-3666)

3. In `moodlight.cpp`:
   - Fuege `#include "sensor_manager.h"` ein
   - Entferne alle verschobenen Funktionsdefinitionen
   - Die Aufrufe in setup() und loop() bleiben unveraendert

ACHTUNG: `handleSentiment()` benutzt `haSentimentScore`, `haSentimentCategory`, `mqtt.isConnected()` — das sind HA-Objekte die in moodlight.cpp als Globals definiert sind. Diese muessen als `extern` in sensor_manager.cpp deklariert werden.
ACHTUNG: `readAndPublishDHT()` benutzt `dht.readTemperature()`, `dht.readHumidity()` — `dht` ist extern.
ACHTUNG: `getSentiment()` ruft `handleSentiment()` und `updateLEDs()` und `setStatusLED()` auf — alles im selben Modul oder extern verfuegbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "void getSentiment\|void readAndPublishDHT\|void handleSentiment\|int mapSentimentToLED\|bool safeHttpGet\|bool fetchBackendStatistics" firmware/src/sensor_manager.cpp` ergibt 6
    - `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle Sensor/Sentiment-Funktionen leben in sensor_manager.cpp, moodlight.cpp inkludiert den Header, Build ist gruen</done>
</task>

</tasks>

<verification>
- `pio run` muss ohne Fehler durchlaufen
- `grep -rn "getSentiment\|readAndPublishDHT\|handleSentiment" firmware/src/sensor_manager.cpp` zeigt Funktionsdefinitionen
- `grep -rn '#include "sensor_manager.h"' firmware/src/moodlight.cpp` zeigt Include
</verification>

<success_criteria>
- sensor_manager.h/.cpp existieren mit allen Sensor/Sentiment-Funktionen
- moodlight.cpp enthaelt keine Sensor/Sentiment-Funktionsdefinitionen mehr
- `pio run` baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/07-modul-extraktion/07-04-SUMMARY.md`
</output>
