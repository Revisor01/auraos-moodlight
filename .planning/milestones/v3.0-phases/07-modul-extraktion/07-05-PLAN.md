---
phase: 07-modul-extraktion
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - firmware/src/mqtt_handler.h
  - firmware/src/mqtt_handler.cpp
  - firmware/src/moodlight.cpp
autonomous: true
requirements: [MOD-04]

must_haves:
  truths:
    - "MQTT/HA-Funktionen (setupHA, sendInitialStates, sendHeartbeat, checkAndReconnectMQTT, alle Callbacks) leben in mqtt_handler.cpp"
    - "ArduinoHA-Entity-Globals (haLight, haMode, haSentimentScore etc.) leben in mqtt_handler.cpp"
    - "pio run baut fehlerfrei durch"
  artifacts:
    - path: "firmware/src/mqtt_handler.h"
      provides: "Header mit MQTT/HA-Funktions- und Entity-Deklarationen"
      exports: ["setupHA", "sendInitialStates", "sendHeartbeat", "checkAndReconnectMQTT"]
    - path: "firmware/src/mqtt_handler.cpp"
      provides: "Implementierung aller MQTT/HA-Funktionen"
      contains: "setupHA"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/mqtt_handler.h"
      via: "#include"
      pattern: '#include "mqtt_handler.h"'
---

<objective>
MQTT/HA-Handler aus moodlight.cpp extrahieren (MOD-04).

Purpose: Alles was mit Home Assistant zusammenhaengt — Entity-Definitionen, Callbacks, Heartbeat, Connect/Reconnect. Das groesste zusammenhaengende Stueck Code. Die ArduinoHA-Globals (haLight, haMode etc.) gehoeren auch hierher, da sie NUR von MQTT-Code benutzt werden.

Output: mqtt_handler.h + mqtt_handler.cpp, moodlight.cpp ohne MQTT/HA-Code
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modul-extraktion/07-CONTEXT.md

@firmware/src/app_state.h
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: mqtt_handler.h/.cpp erstellen und MQTT/HA-Code verschieben</name>
  <files>firmware/src/mqtt_handler.h, firmware/src/mqtt_handler.cpp, firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp, firmware/src/app_state.h, firmware/src/config.h</read_first>
  <action>
1. Erstelle `firmware/src/mqtt_handler.h` mit:
   - `#pragma once`
   - `#include "app_state.h"`
   - `#include <ArduinoHA.h>`
   - Extern-Deklarationen fuer HA-Globals (damit andere Module z.B. `mqtt.isConnected()` pruefen koennen):
     - `extern HAMqtt mqtt;`
     - `extern HASensor haSentimentScore;`
     - `extern HASensor haSentimentCategory;`
     - `extern HASensor haTemperature;`
     - `extern HASensor haHumidity;`
     - `extern HALight haLight;`
     - `extern HASelect haMode;`
     - `extern HAButton haRefreshSentiment;`
     - `extern HANumber haUpdateInterval;`
     - `extern HANumber haDhtInterval;`
     - `extern HASensor haUptime;`
     - `extern HASensor haWifiSignal;`
     - `extern HASensor haSystemStatus;`
   - Funktionsdeklarationen:
     - `void setupHA();`
     - `void sendInitialStates();`
     - `void sendHeartbeat();`
     - `void checkAndReconnectMQTT();`

2. Erstelle `firmware/src/mqtt_handler.cpp` mit:
   - `#include "mqtt_handler.h"`
   - Noetige Includes: `<WiFi.h>`, `<HTTPClient.h>`, `<ArduinoJson.h>`, `<Adafruit_NeoPixel.h>`, `<DHT.h>`
   - DEFINIERE die HA-Globals hier (VERSCHIEBEN aus moodlight.cpp Zeilen 97-115):
     - `WiFiClient wifiClientHA;` (Zeile 97) — VERSCHIEBEN
     - `byte mac[6];` (Zeile 99)
     - `HADevice device;` (Zeile 100)
     - `HAMqtt mqtt(wifiClientHA, device);` (Zeile 101)
     - `HASensor haSentimentScore(...)` etc. (Zeilen 102-115)
   - ACHTUNG: `wifiClientHA` wird NUR von MQTT benutzt — mit verschieben. `wifiClientHTTP` bleibt in moodlight.cpp (wird von sensor_manager benutzt).
   - `extern`-Deklarationen fuer verbleibende Globals in moodlight.cpp:
     - `extern AppState appState;`
     - `extern Adafruit_NeoPixel pixels;` (fuer onRGBColorCommand, sendInitialStates)
     - `extern DHT dht;` (fuer sendInitialStates DHT-Read)
     - `extern void debug(const String &message);`
     - `extern void debug(const __FlashStringHelper *message);`
   - Fuer Callbacks die andere Module aufrufen:
     - `extern void updateLEDs();` (oder `#include "led_controller.h"`)
     - `extern void setStatusLED(int mode);`
     - `extern void saveSettings();` (oder `#include "settings_manager.h"`)
     - `extern void handleSentiment(float);` (oder `#include "sensor_manager.h"`)
     - `extern void getSentiment();` (fuer onRefreshButtonPressed — NEIN, onRefreshButtonPressed hat eigene HTTP-Logik)
   - `extern WiFiClient wifiClientHTTP;` (fuer onRefreshButtonPressed Zeile 3121-3181)
   - `extern String floatToString(float value, int decimalPlaces);`
   - Konstanten:
     - `extern const unsigned long MQTT_HEARTBEAT_INTERVAL;`
   - Verschiebe folgende Funktionen aus moodlight.cpp:
     a. `onStateCommand()` (Zeile 2975-3000)
     b. `onBrightnessCommand()` (Zeile 3002-3022)
     c. `onRGBColorCommand()` (Zeile 3024-3045)
     d. `onModeCommand()` (Zeile 3047-3067)
     e. `onUpdateIntervalCommand()` (Zeile 3069-3091)
     f. `onDHTIntervalCommand()` (Zeile 3095-3114)
     g. `onRefreshButtonPressed()` (Zeile 3116-3181)
     h. `sendHeartbeat()` (Zeile 3184-3228)
     i. `setupHA()` (Zeile 3230-3328)
     j. `sendInitialStates()` (Zeile 3331-3413)
     k. `checkAndReconnectMQTT()` (Zeile 3788-3893)

3. In `moodlight.cpp`:
   - Fuege `#include "mqtt_handler.h"` ein
   - Entferne die HA-Globals (Zeilen 97-115: wifiClientHA, mac, device, mqtt, haSentimentScore, haTemperature, haHumidity, haLight, haMode, haRefreshSentiment, haUpdateInterval, haDhtInterval, haSentimentCategory, haUptime, haWifiSignal, haSystemStatus)
   - Entferne alle verschobenen Funktionsdefinitionen
   - Die Aufrufe in setup() und loop() bleiben unveraendert

ACHTUNG: `onRefreshButtonPressed()` hat seine eigene HTTP-Fetch-Logik (separate von getSentiment). Sie benutzt `wifiClientHTTP` und `handleSentiment()` — beides als extern verfuegbar.
ACHTUNG: `sendInitialStates()` liest direkt vom `dht`-Sensor und ruft `pixels.Color()` sowie `getColorDefinition()` auf. `getColorDefinition` lebt jetzt in led_controller — als extern oder via include verfuegbar machen.
ACHTUNG: sensor_manager.cpp hat bereits `extern HAMqtt mqtt;` und `extern HASensor haSentimentScore;` etc. — wenn die Definitionen jetzt in mqtt_handler.cpp leben, muss die sensor_manager.cpp `#include "mqtt_handler.h"` verwenden ODER die bestehenden extern-Deklarationen funktionieren weiter (Linker findet die Definition in mqtt_handler.cpp). Bestehende extern-Deklarationen funktionieren — keine Aenderung an sensor_manager.cpp noetig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "void setupHA\|void sendInitialStates\|void sendHeartbeat\|void checkAndReconnectMQTT\|void onStateCommand\|void onBrightnessCommand" firmware/src/mqtt_handler.cpp` ergibt mindestens 6
    - `grep "HAMqtt mqtt" firmware/src/mqtt_handler.cpp` findet die Definition
    - `grep "HAMqtt" firmware/src/moodlight.cpp` findet KEINE Definition mehr (nur ggf. Aufrufe von mqtt.loop())
    - `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle MQTT/HA-Funktionen und HA-Globals leben in mqtt_handler.h/.cpp, moodlight.cpp inkludiert den Header, Build ist gruen</done>
</task>

</tasks>

<verification>
- `pio run` muss ohne Fehler durchlaufen
- `grep -rn "setupHA\|checkAndReconnectMQTT\|sendHeartbeat" firmware/src/mqtt_handler.cpp` zeigt Funktionsdefinitionen
- `grep -rn '#include "mqtt_handler.h"' firmware/src/moodlight.cpp` zeigt Include
</verification>

<success_criteria>
- mqtt_handler.h/.cpp existieren mit allen MQTT/HA-Funktionen und HA-Entity-Globals
- moodlight.cpp enthaelt keine MQTT-Funktionsdefinitionen und keine HA-Entity-Globals mehr
- `pio run` baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/07-modul-extraktion/07-05-SUMMARY.md`
</output>
