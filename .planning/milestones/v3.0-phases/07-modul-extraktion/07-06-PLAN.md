---
phase: 07-modul-extraktion
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - firmware/src/web_server.h
  - firmware/src/web_server.cpp
  - firmware/src/moodlight.cpp
autonomous: true
requirements: [MOD-03]

must_haves:
  truths:
    - "Web-Server-Funktionen (setupWebServer, handleStaticFile, handleApiStatus, handleUiUpload, alle API-Handler) leben in web_server.cpp"
    - "JsonBufferPool und JsonBufferGuard leben in web_server.cpp"
    - "moodlight.cpp enthaelt nur noch setup(), loop() und Modul-Init-Aufrufe plus globale Instanzen"
    - "pio run baut fehlerfrei durch"
  artifacts:
    - path: "firmware/src/web_server.h"
      provides: "Header mit Web-Server-Funktionsdeklarationen"
      exports: ["setupWebServer", "handleStaticFile", "handleApiStatus", "handleApiBackendStats"]
    - path: "firmware/src/web_server.cpp"
      provides: "Implementierung aller Web-Server-Funktionen"
      contains: "setupWebServer"
  key_links:
    - from: "firmware/src/moodlight.cpp"
      to: "firmware/src/web_server.h"
      via: "#include"
      pattern: '#include "web_server.h"'
---

<objective>
Web-Server aus moodlight.cpp extrahieren (MOD-03).

Purpose: Der Web-Server ist das groesste Modul — Route-Registration, API-Handler, File-Handler, Upload-Handler. Er braucht Zugriff auf ALLE anderen Module (Settings, LED, Sensor, MQTT), daher kommt er ZULETZT. Nach dieser Extraktion enthaelt moodlight.cpp nur noch setup(), loop() und die globalen Instanzen.

Output: web_server.h + web_server.cpp, moodlight.cpp drastisch reduziert
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modul-extraktion/07-CONTEXT.md

@firmware/src/app_state.h
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: web_server.h/.cpp erstellen und Web-Server-Code verschieben</name>
  <files>firmware/src/web_server.h, firmware/src/web_server.cpp, firmware/src/moodlight.cpp</files>
  <read_first>firmware/src/moodlight.cpp, firmware/src/app_state.h, firmware/src/config.h</read_first>
  <action>
1. Erstelle `firmware/src/web_server.h` mit:
   - `#pragma once`
   - `#include "app_state.h"`
   - `#include <WebServer.h>`
   - Funktionsdeklarationen:
     - `void handleStaticFile(String path);`
     - `void setupWebServer();`
     - `void handleApiStatus();`
     - `void handleApiDeleteDataPoint();`
     - `void handleApiResetAllData();`
     - `void handleApiBackendStats();`
     - `void handleUiUpload();`
     - `void initFS();`
     - `void getStorageInfo(uint64_t &totalBytes, uint64_t &usedBytes, uint64_t &freeBytes);`
     - `String getCurrentUiVersion();`
     - `String getCurrentFirmwareVersion();`
   - Fuer JsonBufferPool-Zugriff (falls andere Module ihn brauchen — wahrscheinlich nicht nach Extraktion):
     - `// JsonBufferPool bleibt intern in web_server.cpp`

2. Erstelle `firmware/src/web_server.cpp` mit:
   - `#include "web_server.h"`
   - Noetige Includes: `<ArduinoJson.h>`, `<WiFi.h>`, `<HTTPClient.h>`, `<Update.h>`, `"LittleFS.h"`, `<Adafruit_NeoPixel.h>`, `"MoodlightUtils.h"`, `<ESP32-targz.h>`, `<Preferences.h>`
   - DEFINE_DEST_FS_USES_LITTLEFS muss VOR ESP32-targz include stehen (oder pruefen ob es in moodlight.cpp global definiert ist)
   - `extern`-Deklarationen fuer globale Objekte:
     - `extern AppState appState;`
     - `extern WebServer server;`
     - `extern Adafruit_NeoPixel pixels;`
     - `extern Preferences preferences;`
     - `extern void debug(const String &message);`
     - `extern void debug(const __FlashStringHelper *message);`
   - Fuer Funktionen aus anderen Modulen:
     - `extern void saveSettings();` (oder `#include "settings_manager.h"`)
     - `extern void loadSettings();`
     - `extern void updateLEDs();` (oder `#include "led_controller.h"`)
     - `extern void setStatusLED(int mode);`
     - `extern void handleSentiment(float);` (oder `#include "sensor_manager.h"`)
     - `extern void getSentiment();`
     - `extern bool fetchBackendStatistics(JsonDocument&, int);`
     - `extern int mapSentimentToLED(float);`
   - Fuer MQTT-Zugriff in handleApiStatus etc.:
     - `#include "mqtt_handler.h"` (fuer `mqtt.isConnected()`)
   - Fuer LED-Farben in Route-Handlern:
     - `#include "led_controller.h"` (fuer `getColorDefinition()`, `ColorDefinition`, `colorNames`)
   - Utility-Instanzen die vom WebServer benutzt werden:
     - `extern MemoryMonitor memMonitor;`
     - `extern NetworkDiagnostics netDiag;`
     - `extern SystemHealthCheck sysHealth;`
     - `extern SafeFileOps fileOps;`
   - `extern String floatToString(float, int);`
   - `extern const String SOFTWARE_VERSION;`
   - VERSCHIEBE JsonBufferPool struct + jsonPool + JsonBufferGuard hierher (Zeilen 250-311)
   - Definiere `JSON_BUFFER_SIZE` und `JSON_BUFFER_COUNT` hier (oder als extern/define)
   - VERSCHIEBE Datei-Hilfsfunktionen:
     - `copyFile()` (Zeile 1080-1125)
     - `moveFile()` (Zeile 1126-1140)
     - `copyDir()` (Zeile 1141-1170)
     - `deleteDir()` (Zeile 1171-1197)
   - Verschiebe folgende Funktionen aus moodlight.cpp:
     a. `handleStaticFile()` (Zeile 213-235)
     b. `initFS()` (Zeile 1023-1079)
     c. `copyFile()` (Zeile 1080-1125)
     d. `moveFile()` (Zeile 1126-1140)
     e. `copyDir()` (Zeile 1141-1170)
     f. `deleteDir()` (Zeile 1171-1197)
     g. `getCurrentUiVersion()` (Zeile 1198-1213)
     h. `getCurrentFirmwareVersion()` (Zeile 1214-1231)
     i. `handleUiUpload()` (Zeile 1232-1451)
     j. `handleApiStatus()` (Zeile 1452-1533)
     k. `handleApiDeleteDataPoint()` (Zeile 1538-1542)
     l. `handleApiResetAllData()` (Zeile 1545-1549)
     m. `getStorageInfo()` (Zeile 1554-1617)
     n. `handleApiBackendStats()` (Zeile 1726-1743)
     o. `setupWebServer()` (Zeile 1746-2916) — die GROSSE Funktion mit allen Route-Registrierungen
     p. `logSystemStatus()` (Zeile 3896-3920) — loggt System-Status, passt thematisch zum WebServer/Diagnostics

3. In `moodlight.cpp`:
   - Fuege `#include "web_server.h"` ein
   - Entferne alle verschobenen Funktionsdefinitionen
   - Entferne JsonBufferPool struct, jsonPool, JsonBufferGuard (Zeilen 250-311)
   - Entferne JSON_BUFFER_SIZE und JSON_BUFFER_COUNT defines (Zeilen 45-46) — verschieben nach web_server.cpp
   - Die Aufrufe in setup() und loop() bleiben unveraendert

ACHTUNG: `setupWebServer()` ist die groesste Funktion (~1170 Zeilen, Zeile 1746-2916). Sie enthaelt INLINE-Lambdas die auf `server`, `appState`, `pixels`, `preferences`, `saveSettings()`, `loadSettings()`, `updateLEDs()`, `handleSentiment()`, etc. zugreifen. Alle diese sind ueber extern verfuegbar.
ACHTUNG: `handleUiUpload()` benutzt `ESP32-targz` — das `#define DEST_FS_USES_LITTLEFS` muss VOR dem `#include <ESP32-targz.h>` stehen. In moodlight.cpp steht es auf Zeile 29. In web_server.cpp muss es ebenfalls definiert werden.
ACHTUNG: `logSystemStatus()` benutzt `mqtt.isConnected()` — braucht `#include "mqtt_handler.h"`.
ACHTUNG: Nach dieser Extraktion sollte moodlight.cpp nur noch enthalten:
  - Includes (alle Modul-Header)
  - `AppState appState;` Definition
  - `WebServer server(80);` + `DNSServer dnsServer;` + Hardware-Instanzen (pixels, dht, preferences)
  - Utility-Instanzen (watchdog, memMonitor, fileOps, netDiag, sysHealth)
  - `debug()` Funktionen (werden von ALLEN Modulen gebraucht)
  - `floatToString()` Hilfsfunktion
  - Konstanten (STARTUP_GRACE_PERIOD etc.)
  - `setup()` und `loop()`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "void setupWebServer\|void handleStaticFile\|void handleApiStatus\|void handleUiUpload\|void initFS" firmware/src/web_server.cpp` ergibt 5
    - `grep "JsonBufferPool" firmware/src/web_server.cpp` findet die Struct-Definition
    - `wc -l firmware/src/moodlight.cpp` ergibt deutlich weniger als 1000 Zeilen
    - `pio run` baut fehlerfrei
  </acceptance_criteria>
  <done>Alle Web-Server-Funktionen, JsonBufferPool und Datei-Hilfsfunktionen leben in web_server.h/.cpp. moodlight.cpp ist auf setup(), loop(), debug(), Globals und Konstanten reduziert. Build ist gruen.</done>
</task>

</tasks>

<verification>
- `pio run` muss ohne Fehler durchlaufen
- `grep -rn "setupWebServer\|handleApiStatus" firmware/src/web_server.cpp` zeigt Funktionsdefinitionen
- `wc -l firmware/src/moodlight.cpp` zeigt drastische Reduktion (Ziel: unter ~800 Zeilen, Phase 8 macht dann weiter auf unter 200)
- Alle 6 Module existieren: settings_manager, wifi_manager, led_controller, sensor_manager, mqtt_handler, web_server
</verification>

<success_criteria>
- web_server.h/.cpp existieren mit allen Web-Server-Funktionen
- JsonBufferPool lebt in web_server.cpp
- moodlight.cpp enthaelt nur noch setup(), loop(), debug(), Globals und Konstanten
- `pio run` baut fehlerfrei
- Alle 6 MOD-Requirements sind erfuellt
</success_criteria>

<output>
After completion, create `.planning/phases/07-modul-extraktion/07-06-SUMMARY.md`
</output>
