---
phase: 08-konsolidierung-qualitaet
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - firmware/src/debug.h
  - firmware/src/debug.cpp
  - firmware/src/moodlight.cpp
  - firmware/src/MoodlightUtils.h
  - firmware/src/led_controller.cpp
  - firmware/src/wifi_manager.cpp
  - firmware/src/web_server.cpp
  - firmware/src/sensor_manager.cpp
  - firmware/src/mqtt_handler.cpp
  - firmware/src/settings_manager.cpp
autonomous: true
requirements: [ARCH-02, QUAL-03]

must_haves:
  truths:
    - "debug()-Funktionen sind in eigenem Modul debug.h/.cpp"
    - "floatToString() ist nicht mehr in moodlight.cpp"
    - "Hardware-Instanzen (pixels, dht, preferences, server, dnsServer) sind in ihren jeweiligen Modulen definiert"
    - "pio run baut fehlerfrei"
  artifacts:
    - path: "firmware/src/debug.h"
      provides: "debug() Deklarationen"
      exports: ["debug"]
    - path: "firmware/src/debug.cpp"
      provides: "debug() Implementierung mit Ringpuffer"
      min_lines: 30
  key_links:
    - from: "firmware/src/debug.h"
      to: "firmware/src/moodlight.cpp"
      via: "#include debug.h"
      pattern: "include.*debug\\.h"
    - from: "firmware/src/debug.h"
      to: "firmware/src/MoodlightUtils.h"
      via: "#include debug.h ersetzt extern-Deklaration"
      pattern: "include.*debug\\.h"
---

<objective>
debug()-Funktionen, floatToString(), und Hardware-Instanzen aus moodlight.cpp in eigene Module verschieben.

Purpose: moodlight.cpp soll nur noch setup()/loop()-Orchestrierung enthalten. Alle Definitionen von Hilfs-Funktionen und Hardware-Instanzen gehoeren in ihre jeweiligen Module. Die debug()-Funktion (63 Zeilen) ist der groesste einzelne Block der verschoben werden kann.
Output: Neues debug.h/.cpp Modul, verschlankte moodlight.cpp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-konsolidierung-qualitaet/08-01-SUMMARY.md
@firmware/src/moodlight.cpp
@firmware/src/app_state.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: debug() in debug.h/.cpp extrahieren und Globals verschieben</name>
  <files>firmware/src/debug.h, firmware/src/debug.cpp, firmware/src/moodlight.cpp, firmware/src/MoodlightUtils.h, firmware/src/led_controller.cpp, firmware/src/wifi_manager.cpp, firmware/src/web_server.cpp, firmware/src/sensor_manager.cpp, firmware/src/mqtt_handler.cpp, firmware/src/settings_manager.cpp</files>
  <action>
**read_first:** firmware/src/moodlight.cpp (vollstaendig, Stand nach Plan 01), firmware/src/MoodlightUtils.h (Zeilen 1-20), firmware/src/web_server.cpp (Zeilen 1-40), firmware/src/sensor_manager.cpp (Zeilen 1-25)

**Step 1 — debug.h erstellen:**
```cpp
#ifndef DEBUG_H
#define DEBUG_H

#include <Arduino.h>

// Debug-Logging mit Ringpuffer und Serial-Ausgabe
void debug(const String &message);
void debug(const __FlashStringHelper *message);

// Hilfsfunktion fuer Float-zu-String-Konvertierung
String floatToString(float value, int decimalPlaces);

#endif // DEBUG_H
```

**Step 2 — debug.cpp erstellen:**
Verschiebe aus moodlight.cpp:
- `#define DEBUG_MODE` (oder als Build-Flag in platformio.ini behalten)
- Beide `debug()`-Ueberladungen (DEBUG_MODE und non-DEBUG_MODE Varianten, ca. 63 Zeilen)
- `floatToString()` Funktion (5 Zeilen)

debug.cpp braucht: `#include "debug.h"`, `#include "app_state.h"`, `#include "config.h"` (fuer LOG_BUFFER_SIZE).

WICHTIG: `#define DEBUG_MODE` muss VOR der `#ifdef DEBUG_MODE`-Pruefung stehen. Am besten als erste Zeile in debug.cpp oder als `-DDEBUG_MODE` Build-Flag in platformio.ini (build_flags).

**Step 3 — moodlight.cpp bereinigen:**
Entferne aus moodlight.cpp:
- `#define DEBUG_MODE` Zeile
- Beide `debug()`-Funktionsbloecke (Zeilen ~94-157)
- `floatToString()` Funktion (Zeilen ~53-58)
- `#include "MoodlightUtils.h"` bleibt (braucht weiterhin WatchdogManager etc.)

Fuege hinzu: `#include "debug.h"` in der Include-Liste.

**Step 4 — Hardware-Instanzen verschieben:**

a) `WiFiClient wifiClientHTTP;` (Zeile ~61) -> nach sensor_manager.cpp verschieben (dort am meisten genutzt, web_server.cpp nutzt es auch).
   - In sensor_manager.cpp: `WiFiClient wifiClientHTTP;` als globale Variable definieren
   - In sensor_manager.h: `extern WiFiClient wifiClientHTTP;` deklarieren
   - In web_server.cpp: `extern WiFiClient wifiClientHTTP;` durch `#include "sensor_manager.h"` ersetzen (falls nicht schon inkludiert) ODER extern-Deklaration beibehalten
   - Aus moodlight.cpp: Zeile entfernen

b) `WebServer server(80);` (Zeile ~64) -> nach web_server.cpp verschieben.
   - In web_server.cpp: `WebServer server(80);` als globale Variable definieren
   - In web_server.h: `extern WebServer server;` deklarieren (wird in moodlight.cpp fuer `server.begin()` und `server.handleClient()` gebraucht)
   - Aus moodlight.cpp: Zeile entfernen, `#include <WebServer.h>` kann bleiben (fuer den Typ)

c) `DNSServer dnsServer;` (Zeile ~48) -> nach wifi_manager.cpp verschieben.
   - In wifi_manager.cpp: `DNSServer dnsServer;` als globale Variable definieren (pruefen ob bereits vorhanden)
   - In wifi_manager.h: `extern DNSServer dnsServer;` deklarieren (wird in moodlight.cpp fuer `dnsServer.processNextRequest()` gebraucht)
   - Aus moodlight.cpp: Zeile entfernen
   - CAPTIVE_PORTAL_IP define ebenfalls nach wifi_manager.cpp verschieben (nur dort gebraucht)

d) `Adafruit_NeoPixel pixels;` (Zeile ~69) -> nach led_controller.cpp verschieben.
   - In led_controller.cpp: `Adafruit_NeoPixel pixels;` als globale Variable definieren (pruefen ob bereits vorhanden)
   - In led_controller.h: `extern Adafruit_NeoPixel pixels;` deklarieren (wird in moodlight.cpp fuer `pixels = Adafruit_NeoPixel(...)`, `pixels.begin()`, `pixels.setBrightness()` gebraucht)
   - Aus moodlight.cpp: Zeile entfernen

e) `DHT dht(DEFAULT_DHT_PIN, DHT22);` (Zeile ~70) -> nach sensor_manager.cpp verschieben.
   - In sensor_manager.cpp: `DHT dht(DEFAULT_DHT_PIN, DHT22);` als globale Variable definieren
   - In sensor_manager.h: `extern DHT dht;` deklarieren (wird in moodlight.cpp fuer `dht.begin()` gebraucht)
   - Aus moodlight.cpp: Zeile entfernen

f) `Preferences preferences;` (Zeile ~71) -> nach settings_manager.cpp verschieben.
   - In settings_manager.cpp: `Preferences preferences;` als globale Variable definieren (pruefen ob bereits vorhanden)
   - In settings_manager.h: `extern Preferences preferences;` deklarieren (wird in moodlight.cpp Health-Check fuer preferences.begin/end gebraucht)
   - Aus moodlight.cpp: Zeile entfernen

g) Utility-Instanzen (`watchdog`, `memMonitor`, `fileOps`, `netDiag`, `sysHealth`) bleiben vorerst in moodlight.cpp — sie werden in Plan 03 bei der Health-Check-Extraktion behandelt.

**Step 5 — extern-Deklarationen in Modulen bereinigen (per QUAL-03):**
Alle Module die `extern void debug(...)` deklarieren:
- `MoodlightUtils.h` (Zeilen 15-16)
- `led_controller.cpp` (Zeilen 5-6)
- `wifi_manager.cpp` (Zeilen 23-24)
- `web_server.cpp` (Zeilen 37-38)
- `sensor_manager.cpp` (Zeilen 19-21)

Ersetze alle `extern void debug(...)` und `extern String floatToString(...)` Deklarationen durch `#include "debug.h"`.

Ebenso: `extern WiFiClient wifiClientHTTP;` in web_server.cpp und sensor_manager.cpp durch korrekten Include ersetzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - debug.h und debug.cpp existieren mit debug() und floatToString()
    - Keine debug()-Definition mehr in moodlight.cpp
    - Keine floatToString()-Definition mehr in moodlight.cpp
    - Hardware-Instanzen (wifiClientHTTP, server, dnsServer, pixels, dht, preferences) nicht mehr in moodlight.cpp definiert
    - Keine `extern void debug(...)` oder `extern String floatToString(...)` Deklarationen in Modul-.cpp-Dateien — alle durch #include "debug.h" ersetzt
    - `pio run` baut fehlerfrei
  </done>
</task>

</tasks>

<verification>
- `pio run` baut fehlerfrei
- `grep -c "void debug" firmware/src/moodlight.cpp` liefert 0
- `grep -c "floatToString" firmware/src/moodlight.cpp` liefert 0 (nur Include)
- `grep -rn "extern void debug" firmware/src/*.cpp` liefert 0
- `grep -rn "extern String floatToString" firmware/src/*.cpp` liefert 0
- `wc -l firmware/src/debug.cpp` zeigt ~70 Zeilen
- `wc -l firmware/src/moodlight.cpp` deutlich reduziert (Ziel: ~450 Zeilen nach diesem Plan)
</verification>

<success_criteria>
debug-Modul existiert, Hardware-Instanzen in ihren Modulen, extern-Duplikate eliminiert, Build gruen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-konsolidierung-qualitaet/08-02-SUMMARY.md`
</output>
