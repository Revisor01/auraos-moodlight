---
phase: 08-konsolidierung-qualitaet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/config.h
  - firmware/src/moodlight.cpp
  - firmware/src/wifi_manager.cpp
  - firmware/src/mqtt_handler.cpp
  - firmware/src/web_server.cpp
  - firmware/src/sensor_manager.cpp
  - firmware/src/led_controller.cpp
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "Alle Magic Numbers in moodlight.cpp und Modulen sind durch benannte Konstanten in config.h ersetzt"
    - "Kein toter Code (auskommentierte Bloecke, v9.0-Kommentare, unused Variablen) in moodlight.cpp"
    - "pio run baut fehlerfrei"
  artifacts:
    - path: "firmware/src/config.h"
      provides: "Alle Timing-Konstanten als #define"
      contains: "STARTUP_GRACE_PERIOD"
    - path: "firmware/src/moodlight.cpp"
      provides: "Bereinigter Hauptdatei ohne Magic Numbers und Dead Code"
  key_links:
    - from: "firmware/src/config.h"
      to: "firmware/src/moodlight.cpp"
      via: "#include config.h"
      pattern: "include.*config\\.h"
---

<objective>
Magic Numbers nach config.h zentralisieren und toten Code aus moodlight.cpp entfernen.

Purpose: Alle verstreuten `extern const`-Definitionen und Magic Numbers werden zu `#define`-Konstanten in config.h. Tote Kommentarblöcke (v9.0-Referenzen, auskommentierter Code) werden entfernt.
Output: Saubere config.h mit allen Timing-Konstanten, bereinigtes moodlight.cpp ohne Dead Code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@firmware/src/config.h
@firmware/src/moodlight.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Magic Numbers nach config.h verschieben</name>
  <files>firmware/src/config.h, firmware/src/moodlight.cpp, firmware/src/wifi_manager.cpp, firmware/src/mqtt_handler.cpp, firmware/src/web_server.cpp, firmware/src/sensor_manager.cpp, firmware/src/led_controller.cpp</files>
  <action>
**read_first:** firmware/src/config.h, firmware/src/moodlight.cpp (Zeilen 44-92, 170, 436, 466, 498, 511, 516-533)

**Step 1 — config.h erweitern:** Folgende Konstanten als `#define` hinzufuegen (per QUAL-02):

```
// Timing-Konstanten
#define DNS_PORT 53
#define LOG_BUFFER_SIZE 20
#define STARTUP_GRACE_PERIOD 15000        // 15s Grace Period nach Boot
#define MAX_RECONNECT_DELAY 300000        // 5 Minuten max WiFi Reconnect
#define STATUS_LED_GRACE_MS 30000         // 30s bevor Status-LED aktiviert
#define MQTT_HEARTBEAT_INTERVAL 60000     // 1 Minute
#define SENTIMENT_FALLBACK_TIMEOUT 3600000 // 1 Stunde ohne Update -> Fallback
#define MAX_SENTIMENT_FAILURES 5
#define STATUS_LOG_INTERVAL 300000        // 5 Minuten
#define AP_TIMEOUT 300000                 // 5 Minuten
#define REBOOT_DELAY 5000                 // 5s bis Reboot
#define HEALTH_CHECK_INTERVAL 3600000     // 1 Stunde
#define HEALTH_CHECK_SHORT_INTERVAL 300000 // 5 Minuten (sysHealth.update in loop)

// Loop-Timing
#define LOOP_SERVER_HANDLE_MS 20
#define LOOP_CONNECTION_CHECK_MS 2000
#define LOOP_MQTT_INTERVAL_MS 100
#define LOOP_DELAY_MS 20
#define LOOP_PULSE_UPDATE_MS 30
#define PULSE_AUTO_DISABLE_CYCLES 3

// Setup-Timing
#define MQTT_CONNECT_TIMEOUT 5000
#define MQTT_INIT_DELAY 500
#define SETUP_HARDWARE_DELAY 200

// NTP
#define NTP_SERVER "pool.ntp.org"
#define NTP_GMT_OFFSET_SEC 3600
#define NTP_DAYLIGHT_OFFSET_SEC 3600

// System Health
#define SYSSTAT_FILE_ROTATION 24
#define STORAGE_WARNING_PERCENT 85
#define NIGHT_REBOOT_HOUR_START 2
#define NIGHT_REBOOT_HOUR_END 4
#define SCHEDULED_REBOOT_HOUR_START 3
#define SCHEDULED_REBOOT_HOUR_END 4
#define NIGHT_REBOOT_DELAY 60000
#define SCHEDULED_REBOOT_DELAY 30000

// Settings
#define SETTINGS_SAVE_DEBOUNCE_MS 2000
```

**Step 2 — extern const Definitionen aus moodlight.cpp entfernen:**
Alle `extern const` Zeilen (49, 66, 81, 84-92, 170) loeschen. Sie werden durch die `#define`-Werte aus config.h ersetzt.

WICHTIG: `SOFTWARE_VERSION` bleibt als `extern const String SOFTWARE_VERSION = MOODLIGHT_FULL_VERSION;` erhalten — String-Literale koennen nicht als `#define` in extern const String Kontexten verwendet werden. ABER: Pruefen ob die Module stattdessen direkt `MOODLIGHT_FULL_VERSION` nutzen koennen — dann kann SOFTWARE_VERSION ganz weg.

**Step 3 — extern-Deklarationen in Modul-.cpp-Dateien anpassen:**
In jedem Modul, das `extern const unsigned long X;` oder `extern const int X;` deklariert:
- Zeile loeschen (die Module inkludieren bereits `config.h` ueber `app_state.h` -> `config.h`, oder direkt)
- Falls ein Modul `config.h` nicht inkludiert: `#include "config.h"` hinzufuegen

Betroffene Dateien und Zeilen:
- `wifi_manager.cpp`: `extern const byte DNS_PORT;`, `extern const unsigned long MAX_RECONNECT_DELAY;`, `extern const unsigned long STATUS_LED_GRACE_MS;`, `extern const unsigned long AP_TIMEOUT;`
- `mqtt_handler.cpp`: `extern const unsigned long MQTT_HEARTBEAT_INTERVAL;`
- `web_server.cpp`: `extern const String SOFTWARE_VERSION;`, `extern const int LOG_BUFFER_SIZE;`, `extern const unsigned long REBOOT_DELAY;`, `extern const unsigned long STATUS_LOG_INTERVAL;`
- `sensor_manager.cpp`: `extern const unsigned long SENTIMENT_FALLBACK_TIMEOUT;`, `extern const int MAX_SENTIMENT_FAILURES;`

Fuer SOFTWARE_VERSION in web_server.cpp: Ersetze Verwendungen von `SOFTWARE_VERSION` durch `MOODLIGHT_FULL_VERSION` (ist ein Makro, funktioniert ueberall).

**Step 4 — Magic Numbers im Loop ersetzen:**
- Zeile 436: `20` -> `LOOP_SERVER_HANDLE_MS`
- Zeile 466: `2000` -> `LOOP_CONNECTION_CHECK_MS`
- Zeile 459: `100` -> `LOOP_MQTT_INTERVAL_MS`
- Zeile 480: `2000` -> `SETTINGS_SAVE_DEBOUNCE_MS`
- Zeile 511: `30` -> `LOOP_PULSE_UPDATE_MS`
- Zeile 516: `3` -> `PULSE_AUTO_DISABLE_CYCLES`
- Zeile 669: `20` -> `LOOP_DELAY_MS`
- Zeile 673: `300000` -> `HEALTH_CHECK_SHORT_INTERVAL`

**Step 5 — NTP-Konstanten in moodlight.cpp ersetzen:**
Zeilen 74-76 loeschen (`ntpServer`, `gmtOffset_sec`, `daylightOffset_sec`). Die Verwendung in `initTime()` (wifi_manager.cpp) muss auf die neuen `#define`-Werte umgestellt werden. Pruefen: Wie werden die NTP-Werte derzeit an `configTime()` uebergeben? Falls via extern — extern-Deklaration in wifi_manager.cpp loeschen und direkt `NTP_SERVER`, `NTP_GMT_OFFSET_SEC`, `NTP_DAYLIGHT_OFFSET_SEC` verwenden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Keine `extern const` Timing-Definitionen mehr in moodlight.cpp (ausser SOFTWARE_VERSION falls nicht eliminierbar)
    - Alle Timing-Konstanten als #define in config.h
    - Keine Magic Numbers in loop() — alle durch benannte Konstanten ersetzt
    - Keine `extern const` Deklarationen mehr in den Modul-.cpp-Dateien fuer die migrierten Konstanten
    - `pio run` baut fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: Dead Code und v9.0-Kommentarreste entfernen</name>
  <files>firmware/src/moodlight.cpp</files>
  <action>
**read_first:** firmware/src/moodlight.cpp (vollstaendig nach Task 1)

Folgende Dead-Code-Bloecke entfernen (per QUAL-01):

1. **v9.0-Kommentare** (reine Referenzen auf entfernte Features):
   - Zeile ~164: `// v9.0: CSVBuffer removed - stats from backend` + auskommentierte Zeile
   - Zeile ~168: `// v9.0: archiveTask removed - archiving handled in backend`
   - Zeile ~172-177: Gesamter Block `// ===== REMOVED IN v9.0: RSS Feed Configuration =====` bis `// ===== NEW IN v9.0: Backend Statistics API =====`
   - Zeile ~179-185: Gesamter Block `// initFS() -> verschoben nach web_server.cpp` und die Auflistung verschobener Funktionen
   - Zeile ~254: `// v9.0: CSV repair and archiving tasks removed`
   - Zeile ~299: `// v9.0: Archivierungsprozess removed`
   - Zeile ~333: `// REMOVED v9.0: RSS configuration now managed in backend` + Folgekommentar
   - Zeile ~448-449: `// Tägliche Archivierung alter Daten` + `// v9.0: Daily archive check removed`
   - Zeile ~654: `// v9.0: Archiving handled in backend, no local action needed`

2. **Migrationskommentare** (aus Phase 06):
   - Zeile ~50: `// appState.isInConfigMode -> appState.appState.isInConfigMode (migriert)`
   - Zeile ~65: `// logBuffer/logIndex -> appState.logBuffer/appState.logIndex (migriert)`
   - Zeile ~77: `// appState.timeInitialized -> appState.appState.timeInitialized (migriert)`
   - Zeile ~79: `// === Globale Variablen (migrierte Variablen sind in AppState appState) ===`
   - Zeile ~83: `// Konstanten bleiben als #define / const (nicht im AppState)`
   - Zeile ~169: `// appState.lastSystemHealthCheckTime -> appState.appState.lastSystemHealthCheckTime (migriert)`

3. **Versionskommentar im Header** (Zeilen 1-8): Aktualisieren auf aktuelle Version. Entferne veraltete v9.0-spezifische Changelog-Eintraege. Neuer Header:
```cpp
// ========================================================
// AuraOS Moodlight — Main Application
// ========================================================
// Orchestriert Module: WiFi, LED, Web-Server, MQTT, Settings, Sensor
// Zentrale setup()/loop() mit Modul-Initialisierung
```

4. **Zeile ~30**: `// #define CONFIG_FREERTOS_UNICORE` (auskommentiert, nie benutzt) -> entfernen
5. **Zeile ~40**: Kommentar `// Zentrale AppState-Instanz — alle geteilten Globals migrieren hierher (Plan 02)` -> Vereinfachen zu `// Zentrale AppState-Instanz`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Keine v9.0-Referenz-Kommentare mehr in moodlight.cpp
    - Keine Phase-06-Migrationskommentare mehr
    - Kein auskommentierter Code
    - Header-Kommentar aktualisiert
    - `pio run` baut fehlerfrei
  </done>
</task>

</tasks>

<verification>
- `pio run` baut fehlerfrei
- `grep -c "extern const" firmware/src/moodlight.cpp` liefert 0 oder 1 (nur SOFTWARE_VERSION falls noetig)
- `grep -c "v9.0" firmware/src/moodlight.cpp` liefert 0
- `grep -c "migriert" firmware/src/moodlight.cpp` liefert 0
- Alle Timing-Werte in config.h: `grep -c "#define.*INTERVAL\|#define.*TIMEOUT\|#define.*DELAY\|#define.*MS" firmware/src/config.h` > 10
</verification>

<success_criteria>
Magic Numbers sind zentralisiert, toter Code ist entfernt, Build ist gruen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-konsolidierung-qualitaet/08-01-SUMMARY.md`
</output>
