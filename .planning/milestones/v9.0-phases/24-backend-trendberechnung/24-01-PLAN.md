---
phase: 24-backend-trendberechnung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentiment-api/database.py
  - sentiment-api/moodlight_extensions.py
autonomous: true
requirements:
  - TREND-01
  - TREND-02

must_haves:
  truths:
    - "GET /api/moodlight/feeds/trends?days=7 liefert HTTP 200 mit JSON-Array"
    - "Jeder Feed-Eintrag hat feed_name, avg_score, headline_count, min_score, max_score"
    - "Die Liste ist absteigend nach avg_score sortiert (positivster Feed zuerst)"
    - "days=30 liefert ein anderes (größeres) Zeitfenster als days=7"
    - "Feeds ohne Headlines im Zeitfenster werden ausgeschlossen"
  artifacts:
    - path: "sentiment-api/database.py"
      provides: "Methode get_feed_trends(days: int) auf Database-Klasse"
      contains: "def get_feed_trends"
    - path: "sentiment-api/moodlight_extensions.py"
      provides: "GET /api/moodlight/feeds/trends Endpoint"
      contains: "feeds/trends"
  key_links:
    - from: "sentiment-api/moodlight_extensions.py"
      to: "sentiment-api/database.py"
      via: "db.get_feed_trends(days)"
      pattern: "get_feed_trends"
    - from: "sentiment-api/database.py"
      to: "headlines JOIN feeds"
      via: "GROUP BY h.feed_id"
      pattern: "GROUP BY h\\.feed_id"
---

<objective>
Durchschnitts-Score pro Feed für konfigurierbare Zeitfenster (7 Tage, 30 Tage) berechnen und als sortiertes Feed-Ranking per GET /api/moodlight/feeds/trends liefern.

Purpose: Phase 25 (Visualisierung) und die GitHub Page brauchen diesen Endpoint als Datenquelle. Ohne ihn kann kein Feed-Ranking dargestellt werden.
Output: Neue DB-Methode get_feed_trends() + öffentlicher Endpoint /api/moodlight/feeds/trends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Verträge — direkt nutzbar, kein Nachschlagen nötig -->

Aus sentiment-api/database.py:

```python
class Database:
    def get_cursor(self, cursor_factory=None):
        """Context Manager — nutze immer with self.get_cursor(cursor_factory=RealDictCursor) as cur:"""

    def get_active_feeds(self) -> List[Dict[str, Any]]:
        """Gibt Feeds mit 'id', 'name', 'url' zurück"""
```

Schema aus init.sql:
```sql
-- headlines-Tabelle
headlines (
    id, sentiment_history_id, feed_id INTEGER REFERENCES feeds(id) ON DELETE SET NULL,
    headline, source_name, sentiment_score FLOAT, strength, link, analyzed_at TIMESTAMPTZ
)
-- feeds-Tabelle
feeds (id, name, url, active BOOLEAN, ...)
```

Indizes vorhanden:
- idx_headlines_feed ON headlines(feed_id)
- idx_headlines_analyzed_at ON headlines(analyzed_at DESC)

Aus sentiment-api/moodlight_extensions.py:

```python
def register_moodlight_endpoints(app):
    # Alle neuen Endpoints werden als innere Funktionen hier registriert
    # Beispiel-Pattern aus get_moodlight_feeds():
    @app.route('/api/moodlight/feeds', methods=['GET'])
    def get_moodlight_feeds():
        try:
            db = get_database()
            # ...
            return jsonify({"status": "success", "count": len(feeds), "feeds": feeds})
        except Exception as e:
            logger.error(f"Fehler in GET /api/moodlight/feeds: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
```

Import-Block oben in moodlight_extensions.py:
```python
from database import get_database, get_cache, compute_led_index
from flask import jsonify, request, session
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: get_feed_trends() in Database-Klasse</name>
  <files>sentiment-api/database.py</files>

  <read_first>
    sentiment-api/database.py — ab Zeile 604 (get_score_percentiles als Orientierung für SQL-Stil und Fehlerbehandlung)
  </read_first>

  <action>
Neue Methode get_feed_trends() an die Database-Klasse anhängen — direkt nach get_score_percentiles() (Zeile 659) einfügen, vor check_connection_health().

```python
def get_feed_trends(self, days: int = 7) -> List[Dict[str, Any]]:
    """
    Berechnet Durchschnitts-Score pro Feed für das angegebene Zeitfenster.

    Args:
        days: Zeitfenster in Tagen (typisch 7 oder 30)

    Returns:
        Liste von Dicts, absteigend sortiert nach avg_score.
        Jeder Eintrag: feed_id, feed_name, avg_score, headline_count, min_score, max_score.
        Feeds ohne Headlines im Zeitfenster werden ausgeschlossen.
        Leere Liste bei Fehler.
    """
    query = """
        SELECT
            f.id          AS feed_id,
            f.name        AS feed_name,
            ROUND(AVG(h.sentiment_score)::numeric, 4) AS avg_score,
            COUNT(h.id)   AS headline_count,
            ROUND(MIN(h.sentiment_score)::numeric, 4) AS min_score,
            ROUND(MAX(h.sentiment_score)::numeric, 4) AS max_score
        FROM headlines h
        JOIN feeds f ON h.feed_id = f.id
        WHERE h.analyzed_at >= NOW() - INTERVAL '%s days'
          AND f.active = TRUE
        GROUP BY f.id, f.name
        HAVING COUNT(h.id) > 0
        ORDER BY avg_score DESC;
    """
    try:
        with self.get_cursor(cursor_factory=RealDictCursor) as cur:
            cur.execute(query, (days,))
            results = cur.fetchall()
            return [
                {
                    "feed_id": row["feed_id"],
                    "feed_name": row["feed_name"],
                    "avg_score": float(row["avg_score"]),
                    "headline_count": int(row["headline_count"]),
                    "min_score": float(row["min_score"]),
                    "max_score": float(row["max_score"]),
                }
                for row in results
            ]
    except Exception as e:
        logger.error(f"Fehler bei get_feed_trends(days={days}): {e}")
        return []
```

Hinweis zum INTERVAL-Parametrisierung: psycopg2 setzt `%s` im Query korrekt als Integer ein — das Pattern `INTERVAL '%s days'` funktioniert genauso wie in get_score_percentiles() (Zeile 633). Keine Änderung am Import-Block nötig (RealDictCursor ist bereits importiert).
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "import ast, sys; ast.parse(open('database.py').read()); print('Syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Methode get_feed_trends() existiert in der Database-Klasse
    - SQL GROUP BY f.id, f.name mit AVG, COUNT, MIN, MAX auf headlines JOIN feeds
    - WHERE-Klausel filtert auf analyzed_at >= NOW() - INTERVAL '%s days'
    - Rückgabe ist eine Liste von Dicts mit den Feldern: feed_id, feed_name, avg_score, headline_count, min_score, max_score
    - Bei Datenbankfehler wird [] zurückgegeben (kein raise)
    - Python-Syntax fehlerfrei (ast.parse-Check grün)
  </acceptance_criteria>

  <done>database.py enthält get_feed_trends(), Syntax-Check besteht</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GET /api/moodlight/feeds/trends in moodlight_extensions.py</name>
  <files>sentiment-api/moodlight_extensions.py</files>

  <read_first>
    sentiment-api/moodlight_extensions.py — Zeilen 404–432 (get_moodlight_feeds als direktes Stilmuster für den neuen Endpoint)
  </read_first>

  <action>
Neuen Endpoint als innere Funktion in register_moodlight_endpoints() einfügen — direkt vor dem abschließenden `logger.info("Moodlight API-Endpunkte registriert")` (letzte Zeile der Funktion, aktuell Zeile 697).

```python
    # ===== FEED-TRENDS ENDPOINT (TREND-01, TREND-02) =====
    @app.route('/api/moodlight/feeds/trends', methods=['GET'])
    def get_moodlight_feed_trends():
        """
        Durchschnitts-Score pro Feed für ein Zeitfenster.

        Query-Parameter:
            days: Zeitfenster in Tagen (default: 7, erlaubt: 7, 30)

        Returns:
            JSON mit status, days, count, feeds (sortiert nach avg_score DESC).
        """
        try:
            days_param = request.args.get('days', 7, type=int)
            if days_param not in (7, 30):
                days_param = 7

            db = get_database()
            feeds = db.get_feed_trends(days=days_param)

            return jsonify({
                "status": "success",
                "days": days_param,
                "count": len(feeds),
                "feeds": feeds
            })

        except Exception as e:
            logger.error(f"Fehler in GET /api/moodlight/feeds/trends: {e}", exc_info=True)
            return jsonify({"status": "error", "message": "Interner Serverfehler"}), 500
```

Kein Auth-Decorator — der Endpoint ist öffentlich (GitHub Page braucht ihn ohne Login). Kein Redis-Cache nötig für v9.0 (Daten ändern sich alle 30 Min, Latenz tolerierbar).
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api && python -c "import ast, sys; ast.parse(open('moodlight_extensions.py').read()); print('Syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Route /api/moodlight/feeds/trends mit GET registriert (innerhalb register_moodlight_endpoints)
    - days-Parameter wird eingelesen; ungültige Werte fallen auf 7 zurück (nur 7 und 30 erlaubt)
    - Ruft db.get_feed_trends(days=days_param) auf
    - Antwort-JSON hat die Felder: status, days, count, feeds
    - Kein @api_login_required Decorator
    - Python-Syntax fehlerfrei (ast.parse-Check grün)
  </acceptance_criteria>

  <done>moodlight_extensions.py enthält den Endpoint, Syntax-Check besteht</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

```bash
# Syntax beider geänderter Dateien prüfen
cd /Users/simonluthe/Documents/auraos-moodlight/sentiment-api
python -c "import ast; ast.parse(open('database.py').read()); ast.parse(open('moodlight_extensions.py').read()); print('Beide Dateien: Syntax OK')"

# Methode und Endpoint vorhanden
grep -n "def get_feed_trends" database.py
grep -n "feeds/trends" moodlight_extensions.py
```
</verification>

<success_criteria>
- database.py: Methode get_feed_trends(days=7) vorhanden, SQL mit GROUP BY feed_id, korrekte Fehlerbehandlung
- moodlight_extensions.py: Route /api/moodlight/feeds/trends registriert, öffentlich, days-Validierung auf 7/30
- Beide Dateien bestehen den Python-Syntax-Check
- TREND-01 erfüllt: Backend kann Durchschnitt pro Feed für 7/30-Tage-Fenster berechnen
- TREND-02 erfüllt: Endpoint /api/moodlight/feeds/trends liefert sortiertes Feed-Ranking
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen unter:
`.planning/phases/24-backend-trendberechnung/24-01-SUMMARY.md`
</output>
