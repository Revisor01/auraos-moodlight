---
status: complete
---

# Summary: Code-Review-Fixes (MQTT-Doppel-Send, Firmware/Backend/UI)

Alle drei Task-Blöcke aus dem Plan wurden vollständig umgesetzt: Firmware (Block A, Findings A1–A11), Backend (Block B, Findings B1–B8), Web-UI + CLAUDE.md (Block C, Findings C1–C10). Alle geforderten Testkommandos sind grün. Drei atomare Commits, einer pro Block.

## Commits

| Task | Commit | Beschreibung |
|------|--------|--------------|
| 1 (Firmware) | `3618b64` | fix(firmware): MQTT-Doppel-Send, Eingabevalidierung und Fallback-Logik korrigieren |
| 2 (Backend) | `b709a8a` | fix(api): Fehlerbehandlung, DB-Connection-Pool und Cache-Invalidierung korrigieren |
| 3 (UI/Doku) | `c7c4139` | fix(ui): Web-Interface-Fehler beheben und Projektdokumentation aktualisieren |

## Umgesetzte Findings

**Block A (Firmware):** A1, A2, A3, A4, A5, A6, A7, A8, A9, A10 (alle Unterpunkte), A11 (alle Unterpunkte) — vollständig umgesetzt. Keine Findings ausgelassen.

**Block B (Backend):** B1, B2, B3, B4, B5, B6, B7, B8 (alle Unterpunkte) — vollständig umgesetzt. Keine Findings ausgelassen.

**Block C (Web-UI/Doku):** C1, C2, C3, C4, C5, C6, C7, C8 (alle Unterpunkte), C9 (alle Unterpunkte), C10 — vollständig umgesetzt. Keine Findings ausgelassen.

### Ausgelassene Findings

Keine. Beide bedingten Findings (A11 Totcode-Entfernung, C8 Totcode-Entfernung) wurden vor jeder Löschung per grep über den jeweiligen Quellbaum (`firmware/src/` bzw. `firmware/data/`) auf Unreferenziertheit geprüft — alle geplanten Entfernungen waren tatsächlich unbenutzt und wurden vollständig entfernt.

## Firmware-Details (Task 1)

- **A1 (Hauptbefund):** `appState.mqttWasConnected = true` fehlte im Erfolgsfall von `connectMQTTOnStartup()` — dadurch löste `checkAndReconnectMQTT()` ~2s später ein zweites `sendInitialStates()` aus. Zusätzlich `setRetain(true)` auf `haLight`, `haMode`, `haUpdateInterval`, `haDhtInterval` entfernt (Broker hätte sonst alte HA-Befehle bei jedem Reconnect erneut zugestellt).
- **A2:** `numLeds` (constrain 1–MAX_LEDS), `ledPin`/`dhtPin` (0–39, GPIO 6–11 gesperrt) serverseitig validiert.
- **A3:** Factory-Reset löscht jetzt zusätzlich `/data/settings.json` und `.bak`.
- **A4/A5:** `handleSentiment()` erhält jetzt die API-Kategorie als Parameter statt sie vor dem Aufruf in `appState.sentimentCategory` zu schreiben (Vergleich lief sonst immer gegen den bereits überschriebenen Wert). Erfolgs-Bookkeeping (`sentimentAPIAvailable`, `consecutiveSentimentFailures`, `lastSuccessfulSentimentUpdate`) aus `handleSentiment()` entfernt, da es fälschlich auch aus den Fehlerpfaden aufgerufen wurde und sich selbst aufhob.
- **A6:** `SystemHealthCheck` unterscheidet jetzt zwischen `_uptimeHours` (reine NVS-Statistik) und `_bootUptimeHours` (aus `millis()` abgeleitet, steuert `isRestartRecommended()`) — behebt die nächtliche Reboot-Schleife nach 30 Tagen kumulierter Laufzeit.
- **A7:** `sprintf` → `snprintf` mit Puffergröße + `& 0xFFFFFF`-Maskierung an allen Farbformatierungsstellen; `sscanf`-Rückgabewert geprüft, führendes `#` übersprungen.
- **A8:** `dhtEnabled` akzeptiert jetzt `bool`/`int`/`float` (ArduinoJson-7-kompatibel).
- **A9:** OTA prüft Magic-Byte `0xE9` im ersten Chunk, `Update.hasError()` liefert jetzt HTTP 500.
- **A10:** Sekunden-Clamping vor `*1000`-Multiplikation, Division-durch-0-Guards, `/logs` als `text/plain`, doppelter `onNotFound`-Handler zusammengeführt (ohne Pfad-Sperrliste), unerreichbarer Code in `fetchBackendStatistics()` entfernt, Preferences-Fallback lädt jetzt symmetrisch zu `saveSettings()`, `saveSettingsToFile()`-Rückgabewert geprüft, MemoryMonitor-NVS-Write auf 10 min/>1KB gedrosselt.
- **A11:** `CSVBuffer`, `TaskManager`, `MoodlightUtils::formatString/safeDelay/getTimestamp/randomString`, `SafeFileOps::moveFile/removeDir`, `startAPMode()`, tote v9.0-Endpoints (`/api/stats/delete`, `/api/stats/reset`, `/api/repair/stats`) entfernt — alle per grep als unreferenziert bestätigt.
- **C9 (Firmware-Anteil):** `/diagnostics`-Route und Backup/Restore-Referenzen auf `diagnostics.html` entfernt.

**Flash-Größe:** 1.333.657 Bytes (81,4%) vorher → 1.282.465 Bytes (78,3%) nachher — Ersparnis ca. 51 KB.

## Backend-Details (Task 2)

- **B1:** `import requests` in `app.py` ergänzt.
- **B2 (Ursache der 30-Tage-Nullserie):** `analyze_sentiment_claude()`/`analyze_headlines_batch()` geben bei API-Fehler oder Teilparse jetzt `None` statt `[0.0]*n` zurück. `background_worker._perform_update()` überspringt den Zyklus dann komplett ohne DB-Write und loggt WARN. `APIStatusError` loggt jetzt zusätzlich den Response-Body. `/api/news` gibt bei `None`-Ergebnis HTTP 502 mit Fehlermeldung statt eines Fake-Erfolgs.
- **B3:** `CACHE_KEY_CURRENT`/`CACHE_KEY_CURRENT_LEGACY` in `shared_config.py` zentralisiert; `background_worker.py` löschte vorher nur den falschen Legacy-Key — jetzt werden beide Keys gelöscht.
- **B4 (wichtigster Fix):** `get_cursor()` ist jetzt ein Context-Manager, der pro Aufruf `pool.getconn()`/`putconn()` nutzt, mit automatischem `commit()`/`rollback()`. `self.conn` als geteilte Instanzverbindung vollständig entfernt (war zuvor zwischen allen gunicorn-Threads und dem Background-Worker-Thread geteilt).
- **B5:** `threading.Event` statt `time.sleep()` im Worker-Loop; `threading.Lock` zwischen `trigger()` und periodischem Update verhindert parallele Analysen.
- **B6:** `max_tokens` auf `max(1024, headlines*25)` erhöht, `temperature` von 1.0 auf 0.
- **B7:** Docker-Compose-Healthcheck + `depends_on: condition: service_healthy`, Bind-Mount entfernt, `.dockerignore` neu, `build-essential` durch `curl` ersetzt (für Healthcheck).
- **B8:** History-Limit auf 50000 gedeckelt, naive `datetime` → `timezone.utc`, `get_database()` weist Singleton erst nach erfolgreichem `connect()` zu.

**Neuer Test:** `sentiment-api/tests/test_background_worker_error_handling.py` (3 Tests: None-Ergebnis → kein DB-Write, fehlendes `total_sentiment`-Feld → kein DB-Write, Erfolgsfall → DB-Write erfolgt).

## Web-UI-Details (Task 3)

- **C1:** `resetWiFi()` implementiert.
- **C2:** `currentHours` in `mood.js` verfolgt den gewählten Zeitraum; Auto-Refresh nutzt ihn.
- **C3:** In-Flight-Guard, Intervall 2000ms → 5000ms, Polling nur bei vorhandenem `#leds`-Element (Dashboard).
- **C4:** `.catch()` an allen vier Steuerfunktionen mit Status-Rollback via `refreshStatus()`.
- **C5:** Upload-Abbruch nur als Erfolg gewertet, wenn Fortschritt vorher 100% erreicht hatte.
- **C6:** `renderHeadlines()` nutzt DOM-Erzeugung mit `textContent` statt `innerHTML`.
- **C7:** Dark-Mode-Selektor korrigiert (`.dark` statt `[data-theme="dark"]`).
- **C8:** ~28 KB toter Code entfernt (siehe Commit-Body für Details).
- **C9:** Favicon-Pfad korrigiert, `LED_COLORS` lädt von `/api/settings/colors` (Fallback erhalten).
- **C10:** CLAUDE.md auf tatsächlichen Stand gebracht (modulare Firmware, Anthropic, gunicorn, GHCR/Portainer).

## Testergebnisse

| Kommando | Ergebnis |
|----------|----------|
| `cd firmware && pio run` | SUCCESS — Flash 1.282.465/1.638.400 Bytes (78,3%), RAM 95.316/327.680 Bytes (29,1%) |
| `cd sentiment-api && python3 -m py_compile app.py database.py background_worker.py moodlight_extensions.py shared_config.py` | Fehlerfrei |
| `cd sentiment-api && python3 -m pytest tests/ -q` | 23 passed (20 bestehend + 3 neu) |
| `node --check firmware/data/js/setup.js` | OK |
| `node --check firmware/data/js/mood.js` | OK |
| `node --check firmware/data/js/script.js` | OK |
| Inline `<script>` in `mood.html`/`setup.html` (zusätzlich geprüft) | OK |

## Nutzer-Vorgaben eingehalten

- Keine Auth-/Passwort-/CSRF-Änderungen (einzige Ausnahme: OTA-Magic-Byte-Check A9, wie vorgegeben).
- Kein Deployment, kein SSH, kein `git push` durchgeführt.
- Umlaute in allen neuen/geänderten Texten korrekt (öäüß).
- Bei `onNotFound` (A10) keine Pfad-Sperrliste eingebaut.

## Bekannte Nebenbefunde (nicht Teil dieses Durchgangs, wie in FINDINGS.md vereinbart)

- 1438 Null-Zeilen in der Produktions-DB bleiben unverändert (heilen sich über das 7-Tage-Fenster selbst; Nutzer kauft Anthropic-Guthaben).
- `/testapi`-SSRF, CDN-Abhängigkeiten (Chart.js/moment.js/Font-Awesome), Chart.js-Adapter, DB-Trigger-vs-Python-Kategorien-Dedup, `INTERVAL '%s hours'`-Umbau — bewusst nicht angefasst.

## Self-Check: PASSED

Alle referenzierten Dateien (firmware/src/mqtt_handler.cpp, sentiment-api/shared_config.py, sentiment-api/.dockerignore, sentiment-api/tests/test_background_worker_error_handling.py, firmware/data/js/setup.js, CLAUDE.md) existieren auf der Festplatte. Alle drei Commit-Hashes (3618b64, b709a8a, c7c4139) sind in der Git-Historie vorhanden.
