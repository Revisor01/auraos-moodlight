---
phase: quick-260729-h73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/mqtt_handler.cpp
  - firmware/src/web_server.cpp
  - firmware/src/sensor_manager.cpp
  - firmware/src/sensor_manager.h
  - firmware/src/settings_manager.cpp
  - firmware/src/wifi_manager.cpp
  - firmware/src/wifi_manager.h
  - firmware/src/MoodlightUtils.cpp
  - firmware/src/MoodlightUtils.h
  - sentiment-api/app.py
  - sentiment-api/database.py
  - sentiment-api/background_worker.py
  - sentiment-api/moodlight_extensions.py
  - sentiment-api/shared_config.py
  - sentiment-api/docker-compose.yaml
  - sentiment-api/Dockerfile
  - sentiment-api/.dockerignore
  - firmware/data/setup.html
  - firmware/data/mood.html
  - firmware/data/index.html
  - firmware/data/js/setup.js
  - firmware/data/js/mood.js
  - firmware/data/js/script.js
  - CLAUDE.md
autonomous: true
requirements: [A1-A11, B1-B8, C1-C10]

must_haves:
  truths:
    - "Beim Boot sendet das Gerät die initialen MQTT-Zustände genau einmal, nicht zweimal"
    - "Ungültige numLeds/Pin-Werte über die Web-API führen nicht mehr zu Out-of-Bounds-Writes"
    - "Factory-Reset löscht auch /data/settings.json, sodass alte Einstellungen nicht wiederkehren"
    - "Ein fehlgeschlagener Anthropic-Call speichert keinen Sentiment-Wert 0.0 mehr in der Datenbank"
    - "Jeder DB-Zugriff nutzt eine eigene Pool-Connection mit commit/rollback und wird zurückgegeben"
    - "Cache-Invalidierung im Worker trifft den tatsächlich genutzten Redis-Key"
    - "Der WLAN-Zurücksetzen-Button in setup.html funktioniert"
    - "Auto-Refresh in mood.js behält den gewählten Zeitraum bei"
    - "CLAUDE.md beschreibt den tatsächlichen Stand (modulare Firmware, Anthropic, gunicorn, GHCR/Portainer)"
  artifacts:
    - path: "firmware/src/mqtt_handler.cpp"
      provides: "MQTT-Startup ohne Doppel-Send, ohne setRetain auf Command-Topics"
      contains: "mqttWasConnected = true"
    - path: "sentiment-api/shared_config.py"
      provides: "Zentrale Redis-Key-Konstanten"
    - path: "sentiment-api/.dockerignore"
      provides: "Ausschluss von .env, __pycache__, tests, *.sh aus dem Image"
    - path: "CLAUDE.md"
      provides: "Aktualisierte Projektbeschreibung"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "firmware/src/mqtt_handler.cpp::connectMQTTOnStartup"
      to: "appState.mqttWasConnected"
      via: "Statusflag beim Erfolgs-Connect setzen"
      pattern: "mqttWasConnected\\s*=\\s*true"
    - from: "sentiment-api/background_worker.py::_perform_update"
      to: "sentiment-api/shared_config.py"
      via: "Import der Cache-Key-Konstanten"
      pattern: "from shared_config import"
    - from: "firmware/data/js/setup.js::resetWiFi"
      to: "/resetwifi"
      via: "fetch POST"
      pattern: "resetwifi"
---

<objective>
Umsetzung aller im Code-Review verifizierten Findings aus `260729-h73-FINDINGS.md` in drei atomaren Blöcken: Firmware (C++), Backend (Python), Web-UI + Dokumentation.

Purpose: Der vom Nutzer gemeldete MQTT-Doppel-Send beim Boot, die 30-Tage-Nullserie im Sentiment-Backend und eine Reihe belegter Korrektheits-, Robustheits- und Totcode-Probleme werden behoben. CLAUDE.md wird auf den tatsächlichen Architekturstand gebracht.

Output: Drei Commits (Firmware, Backend, UI/Doku), jeweils mit grünem Testkommando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260729-h73-code-review-fixes-mqtt-doppel-send-firmw/260729-h73-FINDINGS.md

**FINDINGS.md ist die verbindliche Detailquelle.** Jede Finding-ID (A1–A11, B1–B8, C1–C10) enthält Datei, Zeilennummer, Belegtext und die konkrete Fix-Anweisung. Die Tasks unten referenzieren die IDs; die Details werden dort NICHT dupliziert. Vor jedem Task den zugehörigen Block in FINDINGS.md lesen.

@CLAUDE.md
</context>

<user_constraints>
Verbindliche Nutzer-Vorgaben aus FINDINGS.md — bei jedem Task einhalten:

1. **KEINE Auth-/Passwort-/CSRF-Fixes.** Klartext-Passwörter in `/data/settings.json`, Zugriffsschutz auf LittleFS-Pfade, Auth/CSRF der Geräte-API und OTA-Auth-Token bleiben unangetastet (Gerät nur im Heimnetz). Einzige Ausnahme: der Magic-Byte-Check `0xE9` beim OTA (A9) — reine Bricking-Vorsorge, kein Auth. Bei `onNotFound` (A10) KEINE Pfad-Sperrliste einbauen.
2. **Tests sind Pflicht.** Kein Task gilt als fertig ohne grünes Testkommando (siehe `<verify>`).
3. **Kein Deployment durch Claude.** Nur Code und Commits. Kein SSH, kein OTA-Upload, kein Portainer-Trigger, kein `git push`.
4. **Umlaute korrekt** (öäüß) in allen Texten, Kommentaren, Log-Meldungen, UI-Strings und Commit-Messages — nie oe/ae/ue/ss.
5. **Ein Commit pro Task**, Conventional-Commit-Stil wie im Repo: `fix(firmware): …`, `fix(api): …`, `fix(ui): …`.
6. **Nicht in diesem Durchgang:** Löschen der 1438 Null-Zeilen in der Produktions-DB, `/testapi`-SSRF, CDN-Abhängigkeiten, Chart.js-Adapter, DB-Trigger-Dedup, `INTERVAL '%s hours'`-Umbau.
</user_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Firmware-Fixes (Block A)</name>
  <files>firmware/src/mqtt_handler.cpp, firmware/src/web_server.cpp, firmware/src/sensor_manager.cpp, firmware/src/sensor_manager.h, firmware/src/settings_manager.cpp, firmware/src/wifi_manager.cpp, firmware/src/wifi_manager.h, firmware/src/MoodlightUtils.cpp, firmware/src/MoodlightUtils.h</files>
  <action>
Findings A1 bis A11 aus FINDINGS.md umsetzen. Reihenfolge: erst die Verhaltensfehler (A1–A9), dann Kleinkorrekturen (A10), zuletzt Totcode-Entfernung (A11).

Vor dem Start Flash-Größe des aktuellen Builds notieren (`cd firmware && pio run`, Ausgabezeile "Flash:"), um die Ersparnis aus A11 zu belegen.

Hinweise zur Ausführung:
- A1 ist der vom Nutzer gemeldete Hauptbefund: sowohl das fehlende `appState.mqttWasConnected = true` in `connectMQTTOnStartup()` als auch die vier `setRetain(true)`-Aufrufe entfernen — beides sind Quellen des Doppel-Sends. Die irreführenden Kommentare aus A1 mitkorrigieren.
- A5 (Neutral-Fallback) und A4 (Kategorie-Sensor) betreffen beide `sensor_manager.cpp` und interagieren: nach A5 darf das Erfolgs-Bookkeeping nur noch im Success-Zweig von `getSentiment()` stehen. Beide Findings gemeinsam betrachten, bevor editiert wird.
- A6: Boot-Zeitpunkt merken und Laufzeit aus `millis()/3600000UL` ableiten; der NVS-Wert bleibt als reine Statistik erhalten und darf `isRestartRecommended()` nicht mehr steuern.
- A11 ist bedingt: JEDE Entfernung vorher per grep über `firmware/src/` (und für die v9.0-Endpoints zusätzlich über `firmware/data/`) als unreferenziert nachweisen. Wenn grep eine Referenz findet, die Entfernung auslassen und im Commit-Body vermerken. Zugehörige Includes, Instanzen und Header-Deklarationen mit entfernen.
- Ebenfalls hier erledigen (in FINDINGS.md unter C9 gelistet, technisch Firmware): Route `/diagnostics` in `web_server.cpp` sowie die Backup/Restore-Referenzen auf `diagnostics.html` entfernen.
- Nutzer-Vorgabe 1 beachten: kein Auth, keine Pfad-Sperrliste in `onNotFound`.
- Alle deutschen Log- und Fehlertexte mit korrekten Umlauten.

Abschluss: atomarer Commit `fix(firmware): MQTT-Doppel-Send, Eingabevalidierung und Fallback-Logik korrigieren` mit Verweis auf die umgesetzten Finding-IDs im Body.
  </action>
  <verify>
    <automated>cd firmware && pio run</automated>
  </verify>
  <done>
Build läuft fehlerfrei durch. Flash-Größe vorher/nachher im Commit-Body dokumentiert. `grep -n "mqttWasConnected = true" firmware/src/mqtt_handler.cpp` liefert einen Treffer in `connectMQTTOnStartup()`. `grep -c "setRetain(true)" firmware/src/mqtt_handler.cpp` zeigt keine Treffer mehr auf `haLight`, `haMode`, `haUpdateInterval`, `haDhtInterval`. Genau ein Commit erstellt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend-Fixes (Block B)</name>
  <files>sentiment-api/app.py, sentiment-api/database.py, sentiment-api/background_worker.py, sentiment-api/moodlight_extensions.py, sentiment-api/shared_config.py, sentiment-api/docker-compose.yaml, sentiment-api/Dockerfile, sentiment-api/.dockerignore, sentiment-api/tests/</files>
  <action>
Findings B1 bis B8 aus FINDINGS.md umsetzen. Reihenfolge: B1 (Import), B2 (Fehler-Semantik), B4 (DB-Pool, aufwändigster Fix), B3 (Cache-Key), B5–B8.

Hinweise zur Ausführung:
- B4 ist laut FINDINGS.md der wichtigste Backend-Fix und berührt alle Aufrufstellen: `get_cursor` zum Context-Manager mit `pool.getconn()` / `commit` / `rollback` / `putconn` umbauen, `self.conn` vollständig entfernen, `_ensure_connection` entsprechend vereinfachen. Danach systematisch `app.py`, `moodlight_extensions.py` und `background_worker.py` nach verbliebenen Aufrufstellen durchsuchen und migrieren.
- B2 ist die Ursache der 30-Tage-Nullserie: der Fehlerfall muss vom Aufrufer unterscheidbar sein (None oder Exception), `_perform_update` überspringt den Zyklus ohne DB-Write und loggt WARN. Beim `APIStatusError` zusätzlich den Response-Body loggen. Auch der Teilparse-Fall (`parsed_count != len(batch)`) verwirft den Zyklus.
- B3: Key-Konstanten in `shared_config.py` zentralisieren und von Worker und `moodlight_extensions.py` importieren, damit der Drift nicht wiederkehrt.
- Neuer Test für B2 anlegen (FINDINGS.md fordert ihn explizit): Anthropic-Fehler → kein DB-Write. Bestehende Tests dürfen nicht brechen.
- B7 betrifft ausschließlich Repo-Dateien (`docker-compose.yaml`, `Dockerfile`, neue `.dockerignore`). Nutzer-Vorgabe 3: kein Server-Eingriff, kein Deployment.
- Deutsche Log-/Docstring-Texte mit korrekten Umlauten.

Abschluss: atomarer Commit `fix(api): Fehlerbehandlung, DB-Connection-Pool und Cache-Invalidierung korrigieren` mit Verweis auf die umgesetzten Finding-IDs im Body.
  </action>
  <verify>
    <automated>cd sentiment-api && python3 -m py_compile app.py database.py background_worker.py moodlight_extensions.py shared_config.py && python3 -m pytest tests/ -q</automated>
  </verify>
  <done>
Alle Tests grün, inklusive des neuen B2-Tests (API-Fehler → kein DB-Write). `py_compile` fehlerfrei für alle fünf Module. `grep -rn "self.conn" sentiment-api/database.py` liefert keine Treffer mehr im Cursor-Pfad. `sentiment-api/.dockerignore` existiert mit `.env`, `__pycache__/`, `tests/`, `*.sh`. Genau ein Commit erstellt.
  </done>
</task>

<task type="auto">
  <name>Task 3: Web-UI-Fixes und CLAUDE.md (Block C)</name>
  <files>firmware/data/index.html, firmware/data/setup.html, firmware/data/mood.html, firmware/data/js/setup.js, firmware/data/js/mood.js, firmware/data/js/script.js, CLAUDE.md</files>
  <action>
Findings C1 bis C10 aus FINDINGS.md umsetzen. Reihenfolge: erst Funktionsfehler (C1–C7), dann Totcode (C8), dann Kleinigkeiten (C9), zuletzt CLAUDE.md (C10).

Hinweise zur Ausführung:
- C8 ist bedingt: vor JEDER Löschung per grep über alle Dateien in `firmware/data/` nachweisen, dass die Funktion nirgends referenziert wird. Bei Treffer die Löschung auslassen und im Commit-Body vermerken.
- C3: den DOM-Guard an der tatsächlichen Struktur der Seiten ausrichten, nicht raten — `index.html` und `mood.html` vorher vergleichen, um ein Element zu finden, das nur auf dem Dashboard existiert.
- C9 (LED_COLORS): Farben von `/api/settings/colors` laden, bisherige Werte als Fallback behalten. Die hartkodierte Backend-URL nur anfassen, wenn ein Geräte-Proxy-Endpoint bereits existiert — sonst unverändert lassen.
- Die `/diagnostics`-Route und die diagnostics.html-Referenzen in `web_server.cpp` gehören zu Task 1, hier nicht erneut anfassen.
- C10: CLAUDE.md korrigieren, NICHT löschen. Die Abschnitte „GSD Workflow Enforcement" und „Developer Profile" bleiben unverändert. Zu korrigieren: modulare Firmware-Struktur mit Quellpfad `firmware/src/` und Version 9.11, Anthropic Claude Haiku (`claude-haiku-4-5-20251001`) statt OpenAI, gunicorn statt `python app.py`, Deployment über GitHub Actions → GHCR → Portainer (Container `moodlight-analyzer`) statt des nicht mehr existierenden SSH-Pull-Workflows nach `/opt/auraos-moodlight/sentiment-api/`, Env-Var `ANTHROPIC_API_KEY`, kein `diagnostics.html` mehr (OTA über Update-Tab in setup.html).
- Alle UI-Texte und Fehlermeldungen auf Deutsch mit korrekten Umlauten.

Abschluss: atomarer Commit `fix(ui): Web-Interface-Fehler beheben und Projektdokumentation aktualisieren` mit Verweis auf die umgesetzten Finding-IDs im Body.
  </action>
  <verify>
    <automated>node --check firmware/data/js/setup.js && node --check firmware/data/js/mood.js && node --check firmware/data/js/script.js</automated>
  </verify>
  <done>
Alle drei JS-Dateien bestehen den Syntax-Check einzeln. grep-Nachweis erbracht, dass keine gelöschte Funktion mehr referenziert wird (Ergebnis im Commit-Body). `grep -n "resetWiFi" firmware/data/js/setup.js` zeigt eine Implementierung. `grep -c "favicon.png" firmware/data/*.html` liefert 0. CLAUDE.md nennt `ANTHROPIC_API_KEY` und gunicorn; die Abschnitte „GSD Workflow Enforcement" und „Developer Profile" sind unverändert. Genau ein Commit erstellt.
  </done>
</task>

</tasks>

<verification>
- `cd firmware && pio run` — fehlerfrei
- `cd sentiment-api && python3 -m pytest tests/ -q` — alle Tests grün
- `node --check` für setup.js, mood.js, script.js — fehlerfrei
- `git log --oneline -3` zeigt genau drei neue Commits im Conventional-Commit-Stil
- Kein `git push`, kein SSH, kein OTA-Upload durchgeführt
</verification>

<success_criteria>
- Alle Findings A1–A11, B1–B8, C1–C10 sind umgesetzt oder mit grep-belegter Begründung ausgelassen (nur zulässig bei den bedingten Findings A11 und C8)
- Firmware baut, Backend-Tests grün, JS syntaktisch valide
- Drei atomare Commits, einer pro Task-Block
- Keine Auth-/CSRF-/Passwort-Änderungen (außer OTA-Magic-Byte-Check A9)
- Umlaute in allen neuen Texten korrekt
</success_criteria>

<output>
Erstelle `.planning/quick/260729-h73-code-review-fixes-mqtt-doppel-send-firmw/260729-h73-SUMMARY.md` nach Abschluss, mit: umgesetzte Finding-IDs, ausgelassene Findings samt grep-Begründung, Flash-Größe vorher/nachher, Testergebnisse, Commit-Hashes.
</output>
