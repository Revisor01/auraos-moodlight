---
phase: quick-260730-02i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/moodlight.cpp
  - firmware/src/config.h
  - firmware/src/app_state.h
  - firmware/src/web_server.cpp
  - firmware/src/wifi_manager.cpp
  - firmware/src/mqtt_handler.cpp
  - firmware/src/sensor_manager.cpp
  - firmware/src/MoodlightUtils.cpp
  - sentiment-api/app.py
  - sentiment-api/background_worker.py
  - sentiment-api/database.py
  - sentiment-api/moodlight_extensions.py
  - sentiment-api/init.sql
  - sentiment-api/requirements.txt
  - sentiment-api/migrations/002_drop_category_trigger.sql
  - sentiment-api/tests/
  - firmware/data/js/script.js
  - firmware/data/js/setup.js
  - firmware/data/js/mood.js
  - firmware/data/index.html
  - firmware/data/setup.html
  - firmware/data/mood.html
  - firmware/data/css/style.css
  - build-release.sh
autonomous: true
requirements: [A-KRITISCH-1, A-HOCH-2, A-HOCH-3, A-HOCH-4, A-MITTEL, A-NIEDRIG, B-KRITISCH-1, B-KRITISCH-2, B-HOCH-3, B-HOCH-4, B-MITTEL, B-PERF, C-HOCH-1, C-MITTEL, C-PERF, C-NIEDRIG, D]

must_haves:
  truths:
    - "Firmware kompiliert fehlerfrei und initialisiert NeoPixel ohne Use-after-free"
    - "MQTT-Passwort bleibt beim Speichern der Einstellungen erhalten"
    - "UI-Upload meldet Fehler statt blind Erfolg; UI bricht vor dem Firmware-Flash ab"
    - "Backend startet nicht ohne SECRET_KEY und begrenzt Login-Versuche"
    - "Worker überlebt reconfigure() und startet dabei keine sofortige Analyse"
    - "mood.html lädt History direkt vom Backend, ohne moment.js"
    - "build-release.sh ist syntaktisch valide und referenziert keine gelöschte Datei"
  artifacts:
    - path: "sentiment-api/migrations/002_drop_category_trigger.sql"
      provides: "DROP TRIGGER/FUNCTION für Kategorie-Duplikat"
      contains: "DROP TRIGGER IF EXISTS"
    - path: "firmware/src/moodlight.cpp"
      provides: "NeoPixel-Init via updateType/updateLength/setPin"
      contains: "updateLength"
  key_links:
    - from: "firmware/data/js/mood.js"
      to: "https://analyse.godsapp.de/api/moodlight/history"
      via: "fetch in loadData"
      pattern: "analyse\\.godsapp\\.de/api/moodlight/history"
    - from: "firmware/data/setup.html"
      to: "/ui-upload Statuscode"
      via: "xhr.status-Auswertung in startFullUpdate"
      pattern: "status"
---

<objective>
Review-Runde 2 vollständig umsetzen: die konsolidierten Findings aus fünf Code-Reviews
(Firmware, Backend, Web-UI, Build-Script) beheben.

Purpose: Der NeoPixel-Use-after-free ist die Root Cause für LED-Pulsieren und
Spontan-Reboots. Der MQTT-Passwort-Roundtrip zerstört bei jedem Speichern das
Passwort. Das Backend ist öffentlich im Internet und braucht die gelisteten
Härtungen. Dazu Worker-Lebensdauer-Bugs, UI-Regressionen und Performance.

Output: Vier atomare Commits (Firmware, Backend, UI, Build-Script), alle Tests grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260730-02i-review-runde-2-neopixel-uaf-mqtt-passwor/260730-02i-FINDINGS.md
@CLAUDE.md

**FINDINGS.md ist die verbindliche Arbeitsgrundlage.** Jeder Task unten verweist auf
Finding-Bezeichner (A-KRITISCH-1, B-MITTEL, C-PERF, …). Lies vor jedem Task den
zugehörigen Abschnitt in FINDINGS.md — dort stehen exakte Zeilennummern,
Code-Snippets und Begründungen, die hier nicht dupliziert werden.
</context>

<user_constraints>
## Nutzer-Vorgaben (VERBINDLICH)

- **KEINE Auth/CSRF/Zugriffsschutz-Fixes am GERÄT** (Heimnetz). Das Backend ist
  öffentlich im Internet — dort sind die gelisteten Härtungen ausdrücklich gewollt.
- **Kein Deployment, kein git push.** Macht der Orchestrator danach.
- **Umlaute korrekt (öäüß)** — auch in Commit-Messages.
- **Ein atomarer Commit pro Task.** Nur selbst geänderte Dateien stagen (explizite Pfade).
- SECRET_KEY ist auf Prod gesetzt (verifiziert) — Fail-fast ist deploybar.

## BEWUSST AUSGESCHLOSSEN (NICHT anfassen!)

- **WiFi-Reconnect-Umbau** (disconnect/busy-wait in `wifi_manager.cpp:258-274`):
  Historie mit Instabilität — **NICHT umbauen**. Erlaubt ist ausschließlich
  `WiFi.persistent(true)` → `false` (Z. 152).
- **gzip-Assets, CDN-Ersatz** für Chart.js/Font Awesome: Follow-up, nicht jetzt.
- **`/api/system/metrics|diagnose` entfernen**: bleiben als Debug-APIs
  (nur der cleanup-Iterator-Bug wird gefixt).
- **`/api/restart-counter` + `/api/reset-restart-counter` BEHALTEN** (Debug-APIs des Nutzers).
- **DB-Trigger auf Prod anwenden**: NUR init.sql ändern + Migrationsdatei anlegen.
  Keine Code-Änderung, die den Trigger voraussetzt. Anwendung macht der Orchestrator.
- **`deleteDir` NICHT löschen** — wird für das `/extract`-Leeren jetzt gebraucht.
</user_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Block A — Firmware-Fixes (NeoPixel-UAF, MQTT-Passwort, Upload, loop-Blockaden, Flash/RAM, Perf)</name>
  <files>firmware/src/moodlight.cpp, firmware/src/config.h, firmware/src/app_state.h, firmware/src/web_server.cpp, firmware/src/wifi_manager.cpp, firmware/src/mqtt_handler.cpp, firmware/src/sensor_manager.cpp, firmware/src/MoodlightUtils.cpp</files>
  <action>
Setze Task-Block A aus FINDINGS.md **vollständig** um (Abschnitte A-KRITISCH-1,
A-HOCH-2, A-HOCH-3, A-HOCH-4, A-MITTEL Upload-Robustheit, A-MITTEL Blockaden in loop(),
A-MITTEL Flash/RAM, A-NIEDRIG/PERF). Reihenfolge: kritische Fixes zuerst, dann
Aufräumarbeiten — so bleibt bei einem Build-Fehler die Ursache eng eingegrenzt.

Kernpunkte, jeweils mit Finding-Bezeichner:

- **A-KRITISCH-1 (Root Cause!)**: `moodlight.cpp:90` — Copy-Assignment auf
  `Adafruit_NeoPixel` ersetzen durch `pixels.updateType(...)` /
  `pixels.updateLength(appState.numLeds)` / `pixels.setPin(appState.ledPin)` /
  `pixels.begin()`. Exakte Snippet-Vorlage steht in FINDINGS.md. Der vorhandene
  `pixels.setBrightness(DEFAULT_LED_BRIGHTNESS)` bleibt danach stehen.
- **A-HOCH-2**: `/savemqtt` (web_server.cpp:1175) — Passwort nur übernehmen wenn
  `pass != "****" && pass.length() > 0`; changed-Tracking wie `/saveapi` (Reboot nur
  bei echter Änderung); fehlende JSON-Keys NICHT als leer übernehmen.
  Das zugehörige UI-Teil (Feld leer + `placeholder="(unverändert)"`) ist Task 3.
- **A-HOCH-3**: `/api/stats` hours `constrain(1, 720)`; `fetchBackendStatistics`
  Timeout 15s → 8s; Stats-URL aus `appState.apiUrl` ableiten (Suffix `/current` →
  `/history`) mit Fallback `DEFAULT_STATS_API_URL`; verwaistes `/api/backend/stats`
  (Z. 930) entfernen.
- **A-HOCH-4**: `handleUiUpload` — statisches Erfolgs-/Fehlerflag über Extraktion,
  Kopieren und Platzprüfung führen; Completion-Handler sendet bei Fehler 500 mit
  Klartext statt bedingungslos 200; vor Upload-Start ablehnen wenn freie Bytes
  < 3× Content-Length.
- **A-MITTEL Upload-Robustheit**: `static File` pro Chunk (START öffnen, WRITE
  schreiben, END/ABORTED schließen); `UPLOAD_FILE_ABORTED`-Handling für beide Handler
  (Firmware: `Update.abort()` + `setStatusLED(0)`; UI: Teil-TGZ löschen,
  `isTgzFile=false`, `uploadPath=""`); `extractedVersion = ""` und `uploadPath = ""`
  am Anfang von UPLOAD_FILE_START beider Handler; UI-Versions-Parse nur bei
  `startsWith("UI-")`; `/extract` nach erfolgreicher Installation rekursiv per
  vorhandenem `deleteDir` leeren.
- **A-MITTEL Blockaden in loop()**: MQTT-Reconnect-Busy-Wait (mqtt_handler.cpp:440-453)
  durch reines `mqtt.begin(...)` ersetzen (HAMqtt::loop() verbindet selbst nach) —
  der Startup-Wait in `connectMQTTOnStartup()` bleibt; Config-Modus-Zweig
  (moodlight.cpp:106-123) um `processLEDUpdates(); updateStatusLED();` und
  `delay(LOOP_DELAY_MS)` ergänzen und `apModeStartTime` bei jedem behandelten Request
  auffrischen; stündlichen WiFi-Scan aus `NetworkDiagnostics::fullAnalysis()`
  (MoodlightUtils.cpp:577) entfernen (nur RSSI/Kanal loggen); im `/wifiscan`-Handler
  `delay(10)` pro Netz entfernen und nach Serialisierung `WiFi.scanDelete()`;
  `initTime()` (wifi_manager.cpp:79) nicht-blockierend machen
  (`time(&now) > 1600000000` beim nächsten Loop-Durchlauf).
- **`WiFi.persistent(true)` → `persistent(false)`** (wifi_manager.cpp:152).
  **SONST NICHTS am Reconnect ändern** — siehe user_constraints.
- **A-MITTEL Flash/RAM**: sysstat-Schreibblock in `runSystemHealthCheck()`
  (web_server.cpp:1896-1933) entfernen; `JSON_BUFFER_SIZE` 16384 → 4096 (Z. 57) —
  vorher prüfen, dass keine Pool-Antwort größer ist (Status ~2 KB, die
  `serializeJson`-Aufrufe in Z. 686/706 sind die Referenz); LittleFS-Iterator-Bug in
  `/api/system/cleanup` (Z. 878-908) auf advance-then-delete umbauen (Muster
  Z. 429-434) und `/data/settings.json.bak` NICHT löschen.
- **A-NIEDRIG/PERF**: `LOOP_SERVER_HANDLE_MS`-Gate in loop() streichen und
  `LOOP_DELAY_MS` 20 → 10 (config.h:58 + moodlight.cpp:129-133); Cache-Header
  (`public, max-age=86400` für `/css/*`, `/js/*`, favicon-204; HTML `no-cache`);
  redundanten `updateLEDs()` am Ende von `getSentiment()` entfernen falls vorhanden;
  1h-Fallback `currentLedIndex = 2; updateLEDs();`; Heartbeat-DHT-Status nur bei
  `appState.dhtEnabled`; `initDHT()` bei `dhtEnabled=false` ohne pinMode/dht.begin;
  MQTT-Erfolg setzt `statusLedMode = 0` nur wenn vorher 4; Validierungen für
  `/savewifi` (SSID 1-32, Pass ≤ 63), `/saveapi` (nicht leer + `startsWith("http")`),
  `/savecolors` (temporäres Array, nur komplett übernehmen), `/savehardware`
  (abgelehnte Pins in der Response melden); Rest-`sprintf` → `snprintf`
  (Z. 603, 1097, 1533); `text/plain; charset=utf-8` bei /logs und Umlaut-Fehlertexten;
  verwaiste Routen entfernen (`/status`-Duplikat Z. 1521-1583, `/api/export/settings`,
  `/api/ui-version`); toten `initWatchdog()` und ungenutzten `JsonBufferGuard`
  entfernen; doppeltes lokales `copyFile` auflösen (siehe FINDINGS für die zwei
  erlaubten Varianten); `logBuffer[20]` → `logBuffer[LOG_BUFFER_SIZE]`
  (app_state.h:128 — `LOG_BUFFER_SIZE` ist config.h:28, Wert 20, also verhaltensneutral);
  `millis() > rebootTime` → `(long)(millis() - appState.rebootTime) >= 0`
  (moodlight.cpp:118/136); NTP-Konstanten (config.h:72-74) entfernen ODER in
  `initTime` verwenden — eine Quelle; `LittleFS.begin()`-Aufrufe in `getStorageInfo`
  (Z. 332, 1755) entfernen; `handleStaticFile` direkt öffnen statt `exists()`+`open()`;
  Captive-Portal `canHandle` mit `override` (wifi_manager.cpp:39-51);
  Schein-try/catch um HTTP/DHT/MQTT entfernen (die MQTT-Startup-try/catch darf bleiben).

Nicht anfassen: alles unter „BEWUSST AUSGESCHLOSSEN" in user_constraints.
  </action>
  <verify>
    <automated>cd firmware && pio run</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -c "pixels = Adafruit_NeoPixel" firmware/src/moodlight.cpp | grep -q '^0$' && grep -q "pixels.updateLength" firmware/src/moodlight.cpp && echo "A-KRITISCH-1 ok"</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -n 'WiFi.persistent' firmware/src/wifi_manager.cpp | grep -q 'false' && echo "persistent ok"</automated>
  </verify>
  <done>
`pio run` läuft fehlerfrei durch; Flash-Größe im SUMMARY notiert (Test A aus FINDINGS).
Kein `pixels = Adafruit_NeoPixel(...)` mehr im Code. `WiFi.persistent(false)`.
Reconnect-Logik in wifi_manager.cpp:258-274 unverändert (per `git diff` bestätigt).
Ein Commit: `fix(firmware): NeoPixel-Use-after-free, MQTT-Passwort-Roundtrip, Upload-Robustheit und loop-Blockaden beheben`
  </done>
</task>

<task type="auto">
  <name>Task 2: Block B — Backend-Härtung, Worker-Lebensdauer, Performance</name>
  <files>sentiment-api/app.py, sentiment-api/background_worker.py, sentiment-api/database.py, sentiment-api/moodlight_extensions.py, sentiment-api/init.sql, sentiment-api/requirements.txt, sentiment-api/migrations/002_drop_category_trigger.sql, sentiment-api/tests/</files>
  <action>
Setze Task-Block B aus FINDINGS.md **vollständig** um (B-KRITISCH-1, B-KRITISCH-2,
B-HOCH-3, B-HOCH-4, B-MITTEL Worker-Lebensdauer, B-MITTEL Weitere, B-PERF).

- **B-KRITISCH-1**: `app.py:21` — Fallback `'dev-secret-schluessel-aendern'` entfernen;
  `SECRET_KEY` aus Env lesen, bei leer/fehlend `raise RuntimeError("SECRET_KEY muss gesetzt sein")`.
  Zusätzlich `SESSION_COOKIE_SAMESITE='Lax'`. **KEIN `SESSION_COOKIE_SECURE`** —
  die Seite läuft über http, das würde den Login brechen.
- **B-KRITISCH-2**: `init.sql` — Helper-Funktionen-Sektion (Z. 192-198,
  `update_updated_at_column()`) VOR die settings-Tabelle (Z. 127-131) verschieben.
  Frische DB ist nicht testbar → per Lese-Reihenfolge bzw. Prüfskript verifizieren.
- **B-HOCH-3**: `app.py:366-371` — `headlines_per_source` auf `min(param, 10)` deckeln.
  Kein Auth-Zwang, nur Cap.
- **B-HOCH-4**: Login-Rate-Limit ohne neue Dependency: In-Memory-Dict
  `{ip: (fail_count, locked_until)}`, nach 5 Fehlversuchen 60 s Sperre (HTTP 429),
  Reset bei Erfolg, thread-safe per Lock, max ~1000 IPs (älteste raus).
- **B-MITTEL Worker-Lebensdauer** (die drei bestätigten Bugs in
  `background_worker.py:173-191`): Warte-Schleife mit Deadline implementieren —
  `deadline = now + interval`, innere Schleife `wait(min(rest, 5s))`; bei Wake:
  `not running` → return, sonst `clear()` und Deadline gegen das (ggf. neue)
  `interval_seconds` NEU berechnen, **ohne sofortige Analyse**. Analyse nur bei
  Deadline-Ablauf. `trigger()` bleibt der Weg für Sofort-Analysen. Dieser Umbau deckt
  alle drei Bugs ab (Thread-Tod in den ersten 10 s, verschluckter Wake während
  `_perform_update()`, ungewollter API-Call durch `reconfigure()`).
- **B-MITTEL Weitere**: Open-Redirect-Guard für `next_url` (app.py:590 — nur
  `startswith('/')` und nicht `startswith('//')`); SSRF-Guard in der Feed-Validierung
  (moodlight_extensions.py:461 — nur http/https, Hostname auflösen, private/Loopback/
  Link-local per `ipaddress` ablehnen, `timeout=5` behalten);
  `INTERVAL '%s hours'` → `make_interval(hours => %s)` bzw. `%s * INTERVAL '1 day'`
  (database.py:385, 610, 662); feeds-Status in `_fetch_headlines()` schreiben (neue
  DB-Methode: bei Erfolg `last_fetched_at=NOW(), error_count=0`, bei Fehler
  `error_count=error_count+1`); `next_update_minutes` dynamisch aus
  `worker.interval_seconds // 60` (moodlight_extensions.py:142); `get_cursor()` —
  `conn.cursor(...)` INS try ziehen damit `putconn` auch bei Cursor-Fehler läuft;
  `requests` auf `>=2.32.4` heben (requirements.txt, aktuell `2.32.2`, CVE-2024-47081);
  Kategorie-Duplikat: Trigger `set_sentiment_category` + Funktion
  `get_sentiment_category` aus init.sql entfernen und
  `sentiment-api/migrations/002_drop_category_trigger.sql` mit
  `DROP TRIGGER IF EXISTS ...; DROP FUNCTION IF EXISTS ...;` anlegen (die 002 ist die
  nächste freie Nummer — 001_add_feeds_table.sql existiert bereits); `shared_config`
  bleibt die einzige Wahrheit; **NICHTS deployen**;
  Percentile-Mindestanzahl `count < 3` → `count < 20` (database.py:582-637,
  `get_score_percentiles`), darunter Fallback-Schwellen (das fallback-Flag transportiert
  das bereits) — `tests/test_percentiles.py` entsprechend anpassen.
- **B-PERF**: Redis-Cache 120 s TTL für `/api/moodlight/history`, `/trend`, `/stats`,
  `/feeds/trends` (Key inkl. Parameter, z. B. `moodlight:history:{hours}:{limit}`);
  bestehende Worker-Invalidierung um Pattern-Delete erweitern (`scan_iter` + delete,
  **kein `KEYS`**); Feed-Fetching parallelisieren mit
  `ThreadPoolExecutor(max_workers=6)` in `_fetch_headlines()` und dem app.py-Pendant,
  Timeout 10 s pro Feed, Ergebnisreihenfolge stabil nach Feed-Reihenfolge zusammensetzen.

Neue/angepasste Tests (Test B aus FINDINGS): Worker-Reconfigure-Verhalten (Thread
stirbt nicht, keine Sofort-Analyse), Login-Rate-Limit, `headlines_per_source`-Cap,
init.sql-Reihenfolge (Funktion vor erster Verwendung definiert — als Prüfskript oder
Test). Bestehende Tests dürfen nicht brechen.
  </action>
  <verify>
    <automated>cd sentiment-api && python3 -m py_compile *.py</automated>
    <automated>cd sentiment-api && python3 -m pytest tests/ -q</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -v '^\s*--' sentiment-api/init.sql | grep -c "dev-secret-schluessel-aendern" | grep -q '^0$' && grep -c "dev-secret-schluessel-aendern" sentiment-api/app.py | grep -q '^0$' && echo "SECRET_KEY-Fallback entfernt"</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && test -f sentiment-api/migrations/002_drop_category_trigger.sql && grep -q "DROP TRIGGER IF EXISTS" sentiment-api/migrations/002_drop_category_trigger.sql && echo "Migration ok"</automated>
  </verify>
  <done>
`py_compile` fehlerfrei, `pytest tests/ -q` grün (inkl. neuer Tests für
Worker-Reconfigure, Login-Rate-Limit, headlines-Cap). Kein SECRET_KEY-Fallback mehr.
`migrations/002_drop_category_trigger.sql` existiert; init.sql definiert
`update_updated_at_column()` vor der ersten Verwendung. Nichts deployed.
Ein Commit: `fix(api): SECRET_KEY-Fail-fast, Login-Rate-Limit, Worker-Lebensdauer und Redis-Caching`
  </done>
</task>

<task type="auto">
  <name>Task 3: Block C — Web-UI-Fixes (XSS-Reste, Log-Regression, Direktzugriff aufs Backend, moment.js-Entfernung)</name>
  <files>firmware/data/js/script.js, firmware/data/js/setup.js, firmware/data/js/mood.js, firmware/data/index.html, firmware/data/setup.html, firmware/data/mood.html, firmware/data/css/style.css</files>
  <action>
Setze Task-Block C aus FINDINGS.md **vollständig** um (C-HOCH-1, C-MITTEL, C-PERF, C-NIEDRIG).

- **C-HOCH-1**: `scanWifi()` (setup.js:592-598) SSIDs per DOM-API/`textContent`
  rendern; `showAllSettings()` (setup.js:201-209) key/value escapen bzw. textContent;
  `refreshLog()` (script.js:19-27) auf `log.textContent = data;` umstellen und in
  `css/style.css` `.logs { white-space: pre-wrap; }` ergänzen — behebt zugleich die
  Regression, dass das Log seit dem text/plain-Umbau einzeilig erscheint.
- **C-MITTEL**: `data-hours` auf ALLEN Zeitraum-Tabs (mood.html:66-68 → 24/168/720)
  und `currentHours` bei jedem Tabwechsel setzen + bei Änderung neu laden
  (mood.js:45-55 — aktuell wird wegen `allDataLoaded` nach dem ersten Klick nicht mehr
  nachgeladen); `allDataLoaded = true` erst im Erfolgs-`.then()`;
  `loadStorageInfo2()`-Aufruf vom Hardware- zum Info-Tab (`about`) verschieben
  (setup.js:33-35); Schalter (index.html:101/108) ohne `checked` starten + `disabled`,
  nach erstem `updateSwitches()` enablen; `#mood-class` aus der Backend-Kategorie
  (`data.category` von `/api/status` falls vorhanden, sonst aus `ledIndex`) ableiten
  statt eigener JS-Schwellen (script.js:205-217);
  **MQTT-Passwortfeld leer lassen mit `placeholder="(unverändert)"`** statt `****`
  einzutragen (setup.js:651-653) — Gegenstück zu A-HOCH-2 aus Task 1;
  **UI-Upload**: `startFullUpdate()`/`doUpload()` in setup.html werten HTTP-Status und
  Response-Text des `/ui-upload`-Abschlusses aus (nutzt den Firmware-Fix A-HOCH-4);
  bei Fehler **Abbruch VOR dem Firmware-Flash**;
  **History direkt vom Backend**: `loadData()` in mood.js ruft
  `https://analyse.godsapp.de/api/moodlight/history?hours=${currentHours}`.
  Response-Format ist verifiziert `{count, data: [{timestamp, sentiment_score, category, …}]}`
  (moodlight_extensions.py, jsonify am Ende von `/api/moodlight/history`); Timestamps
  sind ISO-Strings. Fallback auf `/api/stats` des Geräts bei Fetch-Fehler behalten.
- **C-PERF**: `/logs`-Polling nur wenn `#logContent` existiert (script.js:376/391);
  moment.js + chartjs-adapter-moment aus mood.html entfernen (Z. 11-12) und **alle**
  moment-Nutzungen in mood.js durch native `Date`/`Intl.DateTimeFormat('de-DE', …)`-Helfer
  ersetzen. **Achtung:** FINDINGS nennt vier Stellen, tatsächlich sind es zehn —
  mood.js Z. 233, 244, 498, 522, 523, 547, 548, 558, 820, 821 (per grep verifiziert).
  Alle müssen weg, inklusive der `subtract`/`startOf`/`isSameOrAfter`-Aufrufe.
  Alle Charts nutzen category-Achsen → kein Adapter nötig.
  „Gesamter Zeitraum": statt `slice(-500)` Schrittfilter-Dezimierung wie im Day-Chart
  (mood.js:496 vs. 552-555); Dark-Mode-Flash per Mini-Inline-Script direkt nach
  `<body>`-Open in allen drei HTML-Seiten
  (`if(localStorage.getItem('darkMode')==='true')document.body.classList.add('dark');`);
  toten Code entfernen: **nur `updateAllCharts()`** (mood.js:398-441) — per grep
  verifiziert, dass der einzige Aufrufer (Z. 171) im `reprocess=false`-Zweig liegt.
  **`updateChart()` NICHT entfernen** — hat sieben lebende Aufrufer (Z. 492, 518, 543,
  579, 604, 628, 684). Entweder `updateAllCharts` samt dem toten `reprocess=false`-Pfad
  entfernen oder, falls der Pfad doch erreichbar ist, unangetastet lassen und im
  SUMMARY begründen.
- **C-NIEDRIG**: Refresh-Button-Icon nicht per textContent zerstören (innerHTML mit
  Icon wiederherstellen oder span fürs Label, script.js:61-77) und nach `/refresh`
  3× im 3-s-Abstand nachpollen statt einmal 2 s; `getMoodColor` — „Sehr positiv"
  eigene Farbe (z. B. `#12b886` vs. `#20c997`, script.js:198-203); Slider `oninput`
  fürs Label und während des Ziehens (`pointerdown`/`pointerup`-Flag) nicht vom Poll
  überschreiben lassen (index.html:114 + script.js); `xhr.timeout` 120000 → 300000
  (setup.html:383); Anführungszeichen auf deutsche Typografie „…" (mood.html:99, :370);
  `#wifi-status-info` (setup.html:63) mit aktueller SSID aus `/api/settings/all`
  befüllen ODER Element entfernen; `aria-labelledby` der Tabpanels korrigieren
  (mood.html:74-86 — Tabs bekommen IDs, Panels referenzieren sie).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight/firmware/data/js && node --check script.js && node --check setup.js && node --check mood.js</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && ! grep -rn "moment" firmware/data/js/mood.js firmware/data/mood.html && echo "keine moment-Referenz mehr"</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -q "analyse.godsapp.de/api/moodlight/history" firmware/data/js/mood.js && echo "History-Direktzugriff ok"</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -c "updateAllCharts" firmware/data/js/mood.js | grep -q '^0$' && grep -c "function updateChart" firmware/data/js/mood.js | grep -q '^1$' && echo "toter Code weg, updateChart erhalten"</automated>
  </verify>
  <done>
`node --check` für alle drei JS-Dateien fehlerfrei. Keine moment-Referenz in
mood.js/mood.html. Keine Referenz auf entfernte Funktionen (grep). History wird direkt
vom Backend geladen, `/api/stats`-Fallback erhalten. MQTT-Passwortfeld leer mit
Placeholder. UI-Upload bricht bei Fehlerstatus vor dem Firmware-Flash ab.
Ein Commit: `fix(ui): XSS-Reste, Log-Anzeige, History-Direktzugriff und moment.js-Entfernung`
  </done>
</task>

<task type="auto">
  <name>Task 4: Block D — build-release.sh korrigieren</name>
  <files>build-release.sh</files>
  <action>
Setze Task-Block D aus FINDINGS.md um:
- Z. 91: `diagnostics.html` aus der tar-Dateiliste entfernen. Die Datei existiert nicht
  mehr; wegen `set -e` würde `tar` sonst den ganzen Release-Build abbrechen.
- Z. 126: IP `192.168.0.140` → `192.168.0.37` (korrekte Geräte-IP).
- **KEINEN Release bauen** — das macht der Orchestrator danach.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && bash -n build-release.sh</automated>
    <automated>cd /Users/simonluthe/Documents/auraos-moodlight && grep -c "diagnostics.html" build-release.sh | grep -q '^0$' && grep -c "192.168.0.140" build-release.sh | grep -q '^0$' && grep -q "192.168.0.37" build-release.sh && echo "Block D ok"</automated>
  </verify>
  <done>
`bash -n build-release.sh` fehlerfrei. Kein `diagnostics.html` und kein
`192.168.0.140` mehr im Script; `192.168.0.37` ist eingetragen. Kein Release gebaut.
Ein Commit: `fix(build): fehlende diagnostics.html aus tar-Liste entfernen und Geräte-IP korrigieren`
  </done>
</task>

</tasks>

<verification>
Nach allen vier Tasks:

1. `cd firmware && pio run` — fehlerfrei, Flash-Größe im SUMMARY dokumentiert
2. `cd sentiment-api && python3 -m py_compile *.py && python3 -m pytest tests/ -q` — grün
3. `node --check` für script.js, setup.js, mood.js — fehlerfrei
4. `bash -n build-release.sh` — fehlerfrei
5. `git log --oneline -4` — genau vier Commits, Conventional-Commit-Stil,
   Umlaute korrekt (öäüß, nicht oe/ae/ue/ss)
6. `git diff HEAD~4 -- firmware/src/wifi_manager.cpp` — Reconnect-Block Z. 258-274
   unverändert (nur `persistent(false)` und ggf. `initTime`/`canHandle` geändert)
7. `git status` — keine unbeabsichtigt gestagten Fremddateien
8. Kein `git push`, kein Deployment, kein Release-Build
</verification>

<success_criteria>
- Alle Findings der Blöcke A-D aus FINDINGS.md umgesetzt oder mit Begründung im
  SUMMARY als bewusst nicht umgesetzt dokumentiert
- Vier atomare Commits (`fix(firmware):`, `fix(api):`, `fix(ui):`, `fix(build):`)
- Alle vier Test-Kommandos grün
- Keine Änderung an der ausgeschlossenen WiFi-Reconnect-Logik
- Keine Auth/CSRF-Änderung am Gerät; Backend-Härtungen vorhanden
- `/api/restart-counter`, `/api/reset-restart-counter`, `/api/system/metrics`,
  `/api/system/diagnose` und `deleteDir` weiterhin vorhanden
</success_criteria>

<output>
Create `.planning/quick/260730-02i-review-runde-2-neopixel-uaf-mqtt-passwor/260730-02i-SUMMARY.md` when done.
Im SUMMARY dokumentieren: Flash-Größe vor/nach, Liste bewusst nicht umgesetzter
Findings mit Begründung, die vier Commit-Hashes.
</output>
